---
phase: 07-i18n-integration-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Dockerfile
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements: [I18N-01, I18N-02]
must_haves:
  truths:
    - "pytest is installed inside the api container image"
    - "@playwright/test is listed as a devDependency in frontend/package.json"
    - "chromium browser is available to Playwright on the host"
  artifacts:
    - path: backend/Dockerfile
      provides: "installs requirements-dev.txt in the api image"
      contains: "requirements-dev.txt"
    - path: frontend/package.json
      provides: "@playwright/test devDependency"
      contains: "@playwright/test"
  key_links:
    - from: backend/Dockerfile
      to: backend/requirements-dev.txt
      via: "pip install -r requirements-dev.txt"
      pattern: "requirements-dev\\.txt"
---

<objective>
Install the test infrastructure needed by the Phase 7 rebuild harness (Wave 5): pytest inside the api container (D-21) and Playwright on the frontend side (D-22). This is the Wave 0 infrastructure plan from the research recommendation — it runs in parallel with the Wave 1 i18n bootstrap plan.

Purpose: Without pytest inside the `api` container, `docker compose exec api pytest` fails with "command not found" (see RESEARCH Pitfall 2). Without `@playwright/test` in `frontend/package.json` and chromium cached on the host, the visual assertion step of the smoke-rebuild harness cannot run.

Output: `backend/Dockerfile` installs dev deps; `frontend/package.json` gains `@playwright/test@1.59.1`; README documents the one-time `npx playwright install chromium` step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-i18n-integration-and-polish/07-CONTEXT.md
@.planning/phases/07-i18n-integration-and-polish/07-RESEARCH.md
@backend/Dockerfile
@backend/requirements-dev.txt
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install requirements-dev.txt in api Dockerfile</name>
  <files>backend/Dockerfile</files>
  <read_first>
    - backend/Dockerfile (entire file — currently only installs requirements.txt)
    - backend/requirements-dev.txt (pytest 9.0.3, pytest-asyncio 1.3.0, httpx 0.28.1, asgi-lifespan 2.1.0 — do NOT modify)
    - .planning/phases/07-i18n-integration-and-polish/07-RESEARCH.md (Pitfall 2 — "pytest: command not found" root cause + two fix options)
  </read_first>
  <action>
    Modify `backend/Dockerfile` using the **single-stage, always-install dev deps** approach (Research Pitfall 2 option A — recommended for internal app):

    1. Change the `COPY requirements.txt ./` line to `COPY requirements.txt requirements-dev.txt ./`
    2. Change the `RUN pip install --no-cache-dir -r requirements.txt` line to `RUN pip install --no-cache-dir -r requirements.txt -r requirements-dev.txt`

    Do NOT introduce multi-stage builds or a `DEV_MODE` build arg — CLAUDE.md says internal team use; ~30 MB size cost is acceptable. Do NOT reorder other lines.

    Rationale: This unblocks D-23 step 3 (`docker compose exec api pytest ...`) for the Wave 5 rebuild harness.
  </action>
  <verify>
    <automated>docker compose build api && docker compose run --rm --entrypoint "" api which pytest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "requirements-dev.txt" backend/Dockerfile` exits 0
    - `docker compose build api` succeeds
    - `docker compose run --rm --entrypoint "" api which pytest` prints a path containing `pytest` (not empty, no "not found")
    - `docker compose run --rm --entrypoint "" api python -c "import pytest, pytest_asyncio, httpx, asgi_lifespan"` exits 0
  </acceptance_criteria>
  <done>pytest and its async stack are importable inside the api container image. `docker compose exec api pytest --version` works after `up -d`.</done>
</task>

<task type="auto">
  <name>Task 2: Add @playwright/test to frontend and document chromium install</name>
  <files>frontend/package.json, frontend/package-lock.json, README.md</files>
  <read_first>
    - frontend/package.json (current devDependencies block)
    - README.md (find current test/command docs to extend; if no "Testing" section exists, create one)
    - .planning/phases/07-i18n-integration-and-polish/07-RESEARCH.md (§Standard Stack: `@playwright/test` 1.59.1 verified; §Environment Availability; Pitfall 3 — package/browser version coupling)
    - .planning/phases/07-i18n-integration-and-polish/07-CONTEXT.md (D-22)
  </read_first>
  <action>
    Step 1 — Install `@playwright/test@1.59.1` as a devDependency in the frontend:
    ```bash
    cd frontend && npm install -D @playwright/test@1.59.1
    ```
    This updates `frontend/package.json` (adds to `devDependencies`) and `frontend/package-lock.json`. Do NOT install any additional Playwright browsers as npm packages — Playwright ships a separate CLI-driven browser install that lives in `~/.cache/ms-playwright/`.

    Step 2 — Run the one-shot chromium install on the host:
    ```bash
    cd frontend && npx playwright install chromium
    ```
    This downloads chromium (~170 MB) into `~/.cache/ms-playwright/`. It is a HOST operation (D-22, Research §Standard Stack — "run Playwright from host").

    Step 3 — Add a "Testing" section to `README.md` (append if README exists, create the section if missing) containing exactly this content:

    ```markdown
    ## Testing

    ### Backend unit/integration tests

    Run pytest inside the api container (requires `docker compose up -d`):
    ```
    docker compose exec api pytest
    ```

    ### End-to-end rebuild persistence smoke test

    Prereq (one-shot, after any `@playwright/test` version bump):
    ```
    cd frontend && npx playwright install chromium
    ```

    Run the full rebuild persistence harness (seeds settings, rebuilds the stack, asserts persistence, visual check via Playwright):
    ```
    ./scripts/smoke-rebuild.sh
    ```

    Exits 0 on success; non-zero and prints the failing step on failure. The harness preserves `postgres_data` (it uses `docker compose down`, NOT `down -v`).
    ```

    The `./scripts/smoke-rebuild.sh` reference is forward-looking — it is created in plan 07-06.

    Do NOT run `npm install` outside of `frontend/`. Do NOT add any `postinstall` hook (Pitfall 3 — README documentation is sufficient for a manual harness).
  </action>
  <verify>
    <automated>cd frontend && npm ls @playwright/test && test -d ~/.cache/ms-playwright && grep -q "smoke-rebuild.sh" ../README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@playwright/test": "\\^\\?1\\.59\\.1"' frontend/package.json` exits 0
    - `frontend/package-lock.json` contains `@playwright/test`
    - `cd frontend && npx playwright --version` prints `Version 1.59.x`
    - `ls ~/.cache/ms-playwright | grep -q chromium` exits 0
    - `grep -q "## Testing" README.md` exits 0
    - `grep -q "smoke-rebuild.sh" README.md` exits 0
  </acceptance_criteria>
  <done>`@playwright/test@1.59.1` is installed as a devDependency, chromium is cached on the host, and README documents how to run the rebuild harness.</done>
</task>

</tasks>

<verification>
- `docker compose build api` succeeds and `pytest --version` works inside the image
- `cd frontend && npx playwright --version` prints 1.59.x
- `~/.cache/ms-playwright/chromium-*` directory exists
- README has a "Testing" section referencing `./scripts/smoke-rebuild.sh`
</verification>

<success_criteria>
Infrastructure unblocks Wave 5 (plan 07-06):
1. pytest runnable inside api container via `docker compose exec api pytest`
2. `@playwright/test` importable in frontend test files
3. chromium binary available to Playwright
4. README documents the workflow so future developers can reproduce
</success_criteria>

<output>
After completion, create `.planning/phases/07-i18n-integration-and-polish/07-01-SUMMARY.md`
</output>
