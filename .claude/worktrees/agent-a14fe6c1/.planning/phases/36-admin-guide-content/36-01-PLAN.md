---
phase: 36-admin-guide-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/docs/en/admin-guide/intro.md
  - frontend/src/docs/de/admin-guide/intro.md
  - frontend/src/docs/en/admin-guide/system-setup.md
  - frontend/src/docs/de/admin-guide/system-setup.md
  - frontend/src/docs/en/admin-guide/architecture.md
  - frontend/src/docs/de/admin-guide/architecture.md
  - frontend/src/lib/docs/registry.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
autonomous: true
requirements:
  - AGUIDE-01
  - AGUIDE-02
  - I18N-01

must_haves:
  truths:
    - "Admin can read a complete system setup article at /docs/admin-guide/system-setup in both EN and DE"
    - "Admin can read a complete architecture overview at /docs/admin-guide/architecture in both EN and DE"
    - "Admin intro article is a proper overview, not a stub"
    - "New articles appear in the sidebar under Admin Guide"
  artifacts:
    - path: "frontend/src/docs/en/admin-guide/system-setup.md"
      provides: "EN system setup article"
      contains: "docker compose up"
    - path: "frontend/src/docs/de/admin-guide/system-setup.md"
      provides: "DE system setup article"
      contains: "docker compose up"
    - path: "frontend/src/docs/en/admin-guide/architecture.md"
      provides: "EN architecture overview"
      contains: "FastAPI"
    - path: "frontend/src/docs/de/admin-guide/architecture.md"
      provides: "DE architecture overview"
      contains: "FastAPI"
    - path: "frontend/src/docs/en/admin-guide/intro.md"
      provides: "EN admin intro (replaced stub)"
      min_lines: 15
    - path: "frontend/src/docs/de/admin-guide/intro.md"
      provides: "DE admin intro (replaced stub)"
      min_lines: 15
  key_links:
    - from: "frontend/src/lib/docs/registry.ts"
      to: "frontend/src/docs/en/admin-guide/system-setup.md"
      via: "?raw import"
      pattern: 'import enSystemSetup from "../../docs/en/admin-guide/system-setup.md\?raw"'
    - from: "frontend/src/lib/docs/registry.ts"
      to: "sections admin-guide array"
      via: "sections entry"
      pattern: 'slug: "system-setup"'
---

<objective>
Write the first 2 admin guide articles (system-setup, architecture) in EN and DE, replace the admin intro stubs with proper overview articles, wire all into registry and i18n.

Purpose: Deliver AGUIDE-01 (system setup) and AGUIDE-02 (architecture) content so admins can understand how to bring up and reason about the system.
Output: 6 Markdown files (2 new articles x 2 langs + 2 replaced intros), updated registry.ts, updated en.json/de.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-admin-guide-content/36-CONTEXT.md
@.planning/phases/36-admin-guide-content/36-RESEARCH.md

<interfaces>
From frontend/src/lib/docs/registry.ts:
```typescript
export type ArticleEntry = { slug: string; titleKey: string };
export type SectionId = "user-guide" | "admin-guide";
export const sections: Record<SectionId, ArticleEntry[]>;
export const registry: Record<string, Record<string, Record<string, string>>>;
```

Current admin-guide sections array has only: `[{ slug: "intro", titleKey: "docs.nav.adminGuideIntro" }]`
Current admin-guide registry has only: `{ intro: enAdminIntro }` / `{ intro: deAdminIntro }`

Import pattern: `import enSystemSetup from "../../docs/en/admin-guide/system-setup.md?raw";`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author EN+DE system-setup, architecture, and intro replacement articles</name>
  <files>
    frontend/src/docs/en/admin-guide/intro.md,
    frontend/src/docs/de/admin-guide/intro.md,
    frontend/src/docs/en/admin-guide/system-setup.md,
    frontend/src/docs/de/admin-guide/system-setup.md,
    frontend/src/docs/en/admin-guide/architecture.md,
    frontend/src/docs/de/admin-guide/architecture.md
  </files>
  <read_first>
    frontend/src/docs/en/admin-guide/intro.md,
    frontend/src/docs/en/user-guide/intro.md,
    docker-compose.yml,
    .env.example,
    .planning/phases/36-admin-guide-content/36-RESEARCH.md
  </read_first>
  <action>
**Intro replacement (EN + DE):**
Replace the stub content in both `en/admin-guide/intro.md` and `de/admin-guide/intro.md` with a proper Admin Guide overview article. Follow the template from D-05: intro paragraph explaining what the admin guide covers, then `##` sections for each topic (System Setup, Architecture, Personio Integration, User Management) with a brief 1-2 sentence summary of each, ending with a `## Related Articles` section linking to each admin guide article using inline Markdown links per D-06. The EN intro should NOT contain the stub text "Articles will be added in upcoming updates." The DE version is a natural German translation per D-07.

**system-setup.md (EN):**
Per AGUIDE-01 and D-01 (tutorial-style), write a step-by-step article covering:
- `## Prerequisites` — Docker and Docker Compose installed, Git to clone the repo
- `## Environment Configuration` — Copy `.env.example` to `.env`, list all required env vars: `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `DIRECTUS_KEY`, `DIRECTUS_SECRET`, `DIRECTUS_ADMIN_EMAIL`, `DIRECTUS_ADMIN_PASSWORD`, `DIRECTUS_ADMINISTRATOR_ROLE_UUID`, `DIRECTUS_VIEWER_ROLE_UUID`. Include `> **Tip:**` about generating DIRECTUS_KEY/SECRET with `openssl rand -base64 32`. Include `> **Note:**` per D-08 about never committing `.env` to version control.
- `## Starting the Application` — Numbered steps: 1. `docker compose up -d` 2. Wait for services (explain startup sequence: db → migrate → api + directus → frontend) 3. Access frontend at `http://localhost:5173`, Directus at `http://localhost:8055`
- `## Fetching the Administrator Role UUID` — After first boot, fetch via Directus API or admin UI, set `DIRECTUS_ADMINISTRATOR_ROLE_UUID` in `.env`, restart
- `## Related Articles` — link to [Architecture](/docs/admin-guide/architecture), [User Management](/docs/admin-guide/user-management)

Per D-07: assume reader knows Docker basics, explain what to configure not what Docker is. Per D-02: professional casual tone. Per D-03: text-only, no images.

**system-setup.md (DE):** Natural German translation of the EN article. Keep code blocks, env var names, and CLI commands in English. Translate prose, headings, tips, and notes into natural German.

**architecture.md (EN):**
Per AGUIDE-02 and D-09 (service map + data flow level), write:
- `## Services` — List each service with its role, image/tech, and port:
  - `db` — PostgreSQL 17 (postgres:17-alpine), port 5432 (internal only)
  - `migrate` — One-shot Alembic migration runner, exits after completion
  - `api` — FastAPI + SQLAlchemy 2.0 + asyncpg, port 8000
  - `frontend` — React 19 + Vite 8 + TanStack Query + Recharts 3, port 5173
  - `directus` — Directus 11.17.2, port 8055 (localhost only), identity/auth layer
  - `directus-bootstrap-roles` — Idempotent role bootstrap sidecar (curl), runs once
  - `backup` — Scheduled pg_dump service
- `## Startup Sequence` — Describe the `depends_on` chain with `condition: service_healthy`
- `## Data Flow` — Browser → Vite SPA (5173) → FastAPI API (8000) → PostgreSQL (5432); Browser → Directus (8055) for auth tokens; FastAPI → Personio API (external) for HR sync
- `## Tech Stack Summary` — FastAPI + SQLAlchemy 2.0 + asyncpg / PostgreSQL 17 / React 19 + Vite 8 + TanStack Query / Directus 11 / Recharts 3
- `## Related Articles` — link to [System Setup](/docs/admin-guide/system-setup), [Personio Integration](/docs/admin-guide/personio)

Per D-09: no diagrams, text-based descriptions. Mention that `upload_batches`, `sales_records`, `app_settings`, and Personio tables are excluded from Directus Data Model UI via DB_EXCLUDE_TABLES.

**architecture.md (DE):** Natural German translation. Keep tech names, service names, and port numbers in English.

Cross-link per D-10: system-setup links to architecture and user-management; architecture links to system-setup and personio.
  </action>
  <verify>
    <automated>test -f frontend/src/docs/en/admin-guide/system-setup.md && test -f frontend/src/docs/de/admin-guide/system-setup.md && test -f frontend/src/docs/en/admin-guide/architecture.md && test -f frontend/src/docs/de/admin-guide/architecture.md && grep -q "docker compose" frontend/src/docs/en/admin-guide/system-setup.md && grep -q "FastAPI" frontend/src/docs/en/admin-guide/architecture.md && ! grep -q "will be added" frontend/src/docs/en/admin-guide/intro.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/docs/en/admin-guide/system-setup.md exists and contains "docker compose up"
    - frontend/src/docs/de/admin-guide/system-setup.md exists and contains "docker compose up"
    - frontend/src/docs/en/admin-guide/architecture.md exists and contains "FastAPI"
    - frontend/src/docs/de/admin-guide/architecture.md exists and contains "FastAPI"
    - frontend/src/docs/en/admin-guide/intro.md does NOT contain "will be added"
    - frontend/src/docs/de/admin-guide/intro.md does NOT contain "werden in"
    - All 6 files contain at least one `##` heading (for TOC generation)
    - system-setup.md EN contains "> **Note:**" (D-08 secrets callout)
    - system-setup.md EN contains "POSTGRES_USER" and "DIRECTUS_KEY"
    - architecture.md EN contains "5173" and "8000" and "8055" and "5432"
  </acceptance_criteria>
  <done>6 Markdown articles exist: EN+DE intro replacements, EN+DE system-setup, EN+DE architecture. All follow D-01 through D-10 conventions. Content is accurate per docker-compose.yml and .env.example.</done>
</task>

<task type="auto">
  <name>Task 2: Wire system-setup and architecture into registry and i18n</name>
  <files>
    frontend/src/lib/docs/registry.ts,
    frontend/src/locales/en.json,
    frontend/src/locales/de.json
  </files>
  <read_first>
    frontend/src/lib/docs/registry.ts,
    frontend/src/locales/en.json,
    frontend/src/locales/de.json
  </read_first>
  <action>
**registry.ts:**
1. Add 4 new imports at the top (after existing admin-guide imports):
```typescript
import enSystemSetup from "../../docs/en/admin-guide/system-setup.md?raw";
import deSystemSetup from "../../docs/de/admin-guide/system-setup.md?raw";
import enArchitecture from "../../docs/en/admin-guide/architecture.md?raw";
import deArchitecture from "../../docs/de/admin-guide/architecture.md?raw";
```

2. Update `sections["admin-guide"]` array — add after the intro entry:
```typescript
{ slug: "system-setup", titleKey: "docs.nav.adminSystemSetup" },
{ slug: "architecture", titleKey: "docs.nav.adminArchitecture" },
```

3. Update `registry.en["admin-guide"]` object — add:
```typescript
"system-setup": enSystemSetup,
architecture: enArchitecture,
```

4. Update `registry.de["admin-guide"]` object — add:
```typescript
"system-setup": deSystemSetup,
architecture: deArchitecture,
```

**en.json:**
Inside the `"docs"` > `"nav"` nested object, add:
```json
"adminSystemSetup": "System Setup",
"adminArchitecture": "Architecture"
```

**de.json:**
Inside the `"docs"` > `"nav"` nested object, add:
```json
"adminSystemSetup": "Systemeinrichtung",
"adminArchitecture": "Architektur"
```
  </action>
  <verify>
    <automated>grep -q 'enSystemSetup' frontend/src/lib/docs/registry.ts && grep -q 'enArchitecture' frontend/src/lib/docs/registry.ts && grep -q '"system-setup"' frontend/src/lib/docs/registry.ts && grep -q 'adminSystemSetup' frontend/src/locales/en.json && grep -q 'adminArchitecture' frontend/src/locales/de.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - registry.ts contains `import enSystemSetup from "../../docs/en/admin-guide/system-setup.md?raw"`
    - registry.ts contains `import deSystemSetup from "../../docs/de/admin-guide/system-setup.md?raw"`
    - registry.ts contains `import enArchitecture from "../../docs/en/admin-guide/architecture.md?raw"`
    - registry.ts contains `import deArchitecture from "../../docs/de/admin-guide/architecture.md?raw"`
    - registry.ts sections["admin-guide"] contains `slug: "system-setup"`
    - registry.ts sections["admin-guide"] contains `slug: "architecture"`
    - registry.ts en["admin-guide"] contains `"system-setup": enSystemSetup`
    - registry.ts de["admin-guide"] contains `architecture: deArchitecture`
    - en.json contains `"adminSystemSetup": "System Setup"` inside docs.nav
    - en.json contains `"adminArchitecture": "Architecture"` inside docs.nav
    - de.json contains `"adminSystemSetup": "Systemeinrichtung"` inside docs.nav
    - de.json contains `"adminArchitecture": "Architektur"` inside docs.nav
  </acceptance_criteria>
  <done>system-setup and architecture articles appear in the Admin Guide sidebar in both EN and DE with correct titles.</done>
</task>

</tasks>

<verification>
- All 6 .md files exist and contain substantive content (not stubs)
- registry.ts compiles (no import errors) — verified by `cd frontend && npx tsc --noEmit` or dev server start
- Sidebar shows system-setup and architecture under Admin Guide
- Language toggle switches article content between EN and DE
</verification>

<success_criteria>
- Admin can navigate to /docs/admin-guide/system-setup and read Docker Compose setup instructions
- Admin can navigate to /docs/admin-guide/architecture and read the service map and data flow
- Admin intro is a proper overview with links to all admin guide articles
- All content exists in both EN and DE
- Articles follow D-01 through D-10 conventions
</success_criteria>

<output>
After completion, create `.planning/phases/36-admin-guide-content/36-01-SUMMARY.md`
</output>
