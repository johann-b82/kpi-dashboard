---
phase: 36-admin-guide-content
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - frontend/src/docs/en/admin-guide/personio.md
  - frontend/src/docs/de/admin-guide/personio.md
  - frontend/src/docs/en/admin-guide/user-management.md
  - frontend/src/docs/de/admin-guide/user-management.md
  - frontend/src/lib/docs/registry.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
autonomous: true
requirements:
  - AGUIDE-03
  - AGUIDE-04
  - I18N-01

must_haves:
  truths:
    - "Admin can read a complete Personio integration article at /docs/admin-guide/personio in both EN and DE"
    - "Admin can read a complete user management article at /docs/admin-guide/user-management in both EN and DE"
    - "All 5 admin guide articles appear in the sidebar"
    - "Every article in both User Guide and Admin Guide exists in DE and EN"
  artifacts:
    - path: "frontend/src/docs/en/admin-guide/personio.md"
      provides: "EN Personio integration article"
      contains: "Client ID"
    - path: "frontend/src/docs/de/admin-guide/personio.md"
      provides: "DE Personio integration article"
      contains: "Client ID"
    - path: "frontend/src/docs/en/admin-guide/user-management.md"
      provides: "EN user management article"
      contains: "Directus"
    - path: "frontend/src/docs/de/admin-guide/user-management.md"
      provides: "DE user management article"
      contains: "Directus"
  key_links:
    - from: "frontend/src/lib/docs/registry.ts"
      to: "frontend/src/docs/en/admin-guide/personio.md"
      via: "?raw import"
      pattern: 'import enPersonio from "../../docs/en/admin-guide/personio.md\?raw"'
    - from: "frontend/src/lib/docs/registry.ts"
      to: "sections admin-guide array"
      via: "sections entry"
      pattern: 'slug: "user-management"'
---

<objective>
Write the remaining 2 admin guide articles (personio, user-management) in EN and DE, wire into registry and i18n. This completes all admin guide content and closes I18N-01 (full bilingual coverage).

Purpose: Deliver AGUIDE-03 (Personio integration) and AGUIDE-04 (user management) so admins can configure Personio sync and manage users.
Output: 4 Markdown files (2 articles x 2 langs), updated registry.ts, updated en.json/de.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-admin-guide-content/36-CONTEXT.md
@.planning/phases/36-admin-guide-content/36-RESEARCH.md
@.planning/phases/36-admin-guide-content/36-01-SUMMARY.md

<interfaces>
From frontend/src/lib/docs/registry.ts (after plan 01):
- sections["admin-guide"] will contain: intro, system-setup, architecture entries
- registry.en["admin-guide"] will contain: intro, system-setup, architecture
- registry.de["admin-guide"] will contain: intro, system-setup, architecture
- New entries to add: personio, user-management (both sections[] and registry{})
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author EN+DE personio and user-management articles</name>
  <files>
    frontend/src/docs/en/admin-guide/personio.md,
    frontend/src/docs/de/admin-guide/personio.md,
    frontend/src/docs/en/admin-guide/user-management.md,
    frontend/src/docs/de/admin-guide/user-management.md
  </files>
  <read_first>
    frontend/src/components/settings/PersonioCard.tsx,
    backend/app/routers/sync.py,
    backend/app/routers/settings.py,
    .env.example,
    frontend/src/locales/en.json,
    frontend/src/docs/en/user-guide/hr-dashboard.md,
    frontend/src/docs/en/user-guide/uploading-data.md,
    .planning/phases/36-admin-guide-content/36-RESEARCH.md
  </read_first>
  <action>
**personio.md (EN):**
Per AGUIDE-03, D-01 (tutorial-style), D-07 (ops-savvy audience), write:
- `## Overview` — Brief intro: Personio is the HR data source for absence rates, headcount, and skill tracking KPIs.
- `## Entering Credentials` — Navigate to Settings → Personio section. Enter Client ID and Client Secret (write-only password fields). Click "Test connection" to verify against Personio API before saving. Credentials stored in backend database, not `.env`. Include `> **Note:**` per D-08: "Treat API credentials as secrets. They are stored in the application database — ensure your PostgreSQL instance is secured."
- `## Configuring Sync Interval` — Options: Manual only, Hourly, Every 6 hours, Daily. Explain each option briefly.
- `## Mapping Fields` — Three subsections:
  1. **Sick Leave Type** — multiselect from absence types fetched from Personio. Used for Sick Leave Ratio KPI. Options only populate after credentials are saved and verified.
  2. **Production Department** — multiselect from departments. Used for Revenue / Prod. Employee KPI. Same prerequisite.
  3. **Skill Attribute Keys** — multiselect from custom attributes. Used for Skill Development KPI.
- `## Manual Sync` — "Refresh data" button on the HR Dashboard triggers an immediate sync outside the scheduled interval.
- `## Related Articles` — link to [HR Dashboard](/docs/user-guide/hr-dashboard) per D-10, link to [Architecture](/docs/admin-guide/architecture)

**personio.md (DE):** Natural German translation. Keep "Client ID", "Client Secret", field names, and API terms in English.

**user-management.md (EN):**
Per AGUIDE-04, D-01 (tutorial-style), D-07 (ops-savvy audience), write:
- `## Roles` — Two roles: Administrator (full access — upload, settings, admin guide) and Viewer (read-only dashboards, user guide only). Viewer role UUID is fixed: `a2222222-bbbb-bbbb-bbbb-bbbbbbbbbbbb` (set in `bootstrap-roles.sh`).
- `## Creating a User` — Numbered steps: 1. Open Directus admin at `http://localhost:8055` 2. Log in with `DIRECTUS_ADMIN_EMAIL` / `DIRECTUS_ADMIN_PASSWORD` 3. Navigate to Users → click "+ New User" 4. Set email, password, and role (Administrator or Viewer) 5. Save
- `## Promoting a User` — Numbered steps: 1. Open Directus admin 2. Navigate to Users → select the user 3. Change Role to Administrator 4. Save. Include `> **Tip:**` that role changes take effect on next login.
- `## Administrator Role UUID` — Explain that the Administrator role UUID is generated by Directus on first boot. Must be fetched and set as `DIRECTUS_ADMINISTRATOR_ROLE_UUID` in `.env`. Reference the System Setup article for initial fetch instructions.
- `## Related Articles` — link to [System Setup](/docs/admin-guide/system-setup), [Uploading Data](/docs/user-guide/uploading-data) per D-10 noting only Admins can upload. Include `> **Note:**` per D-08: "Store `DIRECTUS_ADMIN_PASSWORD` in a password manager."

**user-management.md (DE):** Natural German translation. Keep "Directus", "Administrator", "Viewer", UUID values, and URLs in English.

Cross-link per D-10: personio links to hr-dashboard (user guide) and architecture (admin guide); user-management links to system-setup (admin guide) and uploading-data (user guide).
  </action>
  <verify>
    <automated>test -f frontend/src/docs/en/admin-guide/personio.md && test -f frontend/src/docs/de/admin-guide/personio.md && test -f frontend/src/docs/en/admin-guide/user-management.md && test -f frontend/src/docs/de/admin-guide/user-management.md && grep -q "Client ID" frontend/src/docs/en/admin-guide/personio.md && grep -q "Directus" frontend/src/docs/en/admin-guide/user-management.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/docs/en/admin-guide/personio.md exists and contains "Client ID" and "Client Secret"
    - frontend/src/docs/de/admin-guide/personio.md exists and contains "Client ID"
    - frontend/src/docs/en/admin-guide/user-management.md exists and contains "Directus" and "Administrator" and "Viewer"
    - frontend/src/docs/de/admin-guide/user-management.md exists and contains "Directus"
    - personio.md EN contains "> **Note:**" (D-08 secrets callout)
    - user-management.md EN contains "> **Note:**" (D-08 password manager callout)
    - personio.md EN contains link to hr-dashboard: "[HR Dashboard](/docs/user-guide/hr-dashboard)"
    - user-management.md EN contains link to uploading-data: "[Uploading Data](/docs/user-guide/uploading-data)"
    - All 4 files contain at least one `##` heading
  </acceptance_criteria>
  <done>4 Markdown articles exist: EN+DE personio, EN+DE user-management. All follow D-01 through D-10 conventions. Content sourced from PersonioCard.tsx, sync.py, .env.example, bootstrap-roles.sh.</done>
</task>

<task type="auto">
  <name>Task 2: Wire personio and user-management into registry and i18n</name>
  <files>
    frontend/src/lib/docs/registry.ts,
    frontend/src/locales/en.json,
    frontend/src/locales/de.json
  </files>
  <read_first>
    frontend/src/lib/docs/registry.ts,
    frontend/src/locales/en.json,
    frontend/src/locales/de.json
  </read_first>
  <action>
**registry.ts:**
1. Add 4 new imports at the top (after the architecture imports added by plan 01):
```typescript
import enPersonio from "../../docs/en/admin-guide/personio.md?raw";
import dePersonio from "../../docs/de/admin-guide/personio.md?raw";
import enUserManagement from "../../docs/en/admin-guide/user-management.md?raw";
import deUserManagement from "../../docs/de/admin-guide/user-management.md?raw";
```

2. Add to `sections["admin-guide"]` array — after the architecture entry:
```typescript
{ slug: "personio", titleKey: "docs.nav.adminPersonio" },
{ slug: "user-management", titleKey: "docs.nav.adminUserManagement" },
```

3. Add to `registry.en["admin-guide"]`:
```typescript
personio: enPersonio,
"user-management": enUserManagement,
```

4. Add to `registry.de["admin-guide"]`:
```typescript
personio: dePersonio,
"user-management": deUserManagement,
```

**en.json:**
Inside `"docs"` > `"nav"` nested object, add:
```json
"adminPersonio": "Personio Integration",
"adminUserManagement": "User Management"
```

**de.json:**
Inside `"docs"` > `"nav"` nested object, add:
```json
"adminPersonio": "Personio-Integration",
"adminUserManagement": "Benutzerverwaltung"
```
  </action>
  <verify>
    <automated>grep -q 'enPersonio' frontend/src/lib/docs/registry.ts && grep -q 'enUserManagement' frontend/src/lib/docs/registry.ts && grep -q 'slug: "personio"' frontend/src/lib/docs/registry.ts && grep -q 'slug: "user-management"' frontend/src/lib/docs/registry.ts && grep -q 'adminPersonio' frontend/src/locales/en.json && grep -q 'adminUserManagement' frontend/src/locales/de.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - registry.ts contains `import enPersonio from "../../docs/en/admin-guide/personio.md?raw"`
    - registry.ts contains `import dePersonio from "../../docs/de/admin-guide/personio.md?raw"`
    - registry.ts contains `import enUserManagement from "../../docs/en/admin-guide/user-management.md?raw"`
    - registry.ts contains `import deUserManagement from "../../docs/de/admin-guide/user-management.md?raw"`
    - registry.ts sections["admin-guide"] contains `slug: "personio"`
    - registry.ts sections["admin-guide"] contains `slug: "user-management"`
    - registry.ts en["admin-guide"] contains `personio: enPersonio`
    - registry.ts de["admin-guide"] contains `"user-management": deUserManagement`
    - en.json contains `"adminPersonio": "Personio Integration"` inside docs.nav
    - en.json contains `"adminUserManagement": "User Management"` inside docs.nav
    - de.json contains `"adminPersonio": "Personio-Integration"` inside docs.nav
    - de.json contains `"adminUserManagement": "Benutzerverwaltung"` inside docs.nav
    - sections["admin-guide"] has exactly 5 entries (intro, system-setup, architecture, personio, user-management)
  </acceptance_criteria>
  <done>All 5 admin guide articles appear in sidebar. Full bilingual coverage achieved (I18N-01). Phase 36 content is complete.</done>
</task>

</tasks>

<verification>
- All admin guide .md files exist: intro, system-setup, architecture, personio, user-management (x2 langs = 10 files)
- registry.ts has 5 entries in sections["admin-guide"]
- registry.ts has 5 entries in both en["admin-guide"] and de["admin-guide"]
- en.json and de.json have all 5 admin guide nav keys
- `cd frontend && npx tsc --noEmit` passes (no import errors)
- Language toggle switches all admin guide articles between EN and DE
</verification>

<success_criteria>
- Admin can navigate to /docs/admin-guide/personio and read Personio integration setup instructions
- Admin can navigate to /docs/admin-guide/user-management and read user management instructions
- All 5 admin guide articles appear in sidebar under Admin Guide
- Every article exists in both EN and DE (I18N-01 complete)
- All cross-links between admin and user guide articles work
</success_criteria>

<output>
After completion, create `.planning/phases/36-admin-guide-content/36-02-SUMMARY.md`
</output>
