---
phase: 34-navigation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/docs/en/user-guide/intro.md
  - frontend/src/docs/de/user-guide/intro.md
  - frontend/src/docs/en/admin-guide/intro.md
  - frontend/src/docs/de/admin-guide/intro.md
  - frontend/src/lib/docs/registry.ts
  - frontend/src/components/docs/DocsSidebar.tsx
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
autonomous: true
requirements:
  - NAV-02
  - I18N-02
must_haves:
  truths:
    - "DocsSidebar renders User Guide section for all roles"
    - "DocsSidebar renders Admin Guide section only for admin role (via AdminOnly)"
    - "Sidebar labels display in both DE and EN via i18n keys"
    - "Article registry resolves intro stubs for both sections and both languages"
  artifacts:
    - path: "frontend/src/components/docs/DocsSidebar.tsx"
      provides: "Role-filtered sidebar navigation"
      exports: ["DocsSidebar"]
    - path: "frontend/src/lib/docs/registry.ts"
      provides: "Article content registry keyed by lang/section/slug"
      exports: ["registry"]
    - path: "frontend/src/docs/en/user-guide/intro.md"
      provides: "English user guide intro stub"
    - path: "frontend/src/docs/de/user-guide/intro.md"
      provides: "German user guide intro stub"
    - path: "frontend/src/docs/en/admin-guide/intro.md"
      provides: "English admin guide intro stub"
    - path: "frontend/src/docs/de/admin-guide/intro.md"
      provides: "German admin guide intro stub"
  key_links:
    - from: "frontend/src/components/docs/DocsSidebar.tsx"
      to: "frontend/src/auth/AdminOnly.tsx"
      via: "AdminOnly wrapper around admin section"
      pattern: "<AdminOnly>"
    - from: "frontend/src/components/docs/DocsSidebar.tsx"
      to: "frontend/src/locales/en.json"
      via: "useTranslation() with docs.nav.* keys"
      pattern: "docs\\.nav\\."
---

<objective>
Create the article registry, stub Markdown files, DocsSidebar component, and i18n keys needed by the navigation shell.

Purpose: Establishes the data layer (registry + stubs) and sidebar UI that Plan 02 wires into DocsPage and App routing.
Output: DocsSidebar component, article registry, 4 stub MD files, i18n keys in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-navigation-shell/34-CONTEXT.md
@.planning/phases/34-navigation-shell/34-RESEARCH.md
@.planning/phases/34-navigation-shell/34-UI-SPEC.md

@frontend/src/auth/AdminOnly.tsx
@frontend/src/auth/useAuth.ts
@frontend/src/locales/en.json
@frontend/src/locales/de.json

<interfaces>
From frontend/src/auth/AdminOnly.tsx:
```tsx
export function AdminOnly({ children }: { children: ReactNode }): JSX.Element | null;
```

From frontend/src/auth/useAuth.ts:
```tsx
export function useRole(): Role | null;
// Role = "admin" | "viewer"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub articles, registry, and i18n keys</name>
  <files>
    frontend/src/docs/en/user-guide/intro.md,
    frontend/src/docs/de/user-guide/intro.md,
    frontend/src/docs/en/admin-guide/intro.md,
    frontend/src/docs/de/admin-guide/intro.md,
    frontend/src/lib/docs/registry.ts,
    frontend/src/locales/en.json,
    frontend/src/locales/de.json
  </files>
  <read_first>
    frontend/src/locales/en.json,
    frontend/src/locales/de.json,
    frontend/src/pages/DocsPage.tsx,
    frontend/src/docs/en/getting-started.md
  </read_first>
  <action>
1. Create 4 stub Markdown files:
   - `frontend/src/docs/en/user-guide/intro.md`: Content = `# Introduction\n\nWelcome to the KPI Dashboard user guide. Articles will be added in upcoming updates.`
   - `frontend/src/docs/de/user-guide/intro.md`: Content = `# Einleitung\n\nWillkommen im Benutzerhandbuch des KPI Dashboards. Artikel werden in kommenden Updates hinzugefuegt.`
   - `frontend/src/docs/en/admin-guide/intro.md`: Content = `# Introduction\n\nWelcome to the KPI Dashboard admin guide. Articles will be added in upcoming updates.`
   - `frontend/src/docs/de/admin-guide/intro.md`: Content = `# Einleitung\n\nWillkommen im Administratorhandbuch des KPI Dashboards. Artikel werden in kommenden Updates hinzugefuegt.`

2. Create `frontend/src/lib/docs/registry.ts`:
   ```ts
   import enUserIntro from "../../docs/en/user-guide/intro.md?raw";
   import deUserIntro from "../../docs/de/user-guide/intro.md?raw";
   import enAdminIntro from "../../docs/en/admin-guide/intro.md?raw";
   import deAdminIntro from "../../docs/de/admin-guide/intro.md?raw";

   export type ArticleEntry = { slug: string; titleKey: string };
   export type SectionId = "user-guide" | "admin-guide";

   /** Sidebar structure — ordered lists of articles per section */
   export const sections: Record<SectionId, ArticleEntry[]> = {
     "user-guide": [{ slug: "intro", titleKey: "docs.nav.userGuideIntro" }],
     "admin-guide": [{ slug: "intro", titleKey: "docs.nav.adminGuideIntro" }],
   };

   /** Content registry: registry[lang][section][slug] = raw Markdown string */
   export const registry: Record<string, Record<string, Record<string, string>>> = {
     en: {
       "user-guide": { intro: enUserIntro },
       "admin-guide": { intro: enAdminIntro },
     },
     de: {
       "user-guide": { intro: deUserIntro },
       "admin-guide": { intro: deAdminIntro },
     },
   };
   ```

3. Add i18n keys inside the existing `"docs"` object in both locale files:

   In `en.json`, add inside `"docs": { ... }` (after existing `"empty"` block):
   ```json
   "nav": {
     "docsLabel": "Documentation",
     "userGuide": "User Guide",
     "adminGuide": "Admin Guide",
     "userGuideIntro": "Introduction",
     "adminGuideIntro": "Introduction"
   }
   ```

   In `de.json`, add inside `"docs": { ... }` (after existing `"empty"` block):
   ```json
   "nav": {
     "docsLabel": "Dokumentation",
     "userGuide": "Benutzerhandbuch",
     "adminGuide": "Administratorhandbuch",
     "userGuideIntro": "Einleitung",
     "adminGuideIntro": "Einleitung"
   }
   ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/docs/en/user-guide/intro.md contains "# Introduction"
    - frontend/src/docs/de/user-guide/intro.md contains "# Einleitung"
    - frontend/src/docs/en/admin-guide/intro.md contains "# Introduction"
    - frontend/src/docs/de/admin-guide/intro.md contains "# Einleitung"
    - frontend/src/lib/docs/registry.ts contains "export const registry"
    - frontend/src/lib/docs/registry.ts contains "export const sections"
    - frontend/src/locales/en.json contains "docsLabel": "Documentation"
    - frontend/src/locales/en.json contains "userGuide": "User Guide"
    - frontend/src/locales/en.json contains "adminGuide": "Admin Guide"
    - frontend/src/locales/de.json contains "docsLabel": "Dokumentation"
    - frontend/src/locales/de.json contains "userGuide": "Benutzerhandbuch"
    - frontend/src/locales/de.json contains "adminGuide": "Administratorhandbuch"
  </acceptance_criteria>
  <done>Registry exports sections and content, 4 stub MD files exist, i18n keys present in both locales, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: DocsSidebar component</name>
  <files>frontend/src/components/docs/DocsSidebar.tsx</files>
  <read_first>
    frontend/src/auth/AdminOnly.tsx,
    frontend/src/auth/useAuth.ts,
    frontend/src/components/docs/TableOfContents.tsx,
    frontend/src/lib/docs/registry.ts
  </read_first>
  <action>
Create `frontend/src/components/docs/DocsSidebar.tsx` per D-01 (grouped flat list), D-02 (Admin Guide hidden for Viewers via AdminOnly):

```tsx
import { Link, useParams } from "wouter";
import { useTranslation } from "react-i18next";
import { AdminOnly } from "@/auth/AdminOnly";
import { sections, type SectionId } from "@/lib/docs/registry";

function SectionGroup({
  titleKey,
  section,
  activeSection,
  activeSlug,
}: {
  titleKey: string;
  section: SectionId;
  activeSection: string | undefined;
  activeSlug: string | undefined;
}) {
  const { t } = useTranslation();
  const articles = sections[section];
  return (
    <div>
      <h3 className="text-xs font-medium text-muted-foreground uppercase tracking-wide px-2 mb-1">
        {t(titleKey)}
      </h3>
      <ul className="flex flex-col gap-0.5">
        {articles.map((a) => {
          const isActive = section === activeSection && a.slug === activeSlug;
          return (
            <li key={a.slug}>
              <Link
                href={`/docs/${section}/${a.slug}`}
                className={
                  isActive
                    ? "block px-2 py-1.5 rounded-md text-sm text-primary bg-accent font-medium"
                    : "block px-2 py-1.5 rounded-md text-sm text-foreground hover:bg-accent/10 transition-colors"
                }
              >
                {t(a.titleKey)}
              </Link>
            </li>
          );
        })}
      </ul>
    </div>
  );
}

export function DocsSidebar() {
  const { section, slug } = useParams<{ section: string; slug: string }>();
  return (
    <nav className="w-56 shrink-0 hidden md:flex flex-col gap-6">
      <SectionGroup
        titleKey="docs.nav.userGuide"
        section="user-guide"
        activeSection={section}
        activeSlug={slug}
      />
      <AdminOnly>
        <SectionGroup
          titleKey="docs.nav.adminGuide"
          section="admin-guide"
          activeSection={section}
          activeSlug={slug}
        />
      </AdminOnly>
    </nav>
  );
}
```

Key details:
- `w-56 shrink-0` width per UI-SPEC
- `hidden md:flex` — collapses below 768px per UI-SPEC responsive rule
- Active state: `text-primary bg-accent font-medium` per UI-SPEC interaction contract
- Inactive: `text-foreground hover:bg-accent/10 transition-colors`
- Section header: `text-xs font-medium text-muted-foreground uppercase tracking-wide px-2 mb-1` per UI-SPEC typography
- Admin Guide section wrapped in `<AdminOnly>` per D-02
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/docs/DocsSidebar.tsx contains "export function DocsSidebar"
    - frontend/src/components/docs/DocsSidebar.tsx contains "<AdminOnly>"
    - frontend/src/components/docs/DocsSidebar.tsx contains "docs.nav.userGuide"
    - frontend/src/components/docs/DocsSidebar.tsx contains "docs.nav.adminGuide"
    - frontend/src/components/docs/DocsSidebar.tsx contains "w-56 shrink-0"
    - frontend/src/components/docs/DocsSidebar.tsx contains "hidden md:flex"
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>DocsSidebar component renders grouped flat list, Admin Guide hidden for Viewers, active article highlighted, responsive collapse below md</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- Registry file exports `registry` and `sections`
- DocsSidebar imports AdminOnly and uses useTranslation
- All 4 stub MD files exist with correct headings
- Both locale files have `docs.nav.*` keys
</verification>

<success_criteria>
- TypeScript compiles cleanly
- DocsSidebar component ready for DocsPage integration (Plan 02)
- Registry ready for DocsPage content lookup (Plan 02)
- i18n keys present for sidebar chrome in both languages
</success_criteria>

<output>
After completion, create `.planning/phases/34-navigation-shell/34-01-SUMMARY.md`
</output>
