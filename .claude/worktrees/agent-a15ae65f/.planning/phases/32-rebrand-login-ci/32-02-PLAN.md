---
phase: 32-rebrand-login-ci
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - frontend/src/pages/LoginPage.tsx
autonomous: false
requirements: [BRAND-02, BRAND-03]

must_haves:
  truths:
    - "Login page displays the uploaded logo above the title"
    - "Login page card styling matches Dashboard/Settings card aesthetic"
    - "When no logo is uploaded, only the KPI Dashboard text title shows"
  artifacts:
    - path: "frontend/src/pages/LoginPage.tsx"
      provides: "Restyled login page with logo"
      contains: "logo/public"
      min_lines: 50
  key_links:
    - from: "frontend/src/pages/LoginPage.tsx"
      to: "/api/settings/logo/public"
      via: "img element src or useEffect fetch"
      pattern: "api/settings/logo/public"
---

<objective>
Restyle the login page to show the uploaded logo and match the app's card aesthetic.

Purpose: BRAND-02 requires the logo above the title. BRAND-03 requires the login card to use the same white-card/subtle-border/blue-accent styling as Settings and Dashboard pages.
Output: LoginPage.tsx updated with logo display and consistent card styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-rebrand-login-ci/32-CONTEXT.md
@.planning/phases/32-rebrand-login-ci/32-01-SUMMARY.md
</context>

<interfaces>
<!-- Public logo endpoint created in plan 01 -->
GET /api/settings/logo/public
  - 200: raw image bytes (Content-Type from logo_mime)
  - 404: { "detail": "No logo set" }
  - Supports ETag / If-None-Match for caching
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add logo display and restyle login card</name>
  <files>frontend/src/pages/LoginPage.tsx</files>
  <read_first>
    - frontend/src/pages/LoginPage.tsx
    - frontend/src/pages/SettingsPage.tsx (for card styling reference)
  </read_first>
  <action>
Per D-01, D-02, D-03, modify `frontend/src/pages/LoginPage.tsx`:

**Logo display (BRAND-02):**
1. Add state: `const [logoUrl, setLogoUrl] = useState<string | null>(null)`
2. Add a useEffect that fetches `/api/settings/logo/public` on mount:
   - Use a plain `fetch` call (not react-query — this is an unauthed page with no QueryProvider)
   - On 200 response: create an object URL from the blob (`URL.createObjectURL(await res.blob())`) and set it to state
   - On 404 or error: leave logoUrl as null (per D-02, show only text title)
   - Cleanup: revoke the object URL on unmount
3. In the CardHeader, above the "KPI Dashboard" text, conditionally render:
   ```tsx
   {logoUrl && (
     <img
       src={logoUrl}
       alt="Logo"
       className="mx-auto h-16 w-16 object-contain mb-2"
     />
   )}
   ```

**Card restyling (BRAND-03):**
Per D-03, minimal tweaks to match app card aesthetic. Update the Card className from `"w-full max-w-sm"` to:
```
"w-full max-w-sm border border-border shadow-sm"
```
This matches the subtle-border + shadow pattern used on Dashboard and Settings cards. The existing Card component already renders white background in light mode and dark surface in dark mode via CSS variable tokens, so no background override is needed.

Ensure the "Sign in" Button keeps `className="w-full"` — shadcn Button already uses the primary color (blue accent) by default.

Add `import { useState, useEffect } from "react"` (useEffect is new).
  </action>
  <verify>
    <automated>cd /Users/johannbechtold/Documents/acm-kpi-light && grep -n "logo/public" frontend/src/pages/LoginPage.tsx && grep -n "border-border" frontend/src/pages/LoginPage.tsx && grep -n "useEffect" frontend/src/pages/LoginPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/LoginPage.tsx` contains `api/settings/logo/public`
    - `frontend/src/pages/LoginPage.tsx` contains `URL.createObjectURL`
    - `frontend/src/pages/LoginPage.tsx` contains `border border-border shadow-sm`
    - `frontend/src/pages/LoginPage.tsx` contains `useEffect`
    - `frontend/src/pages/LoginPage.tsx` contains `<img` with logo rendering
    - `frontend/src/pages/LoginPage.tsx` does NOT contain the string "KPI Light"
  </acceptance_criteria>
  <done>Login page fetches and displays logo from public endpoint, falls back to text-only when no logo exists, card has matching border/shadow styling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify login page appearance</name>
  <files>frontend/src/pages/LoginPage.tsx</files>
  <action>
Human visual verification of the login page. No code changes — this is a checkpoint.

Steps:
1. Start the app with `docker compose up`
2. Navigate to the login page (log out or open incognito)
3. Verify logo appears above "KPI Dashboard" title (if uploaded), or text-only if no logo
4. Verify card has subtle border and shadow matching Settings/Dashboard cards
5. Verify "Sign in" button has blue accent color
6. Verify dark mode adaptation
7. Sign in and verify navbar says "KPI Dashboard"
8. Check browser tab title says "KPI Dashboard"
  </action>
  <verify>User approves visual appearance</verify>
  <done>User confirms login page matches app aesthetic and all branding reads "KPI Dashboard"</done>
</task>

</tasks>

<verification>
- Login page shows logo from public endpoint when one is uploaded
- Login page shows only "KPI Dashboard" text when no logo exists
- Card styling matches app aesthetic (border, shadow, accent button)
- No occurrence of "KPI Light" anywhere in the running app
</verification>

<success_criteria>
- Logo renders on login page from /api/settings/logo/public
- No logo gracefully degrades to text-only
- Card border/shadow matches Dashboard/Settings pages
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/32-rebrand-login-ci/32-02-SUMMARY.md`
</output>
