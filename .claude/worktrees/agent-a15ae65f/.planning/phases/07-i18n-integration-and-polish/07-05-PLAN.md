---
phase: 07-i18n-integration-and-polish
plan: 05
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - frontend/src/locales/de.json
autonomous: true
requirements: [I18N-01]
must_haves:
  truths:
    - "frontend/src/locales/de.json contains exactly the same key set as en.json (parity holds)"
    - "All translations use informal 'du' tone (no Sie/Ihre/Ihnen remain)"
    - "English loanwords per D-18 are preserved (Dashboard, Upload noun, KPI, Logo)"
    - "The Settings page renders fully in German when language = DE"
  artifacts:
    - path: frontend/src/locales/de.json
      provides: "Full German translation of all 111 locale keys (107 existing EN + 4 new from plan 07-03)"
      min_lines: 110
  key_links:
    - from: frontend/src/locales/de.json
      to: frontend/src/locales/en.json
      via: "key-set equality (set(en) == set(de))"
      pattern: "settings.preferences"
---

<objective>
Bring `frontend/src/locales/de.json` to full parity with `frontend/src/locales/en.json` (107 + 4 new keys from plan 07-03 = 111 keys total). Translate all missing keys using informal "du" tone (D-17). Preserve English loanwords per D-18 rules. Rewrite the 2 existing de.json entries that currently use formal "Sie" form to "du" form (D-20: `dashboard.empty.body`, `dashboard.emptyFiltered.body`). Verify parity via a set-diff check.

Purpose: Without this plan, changing the language to DE shows a broken page with EN fallback strings. This satisfies SC3 ("The Settings page UI ... is fully translated in both DE and EN locale files").

Output: Complete `de.json` with full key parity, informal tone, loanwords preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-i18n-integration-and-polish/07-CONTEXT.md
@.planning/phases/07-i18n-integration-and-polish/07-RESEARCH.md
@frontend/src/locales/en.json
@frontend/src/locales/de.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate all missing keys to DE with informal 'du' tone</name>
  <files>frontend/src/locales/de.json</files>
  <read_first>
    - frontend/src/locales/en.json (source of truth — read AFTER plan 07-03 lands so the 4 new settings.preferences.* keys are present)
    - frontend/src/locales/de.json (current 64-line file with 62 keys — preserve existing translations where already "du", rewrite 2 formal-Sie entries)
    - .planning/phases/07-i18n-integration-and-polish/07-CONTEXT.md (D-16, D-17, D-18, D-19, D-20)
    - .planning/phases/07-i18n-integration-and-polish/07-RESEARCH.md (§Locale Key Additions for the 4 new keys' reference DE translation; Pitfall 7 — the 2 specific formal-Sie strings to fix)
  </read_first>
  <action>
    Step 1 — Before any edits, run:
    ```bash
    grep -nE "\\b(Sie|Ihre|Ihnen|Ihrem|Ihren)\\b" frontend/src/locales/de.json
    ```
    This should surface at least:
    - `dashboard.empty.body` — "Laden Sie eine Umsatzdatei ... um Ihre KPIs zu sehen"
    - `dashboard.emptyFiltered.body` — "Wählen Sie einen größeren Zeitraum ..."
    If any additional Sie/Ihre/Ihnen matches appear in the existing 62 keys, they must ALSO be rewritten to "du" form in this task.

    Step 2 — Compute the key delta. Running:
    ```bash
    python3 -c "
    import json
    e = json.load(open('frontend/src/locales/en.json'))
    d = json.load(open('frontend/src/locales/de.json'))
    missing = sorted(set(e) - set(d))
    print('\\n'.join(missing))
    "
    ```
    produces the list of keys to add. After plan 07-03 lands, this is approximately 49 keys (43 pre-existing `settings.*` from Phase 6 + 4 new `settings.preferences.*` from plan 07-03, plus any other gaps). Do NOT hardcode the count — derive it from the actual diff.

    Step 3 — Translate every missing key. Apply these rules verbatim from CONTEXT.md:

    - **Tone (D-17):** informal "du" throughout. Use "Wähle", "Deine Einstellungen", "Möchtest du", "Lade ... hoch". Never "Sie", "Ihre", "Ihnen".
    - **Loanwords (D-18):**
      - "Dashboard" → Dashboard (not Übersicht)
      - "Upload" (noun / section label) → Upload
      - "Upload" (verb) → hochladen
      - "KPI" → KPI (not Kennzahl)
      - "Logo" → Logo
      - "Settings" → Einstellungen
      - "Primary color" → Primärfarbe (preferred over Hauptfarbe for technical precision)
      - "Save" → Speichern
      - "Discard" → Verwerfen
      - "Reset to defaults" → Auf Standard zurücksetzen
      - "Contrast" → Kontrast
    - **Required specific translations (from Research §Locale Key Additions and CONTEXT.md Discretion)**:
      - `settings.preferences.title`: `"Allgemein"` (user's explicit preference — not Präferenzen)
      - `settings.preferences.language.label`: `"Sprache"`
      - `settings.preferences.language.help`: `"Gilt für die gesamte App. Deine Auswahl wird in der Datenbank gespeichert."`
      - `settings.preferences.toggle_disabled_tooltip`: `"Speichere oder verwirf zuerst deine Änderungen"`
    - **Fix the two pre-existing formal-Sie strings (D-20 / Pitfall 7):**
      - `dashboard.empty.body` → `"Lade eine Umsatzdatei auf der Upload-Seite hoch, um deine KPIs zu sehen."`
      - `dashboard.emptyFiltered.body` → `"Wähle einen größeren Zeitraum oder einen der voreingestellten Bereiche."`
    - **Toasts and dialogs (D-19):** every toast (`settings.toasts.saved`, `settings.toasts.save_error`, etc.), every dialog title/body (`settings.unsavedChanges.*`, `settings.reset.*`), every error/aria/placeholder/label must be translated. No EN-only strings remaining.

    Step 4 — Write the full `de.json` as a flat JSON object. Preserve existing keys that are already in "du" form verbatim. The final file must have the same set of keys as `en.json`. Do not introduce nesting — `keySeparator: false` in `i18n.ts` means flat dot-separated keys. Do not reorder existing keys unnecessarily; add new keys at the bottom or group by semantic prefix (settings.*, dashboard.*, upload.*), but be consistent.

    Step 5 — Self-verify before finishing:
    ```bash
    python3 -c "
    import json
    e = set(json.load(open('frontend/src/locales/en.json')))
    d = set(json.load(open('frontend/src/locales/de.json')))
    diff = e ^ d
    assert not diff, f'Key parity mismatch: {sorted(diff)}'
    print(f'OK — {len(e)} keys in both')
    "
    ```
    AND:
    ```bash
    grep -nE "\\b(Sie|Ihre|Ihnen|Ihrem|Ihren)\\b" frontend/src/locales/de.json && echo FAIL || echo OK
    ```
    Both must succeed (parity OK, no formal Sie remaining).

    Do NOT modify `en.json` in this plan — it is the source of truth. The only keys added to `en.json` in Phase 7 are the 4 added by plan 07-03.
  </action>
  <verify>
    <automated>python3 -c "
import json
e = set(json.load(open('frontend/src/locales/en.json')))
d = set(json.load(open('frontend/src/locales/de.json')))
diff = e ^ d
assert not diff, f'Mismatch: {sorted(diff)}'
print(f'OK {len(e)} keys')
" && ! grep -nE "\b(Sie|Ihre|Ihnen|Ihrem|Ihren)\b" frontend/src/locales/de.json</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; e=set(json.load(open('frontend/src/locales/en.json'))); d=set(json.load(open('frontend/src/locales/de.json'))); assert e==d, sorted(e^d)"` exits 0
    - `grep -nE "\\b(Sie|Ihre|Ihnen|Ihrem|Ihren)\\b" frontend/src/locales/de.json` exits NON-zero (no formal Sie remains)
    - `grep -q '"settings.preferences.title":\\s*"Allgemein"' frontend/src/locales/de.json` exits 0
    - `grep -q '"settings.preferences.language.label":\\s*"Sprache"' frontend/src/locales/de.json` exits 0
    - `grep -q '"settings.preferences.toggle_disabled_tooltip":\\s*"Speichere oder verwirf zuerst deine Änderungen"' frontend/src/locales/de.json` exits 0
    - `grep -q '"dashboard.empty.body":' frontend/src/locales/de.json && ! grep -E '"dashboard.empty.body":.*\bSie\b' frontend/src/locales/de.json` exits 0 (rewritten to du)
    - `grep -q "Dashboard" frontend/src/locales/de.json` exits 0 (loanword preserved — D-18)
    - `grep -q "KPI" frontend/src/locales/de.json` exits 0 (loanword preserved — D-18)
    - `cd frontend && npx tsc -b` exits 0 (JSON imports still valid)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>de.json has full parity with en.json, 100% informal "du" tone, D-18 loanwords preserved, 2 pre-existing formal-Sie strings rewritten. Parity check passes.</done>
</task>

</tasks>

<verification>
- Parity check: `python3` set symmetric-difference is empty
- Tone check: no Sie/Ihre/Ihnen matches remain
- Build: `npm run build` passes
- Visual (rolled into plan 07-06 Playwright spec): `/settings` page renders all labels in German when language = DE
</verification>

<success_criteria>
SC3 (Settings page fully translated in DE and EN) satisfied:
1. Key set equality between en.json and de.json
2. Informal "du" tone throughout
3. Loanwords preserved (Dashboard, Upload noun, KPI, Logo)
4. Pre-existing formal-Sie strings rewritten
</success_criteria>

<output>
After completion, create `.planning/phases/07-i18n-integration-and-polish/07-05-SUMMARY.md`
</output>
