---
phase: 30.1-rebrand-kpi-light-to-kpi-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/defaults.py
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/security/auth.py
  - backend/alembic/versions/v1_11_rename_app_name.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "DEFAULT_SETTINGS.app_name equals 'KPI Dashboard' in backend/app/defaults.py"
    - "FastAPI OpenAPI title renders as 'KPI Dashboard' at /api/docs"
    - "Running 'alembic upgrade head' updates app_settings rows where app_name='KPI Light' to 'KPI Dashboard'"
    - "Alembic migration chain points from v1_11_rename_app_name.down_revision='v1_11_app_users' with no broken chain"
    - "Dex client id 'kpi-light' in backend/app/config.py is unchanged (internal identifier, D-01)"
  artifacts:
    - path: "backend/app/defaults.py"
      provides: "Updated default app_name"
      contains: "KPI Dashboard"
    - path: "backend/alembic/versions/v1_11_rename_app_name.py"
      provides: "Data migration renaming existing default row"
      contains: "UPDATE app_settings"
    - path: "backend/app/main.py"
      provides: "FastAPI instance with display title"
      contains: 'title="KPI Dashboard"'
  key_links:
    - from: "backend/alembic/versions/v1_11_rename_app_name.py"
      to: "v1_11_app_users"
      via: "down_revision chain"
      pattern: 'down_revision\s*=\s*["'"'"']v1_11_app_users["'"'"']'
---

<objective>
Rename display surfaces in the backend from "KPI Light" to "KPI Dashboard" (per D-01, D-04, D-05) without touching internal identifiers.

Purpose: The FastAPI-exposed OpenAPI title and the DEFAULT_SETTINGS value are the user-visible "product name" surfaces from the backend. Existing installs carrying the default row get upgraded via an Alembic data migration chained from the current head `v1_11_app_users`.
Output: 5 files modified/created; 4 atomic commits (one per logical change per D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30.1-rebrand-kpi-light-to-kpi-dashboard/30.1-CONTEXT.md
@backend/app/defaults.py
@backend/app/main.py
@backend/app/config.py
@backend/app/security/auth.py
@backend/alembic/versions/v1_11_app_users.py

<interfaces>
Current Alembic head: `v1_11_app_users` (from backend/alembic/versions/v1_11_app_users.py line 11 `revision = "v1_11_app_users"`).

Migration filename + revision convention (observed from v1_11_app_users.py):
  revision = "<snake_case>"
  down_revision = "<parent_revision>"
  branch_labels = None
  depends_on = None

Internal identifiers that MUST NOT change (from backend/app/config.py):
  - DEX_CLIENT_ID = "kpi-light"      (Dex OIDC client id, D-01)
  - validation_alias = "DEX_KPI_SECRET"  (env var name, D-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DEFAULT_SETTINGS.app_name in backend/app/defaults.py</name>
  <files>backend/app/defaults.py</files>
  <action>
    Exact edit — backend/app/defaults.py line 20:
      BEFORE: `    "app_name": "KPI Light",`
      AFTER:  `    "app_name": "KPI Dashboard",`

    Do NOT touch: docstring, other keys, the `Final[dict[str, str]]` typing.

    After the edit, commit with message exactly:
      `feat(30.1): rebrand backend default app_name KPI Light → KPI Dashboard`

    Use `node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "<msg>" --files backend/app/defaults.py` (per D-05 one commit per logical file change).
  </action>
  <verify>
    <automated>grep -q '"app_name": "KPI Dashboard"' backend/app/defaults.py &amp;&amp; ! grep -q "KPI Light" backend/app/defaults.py</automated>
  </verify>
  <done>backend/app/defaults.py contains "KPI Dashboard" and zero "KPI Light" occurrences; dedicated commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Update FastAPI title in backend/app/main.py</name>
  <files>backend/app/main.py</files>
  <action>
    Exact edit — backend/app/main.py line 16:
      BEFORE: `app = FastAPI(title="KPI Light", lifespan=lifespan)`
      AFTER:  `app = FastAPI(title="KPI Dashboard", lifespan=lifespan)`

    Do NOT touch: imports, SessionMiddleware config (session_cookie="kpi_session" stays — that is an internal cookie name, per D-01), router includes, /health handler.

    Commit with message exactly:
      `feat(30.1): rebrand FastAPI OpenAPI title KPI Light → KPI Dashboard`
    Commit only backend/app/main.py.
  </action>
  <verify>
    <automated>grep -q 'title="KPI Dashboard"' backend/app/main.py &amp;&amp; ! grep -q "KPI Light" backend/app/main.py &amp;&amp; grep -q 'session_cookie="kpi_session"' backend/app/main.py</automated>
  </verify>
  <done>main.py FastAPI title is "KPI Dashboard"; internal cookie name "kpi_session" preserved (D-01 negative assertion); commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Update docstrings in backend/app/config.py and backend/app/security/auth.py</name>
  <files>backend/app/config.py, backend/app/security/auth.py</files>
  <action>
    Two exact edits, one commit (both are narrative docstring tweaks of the same class — the "Phase 28 auth surface").

    Edit A — backend/app/config.py line 1:
      BEFORE: `"""Pydantic v2 BaseSettings for KPI Light API (Phase 28).`
      AFTER:  `"""Pydantic v2 BaseSettings for KPI Dashboard API (Phase 28).`

    Do NOT touch: `DEX_CLIENT_ID: str = "kpi-light"` (line 16) or the `validation_alias="DEX_KPI_SECRET"` (line 17). These are internal identifiers per D-01.

    Edit B — backend/app/security/auth.py line 1:
      BEFORE: `"""OIDC (Dex) auth primitives for KPI Light (Phase 28).`
      AFTER:  `"""OIDC (Dex) auth primitives for KPI Dashboard (Phase 28).`

    Do NOT touch the rest of the file.

    Commit message exactly:
      `docs(30.1): rebrand backend auth docstrings KPI Light → KPI Dashboard`
    Commit both files together (single logical change — per-module docstrings for the same feature).
  </action>
  <verify>
    <automated>! grep -q "KPI Light" backend/app/config.py &amp;&amp; ! grep -q "KPI Light" backend/app/security/auth.py &amp;&amp; grep -q 'DEX_CLIENT_ID: str = "kpi-light"' backend/app/config.py &amp;&amp; grep -q 'validation_alias="DEX_KPI_SECRET"' backend/app/config.py</automated>
  </verify>
  <done>config.py + auth.py docstrings rebranded; internal identifiers `kpi-light` (Dex client) and `DEX_KPI_SECRET` (env alias) grep-asserted unchanged per D-01; commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Create Alembic migration v1_11_rename_app_name.py</name>
  <files>backend/alembic/versions/v1_11_rename_app_name.py</files>
  <action>
    Create a new Alembic migration file chained AFTER the current head `v1_11_app_users` (verified via `grep -r "^revision = " backend/alembic/versions/ | grep -c v1_11_app_users` → 1 hit in v1_11_app_users.py).

    Exact file content (copy verbatim, including triple-quoted docstring and blank lines):

    ```python
    """v1.11: rename default app_settings.app_name KPI Light → KPI Dashboard (Phase 30.1, D-04).

    Revision ID: v1_11_rename_app_name
    Revises: v1_11_app_users
    Create Date: 2026-04-15
    """
    from alembic import op


    revision = "v1_11_rename_app_name"
    down_revision = "v1_11_app_users"
    branch_labels = None
    depends_on = None


    def upgrade() -> None:
        op.execute(
            """
            UPDATE app_settings
            SET app_name = 'KPI Dashboard'
            WHERE app_name = 'KPI Light'
            """
        )


    def downgrade() -> None:
        op.execute(
            """
            UPDATE app_settings
            SET app_name = 'KPI Light'
            WHERE app_name = 'KPI Dashboard'
            """
        )
    ```

    Rationale the `WHERE` clause is narrow per D-04: operators who customized app_name (e.g. self-branded) are untouched.

    Commit message exactly:
      `feat(30.1): migrate app_settings.app_name default to KPI Dashboard`
    Commit only the new migration file.
  </action>
  <verify>
    <automated>test -f backend/alembic/versions/v1_11_rename_app_name.py &amp;&amp; grep -q 'revision = "v1_11_rename_app_name"' backend/alembic/versions/v1_11_rename_app_name.py &amp;&amp; grep -q 'down_revision = "v1_11_app_users"' backend/alembic/versions/v1_11_rename_app_name.py &amp;&amp; grep -q "SET app_name = 'KPI Dashboard'" backend/alembic/versions/v1_11_rename_app_name.py &amp;&amp; grep -q "SET app_name = 'KPI Light'" backend/alembic/versions/v1_11_rename_app_name.py</automated>
  </verify>
  <done>Migration file exists, chains from v1_11_app_users, upgrade sets "KPI Dashboard", downgrade restores "KPI Light"; commit landed.</done>
</task>

</tasks>

<verification>
- `! grep -rn "KPI Light" backend/app/` returns zero hits (docstrings + FastAPI title + defaults all rebranded).
- `grep -rn "kpi-light" backend/app/config.py` still returns the Dex `DEX_CLIENT_ID` line (internal ID preserved, D-01).
- `docker compose run --rm migrate alembic heads` (if operator runs it) lists `v1_11_rename_app_name` as the single head.
</verification>

<success_criteria>
After this plan:
1. Fresh installs default to `app_name = "KPI Dashboard"` (defaults.py).
2. Existing installs get their app_settings row rewritten on next `alembic upgrade head`.
3. OpenAPI docs at https://kpi.internal/api/docs display "KPI Dashboard" as the API title.
4. Four atomic commits land (one per logical file change per D-05).
5. Zero internal identifier changed (Dex client id, env var name, cookie name).
</success_criteria>

<output>
After completion, create `.planning/phases/30.1-rebrand-kpi-light-to-kpi-dashboard/30.1-01-SUMMARY.md` capturing:
- The 4 commit hashes
- A copy of the new migration's revision/down_revision chain
- Grep audit results confirming `backend/app/` is clean of "KPI Light" and still carries `kpi-light` as Dex client id
</output>
