---
phase: 30.1-rebrand-kpi-light-to-kpi-dashboard
plan: 03
type: execute
wave: 2
depends_on: [30.1-01, 30.1-02]
files_modified:
  - README.md
  - CLAUDE.md
  - docs/setup.md
  - docker-compose.yml
  - .env.example
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/phases/31-seed-outline-docs/31-CONTEXT.md
  - .planning/phases/31-seed-outline-docs/31-01-PLAN.md
  - .planning/phases/31-seed-outline-docs/31-02-PLAN.md
  - .planning/phases/31-seed-outline-docs/31-03-PLAN.md
  - .planning/phases/31-seed-outline-docs/31-04-PLAN.md
  - .planning/phases/31-seed-outline-docs/31-05-PLAN.md
  - .planning/phases/31-seed-outline-docs/31-06-PLAN.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "README.md title, description, and all narrative prose say 'KPI Dashboard' (not 'KPI Light') — EXCEPT the 'Version History' table and any historical/archival reference, which remain untouched per D-02"
    - "CLAUDE.md '## Project' section title reads '**KPI Dashboard**'"
    - "docs/setup.md narrative references use 'KPI Dashboard'; hostnames (kpi.internal), service names (api, frontend, dex, outline), env var names (DEX_KPI_SECRET), cookie names (kpi_session), and Dex client id ('kpi-light') are ALL preserved (D-01)"
    - "docker-compose.yml narrative comments updated; NO service name or image name changed"
    - ".env.example narrative comments say 'KPI Dashboard'; variable NAMES (DEX_KPI_SECRET, etc.) preserved"
    - ".planning/PROJECT.md title is 'KPI Dashboard' and contains the sentence 'Formerly called KPI Light (renamed 2026-04-15, Phase 30.1)' in the Core Value / description area per D-05"
    - ".planning/ROADMAP.md Phase 31 entry references 'KPI Dashboard collection' (D-06 exception to D-02)"
    - "Six Phase 31 PLAN/CONTEXT files reference 'KPI Dashboard collection' where Phase 31 prose talks about the Outline COLLECTION name (D-06)"
    - "Historical prose in .planning/phases/*/ pre-30.1 artifacts is NOT modified (D-02)"
    - "Repo directory name 'acm-kpi-light' and compose project name are NOT changed (D-01)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Living project identity doc — authoritative name source"
      contains: "Formerly called KPI Light"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 31 entry rebranded for upcoming collection authoring"
      contains: "KPI Dashboard"
    - path: "README.md"
      provides: "Repo-level product identity"
      contains: "# KPI Dashboard"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "all current references to product name"
      via: "rename-note sentence"
      pattern: "Formerly called KPI Light.*Phase 30\\.1"
---

<objective>
Final wave of Phase 30.1: update human-facing docs, the living PROJECT.md, and the (not-yet-executed) Phase 31 artifacts.

Purpose: Close out the rename so that new readers landing on README/CLAUDE/setup.md see "KPI Dashboard" consistently, and so that Phase 31 (next to execute) authors a "KPI Dashboard" Outline collection rather than a "KPI Light" one. Respect the D-02 historical-record rule: `.planning/phases/*/` for PAST phases stays frozen; only Phase 31 artifacts and `PROJECT.md` get touched (D-02 exception + D-06).
Output: ~14 files modified, ~7 atomic commits (grouped by logical domain per D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30.1-rebrand-kpi-light-to-kpi-dashboard/30.1-CONTEXT.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@README.md
@CLAUDE.md
@docs/setup.md
@docker-compose.yml
@.env.example
@.planning/phases/31-seed-outline-docs/31-CONTEXT.md
@.planning/phases/31-seed-outline-docs/31-01-PLAN.md

<interfaces>
Scope boundary (D-01 untouchables — MUST appear unchanged after this plan):
  - Hostname:     `kpi.internal`
  - Dex client:   `kpi-light` (string literal in dex config, env var names, runbook narrative describing the client id)
  - Env var name: `DEX_KPI_SECRET`
  - Cookie name:  `kpi_session`
  - Compose services: `db`, `migrate`, `api`, `frontend`, `npm`, `dex`, `outline`, `outline-db`, `outline-redis`
  - Image names:  `postgres:17-alpine`, `jc21/nginx-proxy-manager:2.11.3`, `ghcr.io/dexidp/dex:v2.43.0`, `outlinewiki/outline:0.86.0`
  - Repo dir:     `acm-kpi-light` (never edited — would change compose project name)

D-02 exception: `.planning/PROJECT.md` IS updated (living doc). Past-phase artifacts under `.planning/phases/{1..30}/**` and `.planning/REQUIREMENTS.md` + ROADMAP Progress table rows for shipped phases are NOT touched.

D-06 exception (to D-02): Phase 31 CONTEXT/plans + Phase 31 entry in ROADMAP.md ARE updated. Phase 31 hasn't run yet; the collection doesn't exist; no history to preserve.

Canonical rename-note sentence for PROJECT.md (per D-05, exact wording):
  `Formerly called KPI Light (renamed 2026-04-15, Phase 30.1).`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand README.md narrative — preserve Version History</name>
  <files>README.md</files>
  <action>
    Rewrite display-surface occurrences of "KPI Light" → "KPI Dashboard" in README.md while preserving the historical "Version History" table.

    Exact substitutions (grep-driven — verify each before editing):
      - Line 1:  `# KPI Light`  →  `# KPI Dashboard`
      - Line 37: `| \`https://kpi.internal\`   | KPI Light frontend + \`/api/*\`              | Phase 26  |` →
                 `| \`https://kpi.internal\`   | KPI Dashboard frontend + \`/api/*\`              | Phase 26  |`

    DO NOT touch:
      - Hostname `kpi.internal` (D-01)
      - The "## Version History" table (historical record — D-02 spirit applied to the README)
      - The "kpi-light/" ASCII project structure diagram on line ~146 (that's the repo dir name `acm-kpi-light` rendered without the `acm-` prefix — it IS a display surface but renaming would falsely imply the repo was renamed). If a human author wants to clean it up later, that's a follow-up. Leave it.

    Run final grep after edits:
      `grep -n "KPI Light" README.md`
    Expected remaining hits: 0 in prose, zero tolerated in title/description. If the "kpi-light/" dir diagram line also matches — leave it (internal identifier per D-01).

    Commit message:
      `docs(30.1): rebrand README.md KPI Light → KPI Dashboard`
  </action>
  <verify>
    <automated>head -1 README.md | grep -q "^# KPI Dashboard$" &amp;&amp; ! grep -q "KPI Light frontend" README.md &amp;&amp; grep -q "kpi.internal" README.md</automated>
  </verify>
  <done>README title is "KPI Dashboard", line 37 hostname table rebranded, kpi.internal preserved, commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Rebrand CLAUDE.md project line</name>
  <files>CLAUDE.md</files>
  <action>
    Exact edit — CLAUDE.md "## Project" section (line 4):
      BEFORE: `**KPI Light**`
      AFTER:  `**KPI Dashboard**`

    DO NOT touch: anything else in CLAUDE.md. Tech Stack section, conventions, Docker Compose pattern, GSD Workflow Enforcement — all unchanged.

    Verify there are no other "KPI Light" hits:
      `grep -n "KPI Light" CLAUDE.md`
    Expected: zero hits after edit.

    Commit message:
      `docs(30.1): rebrand CLAUDE.md project name KPI Light → KPI Dashboard`
  </action>
  <verify>
    <automated>grep -q "^\*\*KPI Dashboard\*\*$" CLAUDE.md &amp;&amp; ! grep -q "KPI Light" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md project name rebranded; zero remaining "KPI Light" occurrences; commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Rebrand docs/setup.md narrative — preserve hostnames, service names, env vars</name>
  <files>docs/setup.md</files>
  <action>
    Rewrite narrative prose in docs/setup.md. "KPI Light" appears 10+ times across lines 1, 44, 70, 234, 273, 438, 442, 444, 448, 479, 487, 536, 543, 544, 637.

    Strategy: do a review-then-edit pass. For EACH line, apply the rename UNLESS it references:
      - An internal identifier (hostname, env var name, cookie name, client id, service name).
      - Historical phrasing that would become factually wrong (e.g. "Phase 28 — KPI Light login via Dex" — Phase 28 really happened under the old name. Keeping the phase heading historical is consistent with the spirit of D-02).

    Recommended edits (review each line in context):
      - Line 1:    `# KPI Light — Setup Runbook` → `# KPI Dashboard — Setup Runbook`
      - Line 44:   `KPI Light is served at three internal hostnames…` → `KPI Dashboard is served at three internal hostnames…`
      - Line 70:   table row `KPI Light frontend (/)` → `KPI Dashboard frontend (/)`
      - Line 234:  `Expected: KPI Light dashboard renders…` → `Expected: KPI Dashboard renders…`
      - Line 273:  `Dex is the OIDC identity provider that KPI Light and (later) Outline wiki both trust.` → `Dex is the OIDC identity provider that KPI Dashboard and (later) Outline wiki both trust.`
      - Line 437:  `… downstream apps (KPI Light in Phase 28, Outline in Phase 29) …` →
                   `… downstream apps (KPI Dashboard in Phase 28, Outline in Phase 29) …`
      - Line 438:  `… each OIDC client (KPI Light, Outline) re-prompts …` → `… each OIDC client (KPI Dashboard, Outline) re-prompts …`
      - Line 442 HEADING:  `## Phase 28 — KPI Light login via Dex` — KEEP as-is (historical phase heading, D-02 spirit). OR rename to `KPI Dashboard`. Executor's call — prefer rename for consistency with the rest of the file; this is a runbook, not a phase artifact.
      - Line 444:  `for KPI Light's OIDC integration` → `for KPI Dashboard's OIDC integration`
      - Line 448:  `KPI Light signs its session cookie` → `KPI Dashboard signs its session cookie`
      - Line 479:  `you should see the KPI Light splash…` → `you should see the KPI Dashboard splash…`
      - Line 487:  `KPI Light's logout only clears its own kpi_session cookie… click **Log out** in KPI Light …` → replace product-name occurrences but KEEP `kpi_session` literal (it's the cookie name, D-01).
      - Line 536:  `… Phases 26 … 27 … and 28 (KPI Light OIDC) green …` → `… Phases 26 … 27 … and 28 (KPI Dashboard OIDC) green …`
      - Line 543:  `SESSION_SECRET — KPI Light session-cookie secret (Phase 28)` → `SESSION_SECRET — KPI Dashboard session-cookie secret (Phase 28)`
      - Line 544:  `POSTGRES_PASSWORD — KPI Light DB password (Phase 26)` → `POSTGRES_PASSWORD — KPI Dashboard DB password (Phase 26)`
      - Line 637:  `Log out in Outline does NOT log the user out of KPI Light (and vice versa)` → `Log out in Outline does NOT log the user out of KPI Dashboard (and vice versa)`

    NEGATIVE assertions after edit:
      - `grep -n "kpi.internal" docs/setup.md` — unchanged count (every instance of the hostname preserved).
      - `grep -n "kpi_session" docs/setup.md` — unchanged count (cookie name preserved).
      - `grep -n "DEX_KPI_SECRET" docs/setup.md` — unchanged count (env var preserved).
      - `grep -n "client_id=kpi-light" docs/setup.md` — unchanged (Dex client id preserved, line 341).
      - `grep -n "KPI Light" docs/setup.md` — zero hits.

    Commit message:
      `docs(30.1): rebrand docs/setup.md narrative KPI Light → KPI Dashboard`
  </action>
  <verify>
    <automated>! grep -q "KPI Light" docs/setup.md &amp;&amp; grep -q "kpi.internal" docs/setup.md &amp;&amp; grep -q "kpi_session" docs/setup.md &amp;&amp; grep -q "DEX_KPI_SECRET" docs/setup.md &amp;&amp; grep -q "client_id=kpi-light" docs/setup.md &amp;&amp; head -1 docs/setup.md | grep -q "KPI Dashboard"</automated>
  </verify>
  <done>Zero "KPI Light" occurrences in docs/setup.md; all five internal-identifier negative assertions pass (hostname, cookie, env var, Dex client id, compose service names untouched); commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Rebrand narrative comments in docker-compose.yml and .env.example</name>
  <files>docker-compose.yml, .env.example</files>
  <action>
    Neither file contains display-surface "KPI Light" occurrences in service/image/variable definitions. Only comments reference the product name.

    Grep first:
      `grep -n "KPI Light" docker-compose.yml .env.example`

    Expected (from audit 2026-04-15):
      - .env.example:28 `# --- KPI Light API auth (Phase 28) ---`
      - .env.example:46 `# POSTGRES_PASSWORD which belongs to KPI Light's \`db\` service).`
      - .env.example:51 `# --- KPI Light frontend (Phase 30) ---`
      - docker-compose.yml — grep should return 0 hits currently (no "KPI Light" comments — confirm)

    Edits for .env.example:
      - Line 28: `# --- KPI Light API auth (Phase 28) ---` → `# --- KPI Dashboard API auth (Phase 28) ---`
      - Line 46: `# POSTGRES_PASSWORD which belongs to KPI Light's \`db\` service).` →
                 `# POSTGRES_PASSWORD which belongs to KPI Dashboard's \`db\` service).`
      - Line 51: `# --- KPI Light frontend (Phase 30) ---` → `# --- KPI Dashboard frontend (Phase 30) ---`

    Edits for docker-compose.yml:
      - If grep returned 0 hits, skip. If hits exist (future audit drift), rename comment-only occurrences; NEVER rename `DEX_KPI_SECRET`, service names (`api`, `frontend`, `db`, `dex`, etc.), or image tags (`kpi-light` substring does NOT appear in images; image is compose-project-derived `acm-kpi-light-api`/etc. which is NEVER hand-edited in this file).

    Negative assertions:
      - `grep -n "DEX_KPI_SECRET" .env.example` — unchanged hit count.
      - `grep -n "DEX_KPI_SECRET" docker-compose.yml` — unchanged hit count.
      - No service/image/variable NAME modified.

    Single commit (both files are "config narrative — comments only"):
      `docs(30.1): rebrand compose/env narrative comments KPI Light → KPI Dashboard`
  </action>
  <verify>
    <automated>! grep -q "KPI Light" .env.example &amp;&amp; ! grep -q "KPI Light" docker-compose.yml &amp;&amp; grep -q "DEX_KPI_SECRET" .env.example &amp;&amp; grep -q "DEX_KPI_SECRET" docker-compose.yml &amp;&amp; grep -q "image: postgres:17-alpine" docker-compose.yml</automated>
  </verify>
  <done>Zero "KPI Light" strings in either file; DEX_KPI_SECRET env var and all image pins unchanged; commit landed.</done>
</task>

<task type="auto">
  <name>Task 5: Update .planning/ROADMAP.md Phase 30.1 entry + Phase 31 entry (D-06 exception)</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Make TWO narrow edits to ROADMAP.md.

    Edit A — Phase 30.1 entry (currently a placeholder under "### Phase 30.1: Rebrand KPI Light to KPI Dashboard (INSERTED)"):
      - Keep the heading literal `### Phase 30.1: Rebrand KPI Light to KPI Dashboard (INSERTED)` unchanged (it IS a historical record of what the phase was named).
      - Update the `**Goal:**` line from `[Urgent work - to be planned]` to:
        `Rename human-facing product name KPI Light → KPI Dashboard (display only — internal identifiers unchanged per D-01).`
      - Update `**Requirements**: TBD` → `**Requirements**: none (mechanical rename; no ROADMAP requirements)`.
      - Update `**Plans:** 0 plans` → `**Plans:** 3 plans`.
      - Replace the placeholder "Plans: - [ ] TBD (run /gsd:plan-phase 30.1 to break down)" with:
        ```
        Plans:
        - [ ] 30.1-01-PLAN.md — Backend rename + Alembic migration (defaults, config, auth docstrings, main.py FastAPI title)
        - [ ] 30.1-02-PLAN.md — Frontend i18n values + defaults.ts
        - [ ] 30.1-03-PLAN.md — Docs + PROJECT.md rename-note + Phase 31 fix-up
        ```

    Edit B — Phase 31 entry (D-06 exception to D-02, because Phase 31 has not yet executed):
      - Under `### Phase 31: Seed Outline Docs` → `**Goal**: …`, rewrite:
        - `The "KPI Light" collection` → `The "KPI Dashboard" collection`
      - Under `**Success Criteria**`:
        - #1: `The "KPI Light" collection exists in Outline …` → `The "KPI Dashboard" collection exists in Outline …`
      - Keep all phase numbers (31-01-PLAN.md through 31-06-PLAN.md), requirement IDs (DOC-01..09, WMP-01..03, E2E-01..05), and plan descriptions textually exact — just rewrite the collection name where it appears.
      - Update the Progress table row for Phase 30.1 if present (add `| 30.1 | v1.11 | 3/3 | Planned | - |` or similar in keeping with the table format — executor verifies current row and updates `Plans Complete` to `0/3`, `Status` to `In progress` or whatever the current convention is).

    Negative assertions:
      - ROADMAP.md `## Phases` nested `<details>` blocks for v1.0..v1.10 milestones MUST NOT change. Only the in-progress v1.11 section entries (Phase 30.1 and Phase 31) are edited.
      - The Progress table row for Phase 30 (line ~280 `| 30 | v1.11 | 1/1 | Complete    | 2026-04-15 |`) MUST NOT change.

    Commit message:
      `docs(30.1): update ROADMAP Phase 30.1 plan + Phase 31 collection name`
  </action>
  <verify>
    <automated>grep -q "Phase 30.1: Rebrand KPI Light to KPI Dashboard" .planning/ROADMAP.md &amp;&amp; grep -q "30.1-01-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "30.1-02-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "30.1-03-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q '"KPI Dashboard" collection' .planning/ROADMAP.md &amp;&amp; ! grep -q '"KPI Light" collection' .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase 30.1 entry reflects 3 plans with actual filenames; Phase 31 goal + SC #1 say "KPI Dashboard collection"; pre-v1.11 milestone sections untouched; commit landed.</done>
</task>

<task type="auto">
  <name>Task 6: Update Phase 31 CONTEXT + all six 31-NN-PLAN.md files (D-06 exception)</name>
  <files>.planning/phases/31-seed-outline-docs/31-CONTEXT.md, .planning/phases/31-seed-outline-docs/31-01-PLAN.md, .planning/phases/31-seed-outline-docs/31-02-PLAN.md, .planning/phases/31-seed-outline-docs/31-03-PLAN.md, .planning/phases/31-seed-outline-docs/31-04-PLAN.md, .planning/phases/31-seed-outline-docs/31-05-PLAN.md, .planning/phases/31-seed-outline-docs/31-06-PLAN.md</files>
  <action>
    Per D-06, rewrite references to the Outline collection name from "KPI Light" → "KPI Dashboard" in Phase 31 planning artifacts (the collection doesn't exist yet; no history to preserve).

    Strategy — grep-audit then edit:
      1. Run: `grep -n "KPI Light" .planning/phases/31-seed-outline-docs/*.md`
      2. For each hit, classify:
         - CHANGE: references to the collection name / product name in Phase 31 prose context (scope, specifics, rationale, task actions, must-have truths, verify greps talking about "KPI Light collection", TOC entries saying "KPI Light" as the product name).
         - KEEP: None — per D-06, this phase's artifacts are fair game before execution. The only exception is if a file references a historical phase number + "KPI Light" in a way that's a statement about past work under the old name (e.g. "Phase 28 KPI Light OIDC integration"). If such phrasing appears, prefer to keep the historical form (mirrors D-02 spirit for consistency).
      3. For each CHANGE hit, substitute string-value `"KPI Light"` → `"KPI Dashboard"` and narrative `KPI Light` → `KPI Dashboard`.

    Specific known hits (from 2026-04-15 grep):
      - 31-CONTEXT.md line ~16: `Create the "KPI Light" collection in Outline` → `Create the "KPI Dashboard" collection in Outline`
      - 31-CONTEXT.md line ~102: `Collection name: \`KPI Light\`` → `Collection name: \`KPI Dashboard\``
      - 31-CONTEXT.md line ~112: `Post-v1.11 milestones re-export the KPI Light collection …` → `Post-v1.11 milestones re-export the KPI Dashboard collection …`
      - 31-01-PLAN.md line ~19: `The 'KPI Light' collection exists in Outline …` → `The 'KPI Dashboard' collection exists in Outline …`
      - 31-01-PLAN.md line ~84: `Empty "KPI Light" collection in Outline …` → `Empty "KPI Dashboard" collection in Outline …`
      - 31-01-PLAN.md line ~88: `       - Name: KPI Light` → `       - Name: KPI Dashboard`
      - 31-01-PLAN.md objectives paragraph mentioning "KPI Light"
      - 31-05-PLAN.md / 31-06-PLAN.md — similar collection-name references (executor greps and applies).

    Additional note for 31-01-PLAN.md `<interfaces>` URL hint (line ~63):
      `Outline collection target URL pattern after creation: https://wiki.internal/collection/kpi-light-<uuid>`
    This is Outline-generated slug produced from the collection name. When the collection is renamed, the slug becomes `kpi-dashboard-<uuid>`. Update this line accordingly:
      → `Outline collection target URL pattern after creation: https://wiki.internal/collection/kpi-dashboard-<uuid>`
    (This is predicted behavior — Outline auto-slugifies. Executor notes it in the Phase 31 CONTEXT / SUMMARY loop when the collection is actually created.)

    NEGATIVE assertions (keep unchanged):
      - Any mention of the REPO NAME `kpi-light` as a github-path-segment (e.g. `https://github.com/johann-b82/kpi-dashboard`) — that URL already uses "kpi-dashboard" per the 31-01-PLAN.md interfaces block. If there's a stale `kpi-light` URL anywhere it should be corrected independently — that's what's in scope.
      - Filenames of the Phase 31 PLAN files themselves remain `31-01-PLAN.md`..`31-06-PLAN.md` (no file renames).
      - Historical Phase references like "Phase 28 KPI Light OIDC integration" — keep as-is per D-02 spirit.
      - Any remaining `KPI Light` occurrence inside a quote from a past `.planning/phases/XX-*/SUMMARY.md` file being transcluded — keep historical.

    Commit message:
      `docs(30.1): rename Phase 31 artifacts Outline collection KPI Light → KPI Dashboard`
    Commit all seven files together (single logical change — "the Phase 31 fix-up per D-06").
  </action>
  <verify>
    <automated>! grep -qE '"KPI Light" collection|Collection name: `KPI Light`|re-export the KPI Light collection|The "KPI Light" collection exists' .planning/phases/31-seed-outline-docs/*.md &amp;&amp; grep -q '"KPI Dashboard" collection' .planning/phases/31-seed-outline-docs/31-01-PLAN.md &amp;&amp; grep -q 'Collection name: `KPI Dashboard`' .planning/phases/31-seed-outline-docs/31-CONTEXT.md</automated>
  </verify>
  <done>All explicit collection-name references in Phase 31 artifacts use "KPI Dashboard"; PLAN filenames preserved; historical-phase references intact; single commit landed.</done>
</task>

<task type="auto">
  <name>Task 7: Update .planning/PROJECT.md with new title + rename-note (final commit per D-05)</name>
  <files>.planning/PROJECT.md</files>
  <action>
    This is the FINAL commit of Phase 30.1 per D-05. It updates the living project identity doc.

    Edits to .planning/PROJECT.md:

    1. Line 1 (H1 title):
       BEFORE: `# KPI Light`
       AFTER:  `# KPI Dashboard`

    2. "## Core Value" section (line 7-9): append the rename-note sentence at the END of the Core Value paragraph. Use the exact canonical wording from D-05:
       Current ending: `... real ERP export (93 orders, €793k) → dashboard in under a minute, auto-refreshing on upload.`
       Add as a new sentence (same paragraph or new line):
       ` Formerly called KPI Light (renamed 2026-04-15, Phase 30.1).`

    3. Do a grep pass on the rest of PROJECT.md for any "KPI Light" occurrences in prose that are statements about the current product (not historical prose about past work). Executor reviews and rewrites contextually. As a heuristic:
       - KEEP: phrases like "v1.0 shipped with KPI Light's first upload" (historical narrative).
       - CHANGE: phrases like "**KPI Light**'s session cookie is …" (present-tense identity).
       - If a phrase is ambiguous, prefer CHANGE for sections that describe the current state (e.g. "Current State"), KEEP for shipped-version summaries in <details> blocks.

       Known current-state occurrences (from 2026-04-15 read):
         - Line 5: `… companion Outline wiki for developer + user documentation.` — no KPI Light mention; no edit needed.
         - Line 11 "Current Milestone: v1.11": `both KPI Light and Outline authenticate through it` → `both KPI Dashboard and Outline authenticate through it`
         - Line 18: `KPI Light SSO integration` → `KPI Dashboard SSO integration`
         - Line 20: `Seeded KPI Light docs` → `Seeded KPI Dashboard docs`
         - Line 24: `Auth is the risky bit — Dex ↔ Outline ↔ KPI Light is three integrations …` → `Auth is the risky bit — Dex ↔ Outline ↔ KPI Dashboard is three integrations …`
       Historical / shipped blocks (lines ~43 onwards in `<details>` blocks and version lists): KEEP AS-IS.

    4. Update the trailer line ("_Last updated: ..._"):
       `*Last updated: 2026-04-14 — v1.11 Phase 27 (dex-idp-setup) complete*`
       → `*Last updated: 2026-04-15 — v1.11 Phase 30.1 (rebrand KPI Light → KPI Dashboard) complete*`

    Commit message (the final, capstone commit of the phase):
      `docs(30.1): rebrand PROJECT.md title + rename-note (Phase 30.1 complete)`
    Commit only .planning/PROJECT.md.
  </action>
  <verify>
    <automated>head -1 .planning/PROJECT.md | grep -q "^# KPI Dashboard$" &amp;&amp; grep -q "Formerly called KPI Light (renamed 2026-04-15, Phase 30.1)" .planning/PROJECT.md &amp;&amp; grep -q "v1.11 Phase 30.1" .planning/PROJECT.md &amp;&amp; ! grep -qE "both KPI Light and Outline|KPI Light SSO integration" .planning/PROJECT.md</automated>
  </verify>
  <done>PROJECT.md title is "KPI Dashboard", rename-note present in Core Value section with exact D-05 wording, current-state prose rebranded, historical shipped-version blocks untouched, trailer date updated; final commit landed.</done>
</task>

</tasks>

<verification>
Run these end-to-end assertions after all 7 tasks (commands verify the complete Phase 30.1 invariants):

1. Display surfaces clean:
   `! grep -rq "KPI Light" frontend/src backend/app README.md CLAUDE.md docs/setup.md .env.example docker-compose.yml`
   (Phase 30.1 clean. Remaining "KPI Light" hits allowed ONLY in: .planning/phases/{1..30}/**, .planning/REQUIREMENTS.md, historical `<details>` blocks in PROJECT.md and shipped-version sections of ROADMAP.md.)

2. Internal identifiers preserved (D-01 guard):
   - `grep -q '"kpi-light"' backend/app/config.py` → PASS
   - `grep -q 'DEX_KPI_SECRET' .env.example docker-compose.yml` → PASS (both files)
   - `grep -q 'kpi_session' backend/app/main.py docs/setup.md` → PASS
   - `grep -q 'kpi.internal' docs/setup.md README.md docker-compose.yml` → PASS
   - `docker compose config --quiet` → exit 0 (compose file still valid)

3. ROADMAP + Phase 31 artifacts consistent:
   - `grep -q 'Plans: 3 plans' .planning/ROADMAP.md || grep -q '\*\*Plans:\*\* 3 plans' .planning/ROADMAP.md` → PASS (Phase 30.1)
   - `! grep -q '"KPI Light" collection' .planning/phases/31-seed-outline-docs/*.md` → PASS

4. PROJECT.md rename-note present + trailer updated.

5. Seven commits landed in git log with `30.1` scope prefix.
</verification>

<success_criteria>
Phase 30.1 completes when:
1. All display-surface references to "KPI Light" are rewritten to "KPI Dashboard" across README, CLAUDE.md, docs/setup.md, .env.example, docker-compose.yml, frontend/src (plan 02), backend/app (plan 01).
2. PROJECT.md (living doc) carries the canonical rename-note sentence in Core Value and has title "KPI Dashboard".
3. ROADMAP Phase 30.1 entry finalized with 3 plan filenames.
4. Phase 31 CONTEXT/plans/ROADMAP-entry reference "KPI Dashboard collection" (D-06 fix-up applied once, before Phase 31 runs).
5. Every internal identifier (hostname, Dex client, env vars, cookie name, compose services, image names, repo dir) is grep-asserted unchanged.
6. Post-plan acceptance tests (from CONTEXT):
   - `curl -s https://kpi.internal/api/settings | jq .app_name` → `"KPI Dashboard"` after `docker compose up` runs the new migration.
   - KPI Dashboard OIDC login flow still works end-to-end (Dex client id `kpi-light` unchanged means no Dex config regen needed).
   - `docker compose config --quiet` validates.
7. Seven atomic commits land in wave-2 order (README, CLAUDE, setup.md, compose+env, ROADMAP, Phase 31 artifacts, PROJECT.md final).
</success_criteria>

<output>
After completion, create `.planning/phases/30.1-rebrand-kpi-light-to-kpi-dashboard/30.1-03-SUMMARY.md` and a phase-level `.planning/phases/30.1-rebrand-kpi-light-to-kpi-dashboard/30.1-SUMMARY.md` capturing:
- Seven commit hashes (this plan) + four from plan 01 + two from plan 02 = 13 total.
- Full grep audit: "KPI Light" hits remaining in the repo, each annotated as historical (expected) or display (bug — fix before closing).
- Confirmation that `docker compose config --quiet` validates, `alembic heads` lists `v1_11_rename_app_name`, and `curl https://kpi.internal/api/settings` returns `app_name: "KPI Dashboard"` after operator restarts the stack.
- D-01 guard summary: bulleted list of internal identifiers (hostname, Dex client, env vars, cookie, services, image pins) each grep-confirmed preserved.
</output>
