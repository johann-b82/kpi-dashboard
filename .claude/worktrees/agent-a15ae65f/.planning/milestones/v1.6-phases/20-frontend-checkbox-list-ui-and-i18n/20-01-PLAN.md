---
phase: 20-frontend-checkbox-list-ui-and-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas.py
  - backend/app/routers/settings.py
  - frontend/src/lib/api.ts
  - frontend/src/hooks/useSettingsDraft.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
autonomous: true
requirements:
  - UI-02
  - UI-03

must_haves:
  truths:
    - "Backend personio-options endpoint returns skill_attributes string array alongside absence_types and departments"
    - "Frontend Settings/SettingsUpdatePayload interfaces use array types for the 3 Personio config fields"
    - "Draft state initializes from API arrays and round-trips correctly through save/reload"
    - "Empty array [] is sent when all checkboxes are deselected (not null or undefined)"
    - "All new i18n keys render in both DE and EN without missing-key fallback"
  artifacts:
    - path: "backend/app/schemas.py"
      provides: "PersonioOptions with skill_attributes field"
      contains: "skill_attributes: list[str]"
    - path: "backend/app/routers/settings.py"
      provides: "Skill attribute extraction from employee raw_json"
      contains: "skill_attributes"
    - path: "frontend/src/lib/api.ts"
      provides: "Array-typed Settings and PersonioOptions interfaces"
      contains: "personio_sick_leave_type_id: number[]"
    - path: "frontend/src/hooks/useSettingsDraft.ts"
      provides: "Array-typed DraftFields with JSON.stringify equality"
      contains: "personio_sick_leave_type_id: number[]"
    - path: "frontend/src/locales/en.json"
      provides: "EN translations for checkbox list labels"
      contains: "settings.personio.no_options"
    - path: "frontend/src/locales/de.json"
      provides: "DE translations for checkbox list labels"
      contains: "settings.personio.no_options"
  key_links:
    - from: "frontend/src/hooks/useSettingsDraft.ts"
      to: "frontend/src/lib/api.ts"
      via: "Settings interface import"
      pattern: "import.*Settings.*from.*api"
    - from: "frontend/src/hooks/useSettingsDraft.ts"
      to: "PUT /api/settings"
      via: "draftToPutPayload sends arrays"
      pattern: "personio_sick_leave_type_id.*draft\\.personio_sick_leave_type_id"
---

<objective>
Extend the backend personio-options endpoint with skill_attributes, migrate all frontend TypeScript interfaces and draft state logic from scalar to array types for the 3 Personio config fields, and add i18n keys for checkbox list labels.

Purpose: Establishes the data contract (backend + frontend types + state management) that Plan 02's CheckboxList UI will consume. Without this, the UI component would have no array-typed state to bind to.

Output: Updated backend endpoint, updated TS interfaces, updated draft hook, new i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-frontend-checkbox-list-ui-and-i18n/20-CONTEXT.md

<interfaces>
<!-- Backend schemas that need extending -->
From backend/app/schemas.py (line 191):
```python
class PersonioOptions(BaseModel):
    absence_types: list[AbsenceTypeOption]
    departments: list[str]
    error: str | None = None
```

<!-- Frontend interfaces that need array migration -->
From frontend/src/lib/api.ts:
```typescript
export interface Settings {
  // ... other fields ...
  personio_sick_leave_type_id: number | null;   // MUST become number[]
  personio_production_dept: string | null;       // MUST become string[]
  personio_skill_attr_key: string | null;        // MUST become string[]
}

export interface SettingsUpdatePayload {
  // ... other fields ...
  personio_sick_leave_type_id?: number;   // MUST become number[]
  personio_production_dept?: string;       // MUST become string[]
  personio_skill_attr_key?: string;        // MUST become string[]
}

export interface PersonioOptions {
  absence_types: AbsenceTypeOption[];
  departments: string[];
  error: string | null;
  // MUST add: skill_attributes: string[]
}
```

From frontend/src/hooks/useSettingsDraft.ts:
```typescript
export interface DraftFields {
  // ... other fields ...
  personio_sick_leave_type_id: number | null;   // MUST become number[]
  personio_production_dept: string | null;       // MUST become string[]
  personio_skill_attr_key: string | null;        // MUST become string[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — add skill_attributes to personio-options endpoint</name>
  <files>backend/app/schemas.py, backend/app/routers/settings.py</files>
  <read_first>
    - backend/app/schemas.py (PersonioOptions model at line 191)
    - backend/app/routers/settings.py (get_personio_options function at lines 85-145)
  </read_first>
  <action>
1. In `backend/app/schemas.py`, add `skill_attributes: list[str] = []` to the `PersonioOptions` model (after `departments`, before `error`). Per D-01, D-02.

2. In `backend/app/routers/settings.py`, inside `get_personio_options()`:
   - After the `departments = sorted(dept_names)` line (~line 133), add skill attribute extraction:
     ```python
     attr_keys: set[str] = set()
     for e in employees_raw:
         attrs = e.get("attributes", {})
         for key, val in attrs.items():
             if val is not None and val != "" and val != []:
                 attr_keys.add(key)
     skill_attributes = sorted(attr_keys)
     ```
   - Update all 3 `PersonioOptions(...)` return statements to include `skill_attributes=`:
     - The no-credentials return (line 95-98): add `skill_attributes=[]`
     - The success return (line 135-139): add `skill_attributes=skill_attributes`
     - The error return (line 141-144): add `skill_attributes=[]`

Per D-01: Extract available attribute keys from employee raw_json attributes, filtering to only keys where at least one employee has a non-null, non-empty value.
  </action>
  <verify>
    <automated>cd /Users/johannbechtold/Documents/acm-kpi-light && python -c "from backend.app.schemas import PersonioOptions; p = PersonioOptions(absence_types=[], departments=[], skill_attributes=['a','b'], error=None); assert p.skill_attributes == ['a','b']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/schemas.py contains `skill_attributes: list[str]` inside PersonioOptions class
    - backend/app/routers/settings.py contains `attr_keys: set[str] = set()` in get_personio_options
    - backend/app/routers/settings.py contains `skill_attributes=skill_attributes` in the success return
    - backend/app/routers/settings.py contains `skill_attributes=[]` in both error/no-credentials returns
  </acceptance_criteria>
  <done>PersonioOptions schema has skill_attributes field; endpoint extracts unique attribute keys from employee data and returns them sorted</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — migrate interfaces, draft state, and i18n to arrays</name>
  <files>frontend/src/lib/api.ts, frontend/src/hooks/useSettingsDraft.ts, frontend/src/locales/en.json, frontend/src/locales/de.json</files>
  <read_first>
    - frontend/src/lib/api.ts (Settings interface at line 145, SettingsUpdatePayload at line 177, PersonioOptions at line 265)
    - frontend/src/hooks/useSettingsDraft.ts (DraftFields at line 15, settingsToDraft at line 56, draftToPutPayload at line 107, shallowEqualDraft at line 133)
    - frontend/src/locales/en.json (settings.personio.* keys)
    - frontend/src/locales/de.json (settings.personio.* keys)
  </read_first>
  <action>
**api.ts changes (per D-10, D-11, D-02):**

1. In `Settings` interface, change the 3 Personio config fields:
   - `personio_sick_leave_type_id: number | null` -> `personio_sick_leave_type_id: number[]`
   - `personio_production_dept: string | null` -> `personio_production_dept: string[]`
   - `personio_skill_attr_key: string | null` -> `personio_skill_attr_key: string[]`

2. In `SettingsUpdatePayload` interface, change the 3 optional Personio config fields:
   - `personio_sick_leave_type_id?: number` -> `personio_sick_leave_type_id?: number[]`
   - `personio_production_dept?: string` -> `personio_production_dept?: string[]`
   - `personio_skill_attr_key?: string` -> `personio_skill_attr_key?: string[]`

3. In `PersonioOptions` interface, add `skill_attributes: string[];` after `departments`.

**useSettingsDraft.ts changes (per D-06, D-07, D-08, D-09):**

4. In `DraftFields` interface, change:
   - `personio_sick_leave_type_id: number | null` -> `personio_sick_leave_type_id: number[]`
   - `personio_production_dept: string | null` -> `personio_production_dept: string[]`
   - `personio_skill_attr_key: string | null` -> `personio_skill_attr_key: string[]`

5. In `settingsToDraft()` function, change the 3 field initializations:
   - `personio_sick_leave_type_id: s.personio_sick_leave_type_id ?? null` -> `personio_sick_leave_type_id: s.personio_sick_leave_type_id ?? []`
   - `personio_production_dept: s.personio_production_dept ?? null` -> `personio_production_dept: s.personio_production_dept ?? []`
   - `personio_skill_attr_key: s.personio_skill_attr_key ?? null` -> `personio_skill_attr_key: s.personio_skill_attr_key ?? []`

6. In `draftToPutPayload()` function, change the 3 Personio field assignments. Remove the `?? undefined` guard — always include the array (empty `[]` means "clear"):
   - `personio_sick_leave_type_id: draft.personio_sick_leave_type_id ?? undefined` -> `personio_sick_leave_type_id: draft.personio_sick_leave_type_id`
   - `personio_production_dept: draft.personio_production_dept ?? undefined` -> `personio_production_dept: draft.personio_production_dept`
   - `personio_skill_attr_key: draft.personio_skill_attr_key ?? undefined` -> `personio_skill_attr_key: draft.personio_skill_attr_key`

7. In `shallowEqualDraft()` function, replace the 3 scalar equality checks with JSON.stringify (per D-09):
   - `a.personio_sick_leave_type_id === b.personio_sick_leave_type_id` -> `JSON.stringify(a.personio_sick_leave_type_id) === JSON.stringify(b.personio_sick_leave_type_id)`
   - `a.personio_production_dept === b.personio_production_dept` -> `JSON.stringify(a.personio_production_dept) === JSON.stringify(b.personio_production_dept)`
   - `a.personio_skill_attr_key === b.personio_skill_attr_key` -> `JSON.stringify(a.personio_skill_attr_key) === JSON.stringify(b.personio_skill_attr_key)`

**i18n changes (per UI-03):**

8. In `en.json`, update existing and add new keys after the `settings.personio.skill_attr_key.placeholder` entry:
   - Change `"settings.personio.sick_leave_type.placeholder": "Select absence type"` -> `"settings.personio.sick_leave_type.placeholder": "No absence types selected"`
   - Change `"settings.personio.production_dept.placeholder": "Select department"` -> `"settings.personio.production_dept.placeholder": "No departments selected"`
   - Change `"settings.personio.skill_attr_key.placeholder": "e.g. dynamic_12345"` -> `"settings.personio.skill_attr_key.placeholder": "No attributes selected"`
   - Add `"settings.personio.no_options": "No options available"`
   - Add `"settings.personio.loading": "Loading..."`
   - Add `"settings.personio.skill_attr_key.label": "Skill attribute keys"` (update existing — now plural since multi-select)

9. In `de.json`, update existing and add new keys in the same positions:
   - Change `"settings.personio.sick_leave_type.placeholder": "Abwesenheitstyp wählen"` -> `"settings.personio.sick_leave_type.placeholder": "Keine Abwesenheitstypen ausgewählt"`
   - Change `"settings.personio.production_dept.placeholder": "Abteilung wählen"` -> `"settings.personio.production_dept.placeholder": "Keine Abteilungen ausgewählt"`
   - Change `"settings.personio.skill_attr_key.placeholder": "z.B. dynamic_12345"` -> `"settings.personio.skill_attr_key.placeholder": "Keine Attribute ausgewählt"`
   - Add `"settings.personio.no_options": "Keine Optionen verfügbar"`
   - Add `"settings.personio.loading": "Wird geladen..."`
   - Update `"settings.personio.skill_attr_key.label": "Skill-Attribut-Schlüssel"` (keep as-is, already works for plural in German)
  </action>
  <verify>
    <automated>cd /Users/johannbechtold/Documents/acm-kpi-light/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/api.ts contains `personio_sick_leave_type_id: number[]` in Settings interface
    - frontend/src/lib/api.ts contains `personio_sick_leave_type_id?: number[]` in SettingsUpdatePayload
    - frontend/src/lib/api.ts contains `skill_attributes: string[]` in PersonioOptions interface
    - frontend/src/hooks/useSettingsDraft.ts contains `personio_sick_leave_type_id: number[]` in DraftFields
    - frontend/src/hooks/useSettingsDraft.ts contains `JSON.stringify(a.personio_sick_leave_type_id)` in shallowEqualDraft
    - frontend/src/hooks/useSettingsDraft.ts `settingsToDraft` contains `s.personio_sick_leave_type_id ?? []`
    - frontend/src/hooks/useSettingsDraft.ts `draftToPutPayload` does NOT contain `?? undefined` for the 3 Personio array fields
    - frontend/src/locales/en.json contains `"settings.personio.no_options": "No options available"`
    - frontend/src/locales/en.json contains `"settings.personio.loading": "Loading..."`
    - frontend/src/locales/de.json contains `"settings.personio.no_options": "Keine Optionen verfügbar"`
    - frontend/src/locales/de.json contains `"settings.personio.loading": "Wird geladen..."`
    - `npx tsc --noEmit` exits 0 (except for PersonioCard.tsx errors which Plan 02 will fix — acceptable to have errors in that one file)
  </acceptance_criteria>
  <done>All TypeScript interfaces use array types for Personio config fields; draft hook initializes/serializes/compares arrays correctly; i18n keys exist in both languages for checkbox list labels; empty array [] round-trips through save as a valid "clear" value</done>
</task>

</tasks>

<verification>
1. Backend: `PersonioOptions` schema accepts and serializes `skill_attributes: list[str]`
2. Frontend: `npx tsc --noEmit` passes (PersonioCard.tsx may have errors — Plan 02 fixes those)
3. i18n: Both en.json and de.json contain `settings.personio.no_options` and `settings.personio.loading` keys
</verification>

<success_criteria>
- Backend PersonioOptions model includes skill_attributes field
- All 3 frontend interfaces (Settings, SettingsUpdatePayload, DraftFields) use array types for Personio config fields
- settingsToDraft maps null/undefined to []
- draftToPutPayload sends arrays directly (no ?? undefined guard)
- shallowEqualDraft uses JSON.stringify for array comparison
- 4 new i18n keys exist in both en.json and de.json
</success_criteria>

<output>
After completion, create `.planning/phases/20-frontend-checkbox-list-ui-and-i18n/20-01-SUMMARY.md`
</output>
