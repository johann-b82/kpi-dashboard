---
phase: 31-seed-outline-docs
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - backend/app/routers/settings.py
  - backend/app/routers/sync.py
  - backend/app/routers/uploads.py
autonomous: true
requirements: [DOC-03]
must_haves:
  truths:
    - "FastAPI Swagger UI at https://kpi.internal/api/docs shows 7 tag groups: auth, data, hr-kpis, kpis, settings, sync, uploads"
    - "No behavioral change to any endpoint — only Swagger grouping is affected"
  artifacts:
    - path: "backend/app/routers/settings.py"
      provides: "APIRouter with tags=['settings']"
      contains: "tags=[\"settings\"]"
    - path: "backend/app/routers/sync.py"
      provides: "APIRouter with tags=['sync']"
      contains: "tags=[\"sync\"]"
    - path: "backend/app/routers/uploads.py"
      provides: "APIRouter with tags=['uploads']"
      contains: "tags=[\"uploads\"]"
  key_links:
    - from: "backend/app/routers/*.py"
      to: "FastAPI Swagger"
      via: "APIRouter tags= kwarg"
      pattern: "tags=\\[\""
---

<objective>
Add `tags=` to the three untagged routers (`settings`, `sync`, `uploads`) so Swagger UI shows 7 tag groups. This is a prerequisite for DOC-03 (next plan) — it lets the thin API Reference page cite the 7 groups CONTEXT D-02 describes verbatim.

Purpose: Align Swagger with CONTEXT D-02 wording before authoring DOC-03 (RESEARCH option 1 — 3-line fix).
Output: Three routers with explicit `tags=` kwargs; no behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-seed-outline-docs/31-RESEARCH.md
@backend/app/routers/settings.py
@backend/app/routers/sync.py
@backend/app/routers/uploads.py

<interfaces>
Current state (from RESEARCH §CLAUDE.md Correction):

backend/app/routers/settings.py:24: `router = APIRouter(` (no tags=)
backend/app/routers/sync.py:23: `router = APIRouter(` (no tags=)
backend/app/routers/uploads.py:14: `router = APIRouter(` (no tags=)

Target pattern (mirror auth.py line 12: `router = APIRouter(prefix="/api/auth", tags=["auth"])`):
Each router gains `tags=["<name>"]` preserving existing prefix/other kwargs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tags= to settings, sync, uploads routers</name>
  <files>backend/app/routers/settings.py, backend/app/routers/sync.py, backend/app/routers/uploads.py</files>
  <action>
    For each of the 3 routers, add a `tags=["<router-name>"]` kwarg to the `APIRouter(...)` call:

    - settings.py line ~24: add `tags=["settings"]`
    - sync.py line ~23: add `tags=["sync"]`
    - uploads.py line ~14: add `tags=["uploads"]`

    Preserve existing kwargs (prefix, dependencies). Match existing indentation/formatting style used by hr_kpis.py / kpis.py (multi-line APIRouter(...) calls keep tags= on its own line).

    DO NOT change any endpoint logic, dependencies, or behavior. Only add the tags kwarg.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; for r in settings sync uploads; do grep -q "tags=\[\"$r\"\]" app/routers/$r.py || { echo "missing tags=[\"$r\"] in $r.py"; exit 1; }; done &amp;&amp; python -c "from app.main import app; tags=sorted({t for r in app.routes for t in (getattr(r,'tags',[]) or [])}); print(tags); assert set(tags) &gt;= {'auth','data','hr-kpis','kpis','settings','sync','uploads'}, tags"</automated>
  </verify>
  <done>All 7 expected tags appear in the FastAPI app route tag set; `python -c` import-and-inspect assertion passes.</done>
</task>

</tasks>

<verification>
docker compose exec api python -c "from app.main import app; ..." shows all 7 tags present. No endpoint test regressions.
</verification>

<success_criteria>
Swagger UI at https://kpi.internal/api/docs renders 7 groups (auth, data, hr-kpis, kpis, settings, sync, uploads) — DOC-03 can now cite them accurately.
</success_criteria>

<output>
Create `.planning/phases/31-seed-outline-docs/31-02-SUMMARY.md` noting the 3-line fix and confirming Swagger tag count.
</output>
