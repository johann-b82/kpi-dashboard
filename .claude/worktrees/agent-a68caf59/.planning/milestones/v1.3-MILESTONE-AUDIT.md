---
status: gaps_found
milestone: v1.3
date: 2026-04-12
---

# v1.3 Milestone Audit: HR KPI Dashboard & Personio-Integration

## Requirements Coverage

| Req ID | Description | Verified | Evidence |
|--------|-------------|----------|----------|
| NAV-01 | "Dashboard" tab renamed to "Sales" | YES | `frontend/src/components/NavBar.tsx:47` — `t("nav.sales")`; locale keys `nav.sales` = "Sales" (EN) / "Vertrieb" (DE); `nav.dashboard` key absent from both locale files |
| NAV-02 | "HR" tab exists in NavBar | YES | `frontend/src/components/NavBar.tsx:52` — `<Link href="/hr">{t("nav.hr")}</Link>`; `/hr` route in `App.tsx:23` |
| NAV-03 | HR tab shows sync freshness indicator | YES | `frontend/src/pages/HRPage.tsx:38–59` — `fetchSyncMeta` via `useQuery`, Intl.DateTimeFormat formatting, "Not yet synced" fallback with Settings link |
| PERS-01 | Write-only credential storage | YES | Backend: `schemas.py:158` `personio_has_credentials: bool` (not raw creds) in `SettingsRead`; `routers/settings.py:157–160` encrypts via Fernet on PUT; `security/fernet.py` encrypt/decrypt helpers. Frontend: `useSettingsDraft.ts:67–68` credentials always start empty |
| PERS-02 | Manual sync trigger button | YES | `HRPage.tsx:62–84` — "Daten aktualisieren" button via `useMutation(triggerSync)`; `api.ts:324–331` calls `POST /api/sync`; `routers/sync.py:36–56` handler calls `hr_sync.run_sync()` |
| PERS-03 | Configurable sync interval | YES | `PersonioCard.tsx:31–36` — INTERVAL_OPTIONS `[0,1,6,24]`; `routers/settings.py:163–181` — reschedules APScheduler on PUT; `scheduler.py:40–56` — lifespan reads interval, starts scheduler |
| PERS-04 | Data fetch and storage | YES | `personio_client.py:144–267` — `fetch_employees/attendances/absences/absence_types`; `hr_sync.py:31–83` — `run_sync()` orchestrates fetch → normalize → upsert via `pg_insert().on_conflict_do_update()` |
| PERS-05 | Absence types dropdown | YES | `routers/settings.py:85–136` — `GET /api/settings/personio-options` fetches live from Personio; `PersonioCard.tsx:169–194` — `<select>` populated from `options?.absence_types` |
| PERS-06 | Departments dropdown | YES | Same endpoint `GET /api/settings/personio-options` extracts departments from employee data; `PersonioCard.tsx:197–222` — `<select>` populated from `options?.departments` |
| HRKPI-01 | Overtime Ratio KPI card with deltas | YES | `hr_kpi_aggregation.py:93–141` — `_overtime_ratio()`; `HrKpiCardGrid.tsx:140–144` — renders with `formatPercent` + `DeltaBadgeStack` |
| HRKPI-02 | Sick Leave Ratio KPI card with deltas | YES | `hr_kpi_aggregation.py:144–224` — `_sick_leave_ratio()`; `HrKpiCardGrid.tsx:145–149`; shows `is_configured=False` when `sick_leave_type_id` is null (`hr_kpi_aggregation.py:338`) |
| HRKPI-03 | Fluctuation KPI card with deltas | YES | `hr_kpi_aggregation.py:227–241` — `_fluctuation()`; `HrKpiCardGrid.tsx:150–154` |
| HRKPI-04 | Skill Development KPI card with "nicht konfiguriert" fallback | YES | `hr_kpi_aggregation.py:244–270` — `_skill_development()` with JSONB query; `hr_kpi_aggregation.py:354` — `is_configured=False` when `skill_key` is None; `HrKpiCardGrid.tsx:64–79` — renders "nicht konfiguriert" + Settings link |
| HRKPI-05 | Revenue per Production Employee with em-dash fallback | YES | `hr_kpi_aggregation.py:273–294` — `_revenue_per_production_employee()` cross-joins SalesRecord revenue with PersonioEmployee headcount; `HrKpiCardGrid.tsx:84` — em-dash when `value === null` |
| HRKPI-06 | Error state handling | YES | `HrKpiCardGrid.tsx:115–124` — destructive-border error banner on `isError`; line 110 — `kpiData = undefined` on error shows em-dash in all cards (not crash) |
| SET-01 | Sick leave absence type dropdown | YES | `PersonioCard.tsx:169–194` — `<select id="personio-sick-leave">` with placeholder and Personio-populated options |
| SET-02 | Production department dropdown | YES | `PersonioCard.tsx:197–222` — `<select id="personio-dept">` with placeholder and Personio-populated options |
| SET-03 | Skill custom attribute key field | YES | `PersonioCard.tsx:225–237` — `<Input id="personio-skill-key">` text input |
| SET-04 | Auto-sync interval selector | YES | `PersonioCard.tsx:145–166` — `<select id="personio-sync-interval">` with 4 options (manual/1h/6h/24h) |
| I18N-01 | Full DE/EN parity for all v1.3 strings | YES | Parity check: 164 keys in both `en.json` and `de.json`, zero missing. PersonioCard fully wired with `t()` calls (24 `settings.personio.*` keys). HR KPI cards use 14 `hr.kpi.*` keys. HR sync uses 6 `hr.sync.*` keys |

**Result: 20/20 requirements verified in codebase.**

## Cross-Phase Integration

| Integration | Status | Evidence |
|-------------|--------|----------|
| Phase 12 → 13 | WIRED | `hr_sync.py` imports `PersonioClient` from `services/personio_client` (Phase 12), calls `decrypt_credential` from `security/fernet` (Phase 12), and upserts into `PersonioEmployee`/`PersonioAttendance`/`PersonioAbsence` models (Phase 12). `PersonioClient.fetch_employees/attendances/absences/absence_types` (added Phase 13) extend the Phase 12 base class with auth/token caching. |
| Phase 13 → 14 | WIRED | `HRPage.tsx` imports `triggerSync` and `fetchSyncMeta` from `api.ts` (Phase 14 plumbing), which call `POST /api/sync` and `GET /api/sync/meta` (Phase 13 routes). `syncKeys` query key factory (Phase 14) used for cache invalidation on sync success. |
| Phase 14 → 15 | WIRED | `HRPage.tsx:95` renders `<HrKpiCardGrid />` (Phase 15 component) inside the HR page shell (Phase 14). `HRPage.tsx:26` invalidates `hrKpiKeys.all()` (Phase 15 query key) on sync success. `HrKpiCardGrid` calls `fetchHrKpis()` → `GET /api/hr/kpis` (Phase 15 backend). |
| Phase 15 → 16 | WIRED | `PersonioCard.tsx:29` — `const { t } = useTranslation()` wired in Phase 16. All 24 `settings.personio.*` keys added to both locale files in Phase 16 (`16-01-SUMMARY.md`). `HrKpiCardGrid` already had i18n from Phase 15 (14 `hr.kpi.*` keys). Dead `hr.placeholder` key removed. |

**Result: 4/4 cross-phase integrations verified.**

## E2E Flows

| Flow | Status | Notes |
|------|--------|-------|
| Settings → Credentials → Test Connection → Save | COMPLETE | PersonioCard inputs (`type="password"`) → `testPersonioConnection()` → `POST /api/sync/test` → `PersonioClient.authenticate()` → success/error feedback in component state. Save via shared ActionBar → `draftToPutPayload()` → `PUT /api/settings` → `encrypt_credential()` → BYTEA storage. |
| Settings → Configure sync interval → Scheduler picks it up | COMPLETE | PersonioCard `<select>` → `setField("personio_sync_interval_h", ...)` → save → `PUT /api/settings` handler line 163–181 → `request.app.state.scheduler.add_job()` or `remove_job()` (APScheduler). On app startup, `scheduler.py:44` reads interval from DB and schedules if > 0. |
| HR Tab → KPI cards → Delta badges → Error states | COMPLETE | NavBar `/hr` link → `HRPage` → `HrKpiCardGrid` → `useQuery(fetchHrKpis)` → `GET /api/hr/kpis` → `compute_hr_kpis()` → 5 KPI computations with 3 windows each. Frontend renders: `KpiCard` + `DeltaBadgeStack` with `computeDelta()`. Error: `isError` → destructive banner + em-dash cards. No-sync: all null values → muted banner. Unconfigured: `is_configured=False` → "nicht konfiguriert" + Settings link. |
| Language toggle → All strings re-render | COMPLETE | `LanguageToggle` → `i18n.changeLanguage()` → react-i18next re-renders all `t()` calls. PersonioCard: `INTERVAL_OPTIONS` declared inside component body (not module scope) so `t()` re-evaluates. All 164 keys present in both locales. |

**Result: 4/4 E2E flows verified.**

## API Route Coverage

| Route | Consumer | Status |
|-------|----------|--------|
| `GET /api/settings` | `fetchSettings()` in `api.ts:164` | CONSUMED |
| `PUT /api/settings` | `updateSettings()` in `api.ts:201` | CONSUMED |
| `GET /api/settings/personio-options` | `fetchPersonioOptions()` in `api.ts:280` → `PersonioCard.tsx:45` | CONSUMED |
| `POST /api/sync` | `triggerSync()` in `api.ts:324` → `HRPage.tsx:23` | CONSUMED |
| `POST /api/sync/test` | `testPersonioConnection()` in `api.ts:291` → `PersonioCard.tsx:56` | CONSUMED |
| `GET /api/sync/meta` | `fetchSyncMeta()` in `api.ts:310` → `HRPage.tsx:14` | CONSUMED |
| `GET /api/hr/kpis` | `fetchHrKpis()` in `api.ts:352` → `HrKpiCardGrid.tsx:22` | CONSUMED |

**Result: 7/7 v1.3 API routes have consumers.**

## Summary

All 20 v1.3 requirements are verified as implemented in the codebase with proper cross-phase wiring. The full integration chain from Personio API client (Phase 12) through sync service (Phase 13), navigation shell (Phase 14), KPI computation and display (Phase 15), and i18n polish (Phase 16) is connected end-to-end. No orphaned exports, no orphaned API routes, no broken data flows.

## Gaps

### Minor: ROADMAP.md plan checkbox inconsistency

**Location:** `.planning/ROADMAP.md:131`

The Phase 16 plan `16-01-PLAN.md` is shown as unchecked (`- [ ]`) in the ROADMAP, while Phase 16 itself is marked as "Complete" on line 59 with `0/1` plans on line 152. The SUMMARY exists and documents successful completion. This is a planning artifact inconsistency only -- the code is fully delivered.

**Impact:** None on functionality. Cosmetic only.

**Fix:** Change `- [ ]` to `- [x]` on ROADMAP.md line 131 and update `0/1` to `1/1` on line 152.
