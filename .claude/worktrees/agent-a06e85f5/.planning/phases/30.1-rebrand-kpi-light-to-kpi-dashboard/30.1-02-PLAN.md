---
phase: 30.1-rebrand-kpi-light-to-kpi-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
  - frontend/src/lib/defaults.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "i18n key common.appName → 'KPI Dashboard' in both en.json and de.json (NOTE: current key is nav.brand not common.appName — grep audit to resolve during execution per D-03)"
    - "i18n key settings.identity.app_name.placeholder → 'KPI Dashboard' in both locales"
    - "DEFAULT_SETTINGS.app_name in frontend/src/lib/defaults.ts equals 'KPI Dashboard'"
    - "All i18n keys remain unchanged — only values are rewritten (D-03)"
    - "Zero 'KPI Light' string literals remain in frontend/src/"
  artifacts:
    - path: "frontend/src/locales/en.json"
      provides: "EN display strings"
      contains: "KPI Dashboard"
    - path: "frontend/src/locales/de.json"
      provides: "DE display strings"
      contains: "KPI Dashboard"
    - path: "frontend/src/lib/defaults.ts"
      provides: "Frontend default settings fallback (used pre-API-response)"
      contains: "KPI Dashboard"
  key_links:
    - from: "frontend/src/components/NavBar.tsx"
      to: "i18n key nav.brand"
      via: "react-i18next t() call"
      pattern: 'nav\.brand|nav"\s*:\s*\{[^}]*"brand"'
---

<objective>
Rename "KPI Light" → "KPI Dashboard" on the frontend's three display surfaces: the two locale JSON files and the frontend defaults fallback module. Keep all i18n KEYS unchanged (D-03) — rewrite VALUES only.

Purpose: Values are the cosmetic surface users see in the NavBar brand and the Settings "App name" placeholder. Keys are contracts with `t()` call sites — preserving them means zero downstream code churn.
Output: 3 files modified, 2 atomic commits (one for locales together — they are a matched pair — and one for defaults.ts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30.1-rebrand-kpi-light-to-kpi-dashboard/30.1-CONTEXT.md
@frontend/src/locales/en.json
@frontend/src/locales/de.json
@frontend/src/lib/defaults.ts

<interfaces>
Observed "KPI Light" hits in frontend (from grep audit 2026-04-15):
  - frontend/src/locales/en.json:28  `"nav.brand": "KPI Light",`
  - frontend/src/locales/en.json:135 `"settings.identity.app_name.placeholder": "KPI Light",`
  - frontend/src/locales/de.json:28  `"nav.brand": "KPI Light",`
  - frontend/src/locales/de.json:135 `"settings.identity.app_name.placeholder": "KPI Light",`
  - frontend/src/lib/defaults.ts:10  `  app_name: "KPI Light",`

Note: CONTEXT D-03 says "`common.appName`" — but the grep audit proves the real key today is `nav.brand` (flat dotted keys, keySeparator:false, per PROJECT.md Tech Stack). Executor must rename VALUES on whatever key(s) the grep finds, per the broader D-03 intent (change values, keep keys). The i18n.ts `keySeparator:false` convention is not an invariant to change here.

Types / exports touched:
  - DEFAULT_SETTINGS: Settings  (from frontend/src/lib/defaults.ts line 3)
  - keys touched: `nav.brand`, `settings.identity.app_name.placeholder`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite "KPI Light" → "KPI Dashboard" in both locale files</name>
  <files>frontend/src/locales/en.json, frontend/src/locales/de.json</files>
  <action>
    Step 1 — Audit first, edit second. Run:
      `grep -n "KPI Light" frontend/src/locales/en.json frontend/src/locales/de.json`
    Expected output (per interfaces block):
      - en.json:28 `"nav.brand": "KPI Light",`
      - en.json:135 `"settings.identity.app_name.placeholder": "KPI Light",`
      - de.json:28 `"nav.brand": "KPI Light",`
      - de.json:135 `"settings.identity.app_name.placeholder": "KPI Light",`
    If grep reveals additional hits, rewrite those too — same before/after transform. D-03 scope is "any other literal KPI Light string in locale files".

    Step 2 — For each hit, apply the exact string-value substitution:
      BEFORE: `"KPI Light"`  (as a JSON string value only)
      AFTER:  `"KPI Dashboard"`

    Do NOT:
      - Rename any key (D-03 keeps keys stable).
      - Reorder keys.
      - Touch other values.
      - Change whitespace/indentation (keep 2-space JSON indent).

    Step 3 — Commit both files together (matched EN/DE pair is a single logical change):
      `feat(30.1): rebrand frontend i18n values KPI Light → KPI Dashboard`
  </action>
  <verify>
    <automated>! grep -q "KPI Light" frontend/src/locales/en.json &amp;&amp; ! grep -q "KPI Light" frontend/src/locales/de.json &amp;&amp; grep -q '"nav.brand": "KPI Dashboard"' frontend/src/locales/en.json &amp;&amp; grep -q '"nav.brand": "KPI Dashboard"' frontend/src/locales/de.json &amp;&amp; grep -q '"settings.identity.app_name.placeholder": "KPI Dashboard"' frontend/src/locales/en.json &amp;&amp; grep -q '"settings.identity.app_name.placeholder": "KPI Dashboard"' frontend/src/locales/de.json &amp;&amp; python3 -c "import json; json.load(open('frontend/src/locales/en.json')); json.load(open('frontend/src/locales/de.json'))"</automated>
  </verify>
  <done>Both locale files parse as valid JSON, zero "KPI Light" remain, both `nav.brand` values and both placeholder values equal "KPI Dashboard"; commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Update DEFAULT_SETTINGS.app_name in frontend/src/lib/defaults.ts</name>
  <files>frontend/src/lib/defaults.ts</files>
  <action>
    Exact edit — frontend/src/lib/defaults.ts line 10:
      BEFORE: `  app_name: "KPI Light",`
      AFTER:  `  app_name: "KPI Dashboard",`

    Do NOT touch: import line, `DEFAULT_SETTINGS: Settings` type, THEME_TOKEN_MAP, any color/personio/target key.

    Commit message exactly:
      `feat(30.1): rebrand frontend default app_name KPI Light → KPI Dashboard`
    Commit only frontend/src/lib/defaults.ts.
  </action>
  <verify>
    <automated>grep -q '  app_name: "KPI Dashboard",' frontend/src/lib/defaults.ts &amp;&amp; ! grep -q "KPI Light" frontend/src/lib/defaults.ts</automated>
  </verify>
  <done>defaults.ts updated; zero "KPI Light" in the file; DEFAULT_SETTINGS still typed as `Settings`; commit landed.</done>
</task>

</tasks>

<verification>
- `! grep -rn "KPI Light" frontend/src/` returns zero hits.
- `grep -rn "KPI Dashboard" frontend/src/locales/ frontend/src/lib/defaults.ts | wc -l` returns >= 5 (2 nav.brand + 2 placeholders + 1 defaults).
- No key rename anywhere — `grep -E '"(nav\.brand|settings\.identity\.app_name\.placeholder)":' frontend/src/locales/*.json | wc -l` returns 4 (2 per file).
- Both locale JSON files remain valid JSON (python3 json.load).
</verification>

<success_criteria>
After this plan:
1. Browser reload on `https://kpi.internal` shows "KPI Dashboard" in the NavBar brand slot (EN and DE).
2. Settings → Appearance → App name placeholder reads "KPI Dashboard" (EN and DE).
3. Pre-API-response fallback during cold start uses "KPI Dashboard" (frontend defaults.ts).
4. Zero i18n key renamed; zero `t("KPI Light")` call-site churn.
5. Two atomic commits land (locales together, defaults.ts alone) per D-05.
</success_criteria>

<output>
After completion, create `.planning/phases/30.1-rebrand-kpi-light-to-kpi-dashboard/30.1-02-SUMMARY.md` with commit hashes and a grep report confirming `frontend/src/` is clean.
</output>
