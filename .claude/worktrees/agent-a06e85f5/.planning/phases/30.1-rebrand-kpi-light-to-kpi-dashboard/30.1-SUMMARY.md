---
phase: 30.1-rebrand-kpi-light-to-kpi-dashboard
plans: [30.1-01, 30.1-02, 30.1-03]
status: complete
completed: 2026-04-15
---

# Phase 30.1 Summary: Rebrand KPI Light → KPI Dashboard

Display-only product rename executed across backend (defaults + FastAPI title + auth docstrings + Alembic data migration), frontend (i18n values + defaults fallback), and documentation (README, CLAUDE.md, setup runbook, env sample, ROADMAP, PROJECT.md, Phase 31 fix-up). All internal identifiers preserved per D-01.

## Plan Breakdown

| Plan | Subsystem | Commits | Summary |
| ---- | --------- | ------- | ------- |
| 30.1-01 | backend | 4 | `1f1976d`, `9351116`, `1fc5f30`, `a5ee17a` — defaults.py, main.py FastAPI title, auth docstrings, `v1_11_rename_app_name` Alembic migration |
| 30.1-02 | frontend-i18n | 2 | `7a93f7c`, `d9c1ab1` — en/de locale values, defaults.ts app_name |
| 30.1-03 | docs + project identity | 7 | `9ace346`, `d13b057`, `dbf5309`, `7bde258`, `636c81b`, `a14d897`, `8d4f1b6` — README, CLAUDE, docs/setup.md, .env.example, ROADMAP, Phase 31 artifacts, PROJECT.md |

Total: **13 atomic commits**.

## Invariants Verified

### Display surfaces clean
```
$ grep -rn "KPI Light" frontend/src backend/app README.md CLAUDE.md docs/setup.md .env.example docker-compose.yml
(zero hits)
```

### D-01 internal identifiers preserved
- `kpi.internal` hostname — unchanged across README, docs/setup.md, docker-compose.yml
- `"kpi-light"` Dex OIDC client id — unchanged in `backend/app/config.py`
- `DEX_KPI_SECRET` env var — unchanged in `.env.example`, `docker-compose.yml`
- `kpi_session` cookie name — unchanged in `backend/app/main.py`, `docs/setup.md`
- Compose services (`api`, `frontend`, `db`, `migrate`, `npm`, `dex`, `outline`, `outline-db`, `outline-redis`) — unchanged
- Image pins (`postgres:17-alpine`, `jc21/nginx-proxy-manager:2.11.3`, `ghcr.io/dexidp/dex:v2.43.0`, `outlinewiki/outline:0.86.0`) — unchanged
- Repo directory `acm-kpi-light` — unchanged (compose project name depends on it)
- `docker compose config --quiet` → exit 0

### D-02 historical-freeze honoured
- Zero edits to `.planning/phases/{1..30}/**`
- Zero edits to `.planning/REQUIREMENTS.md`
- Zero edits to shipped-milestone ROADMAP rows (v1.0..v1.10 `<details>` blocks)
- Living doc exception (D-02 → D-05): `.planning/PROJECT.md` rebranded with canonical rename-note sentence `Formerly called KPI Light (renamed 2026-04-15, Phase 30.1).`

### D-04 Alembic migration (plan 01)
- `backend/alembic/versions/v1_11_rename_app_name.py` chained from `v1_11_app_users`
- Narrow WHERE clause: `UPDATE app_settings SET app_name = 'KPI Dashboard' WHERE app_name = 'KPI Light'`
- Symmetric downgrade; operators with customized `app_name` are not touched

### D-06 Phase 31 fix-up applied pre-execution
- 31-CONTEXT.md, 31-01-PLAN.md, 31-05-PLAN.md, 31-06-PLAN.md — rebranded
- 31-02/03/04-PLAN.md — no hits at audit time
- 31-RESEARCH.md — out of plan scope (documented deviation in 30.1-03-SUMMARY.md)
- ROADMAP Phase 31 entry — Goal + Success Criteria #1 reference "KPI Dashboard" collection

## Post-Restart Acceptance Tests (operator-facing)

The following require a `docker compose up` cycle after this phase lands; they are not runnable here:

- [ ] `curl -s https://kpi.internal/api/settings | jq .app_name` returns `"KPI Dashboard"` once the new Alembic migration has applied
- [ ] NavBar displays "KPI Dashboard" in both EN and DE
- [ ] OIDC login still works end-to-end (Dex client id `kpi-light` unchanged → no Dex config regen)

## Known Remaining "KPI Light" Occurrences

All expected per D-02/D-05:
- `.planning/phases/{1..30}/**` — historical artifacts (frozen)
- `.planning/REQUIREMENTS.md` — historical
- ROADMAP v1.0..v1.10 `<details>` blocks + Phase 28 heading ("Phase 28: KPI Light OIDC Integration") — historical
- `.planning/PROJECT.md` — rename-note sentence + trailer line (intentional)
- `.planning/phases/31-seed-outline-docs/31-RESEARCH.md` — out of plan scope; Phase 31 authors may clean up
- ROADMAP Phase 30.1 heading `### Phase 30.1: Rebrand KPI Light to KPI Dashboard (INSERTED)` — intentional (records the rename itself)

## Self-Check: PASSED

Per-plan self-checks all passed (see individual SUMMARY files). Phase-level invariants grep-verified.
