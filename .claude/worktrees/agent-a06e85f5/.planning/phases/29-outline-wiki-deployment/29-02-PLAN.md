---
phase: 29-outline-wiki-deployment
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - docker-compose.yml
autonomous: true
requirements: [WIK-01, WIK-02, WIK-03, WIK-04, WIK-06]
must_haves:
  truths:
    - "`docker compose config` validates the extended YAML with three new services"
    - "`docker compose up -d outline-db outline-redis outline` brings all three to healthy state"
    - "Outline container trusts mkcert root CA (NODE_EXTRA_CA_CERTS set)"
    - "Outline's OIDC auth-uri is browser-reachable (auth.internal) while token/userinfo hit internal docker DNS (dex:5556)"
    - "outline-db and outline-redis volumes are distinct from KPI Light's postgres_data"
    - "SMTP_* env vars are NOT set on the outline service (WIK-06)"
    - "NPM depends_on gate now includes `outline: condition: service_healthy`"
  artifacts:
    - path: "docker-compose.yml"
      provides: "outline-db, outline-redis, outline service definitions + volumes"
      contains: "outlinewiki/outline:0.86.0"
  key_links:
    - from: "outline service env"
      to: "dex service"
      via: "OIDC_TOKEN_URI=http://dex:5556/dex/token (docker DNS)"
      pattern: "OIDC_TOKEN_URI:\\s*http://dex:5556/dex/token"
    - from: "outline service"
      to: "browser-reachable Dex issuer"
      via: "OIDC_AUTH_URI=https://auth.internal/dex/auth + extra_hosts + mkcert CA"
      pattern: "OIDC_AUTH_URI:\\s*https://auth.internal/dex/auth"
    - from: "outline service"
      to: "outline_uploads named volume"
      via: "/var/lib/outline/data mount"
      pattern: "outline_uploads:/var/lib/outline/data"
    - from: "npm.depends_on"
      to: "outline service_healthy"
      via: "healthcheck gate"
      pattern: "outline:\\s*\\n\\s*condition:\\s*service_healthy"
---

<objective>
Add three pinned compose services — `outline-db` (postgres:17-alpine), `outline-redis` (redis:7-alpine), `outline` (outlinewiki/outline:0.86.0) — plus three named volumes (`outline_db_data`, `outline_redis_data`, `outline_uploads`), wire OIDC with split endpoints per D-05, mount the mkcert rootCA, and extend `npm.depends_on` so NPM waits for Outline healthy.

Purpose: greenfield-deploys Outline behind the existing NPM edge using the Phase 28 CA-mount + extra_hosts pattern. Outline runs its own startup migrations; no `outline-migrate` service needed (RESEARCH §Don't Hand-Roll).
Output: a single YAML edit delivering WIK-01, WIK-02, WIK-03, WIK-04, WIK-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-outline-wiki-deployment/29-CONTEXT.md
@.planning/phases/29-outline-wiki-deployment/29-RESEARCH.md
@.planning/phases/29-outline-wiki-deployment/29-01-SUMMARY.md
@docker-compose.yml

<interfaces>
Existing docker-compose.yml surface relevant to this plan:

- Services already defined: `db`, `migrate`, `api`, `frontend`, `npm`, `dex`
- Existing named volumes: `postgres_data`, `npm_data`, `npm_letsencrypt`, `dex_data`
- `dex` service listens on internal port `5556` (confirmed line 148 — path-prefixed `/dex/...`)
- `npm` service has a `depends_on` block currently gating on `api`, `frontend`, `dex` (lines 118-124) — MUST be extended to include `outline`
- `certs/rootCA.pem` is bind-mounted read-only into `api` as `/etc/ssl/certs/mkcert-rootCA.pem:ro` (lines 52-54) — mirror this exact source path
- The `api` service demonstrates the `extra_hosts: "auth.internal:host-gateway"` pattern (lines 60-62)
- Env reuse: `${DEX_OUTLINE_SECRET}` already passed to dex (line 144) and defined in `.env.example` (confirmed line 26) — Outline reads the same var

Dex OIDC endpoints confirmed from Phase 27 & dex/config.yaml (issuer `https://auth.internal/dex`):
- Browser auth: `https://auth.internal/dex/auth`
- Internal token: `http://dex:5556/dex/token`
- Internal userinfo: `http://dex:5556/dex/userinfo`
- Outline Dex client registered with redirect URI `https://wiki.internal/auth/oidc.callback` (Phase 27)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add outline-db + outline-redis service blocks with distinct named volumes</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (entire file — understand existing service ordering + volume list at end)
    - .planning/phases/29-outline-wiki-deployment/29-RESEARCH.md §outline-db block, §outline-redis block, §Pitfall 8 (volume name collision)
  </read_first>
  <action>
    Insert two new services BETWEEN the existing `dex:` block (ends ~line 153) and the top-level `volumes:` key (~line 155). Use exactly this YAML (2-space indent matching the rest of the file):

    ```yaml
      outline-db:
        image: postgres:17-alpine
        restart: unless-stopped
        environment:
          POSTGRES_DB: outline
          POSTGRES_USER: outline
          POSTGRES_PASSWORD: ${OUTLINE_DB_PASSWORD}
        volumes:
          - outline_db_data:/var/lib/postgresql/data
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U outline -d outline"]
          interval: 5s
          timeout: 5s
          retries: 5
          start_period: 10s

      outline-redis:
        image: redis:7-alpine
        restart: unless-stopped
        volumes:
          - outline_redis_data:/data
        healthcheck:
          test: ["CMD-SHELL", "redis-cli ping | grep -q PONG"]
          interval: 5s
          timeout: 5s
          retries: 5
          start_period: 5s
    ```

    Then at the top-level `volumes:` key (currently holds `postgres_data`, `npm_data`, `npm_letsencrypt`, `dex_data`), APPEND three new entries:

    ```yaml
      outline_db_data:
      outline_redis_data:
      outline_uploads:
    ```

    CRITICAL (Pitfall 8): volume names MUST be `outline_db_data` (NOT `postgres_data` — that belongs to KPI Light's `db` service). `outline_uploads` is added here even though it's consumed by Task 2 — keeps the volumes block change atomic.

    Do NOT add `depends_on` to `outline-db` or `outline-redis` — they have no dependencies.
    Do NOT hardcode the DB password; use `${OUTLINE_DB_PASSWORD}` from `.env` (plan 29-01 Task 1).
    Do NOT bind host ports — NPM is sole edge (Phase 26 D-07).
  </action>
  <verify>
    <automated>docker compose config --quiet && docker compose config | grep -q "outline-db:" && docker compose config | grep -q "outline-redis:" && docker compose config | grep -q "outline_db_data" && docker compose config | grep -q "outline_uploads" && ! docker compose config | grep -qE "^\s*ports:\s*$" | grep -A1 "outline-db\|outline-redis"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` exits 0 (YAML valid, env substitution works)
    - `docker compose config | grep -c "image: postgres:17-alpine"` returns `2` (KPI Light db + outline-db)
    - `docker compose config | grep -q "image: redis:7-alpine"` succeeds
    - `grep -n "outline_db_data:" docker-compose.yml` shows TWO occurrences (one in outline-db volumes, one in top-level volumes)
    - `grep -n "outline_redis_data:" docker-compose.yml` shows TWO occurrences
    - `grep -n "outline_uploads:" docker-compose.yml` shows ONE occurrence (top-level volumes; outline service mount added in Task 2)
    - `grep -q "POSTGRES_DB: outline" docker-compose.yml` succeeds
    - `grep -q "pg_isready -U outline -d outline" docker-compose.yml` succeeds
    - No host port binding on outline-db or outline-redis (no `ports:` under either)
  </acceptance_criteria>
  <done>docker-compose.yml has two new healthcheck-gated service blocks + three new top-level volume entries with names that do NOT collide with KPI Light's `postgres_data`.</done>
</task>

<task type="auto">
  <name>Task 2: Add outline service block (OIDC split endpoints + mkcert CA) and extend npm depends_on</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (confirm outline-db/outline-redis blocks from Task 1 exist)
    - .planning/phases/29-outline-wiki-deployment/29-RESEARCH.md §Outline service block (canonical shape), §Pitfalls 1-7
    - .planning/phases/29-outline-wiki-deployment/29-CONTEXT.md §D-02, §D-05
    - docker-compose.yml lines 22-71 (api service — reference pattern for mkcert CA + extra_hosts)
    - docker-compose.yml lines 118-124 (npm depends_on — the block to extend)
  </read_first>
  <action>
    Two edits in docker-compose.yml:

    **Edit A — Insert `outline` service block** AFTER the `outline-redis` block from Task 1 (still before top-level `volumes:`). Use exactly this YAML:

    ```yaml
      outline:
        image: outlinewiki/outline:0.86.0
        restart: unless-stopped
        env_file: .env
        environment:
          NODE_ENV: production
          PORT: "3000"
          URL: https://wiki.internal
          SECRET_KEY: ${OUTLINE_SECRET_KEY}
          UTILS_SECRET: ${OUTLINE_UTILS_SECRET}
          DATABASE_URL: postgres://outline:${OUTLINE_DB_PASSWORD}@outline-db:5432/outline
          PGSSLMODE: disable
          REDIS_URL: redis://outline-redis:6379
          FILE_STORAGE: local
          FILE_STORAGE_LOCAL_ROOT_DIR: /var/lib/outline/data
          FILE_STORAGE_UPLOAD_MAX_SIZE: "262144000"
          FORCE_HTTPS: "true"
          DEFAULT_LANGUAGE: en_US
          OIDC_CLIENT_ID: outline
          OIDC_CLIENT_SECRET: ${DEX_OUTLINE_SECRET}
          OIDC_AUTH_URI: https://auth.internal/dex/auth
          OIDC_TOKEN_URI: http://dex:5556/dex/token
          OIDC_USERINFO_URI: http://dex:5556/dex/userinfo
          OIDC_USERNAME_CLAIM: preferred_username
          OIDC_DISPLAY_NAME: name
          OIDC_SCOPES: openid profile email offline_access
          NODE_EXTRA_CA_CERTS: /etc/ssl/certs/mkcert-rootCA.pem
        volumes:
          # D-05: mount mkcert root CA read-only; Node reads it via NODE_EXTRA_CA_CERTS
          # (cleaner than the api service's update-ca-certificates dance because Node
          # honours NODE_EXTRA_CA_CERTS natively — RESEARCH §Architecture Patterns).
          - ./certs/rootCA.pem:/etc/ssl/certs/mkcert-rootCA.pem:ro
          # Pitfall 5: named volume (not anonymous) so attachments survive recreate.
          - outline_uploads:/var/lib/outline/data
        extra_hosts:
          # D-05: host-gateway for browser-hostname OIDC discovery + iss validation,
          # even though token/userinfo use internal docker DNS.
          - "auth.internal:host-gateway"
        depends_on:
          outline-db:
            condition: service_healthy
          outline-redis:
            condition: service_healthy
          dex:
            condition: service_healthy
        # No host port binding — NPM is the edge (Phase 26 D-07). Uncomment for debug.
        # ports:
        #   - "3000:3000"
        # Dockerfile HEALTHCHECK already probes http://localhost:3000/_health
        # (v0.86.0 — the 0.79.0 bug is fixed; no compose-level override needed).
    ```

    Rules applied:
    - Per D-02: `OIDC_USERNAME_CLAIM=preferred_username`, `OIDC_DISPLAY_NAME=name`, `OIDC_SCOPES="openid profile email offline_access"`
    - Per D-05: split endpoints (auth=https, token/userinfo=http internal DNS), mkcert CA mount, extra_hosts
    - Per WIK-06: NO `SMTP_*` env vars anywhere in this block
    - Per D-03: NO `TEAM_LOGO_URL` or team-name env
    - Per Pitfall 1: `URL: https://wiki.internal` — no trailing slash, exact match
    - Per Pitfall 2: `SECRET_KEY` and `UTILS_SECRET` point to two different env vars (enforced by plan 29-01 Task 1 comment)
    - Per RESEARCH §Don't Hand-Roll: no custom healthcheck (Dockerfile default handles it); no separate outline-migrate service (Outline runs migrations at startup)

    **Edit B — Extend `npm.depends_on`** (currently at lines ~118-124 with api/frontend/dex entries). Add a fourth entry so NPM waits for Outline healthy before accepting traffic (closes the 502 window on cold boot per INF-05 pattern established in Phase 26). Inside the existing `npm:` service's `depends_on:` mapping, append:

    ```yaml
          outline:
            condition: service_healthy
    ```

    Keep the existing api/frontend/dex entries unchanged.
  </action>
  <verify>
    <automated>docker compose config --quiet && docker compose config | grep -q "image: outlinewiki/outline:0.86.0" && docker compose config | grep -q "OIDC_AUTH_URI: https://auth.internal/dex/auth" && docker compose config | grep -q "OIDC_TOKEN_URI: http://dex:5556/dex/token" && docker compose config | grep -q "OIDC_USERINFO_URI: http://dex:5556/dex/userinfo" && docker compose config | grep -q "NODE_EXTRA_CA_CERTS: /etc/ssl/certs/mkcert-rootCA.pem" && docker compose config | grep -q "auth.internal:host-gateway" && docker compose config | grep -q "outline_uploads:/var/lib/outline/data" && ! docker compose config | grep -qE "^\s*SMTP_" && docker compose config services | grep -qx "outline"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` exits 0
    - `docker compose config services` lists `outline`, `outline-db`, `outline-redis` (three new entries)
    - `grep -q "URL: https://wiki.internal$" docker-compose.yml` succeeds (NO trailing slash — Pitfall 1)
    - `grep -q "OIDC_CLIENT_ID: outline" docker-compose.yml` succeeds
    - `grep -q "OIDC_CLIENT_SECRET: \${DEX_OUTLINE_SECRET}" docker-compose.yml` succeeds (var reuse, not hardcoded)
    - `grep -q "OIDC_AUTH_URI: https://auth.internal/dex/auth" docker-compose.yml` succeeds
    - `grep -q "OIDC_TOKEN_URI: http://dex:5556/dex/token" docker-compose.yml` succeeds
    - `grep -q "OIDC_USERINFO_URI: http://dex:5556/dex/userinfo" docker-compose.yml` succeeds
    - `grep -q "OIDC_USERNAME_CLAIM: preferred_username" docker-compose.yml` succeeds (D-02)
    - `grep -q "OIDC_SCOPES: openid profile email offline_access" docker-compose.yml` succeeds (D-02)
    - `grep -q "FILE_STORAGE: local" docker-compose.yml` succeeds (WIK-03)
    - `grep -q "NODE_EXTRA_CA_CERTS: /etc/ssl/certs/mkcert-rootCA.pem" docker-compose.yml` succeeds
    - `grep -q "./certs/rootCA.pem:/etc/ssl/certs/mkcert-rootCA.pem:ro" docker-compose.yml` succeeds
    - `grep -q "outline_uploads:/var/lib/outline/data" docker-compose.yml` succeeds (Pitfall 5)
    - `grep -q '"auth.internal:host-gateway"' docker-compose.yml` appears at least twice (api + outline)
    - `! grep -qE "^\s*SMTP_" docker-compose.yml` (WIK-06)
    - `! grep -q "TEAM_LOGO_URL" docker-compose.yml` (D-03)
    - `! grep -q "OIDC_REDIRECT_URI" docker-compose.yml` (RESEARCH anti-pattern — Outline constructs this from URL)
    - `! grep -q "outlinewiki/outline:latest" docker-compose.yml` (pinned tag only)
    - The `npm:` service's `depends_on:` now contains an `outline:` entry with `condition: service_healthy` (grep for the pattern in the npm block)
    - `grep -c "condition: service_healthy" docker-compose.yml` increased by 4 vs pre-plan (outline-db gate in outline, outline-redis gate in outline, dex gate in outline, outline gate in npm)
  </acceptance_criteria>
  <done>docker-compose.yml fully declares the outline service with OIDC split endpoints + mkcert CA + uploads volume + healthcheck-gated deps; NPM waits for outline healthy; `docker compose config` validates cleanly; no SMTP/branding/latest-tag leakage.</done>
</task>

</tasks>

<verification>
Full-stack boot check (operator runs after both tasks; informative not blocking for Wave 2 gate):
```bash
docker compose config --quiet && echo "compose YAML OK"
# With real secrets in .env:
docker compose up -d outline-db outline-redis
docker compose ps --format json outline-db outline-redis | jq -r '.Health' | grep -c healthy   # expect 2
docker compose up -d outline
# Wait ~30s for Outline migrations + start
docker compose ps --format json outline | jq -r '.Health' | grep -q healthy
```

Plan 29-03 owns the NPM proxy-host + browser-facing Success Criteria checks; this plan's gate is: YAML valid and the three services come healthy when started directly.
</verification>

<success_criteria>
- `docker compose config --quiet` passes with `.env` present
- Three new services (`outline-db`, `outline-redis`, `outline`) each have healthchecks and correct dependency gates
- Volumes are distinctly named (no collision with `postgres_data`)
- OIDC wiring uses split endpoints exactly per D-05
- No SMTP, no workspace branding, no `latest` tags, no host port bindings on the new services
- NPM now waits for Outline healthy before starting
</success_criteria>

<output>
Create `.planning/phases/29-outline-wiki-deployment/29-02-SUMMARY.md` — list every env var added to outline, confirm requirements closed (WIK-01, WIK-02, WIK-03, WIK-04, WIK-06), and note any execution-time fixes (e.g. if Outline logs show issuer-validation errors or pg_trgm surprise from RESEARCH Open Question 3).
</output>
