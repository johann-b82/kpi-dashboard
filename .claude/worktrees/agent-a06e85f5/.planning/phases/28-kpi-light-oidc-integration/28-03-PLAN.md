---
phase: 28-kpi-light-oidc-integration
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - backend/app/routers/uploads.py
  - backend/app/routers/kpis.py
  - backend/app/routers/settings.py
  - backend/app/routers/sync.py
  - backend/app/routers/hr_kpis.py
  - backend/app/routers/data.py
autonomous: true
requirements: [KPO-07]
must_haves:
  truths:
    - "All six business routers return 401 when called without a kpi_session cookie"
    - "/health remains publicly reachable (200 without auth)"
    - "/api/auth/* remain publicly reachable at the login/callback/logout level (no accidental guard)"
    - "With DISABLE_AUTH=true, all six routers respond normally (synthetic user injected via get_current_user)"
  artifacts:
    - path: backend/app/routers/uploads.py
      provides: "uploads router guarded at APIRouter(dependencies=[Depends(get_current_user)])"
    - path: backend/app/routers/kpis.py
      provides: "kpis router guarded"
    - path: backend/app/routers/settings.py
      provides: "settings router guarded"
    - path: backend/app/routers/sync.py
      provides: "sync router guarded"
    - path: backend/app/routers/hr_kpis.py
      provides: "hr_kpis router guarded"
    - path: backend/app/routers/data.py
      provides: "data router guarded"
  key_links:
    - from: backend/app/routers/*.py
      to: backend/app/security/auth.py (get_current_user)
      via: "APIRouter(prefix=..., dependencies=[Depends(get_current_user)])"
      pattern: "dependencies=\\[Depends\\(get_current_user\\)\\]"
---

<objective>
One-line-per-router edit: add `dependencies=[Depends(get_current_user)]` to every existing APIRouter constructor. This plan is parallel with Plan 02 — it only touches the six business routers, never `main.py`, `auth.py`, or the new auth router.

Purpose: Deliver KPO-07 (auth gates on all business routes) with a minimal, auditable diff.
Output: All `/api/{settings,uploads,kpis,hr/*,sync,data/*}` routes return 401 without a session cookie; no logic changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-kpi-light-oidc-integration/28-CONTEXT.md
@.planning/phases/28-kpi-light-oidc-integration/28-RESEARCH.md
@.planning/phases/28-kpi-light-oidc-integration/28-01-PLAN.md
@CLAUDE.md

@backend/app/routers/uploads.py
@backend/app/routers/kpis.py
@backend/app/routers/settings.py
@backend/app/routers/sync.py
@backend/app/routers/hr_kpis.py
@backend/app/routers/data.py
@backend/app/security/auth.py
</context>

<interfaces>
From Plan 01:
```python
# backend/app/security/auth.py
def get_current_user(request: Request) -> CurrentUser  # 401 if unauth, synthetic if DISABLE_AUTH
```
Existing router pattern in each file (example from settings.py):
```python
router = APIRouter(prefix="/api/settings", tags=["settings"])
```
Target pattern:
```python
router = APIRouter(
    prefix="/api/settings",
    tags=["settings"],
    dependencies=[Depends(get_current_user)],
)
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Depends(get_current_user) to all six business routers</name>
  <files>backend/app/routers/uploads.py, backend/app/routers/kpis.py, backend/app/routers/settings.py, backend/app/routers/sync.py, backend/app/routers/hr_kpis.py, backend/app/routers/data.py</files>
  <read_first>
    - backend/app/routers/uploads.py (see current APIRouter signature; mirror across the six)
    - backend/app/routers/kpis.py
    - backend/app/routers/settings.py
    - backend/app/routers/sync.py
    - backend/app/routers/hr_kpis.py
    - backend/app/routers/data.py
    - backend/app/security/auth.py (exact import path for get_current_user)
    - .planning/phases/28-kpi-light-oidc-integration/28-CONTEXT.md D-17, D-18
    - .planning/phases/28-kpi-light-oidc-integration/28-RESEARCH.md §Code Examples 7, §Open Questions 1
  </read_first>
  <action>
    For EACH of the six files, make two changes:

    1) Ensure imports include `Depends` from fastapi AND `get_current_user` from `app.security.auth`. If `from fastapi import APIRouter` already exists, extend it to `from fastapi import APIRouter, Depends` (merge, don't duplicate). Add a new line `from app.security.auth import get_current_user` near the other app-module imports.

    2) Find the existing `router = APIRouter(...)` call and add `dependencies=[Depends(get_current_user)]` as a new kwarg. Example delta (uploads.py):
       ```python
       # BEFORE
       router = APIRouter(prefix="/api/uploads", tags=["uploads"])

       # AFTER
       router = APIRouter(
           prefix="/api/uploads",
           tags=["uploads"],
           dependencies=[Depends(get_current_user)],
       )
       ```

    Apply the identical pattern to:
    - `uploads.py` (prefix `/api/uploads`)
    - `kpis.py` (prefix `/api/kpis`)
    - `settings.py` (prefix `/api/settings`)
    - `sync.py` (prefix `/api/sync`)
    - `hr_kpis.py` (prefix `/api/hr` or similar — confirm while reading)
    - `data.py` (prefix `/api/data`)

    Do NOT touch `backend/app/routers/auth.py` (new in Plan 02) — auth endpoints stay open per D-18.
    Do NOT touch `backend/app/main.py` `/health` endpoint — it's on `app` directly, not a router, so it's already unaffected.
    Do NOT remove any existing per-endpoint dependencies that may exist — append the router-level one alongside.
  </action>
  <verify>
    <automated>docker compose up -d api &amp;&amp; sleep 5 &amp;&amp; for p in /api/settings /api/uploads /api/kpis /api/sync /api/data; do code=$(curl -sk -o /dev/null -w "%{http_code}" "https://kpi.internal$p"); echo "$p $code"; test "$code" = "401" || exit 1; done &amp;&amp; test "$(curl -sk -o /dev/null -w "%{http_code}" https://kpi.internal/health)" = "200"</automated>
    Also for hr: `curl -sk -o /dev/null -w "%{http_code}" https://kpi.internal/api/hr/kpis` (or equivalent hr_kpis endpoint) should be 401.
  </verify>
  <acceptance_criteria>
    - `grep -l "dependencies=\[Depends(get_current_user)\]" backend/app/routers/{uploads,kpis,settings,sync,hr_kpis,data}.py | wc -l` equals 6
    - `grep -q "from app.security.auth import get_current_user" backend/app/routers/uploads.py` (and each of the other five)
    - `GET /api/settings`, `/api/uploads`, `/api/kpis`, `/api/sync`, `/api/hr/kpis`, `/api/data/...` all return 401 without a session
    - `GET /health` returns 200
    - `GET /api/auth/me` without session still returns 401 (did NOT regress to something else)
    - `GET /api/auth/login` is still reachable (NOT 401 — must be 302/303/307 toward Dex)
    - With `DISABLE_AUTH=true` set, the same six endpoints return 200 (or their real response) — not 401
  </acceptance_criteria>
  <done>Every business endpoint requires auth; /health and /api/auth/* stay open; diff is six files, each ~3 lines changed.</done>
</task>

</tasks>

<verification>
1. `docker compose up -d api` clean boot, no ImportError.
2. The 5-endpoint curl loop above all returns 401.
3. `/health` returns 200.
4. `/api/auth/login` redirects (not 401).
5. Flip `DISABLE_AUTH=true`, restart api, confirm `curl -sk https://kpi.internal/api/settings` returns 200 (synthetic user passes the gate).
</verification>

<success_criteria>
- KPO-07 requirement met: six business routers all return 401 without a session, 200 with a valid session or under DISABLE_AUTH.
- No regressions on `/health` or `/api/auth/*`.
</success_criteria>

<output>
After completion, create `.planning/phases/28-kpi-light-oidc-integration/28-03-SUMMARY.md` listing the actual prefix strings found in each router (useful reference for Plan 05 docs).
</output>
