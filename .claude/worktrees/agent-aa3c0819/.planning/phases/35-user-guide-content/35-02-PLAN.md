---
phase: 35-user-guide-content
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - frontend/src/docs/en/user-guide/filters.md
  - frontend/src/docs/de/user-guide/filters.md
  - frontend/src/docs/en/user-guide/language-and-theme.md
  - frontend/src/docs/de/user-guide/language-and-theme.md
  - frontend/src/lib/docs/registry.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
autonomous: true
requirements:
  - UGUIDE-04
  - UGUIDE-05

must_haves:
  truths:
    - "User can read a complete article on filters, date ranges, and chart controls"
    - "User can read a complete article on language switching and dark mode"
    - "Both articles exist in EN and DE"
    - "All 6 user guide articles (intro + 5 features) appear in the sidebar"
    - "All 6 articles render content when clicked in the sidebar"
  artifacts:
    - path: "frontend/src/docs/en/user-guide/filters.md"
      provides: "EN filters article"
      contains: "# Filters"
    - path: "frontend/src/docs/de/user-guide/filters.md"
      provides: "DE filters article"
      contains: "# Filter"
    - path: "frontend/src/docs/en/user-guide/language-and-theme.md"
      provides: "EN language/theme article"
      contains: "# Language"
    - path: "frontend/src/docs/de/user-guide/language-and-theme.md"
      provides: "DE language/theme article"
      contains: "# Sprache"
    - path: "frontend/src/lib/docs/registry.ts"
      provides: "Registry with all 6 user guide articles"
      contains: "uploading-data"
    - path: "frontend/src/locales/en.json"
      provides: "EN i18n nav keys for all articles"
      contains: "uploadingData"
    - path: "frontend/src/locales/de.json"
      provides: "DE i18n nav keys for all articles"
      contains: "uploadingData"
  key_links:
    - from: "frontend/src/lib/docs/registry.ts"
      to: "frontend/src/docs/en/user-guide/*.md"
      via: "Vite ?raw imports"
      pattern: "import.*user-guide.*\\.md\\?raw"
    - from: "frontend/src/lib/docs/registry.ts sections"
      to: "frontend/src/locales/en.json"
      via: "titleKey references"
      pattern: "docs\\.nav\\."
---

<objective>
Write the remaining 2 user guide articles (filters + language/theme) in EN and DE, then wire ALL 5 new articles (from Plan 01 and this plan) into the registry and i18n locale files.

Purpose: Complete the user guide content and make all articles accessible via the sidebar navigation.
Output: 4 Markdown files + updated registry.ts + updated en.json and de.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-user-guide-content/35-CONTEXT.md
@.planning/phases/35-user-guide-content/35-RESEARCH.md
@.planning/phases/35-user-guide-content/35-UI-SPEC.md
@.planning/phases/35-user-guide-content/35-01-SUMMARY.md

<interfaces>
From frontend/src/lib/docs/registry.ts (current state):
```typescript
import enUserIntro from "../../docs/en/user-guide/intro.md?raw";
import deUserIntro from "../../docs/de/user-guide/intro.md?raw";
import enAdminIntro from "../../docs/en/admin-guide/intro.md?raw";
import deAdminIntro from "../../docs/de/admin-guide/intro.md?raw";

export type ArticleEntry = { slug: string; titleKey: string };
export type SectionId = "user-guide" | "admin-guide";

export const sections: Record<SectionId, ArticleEntry[]> = {
  "user-guide": [{ slug: "intro", titleKey: "docs.nav.userGuideIntro" }],
  "admin-guide": [{ slug: "intro", titleKey: "docs.nav.adminGuideIntro" }],
};

export const registry: Record<string, Record<string, Record<string, string>>> = {
  en: {
    "user-guide": { intro: enUserIntro },
    "admin-guide": { intro: enAdminIntro },
  },
  de: {
    "user-guide": { intro: deUserIntro },
    "admin-guide": { intro: deAdminIntro },
  },
};
```

Current i18n nav keys (en.json):
```json
"docs.nav.userGuideIntro": "Introduction",
"docs.nav.adminGuideIntro": "Introduction"
```

Current i18n nav keys (de.json):
```json
"docs.nav.userGuideIntro": "Einleitung",
"docs.nav.adminGuideIntro": "Einleitung"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write EN + DE articles for filters and language/theme</name>
  <files>
    frontend/src/docs/en/user-guide/filters.md
    frontend/src/docs/de/user-guide/filters.md
    frontend/src/docs/en/user-guide/language-and-theme.md
    frontend/src/docs/de/user-guide/language-and-theme.md
  </files>
  <read_first>
    frontend/src/locales/en.json
    frontend/src/locales/de.json
    frontend/src/components/dashboard/DateRangeFilter.tsx
    frontend/src/components/dashboard/RevenueChart.tsx
    frontend/src/components/hr/HrKpiCharts.tsx
    frontend/src/components/LanguageToggle.tsx
    frontend/src/components/ThemeToggle.tsx
    .planning/phases/35-user-guide-content/35-RESEARCH.md
    .planning/phases/35-user-guide-content/35-UI-SPEC.md
  </read_first>
  <action>
Write 4 Markdown files following all locked decisions (D-01 through D-07). Same D-05 template. UI labels from locale files verbatim.

**EN filters.md** (UGUIDE-04) — `# Filters & Date Ranges`:
- Date range filter (Sales dashboard only — HR has no date filter): location at top of Sales dashboard, 4 presets: "This month" | "This quarter" | "This year" | "All time" as segmented control, default "This month", persists while on page, resets on navigation
- Chart type control on Sales: "Bar" | "Area" segmented control top-right of Revenue chart, Bar is default
- Chart type control on HR: "Area" | "Bar" segmented control on HR charts
- Delta badge behavior by preset: This month = vs prior month + prior year; This quarter = vs prior quarter + prior year; This year = single badge vs prior year YTD; All time = no badges
- `> **Note:** The date range filter applies to the Sales dashboard only. The HR dashboard always shows data relative to the current date.`
- Related articles: Sales Dashboard, HR Dashboard

**DE filters.md** — `# Filter & Zeitraeume` (or natural DE equivalent): Same scope, natural German, DE labels from de.json.

**EN language-and-theme.md** (UGUIDE-05) — `# Language & Dark Mode`:
- Language toggle: location in top nav bar right side; shows target language label ("DE" when in English, "EN" when in German); click switches immediately, entire app re-renders; persisted via localStorage, survives refresh; no server round-trip
- Dark mode toggle: location in top nav bar right side next to language; Moon icon in light mode, Sun icon in dark mode; follows OS `prefers-color-scheme` until first manual click; after manual click, user preference locks in localStorage; OS changes no longer override
- `> **Tip:** If you want to go back to following your operating system's theme, clear your browser's local storage for this site.`
- Related articles: link back to intro

**DE language-and-theme.md** — `# Sprache & Dunkelmodus`: Same scope, natural German, DE labels from de.json.
  </action>
  <verify>
    <automated>for f in filters.md language-and-theme.md; do for lang in en de; do test -f frontend/src/docs/$lang/user-guide/$f && echo "OK: $lang/$f" || echo "MISSING: $lang/$f"; done; done</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/docs/en/user-guide/filters.md contains "# Filters"
    - frontend/src/docs/en/user-guide/filters.md contains "This month"
    - frontend/src/docs/en/user-guide/filters.md contains "> **Note:**"
    - frontend/src/docs/en/user-guide/filters.md contains "## Related Articles"
    - frontend/src/docs/de/user-guide/filters.md contains at least 3 `##` headings
    - frontend/src/docs/en/user-guide/language-and-theme.md contains "# Language"
    - frontend/src/docs/en/user-guide/language-and-theme.md contains "localStorage"
    - frontend/src/docs/en/user-guide/language-and-theme.md contains "## Related Articles"
    - frontend/src/docs/de/user-guide/language-and-theme.md contains "# Sprache"
  </acceptance_criteria>
  <done>4 Markdown files (2 EN + 2 DE) covering filters/date ranges and language/theme switching</done>
</task>

<task type="auto">
  <name>Task 2: Wire all 5 new articles into registry.ts and i18n locale files</name>
  <files>
    frontend/src/lib/docs/registry.ts
    frontend/src/locales/en.json
    frontend/src/locales/de.json
  </files>
  <read_first>
    frontend/src/lib/docs/registry.ts
    frontend/src/locales/en.json
    frontend/src/locales/de.json
  </read_first>
  <action>
**registry.ts** — Add 10 new raw imports and wire into sections + registry. The final file must contain:

Imports to ADD (below existing imports):
```typescript
import enUploadingData from "../../docs/en/user-guide/uploading-data.md?raw";
import deUploadingData from "../../docs/de/user-guide/uploading-data.md?raw";
import enSalesDashboard from "../../docs/en/user-guide/sales-dashboard.md?raw";
import deSalesDashboard from "../../docs/de/user-guide/sales-dashboard.md?raw";
import enHrDashboard from "../../docs/en/user-guide/hr-dashboard.md?raw";
import deHrDashboard from "../../docs/de/user-guide/hr-dashboard.md?raw";
import enFilters from "../../docs/en/user-guide/filters.md?raw";
import deFilters from "../../docs/de/user-guide/filters.md?raw";
import enLanguageAndTheme from "../../docs/en/user-guide/language-and-theme.md?raw";
import deLanguageAndTheme from "../../docs/de/user-guide/language-and-theme.md?raw";
```

Sections `"user-guide"` array must become:
```typescript
[
  { slug: "intro", titleKey: "docs.nav.userGuideIntro" },
  { slug: "uploading-data", titleKey: "docs.nav.uploadingData" },
  { slug: "sales-dashboard", titleKey: "docs.nav.salesDashboard" },
  { slug: "hr-dashboard", titleKey: "docs.nav.hrDashboard" },
  { slug: "filters", titleKey: "docs.nav.filters" },
  { slug: "language-and-theme", titleKey: "docs.nav.languageAndTheme" },
]
```

Registry `en["user-guide"]` must become:
```typescript
{
  intro: enUserIntro,
  "uploading-data": enUploadingData,
  "sales-dashboard": enSalesDashboard,
  "hr-dashboard": enHrDashboard,
  filters: enFilters,
  "language-and-theme": enLanguageAndTheme,
}
```
Same pattern for `de["user-guide"]` with `de*` imports.

**en.json** — Add these keys inside `docs.nav` (keep existing keys):
```json
"uploadingData": "Uploading Data",
"salesDashboard": "Sales Dashboard",
"hrDashboard": "HR Dashboard",
"filters": "Filters & Date Ranges",
"languageAndTheme": "Language & Dark Mode"
```

**de.json** — Add these keys inside `docs.nav` (keep existing keys):
```json
"uploadingData": "Daten hochladen",
"salesDashboard": "Umsatz-Dashboard",
"hrDashboard": "HR-Dashboard",
"filters": "Filter & Zeitraeume",
"languageAndTheme": "Sprache & Dunkelmodus"
```

Note: Use the actual German characters. "Zeitraeume" should be "Zeiträume" (with umlaut).
  </action>
  <verify>
    <automated>grep -c "uploading-data" frontend/src/lib/docs/registry.ts && grep -c "uploadingData" frontend/src/locales/en.json && grep -c "uploadingData" frontend/src/locales/de.json && cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/docs/registry.ts contains `import enUploadingData from`
    - frontend/src/lib/docs/registry.ts contains `import deLanguageAndTheme from`
    - frontend/src/lib/docs/registry.ts sections["user-guide"] has 6 entries (grep for `slug:` lines gives 6+ in user-guide block)
    - frontend/src/lib/docs/registry.ts contains `"uploading-data": enUploadingData`
    - frontend/src/lib/docs/registry.ts contains `"language-and-theme": enLanguageAndTheme`
    - frontend/src/locales/en.json contains `"uploadingData": "Uploading Data"`
    - frontend/src/locales/en.json contains `"languageAndTheme": "Language & Dark Mode"`
    - frontend/src/locales/de.json contains `"uploadingData": "Daten hochladen"`
    - frontend/src/locales/de.json contains `"languageAndTheme":`
    - `npx tsc --noEmit` passes with no errors related to registry.ts
  </acceptance_criteria>
  <done>All 5 new user guide articles wired into registry with imports, sections entries, registry entries, and i18n title keys in both languages; TypeScript compiles clean</done>
</task>

</tasks>

<verification>
- All 10 new/modified markdown files exist (2 intro rewrites + 8 new articles)
- Registry has 6 user-guide entries in sections array
- Registry has 6 entries in both en["user-guide"] and de["user-guide"]
- en.json and de.json each have 5 new docs.nav keys
- TypeScript compilation passes
- `npm run build` in frontend succeeds (Vite resolves all ?raw imports)
</verification>

<success_criteria>
All 5 user guide feature articles (UGUIDE-01 through UGUIDE-05) exist in both EN and DE, are wired into the registry and sidebar, and the app builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/35-user-guide-content/35-02-SUMMARY.md`
</output>
