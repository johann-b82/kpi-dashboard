---
phase: 31-seed-outline-docs
plan: 04
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - docs/wiki-seed/05-sales-dashboard.md
  - docs/wiki-seed/06-hr-dashboard.md
  - docs/wiki-seed/07-settings.md
autonomous: false
requirements: [DOC-05, DOC-06, DOC-07]
must_haves:
  truths:
    - "DOC-05 covers KPI card reading, delta labels, chart toggle, date-range presets, sales table"
    - "DOC-06 covers 5 HR KPIs, 12-month trend charts, target reference lines (Sollwerte), employee table filters"
    - "DOC-07 covers Appearance (branding, colors, logo), HR section (Personio + Sollwerte), dark-mode toggle, language toggle"
    - "All UI label references use EN strings from frontend/src/locales/en.json (D-07)"
  artifacts:
    - path: "docs/wiki-seed/05-sales-dashboard.md"
      provides: "DOC-05 snapshot"
    - path: "docs/wiki-seed/06-hr-dashboard.md"
      provides: "DOC-06 snapshot"
    - path: "docs/wiki-seed/07-settings.md"
      provides: "DOC-07 snapshot"
---

<objective>
Author the three user-facing guides (Sales Dashboard, HR Dashboard, Settings Walkthrough) in Outline and snapshot them. These are parallelizable with plan 31-03 (no file conflicts).

Purpose: Cover end-user guidance.
Output: Three authored Outline pages + three snapshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-seed-outline-docs/31-CONTEXT.md
@.planning/phases/31-seed-outline-docs/31-RESEARCH.md
@frontend/src/locales/en.json
@frontend/src/components/NavBar.tsx

<interfaces>
UI label invariant (D-07): reference EN labels as they appear in the app (e.g. "Settings → Appearance → Logo").

5 HR KPIs (RESEARCH §Content Grounding DOC-06): overtimeRatio, sickLeaveRatio, fluctuation, skillDevelopment, revenuePerProductionEmployee.

Settings sections (from en.json settings.*): Appearance (branding, colors, logo), HR (Personio + Sollwerte targets).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Author DOC-05 Sales Dashboard User Guide in Outline + snapshot</name>
  <what-built>DOC-05 prose guide.</what-built>
  <how-to-verify>
    Operator authors covering (RESEARCH §DOC-05):
      - Reading KPI cards (primary metric, delta labels vs. prior period)
      - Delta label interpretation (vsMonth/vsQuarter/vsYear templates)
      - Chart toggle (line vs bar where applicable)
      - Date-range presets
      - Sales table usage (sort, filter, pagination)
      - Cross-links to DOC-02 Architecture + DOC-07 Settings
    Then: ⋯ → Download → Markdown → paste.
  </how-to-verify>
  <resume-signal>Paste exported markdown</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 2: Write DOC-05 snapshot + invariants</name>
  <files>docs/wiki-seed/05-sales-dashboard.md</files>
  <action>Write pasted content with sanitization pass.</action>
  <verify>
    <automated>grep -qi "KPI" docs/wiki-seed/05-sales-dashboard.md &amp;&amp; grep -qi "date" docs/wiki-seed/05-sales-dashboard.md &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/05-sales-dashboard.md</automated>
  </verify>
  <done>DOC-05 snapshot committed, invariants clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author DOC-06 HR Dashboard User Guide in Outline + snapshot</name>
  <what-built>DOC-06 prose guide.</what-built>
  <how-to-verify>
    Operator authors covering (RESEARCH §DOC-06):
      - 5 HR KPIs: Overtime Ratio, Sick Leave Ratio, Fluctuation, Skill Development, Revenue per Production Employee
      - 12-month trend charts
      - Target reference lines (Sollwerte)
      - Employee table filters (multi-select criteria)
      - Cross-links to DOC-07 Settings (where Sollwerte are configured) + DOC-04 Personio Sync
    Then: ⋯ → Download → Markdown → paste.
  </how-to-verify>
  <resume-signal>Paste exported markdown</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 4: Write DOC-06 snapshot + invariants</name>
  <files>docs/wiki-seed/06-hr-dashboard.md</files>
  <action>Write pasted content with sanitization pass. Confirm all 5 HR KPIs referenced.</action>
  <verify>
    <automated>for kpi in "Overtime" "Sick" "Fluctuation" "Skill" "Revenue"; do grep -qi "$kpi" docs/wiki-seed/06-hr-dashboard.md || { echo "missing $kpi"; exit 1; }; done &amp;&amp; grep -qi "Sollwerte" docs/wiki-seed/06-hr-dashboard.md &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/06-hr-dashboard.md</automated>
  </verify>
  <done>DOC-06 snapshot covers 5 HR KPIs + Sollwerte + invariants clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Author DOC-07 Settings Walkthrough in Outline + snapshot</name>
  <what-built>DOC-07 prose guide.</what-built>
  <how-to-verify>
    Operator authors covering (RESEARCH §DOC-07):
      - Appearance section: branding, colors, logo upload
      - HR section: Personio credentials + HR Targets (Sollwerte)
      - Dark-mode toggle (Moon/Sun icon in NavBar — see Phase 22)
      - Language toggle (EN/DE)
      - Cross-link to DOC-04 Personio Sync Runbook + DOC-06 HR Dashboard
    Then: ⋯ → Download → Markdown → paste.
  </how-to-verify>
  <resume-signal>Paste exported markdown</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 6: Write DOC-07 snapshot + invariants</name>
  <files>docs/wiki-seed/07-settings.md</files>
  <action>Write pasted content with sanitization pass.</action>
  <verify>
    <automated>for kw in "Appearance" "Personio" "Sollwerte" "dark" "language"; do grep -qi "$kw" docs/wiki-seed/07-settings.md || { echo "missing $kw"; exit 1; }; done &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/07-settings.md</automated>
  </verify>
  <done>DOC-07 snapshot covers all required settings areas, invariants clean.</done>
</task>

</tasks>

<success_criteria>
DOC-05, DOC-06, DOC-07 authored in Outline and snapshotted. User guides reflect v1.10 state.
</success_criteria>

<output>
Create `.planning/phases/31-seed-outline-docs/31-04-SUMMARY.md`.
</output>
