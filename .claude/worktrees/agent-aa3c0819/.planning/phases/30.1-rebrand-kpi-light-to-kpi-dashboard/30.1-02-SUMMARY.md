---
phase: 30.1-rebrand-kpi-light-to-kpi-dashboard
plan: 02
subsystem: frontend-i18n-defaults
tags: [rebrand, i18n, frontend, cosmetic]
requires: []
provides:
  - "EN+DE display strings reading 'KPI Dashboard'"
  - "Frontend DEFAULT_SETTINGS.app_name fallback = 'KPI Dashboard'"
affects:
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
  - frontend/src/lib/defaults.ts
tech-stack:
  added: []
  patterns:
    - "Value-only i18n rewrite — keys preserved (D-03)"
key-files:
  created: []
  modified:
    - frontend/src/locales/en.json
    - frontend/src/locales/de.json
    - frontend/src/lib/defaults.ts
decisions:
  - "Honored D-03: rewrote i18n VALUES only; keys (nav.brand, settings.identity.app_name.placeholder) unchanged → zero t() call-site churn"
  - "Committed EN+DE locale pair together (matched pair, single logical change); defaults.ts committed separately per D-05"
metrics:
  duration: "~1 min"
  completed: 2026-04-15
---

# Phase 30.1 Plan 02: Frontend i18n Values + Defaults Rename Summary

Rebranded frontend display strings from "KPI Light" → "KPI Dashboard" across both locale files (EN/DE) and `DEFAULT_SETTINGS.app_name` fallback, keeping all i18n keys intact per D-03.

## What Was Built

- **`frontend/src/locales/en.json`** — `nav.brand` and `settings.identity.app_name.placeholder` values changed "KPI Light" → "KPI Dashboard".
- **`frontend/src/locales/de.json`** — same two keys updated (German UI uses the same proper noun).
- **`frontend/src/lib/defaults.ts`** — `DEFAULT_SETTINGS.app_name` changed "KPI Light" → "KPI Dashboard" (pre-API-response cold-start fallback).

## Commits

| Hash      | Message                                                                   | Files                                                 |
| --------- | ------------------------------------------------------------------------- | ----------------------------------------------------- |
| `7a93f7c` | feat(30.1): rebrand frontend i18n values KPI Light → KPI Dashboard        | frontend/src/locales/en.json, frontend/src/locales/de.json |
| `d9c1ab1` | feat(30.1): rebrand frontend default app_name KPI Light → KPI Dashboard   | frontend/src/lib/defaults.ts                          |

## Verification

Grep report on `frontend/src/`:

```
$ grep -rn "KPI Light" frontend/src/
(zero hits)

$ grep -rn "KPI Dashboard" frontend/src/locales/ frontend/src/lib/defaults.ts | wc -l
5      # 2 nav.brand + 2 placeholders + 1 defaults.ts

$ grep -E '"(nav\.brand|settings\.identity\.app_name\.placeholder)":' frontend/src/locales/*.json | wc -l
4      # 2 keys × 2 locale files — all keys preserved
```

Both locale files parse as valid JSON (python3 `json.load` — passed).

## Deviations from Plan

None — plan executed exactly as written. The interfaces block already noted that the real key is `nav.brand` (not the `common.appName` mentioned in D-03's prose); grep confirmed this and the exact edits matched the plan's observed-hits list.

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: frontend/src/locales/en.json (modified — zero "KPI Light")
- FOUND: frontend/src/locales/de.json (modified — zero "KPI Light")
- FOUND: frontend/src/lib/defaults.ts (modified — app_name: "KPI Dashboard")
- FOUND: commit 7a93f7c
- FOUND: commit d9c1ab1
