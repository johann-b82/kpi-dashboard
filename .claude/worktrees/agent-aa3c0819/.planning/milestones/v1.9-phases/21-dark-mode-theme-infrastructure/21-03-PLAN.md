---
phase: 21-dark-mode-theme-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/UploadHistory.tsx
  - frontend/src/components/DropZone.tsx
  - frontend/src/components/ErrorList.tsx
  - frontend/src/components/dashboard/EmployeeTable.tsx
  - frontend/src/components/settings/PersonioCard.tsx
autonomous: true
requirements: [DM-01, DM-02]
must_haves:
  truths:
    - "No file in files_modified contains hardcoded Tailwind gray/slate/neutral/zinc utilities on the UI surface"
    - "Status/semantic colors (success, warning, destructive) use CSS variable references that keep semantic consistency across modes"
    - "Drag-state and primary button colors use --primary token — brand-coherent in both modes"
  artifacts:
    - path: "frontend/src/components/UploadHistory.tsx"
      provides: "Upload history list with token-based text and status badges"
    - path: "frontend/src/components/DropZone.tsx"
      provides: "File drop zone with token-based drag/hover/error states"
    - path: "frontend/src/components/ErrorList.tsx"
      provides: "Error list with destructive-token styling"
    - path: "frontend/src/components/dashboard/EmployeeTable.tsx"
      provides: "Employee table with token-based status badges"
    - path: "frontend/src/components/settings/PersonioCard.tsx"
      provides: "Personio card with token-based success indicators"
  key_links:
    - from: "frontend/src/components/UploadHistory.tsx"
      to: "index.css --color-success / --color-warning / --destructive tokens"
      via: "Tailwind arbitrary value utilities bg-[var(--color-success)] etc."
      pattern: "var\\(--color-(success|warning)\\)|bg-destructive"
    - from: "frontend/src/components/DropZone.tsx"
      to: "--primary token"
      via: "bg-primary / text-primary / border-primary utilities"
      pattern: "(bg|text|border)-primary"
---

<objective>
Replace every hardcoded color in the non-chart frontend components identified in the 21-RESEARCH.md audit with token-based equivalents so these surfaces render correctly in dark mode (DM-01, DM-02).

Purpose: shadcn primitives already adapt (DM-02 largely satisfied at the primitive layer). The remaining DM-01/DM-02 gap is in consumer components that inherited hardcoded colors from the pre-dark-mode era. Without this audit pass, these surfaces stay white-on-dark or blue-on-dark in dark mode.
Output: Five component files updated per the non-negotiable substitution table in 21-UI-SPEC.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-dark-mode-theme-infrastructure/21-CONTEXT.md
@.planning/phases/21-dark-mode-theme-infrastructure/21-RESEARCH.md
@.planning/phases/21-dark-mode-theme-infrastructure/21-UI-SPEC.md
@frontend/src/index.css
@frontend/src/components/UploadHistory.tsx
@frontend/src/components/DropZone.tsx
@frontend/src/components/ErrorList.tsx
@frontend/src/components/dashboard/EmployeeTable.tsx
@frontend/src/components/settings/PersonioCard.tsx

<interfaces>
Substitution table (from 21-UI-SPEC.md §"Hardcoded Color Elimination" and 21-RESEARCH.md §"Hardcoded Color Inventory"):

Gray scale:
- `text-slate-900` → `text-foreground`
- `text-slate-700` → `text-foreground`
- `text-slate-500` → `text-muted-foreground`
- `text-slate-400` → `text-muted-foreground`
- `bg-slate-200` → `bg-muted`
- `bg-slate-100` → `bg-muted`
- `border-slate-300` → `border-border`
- `bg-gray-100 text-gray-600` → `bg-muted text-muted-foreground`

Semantic (these tokens are fixed in both modes per @theme block, semantic consistency per D-09):
- `bg-green-600 text-white` → `bg-[var(--color-success)] text-white`
- `bg-yellow-400 text-slate-900` → `bg-[var(--color-warning)] text-foreground`
- `bg-red-600 text-white` → `bg-destructive text-destructive-foreground`
- `hover:text-red-600 hover:bg-red-50` → `hover:text-destructive hover:bg-destructive/10`
- `border-red-600` → `border-destructive`
- `text-red-600` → `text-destructive`
- `bg-green-100 text-green-800` → `bg-[var(--color-success)]/20 text-[var(--color-success)]`
- `text-green-600` → `text-[var(--color-success)]`
- `text-green-600 dark:text-green-400` → `text-[var(--color-success)]` (single token replaces both)

Primary / brand:
- `bg-blue-50 border-blue-600` (drag-active) → `bg-primary/5 border-primary`
- `text-blue-600` → `text-primary`
- `bg-blue-600 hover:bg-blue-700 text-white` on a `<Button variant="default">` → remove the className color overrides, let the shadcn default variant render with --primary.

Tokens confirmed defined in frontend/src/index.css @theme block:
- `--color-success: #16a34a`
- `--color-warning: #facc15`
- `--background`, `--foreground`, `--muted`, `--muted-foreground`, `--border`, `--primary`, `--destructive`, `--destructive-foreground` (mode-adaptive via :root / .dark)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert UploadHistory, DropZone, ErrorList to token utilities</name>
  <files>frontend/src/components/UploadHistory.tsx, frontend/src/components/DropZone.tsx, frontend/src/components/ErrorList.tsx</files>
  <read_first>
    - frontend/src/components/UploadHistory.tsx (the file being modified — verify exact line numbers + classes)
    - frontend/src/components/DropZone.tsx (the file being modified — verify exact line numbers + classes)
    - frontend/src/components/ErrorList.tsx (the file being modified — verify exact line numbers + classes)
    - .planning/phases/21-dark-mode-theme-infrastructure/21-RESEARCH.md (§"Hardcoded Color Inventory" lines-mapping table)
    - .planning/phases/21-dark-mode-theme-infrastructure/21-UI-SPEC.md (§"Hardcoded Color Elimination" substitution table)
    - frontend/src/index.css (confirm --color-success, --color-warning exist in @theme block)
  </read_first>
  <behavior>
    After this task:
    - Zero occurrences of slate-* / gray-* / green-* / red-* / blue-* / yellow-* hardcoded Tailwind color utilities in the three files
    - Success badge uses `bg-[var(--color-success)] text-white`
    - Partial badge uses `bg-[var(--color-warning)] text-foreground`
    - Failed badge uses `bg-destructive text-destructive-foreground`
    - Delete button hover uses `hover:text-destructive hover:bg-destructive/10`
    - Drag-active uses `bg-primary/5 border-primary` + `text-primary`
    - Browse button in DropZone uses default shadcn button variant (no hardcoded color className)
    - Error invalid-file-type text uses `text-destructive`
    - ErrorList uses `border-destructive` + `text-destructive` + `text-foreground` for error text body
  </behavior>
  <action>
    **UploadHistory.tsx** — apply substitutions per 21-RESEARCH.md §"Hardcoded Tailwind gray/slate/neutral utilities" and §"Hardcoded semantic color utilities":

    | Existing class fragment | Replace with |
    |---|---|
    | `bg-green-600 text-white hover:bg-green-600` | `bg-[var(--color-success)] text-white hover:bg-[var(--color-success)]` |
    | `bg-yellow-400 text-slate-900 hover:bg-yellow-400` | `bg-[var(--color-warning)] text-foreground hover:bg-[var(--color-warning)]` |
    | `bg-red-600 text-white hover:bg-red-600` | `bg-destructive text-destructive-foreground hover:bg-destructive` |
    | `text-slate-900` | `text-foreground` |
    | `text-slate-500` | `text-muted-foreground` |
    | `bg-slate-200` | `bg-muted` |
    | `text-slate-700` | `text-foreground` |
    | `text-slate-400` | `text-muted-foreground` |
    | `hover:text-red-600 hover:bg-red-50` | `hover:text-destructive hover:bg-destructive/10` |

    Apply to every occurrence; do not change layout, spacing, or functional props.

    **DropZone.tsx** — apply substitutions per 21-RESEARCH.md §"Hardcoded semantic color utilities" for DropZone rows:

    | Existing class fragment | Replace with |
    |---|---|
    | `bg-slate-100 border-slate-300` (idle) | `bg-muted border-border` |
    | `bg-blue-50 border-blue-600` (drag active) | `bg-primary/5 border-primary` |
    | `text-slate-500` | `text-muted-foreground` |
    | `text-slate-400` | `text-muted-foreground` |
    | `text-blue-600` (spinner + drag-active label) | `text-primary` |
    | `text-red-600` (invalid file) | `text-destructive` |

    Browse button specifically: if the existing JSX is `<Button variant="default" className="bg-blue-600 hover:bg-blue-700 text-white">`, remove the `bg-blue-600 hover:bg-blue-700 text-white` classes from the className (keep other non-color classes, if any). The `variant="default"` shadcn styling will pick up `--primary` automatically. If the className becomes empty, drop the prop entirely.

    **ErrorList.tsx**:

    | Existing class fragment | Replace with |
    |---|---|
    | `border-red-600` | `border-destructive` |
    | `text-red-600` | `text-destructive` |
    | `text-slate-700` | `text-foreground` |

    Addresses DM-01 and DM-02 for these three files.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && ! grep -nE "\\b(bg|text|border|ring)-(slate|gray|neutral|zinc|red|green|blue|yellow)-[0-9]+\\b" src/components/UploadHistory.tsx src/components/DropZone.tsx src/components/ErrorList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\\b(bg|text|border|ring)-(slate|gray|neutral|zinc)-[0-9]+\\b" frontend/src/components/UploadHistory.tsx` returns 0
    - `grep -cE "\\b(bg|text|border|ring)-(red|green|blue|yellow)-[0-9]+\\b" frontend/src/components/UploadHistory.tsx` returns 0
    - Same two greps applied to `frontend/src/components/DropZone.tsx` return 0
    - Same two greps applied to `frontend/src/components/ErrorList.tsx` return 0
    - `grep -c "var(--color-success)" frontend/src/components/UploadHistory.tsx` >= 1
    - `grep -c "var(--color-warning)" frontend/src/components/UploadHistory.tsx` >= 1
    - `grep -c "bg-destructive" frontend/src/components/UploadHistory.tsx` >= 1
    - `grep -c "hover:text-destructive" frontend/src/components/UploadHistory.tsx` >= 1
    - `grep -c "bg-primary/5" frontend/src/components/DropZone.tsx` >= 1
    - `grep -c "text-primary" frontend/src/components/DropZone.tsx` >= 2
    - `grep -c "text-destructive" frontend/src/components/DropZone.tsx` >= 1
    - `grep -c "border-destructive" frontend/src/components/ErrorList.tsx` >= 1
    - `grep -c "text-destructive" frontend/src/components/ErrorList.tsx` >= 1
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All three files pass the grep assertions; hardcoded Tailwind color utilities removed; token replacements applied; TypeScript clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert EmployeeTable and PersonioCard to token utilities and full-frontend audit pass</name>
  <files>frontend/src/components/dashboard/EmployeeTable.tsx, frontend/src/components/settings/PersonioCard.tsx</files>
  <read_first>
    - frontend/src/components/dashboard/EmployeeTable.tsx (the file being modified — verify line ~135-136 status badge class)
    - frontend/src/components/settings/PersonioCard.tsx (the file being modified — verify lines ~154 and ~208 green-* usage)
    - .planning/phases/21-dark-mode-theme-infrastructure/21-RESEARCH.md (§"Hardcoded Color Inventory" rows for EmployeeTable + PersonioCard)
    - .planning/phases/21-dark-mode-theme-infrastructure/21-UI-SPEC.md (§"Hardcoded Color Elimination" substitution table + §"Explicit exceptions")
    - frontend/src/index.css (confirm --color-success in @theme block, token for slash-opacity syntax)
  </read_first>
  <behavior>
    After this task:
    - EmployeeTable.tsx status badge uses `bg-[var(--color-success)]/20 text-[var(--color-success)]` for active-status rows
    - EmployeeTable.tsx inactive/other-status fallback uses `bg-muted text-muted-foreground`
    - PersonioCard.tsx green success indicators use `text-[var(--color-success)]` (replacing both line 154 `text-green-600 dark:text-green-400` and line 208 `text-green-600`)
    - No remaining hardcoded Tailwind color utilities in these two files
    - Repo-wide audit: only explicit exceptions from 21-UI-SPEC.md remain (index.css, defaults.ts DEFAULT_SETTINGS, ColorPicker.tsx placeholder, dialog.tsx bg-black/10 overlay, lib/color.ts fallbacks)
  </behavior>
  <action>
    **EmployeeTable.tsx** — apply per 21-RESEARCH.md row for line 135-136:

    | Existing class fragment | Replace with |
    |---|---|
    | `bg-green-100 text-green-800` (active status badge) | `bg-[var(--color-success)]/20 text-[var(--color-success)]` |
    | `bg-gray-100 text-gray-600` (inactive/fallback badge) | `bg-muted text-muted-foreground` |

    **PersonioCard.tsx** — apply per 21-RESEARCH.md rows for lines 154 and 208:

    | Existing class fragment | Replace with |
    |---|---|
    | `text-green-600 dark:text-green-400` | `text-[var(--color-success)]` |
    | `text-green-600` (any other occurrence) | `text-[var(--color-success)]` |

    **Final repo-wide audit — PROOF, not code changes:**

    Run the four audit greps from 21-UI-SPEC.md §"Grep patterns for the audit" against `frontend/src`. Expected results after this plan completes (across Plans 01/02/03 combined):

    1. `rg -nE "\\b(bg|text|border|ring|fill|stroke)-(white|black|(gray|slate|neutral|zinc)-[0-9]+)\\b" frontend/src --glob '!**/index.css'` — only acceptable remaining match is `dialog.tsx` `bg-black/10` overlay (shadcn default per D-05, explicit exception in UI-SPEC).

    2. `rg -nE "\\b(bg|text|border|ring|fill|stroke)-(red|green|blue|yellow|amber)-[0-9]+\\b" frontend/src --glob '!**/index.css'` — zero matches.

    3. `rg -nE "#[0-9a-fA-F]{3,8}\\b" frontend/src --glob '!**/index.css'` — acceptable remaining matches ONLY in: `frontend/src/lib/color.ts` (fallback), `frontend/src/components/settings/ColorPicker.tsx` (placeholder text). Any other hex literal is a defect — fix it.

    4. `rg -nE "\\b(rgb|rgba|hsl|hsla|oklch)\\s*\\(" frontend/src --glob '!**/index.css'` — acceptable remaining matches ONLY in: `frontend/src/lib/defaults.ts` (DEFAULT_SETTINGS brand seed values), `frontend/src/lib/color.ts` (conversion template literals), `frontend/src/pages/SettingsPage.tsx` (`"oklch(0 0 0)"` fallback helper). Any other match is a defect — fix it.

    If any defect is found, replace per the UI-SPEC substitution table and re-run the grep. Document the cleanup (if any) in the SUMMARY.md created at plan end.

    Addresses DM-01 and DM-02.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\\b(bg|text)-(green|gray|red|blue|yellow)-[0-9]+\\b" frontend/src/components/dashboard/EmployeeTable.tsx` returns 0
    - `grep -c "var(--color-success)" frontend/src/components/dashboard/EmployeeTable.tsx` >= 1
    - `grep -c "bg-muted" frontend/src/components/dashboard/EmployeeTable.tsx` >= 1
    - `grep -cE "\\btext-green-[0-9]+\\b" frontend/src/components/settings/PersonioCard.tsx` returns 0
    - `grep -cE "\\bdark:text-green-[0-9]+\\b" frontend/src/components/settings/PersonioCard.tsx` returns 0
    - `grep -c "var(--color-success)" frontend/src/components/settings/PersonioCard.tsx` >= 2
    - Full-frontend audit grep 1 (gray scale): `grep -rnE "\\b(bg|text|border|ring|fill|stroke)-(white|black|(gray|slate|neutral|zinc)-[0-9]+)\\b" frontend/src --include='*.tsx' --include='*.ts' | grep -v "index.css" | grep -v "dialog.tsx.*bg-black/10"` returns nothing
    - Full-frontend audit grep 2 (semantic colors): `grep -rnE "\\b(bg|text|border|ring|fill|stroke)-(red|green|blue|yellow|amber)-[0-9]+\\b" frontend/src --include='*.tsx' --include='*.ts'` returns nothing
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>EmployeeTable and PersonioCard token-converted. Full-frontend audit grep confirms only the documented exceptions (dialog.tsx overlay, lib/color.ts fallbacks, ColorPicker placeholder, defaults.ts brand seeds) remain. Build passes.</done>
</task>

</tasks>

<verification>
Final manual verification (in browser with devtools):
1. `npm run dev`, navigate through Sales dashboard, HR dashboard, Upload page, Settings page.
2. Devtools: `document.documentElement.classList.add('dark')`.
3. Check each surface:
   - Upload History: rows readable, status badges (success/partial/failed) legible on dark background
   - DropZone: idle state uses muted bg + border-border, drag-active uses primary border + primary/5 background, spinner uses primary
   - ErrorList: border + text-destructive visible and readable
   - EmployeeTable: active-status badge uses success color with readable foreground
   - Personio card: green success indicator visible
4. Remove `.dark`: all surfaces return to brand light palette.
5. No white patches, no hardcoded blue/green/red backgrounds that fail to adapt.
</verification>

<success_criteria>
- All 5 files updated per the substitution table
- Zero hardcoded Tailwind color utilities remain (except explicit UI-SPEC exceptions)
- Zero hex literals outside documented exceptions
- TypeScript and Vite build pass
- DM-01, DM-02 covered across the non-shadcn-primitive consumer layer
</success_criteria>

<output>
After completion, create `.planning/phases/21-dark-mode-theme-infrastructure/21-03-SUMMARY.md`
</output>
