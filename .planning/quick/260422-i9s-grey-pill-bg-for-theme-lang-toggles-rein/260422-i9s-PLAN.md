---
phase: 260422-i9s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/toggle.tsx
  - frontend/src/components/ThemeToggle.tsx
  - frontend/src/components/LanguageToggle.tsx
  - frontend/src/components/NavBar.tsx
autonomous: false
requirements:
  - NAV-PILL-BG
  - NAV-DOCS-RELINK
must_haves:
  truths:
    - "The three right-hand NavBar controls (ThemeToggle, LanguageToggle, UserMenu) share the same grey resting background so they read as one visual group."
    - "The active segment of each Toggle stays blue (bg-primary with primary-foreground text) — only the container/unselected surface changes."
    - "A docs icon link (BookOpenText) appears between LanguageToggle and UserMenu and navigates to /docs."
    - "The docs link has an accessible name via aria-label using the existing t('docs.nav.docsLabel') key (no new i18n keys added)."
    - "The docs link has a visible focus-visible ring matching the Phase 59-02 Path A utility."
    - "Other Toggle consumers (SubHeader Sales/HR pill, RevenueChart/HrKpiCharts chart-type toggles) remain visually unchanged (opt-in variant, default bg preserved)."
  artifacts:
    - path: "frontend/src/components/ui/toggle.tsx"
      provides: "Toggle primitive with new surface variant prop (default | muted)"
      contains: "variant"
    - path: "frontend/src/components/ThemeToggle.tsx"
      provides: "ThemeToggle passes variant='muted' to Toggle"
    - path: "frontend/src/components/LanguageToggle.tsx"
      provides: "LanguageToggle passes variant='muted' to Toggle"
    - path: "frontend/src/components/NavBar.tsx"
      provides: "Header cluster with BookOpenText docs link between LanguageToggle and UserMenu"
      contains: "BookOpenText"
  key_links:
    - from: "frontend/src/components/NavBar.tsx"
      to: "/docs route"
      via: "wouter Link href=\"/docs\""
      pattern: "href=\"/docs\""
    - from: "frontend/src/components/NavBar.tsx"
      to: "docs.nav.docsLabel i18n key"
      via: "t('docs.nav.docsLabel')"
      pattern: "docs.nav.docsLabel"
    - from: "frontend/src/components/ThemeToggle.tsx"
      to: "frontend/src/components/ui/toggle.tsx"
      via: "variant='muted' prop"
      pattern: "variant=\"muted\""
    - from: "frontend/src/components/LanguageToggle.tsx"
      to: "frontend/src/components/ui/toggle.tsx"
      via: "variant='muted' prop"
      pattern: "variant=\"muted\""
---

<objective>
Re-align the right-hand NavBar cluster so ThemeToggle + LanguageToggle pills share the UserMenu avatar's grey resting background, and re-introduce the docs (/docs) icon link (BookOpenText) between LanguageToggle and UserMenu. The visual goal: three controls read as one grouped cluster; active toggle segments stay blue.

Purpose: Visual consistency in the top-right header cluster + restore the docs entry point that was removed in commit b1b9612 (Phase 56-03).

Output:
- Toggle primitive gains an opt-in `variant="muted"` prop so ONLY NavBar consumers change — SubHeader Sales/HR, RevenueChart, and HrKpiCharts Toggle call-sites remain visually identical.
- ThemeToggle + LanguageToggle pass `variant="muted"`.
- NavBar renders a circular BookOpenText link (size-9, bg-muted) between LanguageToggle and UserMenu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<!-- Files the executor will modify -->
@frontend/src/components/NavBar.tsx
@frontend/src/components/ThemeToggle.tsx
@frontend/src/components/LanguageToggle.tsx
@frontend/src/components/ui/toggle.tsx
@frontend/src/components/UserMenu.tsx

<interfaces>
<!-- Reference visual treatment from UserMenu trigger (bg-muted, size-9, rounded-full). -->
<!-- Copied here so the executor does not need to re-read UserMenu during implementation. -->

UserMenu trigger className (for visual parity):
```tsx
"inline-flex items-center justify-center rounded-full size-9 bg-muted text-sm font-medium",
"hover:bg-accent/20 transition-colors",
"focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring",
```

Toggle primitive current root className (relevant slice):
```ts
`relative inline-flex items-center bg-background border border-primary rounded-full p-1 gap-0`
```

Toggle primitive active-indicator className (unchanged):
```ts
"absolute top-1 left-1 h-6 w-[calc(50%-0.25rem)] rounded-full bg-primary"
```

Existing i18n key (DO NOT add new keys — Phase 59-01 parity guard):
- `docs.nav.docsLabel` → "Documentation" (en), "Dokumentation" (de), at line 509 of both locale files.

Prior docs-link reference (commit 203893c, for shape only — NOT the final treatment):
```tsx
<Link href="/docs" aria-label={t("docs.nav.docsLabel")} className={docsLinkClass}>
  <Library className="h-5 w-5" />
</Link>
```
</interfaces>

<constraints>
- Phase 59-03 CI guards:
  - No hardcoded color literals — use Tailwind tokens (`bg-muted`, `bg-accent/20`, `text-foreground`, `text-muted-foreground`, `ring-ring`).
  - Icon-only buttons MUST carry an aria-label. Docs link uses `aria-label={t("docs.nav.docsLabel")}`.
- Phase 59-02 focus-visible parity:
  - Docs Link MUST include `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` (match UserMenu trigger).
  - Toggle primitive's existing `focus-visible:ring-3 focus-visible:ring-ring/50 focus-visible:z-20` on segment buttons MUST be preserved verbatim.
- Phase 59-01 i18n parity: do NOT add new i18n keys. Reuse `docs.nav.docsLabel`.
- No new npm dependencies. BookOpenText is already exported by lucide-react (existing dep).
- Toggle consumers that must remain visually unchanged (verified by grep):
  - `frontend/src/components/SubHeader.tsx` (Sales/HR dashboard toggle)
  - `frontend/src/components/dashboard/RevenueChart.tsx` (chart-type toggle)
  - `frontend/src/components/dashboard/HrKpiCharts.tsx` (chart-type toggle)
  These call Toggle WITHOUT the new `variant` prop — default behavior must preserve current `bg-background border border-primary` rendering.
</constraints>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `variant="muted"` prop to Toggle primitive and opt-in from ThemeToggle + LanguageToggle</name>
  <files>
    frontend/src/components/ui/toggle.tsx
    frontend/src/components/ThemeToggle.tsx
    frontend/src/components/LanguageToggle.tsx
  </files>
  <action>
1) Edit `frontend/src/components/ui/toggle.tsx`:
   - Add `variant?: "default" | "muted"` to `ToggleProps<T>` (default: `"default"`).
   - Destructure `variant = "default"` in the function signature.
   - Replace the hardcoded container className string. Compute it as:
     ```ts
     const containerSurface =
       variant === "muted"
         ? "bg-muted border-transparent hover:bg-accent/20 transition-colors"
         : "bg-background border-primary";
     const containerClassName = `relative inline-flex items-center ${containerSurface} border rounded-full p-1 gap-0${disabled ? " opacity-50 pointer-events-none" : ""}${extraClassName ? ` ${extraClassName}` : ""}`;
     ```
     (Keep the single `border` utility on the shared slot so the default retains its `border-primary`; the `muted` variant neutralizes it with `border-transparent`.)
   - Pass `containerClassName` to the root `<div role="radiogroup">`.
   - Do NOT touch the active-indicator span (`bg-primary` must stay) or the per-segment button classes — focus-visible ring and `text-primary-foreground` / `text-muted-foreground` are preserved byte-for-byte (Phase 59-02 invariant).
   - Only tokens; no color literals.

2) Edit `frontend/src/components/ThemeToggle.tsx`:
   - Add `variant="muted"` prop to the `<Toggle<ThemeMode>`. All other props unchanged.

3) Edit `frontend/src/components/LanguageToggle.tsx`:
   - Add `variant="muted"` prop to the `<Toggle<Language>`. All other props unchanged.

Do NOT modify `SubHeader.tsx`, `dashboard/RevenueChart.tsx`, or `dashboard/HrKpiCharts.tsx`. They must continue to use the default variant and render unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -20</automated>
    Manual spot-checks (fold into Task 3 human-verify checkpoint):
    - ThemeToggle + LanguageToggle container is grey (bg-muted), no visible border.
    - Active segment (sun/moon or DE/EN) still renders on a blue pill (bg-primary).
    - SubHeader Sales/HR toggle and Sales/HR chart-type toggles LOOK IDENTICAL to before.
  </verify>
  <done>
    - `toggle.tsx` exports an optional `variant` prop typed `"default" | "muted"`.
    - Default behavior is unchanged (still `bg-background border-primary`).
    - When `variant="muted"`: container uses `bg-muted` + `border-transparent` + `hover:bg-accent/20 transition-colors`.
    - ThemeToggle + LanguageToggle each pass `variant="muted"`.
    - Focus-visible ring on segments is untouched (Phase 59-02 preserved).
    - TypeScript builds with no new errors; Vite build succeeds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-introduce docs icon link in NavBar header cluster</name>
  <files>
    frontend/src/components/NavBar.tsx
  </files>
  <action>
Edit `frontend/src/components/NavBar.tsx`:

1) Add imports:
   - `import { useTranslation } from "react-i18next";`
   - `import { BookOpenText } from "lucide-react";`
   - (Link and wouter already imported — leave the existing `import { Link, useLocation } from "wouter";` untouched.)
   - Import `cn` from `@/lib/utils`.

2) Inside the `NavBar` component, add:
   - `const { t } = useTranslation();`

3) In the right-hand cluster `<div className="ml-auto flex items-center gap-4">`, insert a `<Link>` between `<LanguageToggle />` and `<UserMenu />`:

   ```tsx
   <Link
     href="/docs"
     aria-label={t("docs.nav.docsLabel")}
     className={cn(
       "inline-flex items-center justify-center rounded-full size-9 bg-muted text-foreground",
       "hover:bg-accent/20 transition-colors",
       "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring",
     )}
   >
     <BookOpenText className="h-5 w-5" aria-hidden="true" />
   </Link>
   ```

   Rationale for visual choices:
   - `rounded-full size-9 bg-muted` — byte-identical to UserMenu trigger so the three controls read as one cluster.
   - `hover:bg-accent/20 transition-colors` — matches UserMenu hover.
   - `focus-visible:ring-2 focus-visible:ring-ring` — matches UserMenu (Phase 59-02 Path A equivalent; `ring-2` is the canonical size for non-Toggle controls in this codebase, confirmed by UserMenu).
   - Icon size `h-5 w-5` matches UserMenu UserIcon fallback.
   - `aria-hidden="true"` on the icon — redundant with aria-label on the anchor but explicit (Phase 59-03 guard tolerates this).

4) Do not remove or reorder `<ThemeToggle />` or `<LanguageToggle />`. Final order in the right cluster: ThemeToggle → LanguageToggle → Docs Link → UserMenu.

5) No changes outside NavBar.tsx. No i18n key additions.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -20 && grep -n "docs.nav.docsLabel\|BookOpenText" frontend/src/components/NavBar.tsx</automated>
  </verify>
  <done>
    - NavBar.tsx imports BookOpenText from lucide-react and useTranslation from react-i18next.
    - A wouter Link with href="/docs" and aria-label={t("docs.nav.docsLabel")} is rendered between LanguageToggle and UserMenu.
    - Link container uses bg-muted + rounded-full + size-9 + hover + focus-visible ring (tokens only, no color literals).
    - Navigating to /docs works client-side (wouter Link, no full reload).
    - No new i18n keys added.
    - TypeScript + Vite build pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of header cluster + regression check on other Toggle consumers</name>
  <what-built>
Grey pill backgrounds on ThemeToggle + LanguageToggle (via new Toggle `variant="muted"` prop) and a BookOpenText docs link in the NavBar right-hand cluster. SubHeader + dashboard chart toggles were intentionally NOT changed.
  </what-built>
  <how-to-verify>
1) Run the app locally (`docker compose up` or the existing dev command) and load any authenticated page, e.g. `/sales`.
2) Top-right cluster — verify visual grouping:
   - ThemeToggle container: grey (bg-muted), no visible blue border at rest.
   - LanguageToggle container: grey (bg-muted), no visible blue border at rest.
   - Docs icon (BookOpenText, /docs): grey circle, same diameter as UserMenu avatar.
   - UserMenu avatar: unchanged.
   - The four items (ThemeToggle, LanguageToggle, Docs, UserMenu) read as one horizontal grey cluster with consistent radii.
3) Active segment check:
   - Click sun/moon on ThemeToggle — active segment pill is blue (bg-primary) with white/foreground icon. Inactive icon is grey.
   - Click DE/EN on LanguageToggle — active segment pill is blue, language switches.
4) Docs link behavior:
   - Tab-focus the docs icon — visible focus ring (ring-2 ring-ring).
   - Click → navigates to /docs (wouter, no full page reload).
   - Hover → bg tints slightly (bg-accent/20).
5) Regression sweep — these must LOOK IDENTICAL to before this change:
   - Go to `/sales`: SubHeader Sales/HR pill still renders with default (non-muted) styling (white/background container, primary-colored border).
   - On the Sales dashboard, the revenue chart type toggle (line/bar) retains default styling.
   - On the HR dashboard (`/hr`), the HR KPI chart type toggles retain default styling.
6) Dark mode smoke:
   - Toggle to dark mode; grey cluster in NavBar still reads correctly (bg-muted resolves in dark tokens).
   - Active segment stays blue.
7) i18n parity:
   - Switch to DE; docs icon aria-label should be "Dokumentation" (verifiable via DevTools inspect).
8) Run `cd frontend && npm run check:phase-59` (CI guards) — must pass (no new color literals, no missing aria-labels, focus-visible preserved).
  </how-to-verify>
  <resume-signal>Type "approved" to confirm visuals pass, or describe any visual/regression issues (e.g. "SubHeader pill changed color" or "focus ring missing").</resume-signal>
</task>

</tasks>

<verification>
Overall checks:
- `cd frontend && npx tsc --noEmit` — no type errors.
- `cd frontend && npm run build` — Vite build succeeds.
- `cd frontend && npm run check:phase-59` — Phase 59 CI guards pass (color literals + icon aria + focus-visible).
- `cd frontend && npm test 2>&1 | tail -20` — existing Toggle/NavBar tests (if any) still pass.
- Human verification (Task 3): visual grouping correct, regressions absent.
</verification>

<success_criteria>
- ThemeToggle + LanguageToggle render with `bg-muted` resting container; active segments remain `bg-primary`.
- BookOpenText docs link renders between LanguageToggle and UserMenu with `/docs` href and `docs.nav.docsLabel` aria-label.
- SubHeader Sales/HR Toggle and chart-type Toggles unchanged (default variant).
- Zero new i18n keys.
- Zero new dependencies.
- Phase 59-01/59-02/59-03 guards all green.
</success_criteria>

<output>
After completion, create `.planning/quick/260422-i9s-grey-pill-bg-for-theme-lang-toggles-rein/260422-i9s-SUMMARY.md`.
</output>
