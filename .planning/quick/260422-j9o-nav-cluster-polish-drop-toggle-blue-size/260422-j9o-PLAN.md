---
quick_id: 260422-j9o
date: 2026-04-22
---

# Quick Task 260422-j9o — Nav cluster polish

## Goal
Harmonize the top-nav control cluster so theme + language toggles, Sales/HR toggle, docs icon, and user-menu avatar share the same understated pill aesthetic (no blue primary fill, 32px height, normal-weight labels).

## Tasks

### T1 — Toggle primitive: muted variant active indicator + dynamic pill sizing
**Files:** `frontend/src/components/ui/toggle.tsx`
**Action:**
- Muted variant active indicator uses `bg-background shadow-sm` (was `bg-primary`); active segment label uses `text-foreground` (was `text-primary-foreground`).
- Indicator width and x-offset are measured from the active button via `useLayoutEffect` + `ResizeObserver` (instead of hardcoded `w-[calc(50%-0.25rem)]`). Keeps pill aligned when segment labels have unequal widths (e.g. `Vertrieb` vs `HR`).

### T2 — NavBar cluster parity: size-8 circles, muted Sales/HR
**Files:**
- `frontend/src/components/NavBar.tsx` — docs link `size-9` → `size-8`
- `frontend/src/components/UserMenu.tsx` — avatar `size-9 font-medium` → `size-8 font-normal`
- `frontend/src/components/SubHeader.tsx` — Sales/HR Toggle adopts `variant="muted"` (drops the `border-transparent` overlay)
- `frontend/src/locales/de.json` — `nav.sales`: `VERTRIEB` → `Vertrieb`
- `frontend/src/locales/en.json` — `nav.sales`: `SALES` → `Sales`

## Verification
Chrome DevTools MCP inspection on `/sales` (dark mode):
- All three muted toggles (Theme, Language, Dashboard) show `bg-background` indicator, not `bg-primary`.
- Active-pill bounding rect delta vs active button: 0px offset, ≤0.35px width (subpixel rounding).
- UserMenu trigger computed `font-weight: 400`.
