---
phase: quick
plan: 260417-e45
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/NavBar.tsx
  - frontend/src/components/SubHeader.tsx
  - frontend/src/pages/LauncherPage.tsx
  - frontend/src/auth/AuthGate.tsx
  - frontend/src/pages/LoginPage.tsx
autonomous: false
requirements: [QUICK-260417-e45]
must_haves:
  truths:
    - "Visiting bare `/` redirects to `/home` (launcher)"
    - "Visiting `/sales` renders the Sales Dashboard (DashboardPage)"
    - "Successful login lands on `/home` (unchanged — AuthGate already does this)"
    - "KPI Dashboard tile on `/home` navigates to `/sales`"
    - "SALES/HR SegmentedControl in NavBar uses `/sales` for the Sales segment and toggles correctly between `/sales` and `/hr`"
    - "Back button from `/upload` or `/settings` returns to last dashboard (`/sales` or `/hr`) — never `/`"
    - "SubHeader's DateRangeFilter renders on `/sales` (was `/`) and remains hidden on `/home`, `/hr`, `/upload`, `/settings`, `/docs`"
    - "sessionStorage `lastDashboard` value is `/sales` or `/hr` — never `/`"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Route table with `/sales` → DashboardPage and `/` → Redirect to `/home`"
      contains: "path=\"/sales\""
    - path: "frontend/src/components/NavBar.tsx"
      provides: "Dashboard type + SegmentedControl + lastDashboard tracking all keyed on `/sales`"
      contains: "\"/sales\""
    - path: "frontend/src/components/SubHeader.tsx"
      provides: "DateRangeFilter visibility gated on `/sales`"
      contains: "location === \"/sales\""
    - path: "frontend/src/pages/LauncherPage.tsx"
      provides: "KPI Dashboard tile navigates to `/sales`"
      contains: "setLocation(\"/sales\")"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "wouter Redirect component"
      via: "import from 'wouter' + <Redirect to=\"/home\" /> at path \"/\""
      pattern: "Redirect.*to=\"/home\""
    - from: "frontend/src/pages/LauncherPage.tsx"
      to: "/sales route in App.tsx"
      via: "setLocation(\"/sales\") onClick of KPI Dashboard tile"
      pattern: "setLocation\\(\"/sales\"\\)"
    - from: "frontend/src/components/NavBar.tsx SegmentedControl"
      to: "/sales and /hr routes"
      via: "segment values `/sales` and `/hr` passed to navigate()"
      pattern: "value: \"/sales\""
---

<objective>
Rename the Sales Dashboard route from `/` to `/sales`. Make bare `/` redirect to `/home` (the App Launcher). Propagate the rename to every consumer (NavBar SegmentedControl, SubHeader DateRangeFilter gate, LauncherPage tile, sessionStorage default, stale JSDoc comments).

Purpose: `/sales` is a more honest URL — it names the dashboard, not "app root." With v1.14's App Launcher at `/home` as the new app-home, `/` should bounce the user to the launcher rather than silently render the Sales dashboard. This makes the URL space match the mental model: `/home` is the launcher, `/sales` is one of the apps.

Output: App.tsx route table with `/sales` → DashboardPage and `/` → `<Redirect to="/home" />`. NavBar, SubHeader, LauncherPage, sessionStorage default, and JSDoc comments all updated to use `/sales`. No backend, no i18n string changes (labels stay `nav.sales`), no CSS changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/App.tsx
@frontend/src/components/NavBar.tsx
@frontend/src/components/SubHeader.tsx
@frontend/src/pages/LauncherPage.tsx
@frontend/src/auth/AuthGate.tsx
@frontend/src/pages/LoginPage.tsx

<interfaces>
<!-- wouter 3.9.0 (confirmed in frontend/package.json) exports `Redirect`. -->
<!-- Confirmed in frontend/node_modules/wouter/types/index.d.ts: -->
<!--   export function Redirect<H>(props: RedirectProps<H>, context?: any): ReactElement | null; -->
<!-- Usage inside <Switch>: -->
<!--   <Route path="/"><Redirect to="/home" /></Route> -->
<!-- (wouter's <Redirect> is a component that fires setLocation on mount — it must be a Route's child, -->
<!--  not a standalone child of <Switch>. This pattern is idiomatic for wouter 3.x.) -->

<!-- AuthGate currently already redirects authed user on /login → /home (line 26). -->
<!-- LoginPage never redirects directly — AuthGate owns post-login navigation. -->
<!-- Only JSDoc/inline comments in those two files still say "redirect to /" — they're stale. -->
</interfaces>

<call_sites>
<!-- Complete enumeration from grep across frontend/src. -->
<!-- Every root-slash routing call site that needs updating: -->

1. frontend/src/App.tsx:39
   `<Route path="/" component={DashboardPage} />`
   → `<Route path="/sales" component={DashboardPage} />`
   + add `<Route path="/"><Redirect to="/home" /></Route>` AFTER the `/sales` route (order-sensitive inside <Switch>: more specific `/sales` first, exact `/` redirect second — but `/` is literal, not a wildcard, so order is actually not the concern; see note below).
   + add `Redirect` to the existing wouter import: `import { Redirect, Route, Switch, useLocation } from "wouter";`

2. frontend/src/components/NavBar.tsx:8
   `type Dashboard = "/" | "/hr";`
   → `type Dashboard = "/sales" | "/hr";`

3. frontend/src/components/NavBar.tsx:17
   `return "/";`
   → `return "/sales";`

4. frontend/src/components/NavBar.tsx:40
   `if (location === "/" || location === "/hr") {`
   → `if (location === "/sales" || location === "/hr") {`

5. frontend/src/components/NavBar.tsx:44 (inside catch comment)
   `/* sessionStorage unavailable — back button falls back to "/" */`
   → `/* sessionStorage unavailable — back button falls back to "/sales" */`

6. frontend/src/components/NavBar.tsx:95
   `{ value: "/", label: t("nav.sales") },`
   → `{ value: "/sales", label: t("nav.sales") },`

7. frontend/src/components/NavBar.tsx:98
   `value={location === "/hr" ? "/hr" : "/"}`
   → `value={location === "/hr" ? "/hr" : "/sales"}`

8. frontend/src/components/SubHeader.tsx:52
   `{location === "/" && (`
   → `{location === "/sales" && (`

9. frontend/src/pages/LauncherPage.tsx:28 (comment above the button)
   `{/* Active tile: KPI Dashboard → navigates to / (Sales Dashboard route) */}`
   → `{/* Active tile: KPI Dashboard → navigates to /sales (Sales Dashboard route) */}`

10. frontend/src/pages/LauncherPage.tsx:31
    `onClick={() => setLocation("/")}`
    → `onClick={() => setLocation("/sales")}`

11. frontend/src/auth/AuthGate.tsx:12 (JSDoc comment)
    ` *   - Authed on /login → redirect to /.`
    → ` *   - Authed on /login → redirect to /home.`
    (NOTE: the actual behavior at line 26 is already `setLocation("/home")` — only the doc comment is stale. No functional change to this file.)

12. frontend/src/pages/LoginPage.tsx:34 (JSDoc comment)
    ` * - On success, AuthGate handles navigation to /.`
    → ` * - On success, AuthGate handles navigation to /home.`
    (NOTE: LoginPage does no routing itself. Only the doc comment is stale. No functional change.)

<!-- Grep confirmed NO additional root-slash routing references in: -->
<!--   - frontend/src/**/*.ts / .tsx (tests, fixtures, hooks, other components) -->
<!--   - frontend/tests/e2e/ (only `/settings` is referenced, never `/`) -->
<!-- Non-routing root slashes (API paths like `/api/settings/logo/public` in LoginPage, -->
<!-- CSS, `className` strings, JSDoc for hooks/useUnsavedGuard) are intentionally untouched. -->
</call_sites>

<wouter_redirect_pattern>
<!-- Verified against wouter 3.9.0 types. Redirect is a component: -->

import { Redirect, Route, Switch, useLocation } from "wouter";

<Switch>
  <Route path="/login" component={LoginPage} />
  <Route path="/home" component={LauncherPage} />
  <Route path="/sales" component={DashboardPage} />
  <Route path="/">
    <Redirect to="/home" />
  </Route>
  <Route path="/upload" component={UploadPage} />
  ...
</Switch>

<!-- <Switch> picks the first matching Route. Since routes are literal (not wildcard), -->
<!-- the order between `/sales` and `/` does not matter for correctness — but putting -->
<!-- `/` near the top is a common convention. Keeping it after `/sales` here minimises -->
<!-- the diff against the current layout. -->
</wouter_redirect_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename route to /sales and redirect / to /home in App.tsx</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Edit `frontend/src/App.tsx`:

    (A) Update the wouter import on line 4 from:
        `import { Route, Switch, useLocation } from "wouter";`
      to:
        `import { Redirect, Route, Switch, useLocation } from "wouter";`

    (B) Inside the `<Switch>` block, change the existing Sales route on line 39:
        `<Route path="/" component={DashboardPage} />`
      to:
        `<Route path="/sales" component={DashboardPage} />`

    (C) Immediately after the `/sales` route (i.e. after the line inserted in step B), add a new Route element that redirects bare `/` to `/home`:
        ```tsx
        <Route path="/">
          <Redirect to="/home" />
        </Route>
        ```

    Do NOT change any other routes (/login, /home, /upload, /hr, /settings, /docs, /docs/:section/:slug), the AppShell wrapper, provider nesting, or the main element className logic. Do NOT add any other imports.

    Rationale (per call_sites #1 in context): wouter 3.9.0 exports `Redirect` as a component that must be rendered inside a `<Route>` child position. On mount it calls setLocation to the target, causing the browser URL to become `/home`. This matches the `<Route path="/home" component={LauncherPage} />` already above.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `frontend/src/App.tsx` compiles with no TypeScript errors (`tsc --noEmit` exits 0).
    - The file imports `Redirect` from `wouter`.
    - `<Switch>` contains `<Route path="/sales" component={DashboardPage} />` and `<Route path="/"><Redirect to="/home" /></Route>`.
    - No `<Route path="/" component={DashboardPage} />` remains anywhere in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Propagate /sales across NavBar, SubHeader, LauncherPage, and stale JSDoc</name>
  <files>frontend/src/components/NavBar.tsx, frontend/src/components/SubHeader.tsx, frontend/src/pages/LauncherPage.tsx, frontend/src/auth/AuthGate.tsx, frontend/src/pages/LoginPage.tsx</files>
  <action>
    Apply the following edits (every call site from `<call_sites>` in context):

    (A) `frontend/src/components/NavBar.tsx` — 6 edits:
      1. Line 8: `type Dashboard = "/" | "/hr";` → `type Dashboard = "/sales" | "/hr";`
      2. Line 17: `return "/";` → `return "/sales";`
      3. Line 40: `if (location === "/" || location === "/hr") {` → `if (location === "/sales" || location === "/hr") {`
      4. Line 44 (catch-block comment): `/* sessionStorage unavailable — back button falls back to "/" */` → `/* sessionStorage unavailable — back button falls back to "/sales" */`
      5. Line 95: `{ value: "/", label: t("nav.sales") },` → `{ value: "/sales", label: t("nav.sales") },`
      6. Line 98: `value={location === "/hr" ? "/hr" : "/"}` → `value={location === "/hr" ? "/hr" : "/sales"}`

      Do NOT change the i18n label `t("nav.sales")` — that's user-facing copy, not a route. Do NOT touch the `sessionStorage.setItem("lastDashboard", location)` call on line 42: once the guard above switches to `/sales`, the stored value is automatically `/sales` or `/hr`.

    (B) `frontend/src/components/SubHeader.tsx` — 1 edit:
      7. Line 52: `{location === "/" && (` → `{location === "/sales" && (`

      Do NOT touch the `/home` null-guard on line 46. Do NOT touch the `/hr` branch on line 60.

    (C) `frontend/src/pages/LauncherPage.tsx` — 2 edits:
      8. Line 28 (comment above the button): `{/* Active tile: KPI Dashboard → navigates to / (Sales Dashboard route) */}` → `{/* Active tile: KPI Dashboard → navigates to /sales (Sales Dashboard route) */}`
      9. Line 31: `onClick={() => setLocation("/")}` → `onClick={() => setLocation("/sales")}`

      Do NOT touch `aria-label`, className, the coming-soon tiles, or the isAdmin hook.

    (D) `frontend/src/auth/AuthGate.tsx` — 1 edit (documentation only, no behavior change):
      10. Line 12 (JSDoc comment): ` *   - Authed on /login → redirect to /.` → ` *   - Authed on /login → redirect to /home.`

      NOTE: The actual setLocation call on line 26 already reads `setLocation("/home")` — only the JSDoc was stale. No runtime change.

    (E) `frontend/src/pages/LoginPage.tsx` — 1 edit (documentation only, no behavior change):
      11. Line 34 (JSDoc comment): ` * - On success, AuthGate handles navigation to /.` → ` * - On success, AuthGate handles navigation to /home.`

      NOTE: LoginPage does no routing itself — AuthGate owns post-login navigation and already targets /home. Only the JSDoc was stale.

    No new imports in any file. No CSS/className changes. No i18n/translation changes.

    After all edits, `grep -rn '"/"' frontend/src` should return ONLY non-routing matches (none of the call sites above should still contain a bare `"/"` in a routing position). The following should return zero matches: `grep -rn 'location === "/"' frontend/src`, `grep -rn 'path="/"[^s]' frontend/src` (except the new Redirect Route), `grep -rn 'setLocation("/")' frontend/src`, `grep -rn 'value: "/"' frontend/src`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    - All five files compile with no TypeScript errors.
    - NavBar.tsx: Dashboard type is `"/sales" | "/hr"`, getLastDashboard default returns `"/sales"`, useEffect guard compares against `"/sales"`, SegmentedControl segment value is `"/sales"`, fallback ternary returns `"/sales"`.
    - SubHeader.tsx: DateRangeFilter render gate is `location === "/sales"`.
    - LauncherPage.tsx: KPI tile onClick calls `setLocation("/sales")` and comment reflects this.
    - AuthGate.tsx + LoginPage.tsx: JSDoc comments reference `/home` (no behavior change).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Sales Dashboard is now served at `/sales` instead of `/`. Bare `/` redirects to `/home` (App Launcher). NavBar SegmentedControl, SubHeader DateRangeFilter gate, LauncherPage KPI tile, sessionStorage `lastDashboard` default, and AuthGate/LoginPage JSDoc all updated. HMR should pick the changes up without restart.
  </what-built>
  <how-to-verify>
    The user's dev server is running at http://localhost:5173 with HMR — no rebuild needed.

    1. Open http://localhost:5173/ in the browser (log in first if prompted). The URL should IMMEDIATELY change to http://localhost:5173/home and the App Launcher grid should render (KPI Dashboard tile + 3 coming-soon tiles). No flash of Sales content.
    2. From /home, click the KPI Dashboard tile. The URL should become http://localhost:5173/sales and the full Sales Dashboard (KPI cards, date-range filter in SubHeader, freshness indicator, charts) should render.
    3. On /sales, confirm the SubHeader shows the DateRangeFilter on the left. Change the preset (e.g. to "Last 30 days") — charts should refetch and update.
    4. On /sales, click the SALES/HR SegmentedControl → switch to HR. URL becomes /hr, HR dashboard renders, SubHeader shows HrFreshnessIndicator (not DateRangeFilter). Switch back to SALES — URL becomes /sales (NOT /), DateRangeFilter reappears.
    5. From /sales, click the settings gear (top-right). URL becomes /settings. Confirm the NavBar shows a "Back to Sales" button (German: "Zurück zu Vertrieb"). Click it — should return to /sales.
    6. Repeat step 5 from /hr: settings → "Back to HR" button → /hr.
    7. From /sales, click upload arrow (admin only) → /upload. Back button label = "Back to Sales" → /sales.
    8. Open a new tab, type http://localhost:5173/ directly — redirects to /home.
    9. Sign out from /home → /login. Sign back in → lands on /home (AuthGate behavior unchanged).
    10. Open DevTools → Application → Session Storage → http://localhost:5173 → confirm the `lastDashboard` key value is `/sales` or `/hr`, never `/`. (Clear the key first if it was set to `/` from a previous session, then visit /sales and re-check — should be `/sales`.)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles cleanly: `cd frontend && npx tsc --noEmit` exits 0 after each task
- `grep -rn 'path="/"[^a-z]' frontend/src` returns ONLY the `<Route path="/">` that wraps `<Redirect to="/home" />` (one match) — no `path="/" component=`
- `grep -rn 'location === "/"' frontend/src` returns zero matches
- `grep -rn 'setLocation("/")' frontend/src` returns zero matches
- `grep -rn 'value: "/"' frontend/src` returns zero matches
- All 10 visual verification steps pass
</verification>

<success_criteria>
- Bare `/` → redirects to `/home` launcher (no Sales content flash)
- `/sales` → Sales Dashboard (DashboardPage) with full chrome (NavBar + SubHeader DateRangeFilter + KPI cards + charts)
- `/home` KPI Dashboard tile → `/sales`
- SALES/HR SegmentedControl toggles between `/sales` and `/hr`
- `/settings` and `/upload` back button returns to `/sales` or `/hr` based on lastDashboard sessionStorage
- sessionStorage `lastDashboard` value ∈ {`/sales`, `/hr`} — never `/`
- Login lands on `/home` (unchanged)
- `npx tsc --noEmit` in frontend/ exits 0
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/quick/260417-e45-rename-sales-dashboard-route-from-to-sal/260417-e45-SUMMARY.md`
</output>
