---
phase: quick/260422-i41
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/signage/pages/MediaPage.tsx
  - frontend/src/signage/pages/PlaylistsPage.tsx
  - frontend/src/signage/pages/DevicesPage.tsx
  - frontend/src/signage/pages/SchedulesPage.tsx
autonomous: false
requirements:
  - i41-UI-01
must_haves:
  truths:
    - "On /signage/media the 'URL / HTML registrieren' button renders below the media grid (or below the empty state), right-aligned; the drop-zone's own 'Dateien auswählen' button stays inside the drop zone."
    - "On /signage/playlists the 'Neue Playlist' button renders below the playlists table, right-aligned, when the list is non-empty."
    - "On /signage/devices the 'Gerät koppeln' button renders below the devices table, right-aligned, when the list is non-empty."
    - "On /signage/schedules the 'Neuer Zeitplan' button renders below the schedules table, right-aligned, when the list is non-empty."
    - "Empty-state CTAs inside PlaylistsPage, DevicesPage, and SchedulesPage empty cards remain functional and unchanged."
    - "The MediaPage register button remains rendered whether or not media exists (no gating), so an empty library still exposes URL/HTML registration."
  artifacts:
    - path: "frontend/src/signage/pages/MediaPage.tsx"
      provides: "Register URL button moved below grid/empty-state"
      contains: "signage.admin.media.register_url_button"
    - path: "frontend/src/signage/pages/PlaylistsPage.tsx"
      provides: "New-playlist button moved below table"
      contains: "signage.admin.playlists.new_button"
    - path: "frontend/src/signage/pages/DevicesPage.tsx"
      provides: "Pair-device button moved below table"
      contains: "signage.admin.devices.pair_button"
    - path: "frontend/src/signage/pages/SchedulesPage.tsx"
      provides: "New-schedule button moved below table"
      contains: "signage.admin.schedules.new_cta"
  key_links:
    - from: "MediaPage.tsx"
      to: "MediaRegisterUrlDialog"
      via: "setRegisterOpen(true) handler on relocated Button"
      pattern: "setRegisterOpen\\(true\\)"
    - from: "DevicesPage.tsx"
      to: "/signage/pair route"
      via: "setLocation('/signage/pair') handler on relocated Button"
      pattern: "setLocation\\(\"/signage/pair\"\\)"
    - from: "PlaylistsPage.tsx"
      to: "PlaylistNewDialog"
      via: "setNewOpen(true) handler on relocated Button"
      pattern: "setNewOpen\\(true\\)"
    - from: "SchedulesPage.tsx"
      to: "ScheduleEditDialog (create mode)"
      via: "setEditing(null) handler on relocated Button"
      pattern: "setEditing\\(null\\)"
---

<objective>
Establish a consistent "primary action lives below the list, right-aligned" pattern across all four Digital Signage list pages. Today each page renders its primary CTA above the table/grid; user feedback is to relocate the CTA below the list on /signage/media, /signage/playlists, /signage/devices, and /signage/schedules.

Purpose: Visual consistency + post-scan affordance — users see the list first, then the action they can take against or in addition to it.
Output: Four page-file edits relocating a single `<div className="flex justify-end">…Button…</div>` block (or equivalent) from above the list to after it. No new i18n keys; no label changes; no backend changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/signage/pages/MediaPage.tsx
@frontend/src/signage/pages/PlaylistsPage.tsx
@frontend/src/signage/pages/DevicesPage.tsx
@frontend/src/signage/pages/SchedulesPage.tsx

<interfaces>
Each page today follows this shape (extracted from reads):

MediaPage.tsx (lines ~100–118):
  <SectionHeader … />
  <MediaUploadDropZone />
  <div className="flex justify-end">
    <Button variant="outline" size="sm" onClick={() => setRegisterOpen(true)}>
      {t("signage.admin.media.register_url_button")}
    </Button>
  </div>
  {/* loading / error / empty / grid … */}

PlaylistsPage.tsx (lines ~131–135):
  <div className="flex justify-end">
    <Button type="button" onClick={() => setNewOpen(true)}>
      {t("signage.admin.playlists.new_button")}
    </Button>
  </div>
  {/* loading / error / empty-card-with-own-CTA / table … */}

DevicesPage.tsx (lines ~125–129, renders only when devices.length > 0; empty state early-returns with own CTA):
  <div className="flex items-center justify-between">
    <Button onClick={() => setLocation("/signage/pair")}>
      {t("signage.admin.devices.pair_button")}
    </Button>
  </div>
  {/* table */}

SchedulesPage.tsx (lines ~216–222, gated by !isEmpty):
  {!isEmpty && (
    <div className="flex justify-end">
      <Button type="button" onClick={() => setEditing(null)}>
        {t("signage.admin.schedules.new_cta")}
      </Button>
    </div>
  )}
  {/* empty-card-with-own-CTA OR table */}
</interfaces>

Phase 57 SectionHeader conventions (from STATE.md): SectionHeader owns title/description; wrapper uses `mt-8` + `space-y-4`/`gap-6`. Do not change these — only relocate the button block.

Phase 59 Plan 59-03 CI guards (STATE.md): icon-only buttons need `aria-label`; the relocated buttons are text-labelled Buttons (not icon-only), so no aria-label addition is required. No hardcoded color literals introduced (moves preserve existing token classes).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate primary action buttons below each list on all four signage pages</name>
  <files>
    frontend/src/signage/pages/MediaPage.tsx,
    frontend/src/signage/pages/PlaylistsPage.tsx,
    frontend/src/signage/pages/DevicesPage.tsx,
    frontend/src/signage/pages/SchedulesPage.tsx
  </files>
  <action>
    Move the page-level primary-action button block from above the list to after the list in each of the four signage pages. Preserve handlers, labels, i18n keys, variants, sizes, and disabled logic exactly. The only structural change is position in JSX.

    1) MediaPage.tsx
       - DELETE the existing block at ~lines 110–118:
         `<div className="flex justify-end"><Button variant="outline" size="sm" onClick={() => setRegisterOpen(true)}>{t("signage.admin.media.register_url_button")}</Button></div>`
       - INSERT the same block immediately AFTER the final list-rendering block (after the closing `)}` of the `{mediaQuery.data && mediaQuery.data.length > 0 && ( … grid … )}` expression, and BEFORE the `<MediaRegisterUrlDialog …>` element at ~line 224).
       - Always-rendered: do NOT wrap in `{mediaQuery.data && …}`. Per user constraint, the register button stays accessible even when the media library is empty (the empty-state card above it does not provide a register CTA, and it is the page-level primary action, not a list-contextual action).
       - Keep the `MediaUploadDropZone` in place (above where the button used to be). The dropzone's own "Dateien auswählen" button is INSIDE the dropzone and must not be touched.

    2) PlaylistsPage.tsx
       - DELETE the block at ~lines 131–135:
         `<div className="flex justify-end"><Button type="button" onClick={() => setNewOpen(true)}>{t("signage.admin.playlists.new_button")}</Button></div>`
       - INSERT the same block AFTER the `{isLoading ? … : isError ? … : playlists.length === 0 ? <empty-card-with-own-CTA> : <table-wrapper>}` ternary and BEFORE `<PlaylistNewDialog … />`.
       - Leave the empty-state card (lines ~145–156) and its own "empty_cta" Button untouched — that remains the empty-state affordance. The relocated top-level button will render redundantly in the empty case, but that is acceptable and matches MediaPage's "always render" constraint. (Optional: gate with `playlists.length > 0` to avoid duplication in empty state. Choose the gated variant to avoid two near-identical CTAs stacking — wrap the relocated block with `{playlists.length > 0 && ( … )}`.)

    3) DevicesPage.tsx
       - DELETE the block at ~lines 125–129:
         `<div className="flex items-center justify-between"><Button onClick={() => setLocation("/signage/pair")}>{t("signage.admin.devices.pair_button")}</Button></div>`
       - INSERT a new block AFTER the closing `</div>` of the `<div className="rounded-md border border-border bg-card"> … </div>` table wrapper (at ~line 227) and BEFORE `<DeviceEditDialog …>`:
         `<div className="flex justify-end"><Button onClick={() => setLocation("/signage/pair")}>{t("signage.admin.devices.pair_button")}</Button></div>`
       - Note the wrapper class change from `flex items-center justify-between` to `flex justify-end` — the original wrapper used `justify-between` as a legacy of a two-item layout but only ever contained one button. Right-alignment is what the user asked for.
       - The empty-state early-return path (lines ~99–116) already has its own CTA; do NOT duplicate the relocated block there.

    4) SchedulesPage.tsx
       - DELETE the block at ~lines 216–222 (the whole `{!isEmpty && ( … )}` wrapper):
         `{!isEmpty && (<div className="flex justify-end"><Button type="button" onClick={() => setEditing(null)}>{t("signage.admin.schedules.new_cta")}</Button></div>)}`
       - INSERT the same gated block AFTER the `{isEmpty ? <empty-card> : <table-wrapper>}` ternary and BEFORE `<ScheduleEditDialog …>` at ~line 343.
       - Keep the `!isEmpty` gate — SchedulesPage's empty-state card already owns the create CTA, so the relocated button remains hidden in the empty state.

    Rationale for the gating choices:
    - MediaPage: always render (no empty-state CTA exists; user wants register to stay accessible).
    - PlaylistsPage / SchedulesPage / DevicesPage: gate on non-empty list — the empty state card already provides a CTA, and stacking two of them reads as a bug.

    Do NOT:
    - Change button variants, sizes, labels, or i18n keys.
    - Touch the MediaUploadDropZone internals.
    - Add aria-labels (these are text-labelled Buttons; adding aria-label would duplicate the accessible name).
    - Introduce hardcoded colors or `dark:` variants.

    After edits, run the frontend typecheck and the phase-59 guard (which owns icon-button aria + color-literal enforcement) to confirm no regressions.
  </action>
  <verify>
    <automated>cd frontend && npm run typecheck && npm run check:phase-59 && npm run check:phase-57</automated>
  </verify>
  <done>
    All four pages render their primary CTA below the list (right-aligned). typecheck passes. check:phase-59 passes (no icon-button aria or color-literal regressions). check:phase-57 passes (SectionHeader conventions preserved).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — visual relocation across four signage list pages</name>
  <what-built>
    Page-level primary action buttons relocated from above to below the list on /signage/media, /signage/playlists, /signage/devices, and /signage/schedules. No label or handler changes.
  </what-built>
  <how-to-verify>
    1. Start the dev stack: `docker compose up -d` (or whatever the user normally runs).
    2. Sign in as admin.
    3. Visit http://localhost:{admin-port}/signage/media:
       - SectionHeader + dropzone at top; the "URL / HTML registrieren" button appears BELOW the media grid (or below the empty-state card if the library is empty), right-aligned.
       - Confirm the drop-zone's internal "Dateien auswählen" button is still present INSIDE the drop zone.
       - Click the relocated button → MediaRegisterUrlDialog opens.
    4. Visit /signage/playlists (with at least one playlist):
       - "Neue Playlist" button is BELOW the table, right-aligned; clicking opens PlaylistNewDialog.
       - With zero playlists, the empty-state card's own CTA still works; no stacked duplicate CTA.
    5. Visit /signage/devices (with at least one device):
       - "Gerät koppeln" button is BELOW the device table, right-aligned; clicking navigates to /signage/pair.
       - With zero devices, the empty-state card's own CTA still works; no stacked duplicate.
    6. Visit /signage/schedules (with at least one schedule):
       - "Neuer Zeitplan" button is BELOW the schedules table, right-aligned; clicking opens ScheduleEditDialog in create mode.
       - With zero schedules, empty-state card's own CTA still works; no stacked duplicate.
    7. Switch language DE ↔ EN; confirm labels flip correctly on all four relocated buttons.
    8. Tab through each page with the keyboard; confirm the relocated buttons receive a visible focus ring (Phase 59 Plan 59-02 convergence).
  </how-to-verify>
  <resume-signal>Type "approved" to complete, or describe any visual/behavioral issue to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npm run typecheck` clean
- `npm run check:phase-59` clean (Plan 59-03 CI guards)
- `npm run check:phase-57` clean (SectionHeader conventions)
- Human UAT: all four pages place the primary CTA below the list, right-aligned; empty states still functional; no duplicate CTAs.
</verification>

<success_criteria>
- All four must-have truths observable in the running app.
- No i18n key added or renamed; parity script untouched.
- No backend edits.
- Single commit touching the four page files.
</success_criteria>

<output>
After completion, create `.planning/quick/260422-i41-move-primary-action-buttons-below-tables/260422-i41-SUMMARY.md` recording: which line ranges moved, the gate choice per page (always-render vs. non-empty-gated), and UAT sign-off.
</output>
