---
phase: 40-admin-settings-docs-hardening
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - frontend/src/pages/SettingsPage.tsx
  - frontend/src/pages/SensorsSettingsPage.tsx
  - frontend/src/components/settings/sensors/SensorRowForm.tsx
  - frontend/src/components/settings/sensors/SensorProbeButton.tsx
  - frontend/src/components/settings/sensors/SnmpWalkCard.tsx
  - frontend/src/components/settings/sensors/SensorRemoveDialog.tsx
  - frontend/src/components/DeleteConfirmDialog.tsx
  - frontend/src/lib/api.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
autonomous: false
requirements:
  - SEN-ADM-06
  - SEN-ADM-07
  - SEN-ADM-02
  - SEN-I18N-01
must_haves:
  truths:
    - "Admin can expand the SNMP-Walk section, enter host/port/community/base-OID, click Walk, and see a scrollable results table of {oid, type, value}."
    - "Admin can click an OID row in the Walk results and assign it to the Temperature-OID or Humidity-OID field of a target sensor row via a target-picker dropdown."
    - "Admin can click 'Probe' next to a sensor row and see the live {temperature, humidity} result inline (success) or a destructive toast (failure) — using the uncommitted draft config."
    - "Removing a sensor opens a dedicated confirmation dialog (no browser confirm()) that names the sensor; confirm issues DELETE; cancel leaves the draft untouched."
    - "From the main /settings page, an admin sees a 'Sensor configuration' link/row that navigates to /settings/sensors; Viewer does not see the link."
    - "Full admin onboarding flow — walk → pick OID → fill row → probe → save — works end-to-end against the live API."
  artifacts:
    - path: "frontend/src/components/settings/sensors/SnmpWalkCard.tsx"
      provides: "Collapsible OID-Finder with host/port/community/base-OID inputs, Walk button, results table, click-to-assign"
      min_lines: 100
    - path: "frontend/src/components/settings/sensors/SensorProbeButton.tsx"
      provides: "Per-row Probe button; state machine idle → running → success(inline chip) / failure(toast)"
      min_lines: 60
    - path: "frontend/src/components/settings/sensors/SensorRemoveDialog.tsx"
      provides: "Sensor-specific confirmation dialog (generic DeleteConfirmDialog refactored to take title/body/name props)"
      min_lines: 50
    - path: "frontend/src/pages/SettingsPage.tsx"
      provides: "New admin-only row/link to /settings/sensors"
      contains: "/settings/sensors"
  key_links:
    - from: "frontend/src/components/settings/sensors/SnmpWalkCard.tsx"
      to: "backend /api/sensors/snmp-walk"
      via: "POST via new runSnmpWalk() api.ts fetcher"
      pattern: "/api/sensors/snmp-walk"
    - from: "frontend/src/components/settings/sensors/SensorProbeButton.tsx"
      to: "backend /api/sensors/snmp-probe"
      via: "POST via new runSnmpProbe() api.ts fetcher"
      pattern: "/api/sensors/snmp-probe"
    - from: "frontend/src/components/settings/sensors/SnmpWalkCard.tsx"
      to: "frontend/src/hooks/useSensorDraft.ts"
      via: "useSensorDraft().updateRow() called on click-to-assign"
      pattern: "updateRow"
    - from: "frontend/src/pages/SettingsPage.tsx"
      to: "frontend/src/pages/SensorsSettingsPage.tsx"
      via: "wouter <Link to=\"/settings/sensors\">"
      pattern: "/settings/sensors"
---

<objective>
Layer the interactive SNMP tooling on top of the 40-01 foundation: SNMP-Walk OID-Finder with click-to-assign, per-row Probe button with inline result / toast, sensor-specific confirmation dialog replacing the browser `confirm()` stub, and the admin-only link from `/settings` → `/settings/sensors`. Close the full-flow human UAT so Phase 40's code scope is verified end-to-end before the docs-only Plan 40-03 ships.

Purpose: satisfies SEN-ADM-06, SEN-ADM-07, completes SEN-ADM-02, and exercises the SEN-ADM-01..05 flow under realistic admin usage. Also completes the SEN-I18N-01 admin-scope additions that are Walk/Probe-specific (not covered in 40-01).

Output: An admin can onboard a brand-new sensor in one session — discover its OIDs via Walk, assign them to a row with one click, fill the remaining config, verify with Probe, save, and see the dashboard update on next refetch. A confirm dialog for removals mirrors the project-wide DeleteConfirmDialog pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-admin-settings-docs-hardening/40-CONTEXT.md
@.planning/phases/40-admin-settings-docs-hardening/40-01-PLAN.md
@.planning/phases/38-backend-schema-scheduler/38-02-SUMMARY.md

@frontend/src/pages/SettingsPage.tsx
@frontend/src/components/DeleteConfirmDialog.tsx
@frontend/src/components/sensors/PollNowButton.tsx
@frontend/src/components/settings/PersonioCard.tsx
@frontend/src/lib/api.ts
@backend/app/routers/sensors.py

<interfaces>
<!-- Backend endpoints live since Phase 38-02 — use directly. -->

```
POST /api/sensors/snmp-probe   body: {host, port, community, temperature_oid, humidity_oid, temperature_scale, humidity_scale}
                                returns: {temperature: number|null, humidity: number|null}  (200 on reachable; 504 on timeout)

POST /api/sensors/snmp-walk    body: {host, port, community, base_oid, max_results?}
                                returns: [{oid: string, value: string, type: string}]       (200; 504 on timeout; capped at 500)
```

Both are admin-gated at router level (Phase 38 decision). 30s asyncio.wait_for wrap server-side — client should match with Promise.race(30_000) for symmetry (pattern in `frontend/src/components/sensors/PollNowButton.tsx`).

From 40-01 (will be in place when this plan runs):
- `useSensorDraft()` exposes `rows`, `updateRow(_localId, patch)`, `markRowDeleted(_localId)`.
- Each row has `_localId: string`, `temperature_oid: string`, `humidity_oid: string`.
- `SensorCreatePayload`, `SensorUpdatePayload`, `createSensor`, `updateSensor`, `deleteSensor` exist in api.ts.
- i18n keys under `sensors.admin.walk.*`, `sensors.admin.probe.*`, `sensors.admin.remove_confirm.*` already added by 40-01 (remove_confirm) and this plan (walk, probe).

From frontend/src/components/DeleteConfirmDialog.tsx (generic candidate):
Current signature is batch-specific (`batch: UploadBatchSummary | null`). Must be refactored in Task 1 into a generic version `{title, body, confirmLabel, cancelLabel, onConfirm}` — OR a new `<SensorRemoveDialog>` that wraps `<Dialog>` + `<DialogContent>` primitives directly (decision inside task). Keep the uploads page's current usage working (Tasks.md — existing `<DeleteConfirmDialog>` call site in UploadPage must not break).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SNMP Walk UI + Probe button + Remove dialog + SettingsPage link + new i18n keys</name>
  <files>
    frontend/src/lib/api.ts
    frontend/src/components/settings/sensors/SensorProbeButton.tsx
    frontend/src/components/settings/sensors/SnmpWalkCard.tsx
    frontend/src/components/settings/sensors/SensorRemoveDialog.tsx
    frontend/src/components/settings/sensors/SensorRowForm.tsx
    frontend/src/pages/SensorsSettingsPage.tsx
    frontend/src/pages/SettingsPage.tsx
    frontend/src/locales/en.json
    frontend/src/locales/de.json
  </files>
  <action>
Ship the interactive tooling.

**1. frontend/src/lib/api.ts** — add two new fetchers immediately after `deleteSensor` (from 40-01):

```typescript
export interface SnmpProbeRequestPayload {
  host: string;
  port: number;
  community: string;
  temperature_oid: string | null;
  humidity_oid: string | null;
  temperature_scale: string;
  humidity_scale: string;
}

export interface SnmpProbeResult {
  temperature: number | null;
  humidity: number | null;
}

export async function runSnmpProbe(body: SnmpProbeRequestPayload): Promise<SnmpProbeResult> {
  return apiClient<SnmpProbeResult>("/api/sensors/snmp-probe", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  });
}

export interface SnmpWalkRequestPayload {
  host: string;
  port: number;
  community: string;
  base_oid: string;
  max_results?: number;
}

export interface SnmpWalkEntry {
  oid: string;
  value: string;
  type: string;
}

export async function runSnmpWalk(body: SnmpWalkRequestPayload): Promise<SnmpWalkEntry[]> {
  return apiClient<SnmpWalkEntry[]>("/api/sensors/snmp-walk", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  });
}
```

**2. SensorProbeButton.tsx** — state-machine component, props `{ row: SensorDraftRow }`. Click calls `runSnmpProbe({host, port, community, temperature_oid, humidity_oid, temperature_scale, humidity_scale})`. Community source: if `row.communityDirty` send `row.community`; else the UI cannot probe without community (show disabled with tooltip `"Enter community to probe"` / DE `"Community eingeben um zu prüfen"` — add i18n key `sensors.admin.probe.need_community`). Wrap the fetch in a Promise.race with 30_000ms timeout (mirror `PollNowButton.tsx` — identical Error("timeout") sentinel). States:
  - idle → button `{t("sensors.admin.probe.test")}` (EN "Probe" / DE "Prüfen")
  - running → disabled + `{t("sensors.admin.probe.running")}`  ("Probing…" / "Prüfe…")
  - success → inline chip for 5s showing `"23.4 °C · 45 %"` (formatted via `toFixed(1)` / `toFixed(0)`) + toast.success(`sensors.admin.probe.success`)
  - failure → toast.error(`sensors.admin.probe.failure`, {detail})
  - timeout → toast.error(`sensors.admin.probe.timeout`)
Tokens only; use `bg-primary/10 text-primary` for success chip, `text-destructive` for error text.

**3. SnmpWalkCard.tsx** — a collapsible Card using existing `<Card>` + a button-driven open state. Layout:
  - Header row: CardTitle `{t("sensors.admin.walk.title")}` + Chevron toggle
  - When expanded:
    - Inputs row (4-column grid on md+): host | port | community (password) | base-OID
    - Run Walk button (primary variant) with Promise.race(30_000) timeout, same sentinel pattern
    - Results panel: if loading → spinner; if empty → `{t("sensors.admin.walk.empty")}`; if results → scrollable `<div className="max-h-96 overflow-y-auto">` table (oid | type | value)
    - Each results row has an "Assign" button that opens a small inline picker: target sensor row (dropdown listing `rows.map(r => r.name || "(unnamed)")`) + field selector (Temperature OID / Humidity OID) + Confirm. Confirm calls `updateRow(_localId, {temperature_oid: oid})` or `{humidity_oid: oid}` from `useSensorDraft`.
    - Alternative lower-friction UX (optional): clicking "Assign" offers two buttons directly — "→ Temp OID" and "→ Humidity OID" for each sensor row, grouped visually. Pick whichever is cleaner; document choice in code comment. **Recommended:** the dropdown-then-button is clearer for 3+ sensors.
  - Seed the Walk inputs from the first existing sensor row (if any) on mount so admin doesn't re-type host/community — but never auto-fill community from stored ciphertext (community is write-only; admin must type it).

**4. SensorRemoveDialog.tsx** — wrap `<Dialog>` + `<DialogContent>` primitives (same as DeleteConfirmDialog but sensor-specific):

```tsx
interface Props {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  sensorName: string;
  onConfirm: () => void;
}
// Body: t("sensors.admin.remove_confirm.body", { name: sensorName })
// Confirm button: variant="destructive"
```

Wire into SensorRowForm.tsx — replace the `confirm()` TODO stub from 40-01 with local dialog state + `<SensorRemoveDialog>`. When user clicks Remove: if `id === null` (unsaved row) → immediate local removal, no dialog (no server consequences to warn about); else → open dialog; confirm triggers `markRowDeleted(_localId)`.

**Do NOT** modify `DeleteConfirmDialog.tsx` for this plan — its UploadPage consumer depends on the `batch` prop shape. Generalizing it is a separate refactor; a parallel `SensorRemoveDialog` is cleaner and avoids cross-feature blast radius.

**5. SensorsSettingsPage.tsx** — render `<SnmpWalkCard />` between the sensor list Card and `<PollIntervalCard />`. Pass `rows` and `updateRow` so it can assign OIDs.

**6. SettingsPage.tsx** — add a new admin-only Card (or inline row — match the existing Card idiom) BEFORE the HR card:

```tsx
<AdminOnly>
  <Card>
    <CardHeader>
      <CardTitle className="text-xl font-semibold">{t("settings.sensors_link.title")}</CardTitle>
    </CardHeader>
    <CardContent>
      <p className="text-sm text-muted-foreground mb-4">{t("settings.sensors_link.body")}</p>
      <Link to="/settings/sensors" className="inline-flex items-center gap-2 rounded-md border border-border bg-card px-3 h-9 text-sm hover:bg-accent/10 transition-colors">
        <Thermometer className="h-4 w-4" aria-hidden="true" />
        {t("settings.sensors_link.cta")}
      </Link>
    </CardContent>
  </Card>
</AdminOnly>
```

Import `Link` from `wouter`, `Thermometer` from `lucide-react`.

**7. i18n additions** — append to BOTH locale files (below the 40-01 admin keys):

EN:
- `sensors.admin.walk.title` — "SNMP Walk (OID finder)"
- `sensors.admin.walk.host` — "Host"
- `sensors.admin.walk.port` — "Port"
- `sensors.admin.walk.community` — "Community"
- `sensors.admin.walk.base_oid` — "Base OID"
- `sensors.admin.walk.run` — "Run walk"
- `sensors.admin.walk.running` — "Walking…"
- `sensors.admin.walk.empty` — "No results"
- `sensors.admin.walk.timeout` — "Walk timed out after 30 s"
- `sensors.admin.walk.failure` — "Walk failed: {{detail}}"
- `sensors.admin.walk.results_count` — "{{count}} OIDs found"
- `sensors.admin.walk.assign_to_temp` — "→ Temp OID"
- `sensors.admin.walk.assign_to_humidity` — "→ Humidity OID"
- `sensors.admin.walk.assign_target` — "Assign to sensor"
- `sensors.admin.walk.assign_confirm` — "Assign"
- `sensors.admin.probe.test` — "Probe"
- `sensors.admin.probe.running` — "Probing…"
- `sensors.admin.probe.success` — "Probe succeeded"
- `sensors.admin.probe.failure` — "Probe failed: {{detail}}"
- `sensors.admin.probe.timeout` — "Probe timed out after 30 s"
- `sensors.admin.probe.need_community` — "Enter community to probe"
- `sensors.admin.probe.result` — "{{temp}} °C · {{humidity}} %"
- `settings.sensors_link.title` — "Sensor monitoring"
- `settings.sensors_link.body` — "Configure sensors, polling cadence, and thresholds."
- `settings.sensors_link.cta` — "Open sensor configuration"

DE (full parity, "du" tone):
- `sensors.admin.walk.title` — "SNMP-Walk (OID-Finder)"
- `sensors.admin.walk.host` — "Host"
- `sensors.admin.walk.port` — "Port"
- `sensors.admin.walk.community` — "Community"
- `sensors.admin.walk.base_oid` — "Basis-OID"
- `sensors.admin.walk.run` — "Walk starten"
- `sensors.admin.walk.running` — "Läuft…"
- `sensors.admin.walk.empty` — "Keine Ergebnisse"
- `sensors.admin.walk.timeout` — "Walk nach 30 s abgebrochen"
- `sensors.admin.walk.failure` — "Walk fehlgeschlagen: {{detail}}"
- `sensors.admin.walk.results_count` — "{{count}} OIDs gefunden"
- `sensors.admin.walk.assign_to_temp` — "→ Temp-OID"
- `sensors.admin.walk.assign_to_humidity` — "→ Luftfeuchte-OID"
- `sensors.admin.walk.assign_target` — "Zu welchem Sensor"
- `sensors.admin.walk.assign_confirm` — "Zuweisen"
- `sensors.admin.probe.test` — "Prüfen"
- `sensors.admin.probe.running` — "Prüfe…"
- `sensors.admin.probe.success` — "Prüfung erfolgreich"
- `sensors.admin.probe.failure` — "Prüfung fehlgeschlagen: {{detail}}"
- `sensors.admin.probe.timeout` — "Prüfung nach 30 s abgebrochen"
- `sensors.admin.probe.need_community` — "Community eingeben um zu prüfen"
- `sensors.admin.probe.result` — "{{temp}} °C · {{humidity}} %"
- `settings.sensors_link.title` — "Sensor-Monitoring"
- `settings.sensors_link.body` — "Sensoren, Abfrage-Takt und Schwellen konfigurieren."
- `settings.sensors_link.cta` — "Sensor-Konfiguration öffnen"

Token-only Tailwind throughout. No `dark:`, no hex.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && node -e "const en=JSON.parse(require('fs').readFileSync('src/locales/en.json','utf8'));const de=JSON.parse(require('fs').readFileSync('src/locales/de.json','utf8'));const keys=Object.keys(en).filter(k=>k.startsWith('sensors.admin.walk.')||k.startsWith('sensors.admin.probe.')||k.startsWith('settings.sensors_link.'));const missing=keys.filter(k=>!(k in de));if(missing.length){console.error('DE missing:',missing);process.exit(1)}console.log('OK — '+keys.length+' new keys, DE parity.')"</automated>
  </verify>
  <done>
- `runSnmpProbe` + `runSnmpWalk` exported from `api.ts` with 30s Promise.race wrapped at call sites.
- `SnmpWalkCard` renders inside `SensorsSettingsPage` with collapsible state, inputs, results table, click-to-assign.
- `SensorProbeButton` wired into `SensorRowForm` next to each row; disabled state when community blank.
- `SensorRemoveDialog` replaces the `confirm()` stub in SensorRowForm.
- `SettingsPage` has a new admin-only Card linking to `/settings/sensors`; viewer does not see it.
- All new i18n keys present in EN + DE with parity check passing.
- `grep -rE 'dark:' frontend/src/components/settings/sensors/` → zero.
- `grep -rE '#[0-9a-fA-F]{6}' frontend/src/components/settings/sensors/ frontend/src/pages/SensorsSettingsPage.tsx` → zero.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — full admin onboarding flow + all admin-page interactions</name>
  <what-built>
The complete `/settings/sensors` admin experience:
- Sub-page foundation (40-01): route + AdminOnly + CRUD form + polling interval + thresholds + dirty-guard + admin i18n.
- Interactive tooling (40-02): SNMP Walk OID-Finder with click-to-assign, per-row Probe with inline result/toast, confirmation dialog on sensor removal, and the /settings link.
  </what-built>
  <how-to-verify>
**Prerequisites:**
- Stack up: `docker compose up -d` (5 services healthy).
- Phase 38-03 scheduler live (so `reschedule_sensor_poll` is wired end-to-end).
- Phase 39 `/sensors` dashboard accessible (for verifying interval + threshold changes propagate).
- One Admin account + one Viewer account.
- Seeded sensor `Produktion` (192.9.201.27) present from Phase 38-01.
- Open http://localhost:5173/settings/sensors

**12-step verification:**

1. **Route guard** — As Viewer: visit http://localhost:5173/settings/sensors. Expected: "Admin only" shell, no form, no data leaked. As Admin: same URL renders the full page.

2. **Settings link** — As Admin on http://localhost:5173/settings. Expected: a "Sensor monitoring" / "Sensor-Monitoring" Card appears. Click "Open sensor configuration" / "Sensor-Konfiguration öffnen" → navigates to `/settings/sensors`. Viewer: the Card is absent.

3. **List existing sensors** — At least the seeded `Produktion` row appears with host 192.9.201.27. Community field shows placeholder `••••••` + stored-hint under it. temperature_oid and humidity_oid populated from seed.

4. **Add new sensor** — Click "+ Sensor hinzufügen". Expected: a new empty row appears. Fill: name `Test-Lager`, host `192.9.201.27`, community `public`, leave OIDs empty. Click Probe (inline button on the row). Expected: toast `Prüfung fehlgeschlagen: ...` (OIDs empty = probe returns null temp/hum) OR success with `null · null` — either is contract-correct. Now: open SNMP Walk section.

5. **SNMP Walk — happy path** — Expand the "SNMP-Walk (OID-Finder)" card. Inputs should be pre-filled with the first-existing-sensor's host + port (community blank). Type `public` into community, base-OID `.1.3.6.1.4.1.21796.4.9.3.1` and click "Walk starten". Expected: within 10–30s, a results table appears listing OIDs from the Kirchhoff tree; result count shown `"X OIDs gefunden"`. Scrollable.

6. **Click-to-assign** — In the Walk results, find an OID ending in `.5.1` (temperature) or similar. Click "Assign". Pick the `Test-Lager` row as target. Pick "→ Temp-OID". Click Zuweisen. Expected: the target row's temperature_oid field now shows the clicked OID value; field is marked dirty (sticky ActionBar shows "Nicht gespeichert").

7. **Probe with valid OIDs** — With Test-Lager populated (host, community, temp_oid), click Probe on its row. Expected: running state, then success chip shows "`23.4 °C · null %`" (humidity OID still empty) or `null · null` if the host rejects. Toast `Prüfung erfolgreich`. Error path: set host to `192.9.201.99` (unreachable), click Probe → destructive toast after ≤30s.

8. **Save flow** — Fill a valid humidity_oid via another Walk assign. Click Save. Expected: toast `Sensor-Konfiguration gespeichert`. ActionBar returns to pristine. Refresh page → new `Test-Lager` row persists; community field empty, stored-hint present.

9. **Edit existing + community omit** — Change `Test-Lager` name to `Test-Lager-2`. Do NOT touch community. Save. Expected: success. Refresh. Row shows new name; row is still reachable by Probe (community ciphertext preserved server-side). To verify: click Probe → success with live values.

10. **Remove with dialog** — Click Remove on the `Test-Lager-2` row. Expected: custom dialog "Sensor entfernen?" with copy `Damit werden Test-Lager-2 und alle Messwerte gelöscht...`. Click Abbrechen → dialog closes, row still present, draft un-dirtied. Click Remove again → Entfernen → row marked for deletion visually (e.g. struck through or removed immediately from DOM). Save. Expected: toast success; refresh → row gone.

11. **Polling interval reschedule** — Change polling interval from 60 to 30. Save. Expected: toast success. `docker compose logs api --tail=50 | grep reschedule_sensor_poll` shows a log line with old=60 new=30 and a new `next_run_time`. Visit http://localhost:5173/sensors (Phase 39 dashboard). SubHeader freshness should now tick every ~30s. Revert interval to 60 and save.

12. **Threshold writes** — Set temperature_min=18, temperature_max=26. Save. Navigate to http://localhost:5173/sensors. Expected: two dashed destructive ReferenceLines appear on the temperature chart; if current value outside 18..26, card shows destructive color + "Außerhalb des Bereichs". Revert (set min=0, max=100 — or leave as-is; document that blank-to-clear is NOT supported this phase).

13. **Dirty-guard navigate-away** — Add a new empty row (makes draft dirty). Click the NavBar brand/logo (or manually navigate to `/home`). Expected: UnsavedChangesDialog appears ("Ungespeicherte Änderungen"). Click Stay → stays on /settings/sensors, row preserved. Click again → Discard & leave → navigates away, draft reset.

14. **i18n parity end-to-end** — Toggle EN/DE. Expected: every visible string on /settings/sensors (header, fields, walk labels, probe labels, help text, toasts, dialog buttons) flips. Browser console: zero `i18next::translator: missingKey` warnings.

**Resume signal:** Reply `approved` to mark Plan 40-02 complete. If issues: list them and I'll create revision plans.
  </how-to-verify>
  <files>(N/A — checkpoint task: user verifies the combined output of 40-01 + 40-02 in a running stack; no files written)</files>
  <action>Execute the 14-step verification below against a running stack. This checkpoint is Plan 40-02's acceptance gate and the final guardrail for Phase 40 code scope before the docs-only Plan 40-03 ships. See &lt;how-to-verify&gt; for the full protocol.</action>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; automated gating is not applicable. Execute the 14-step manual protocol in &lt;how-to-verify&gt;.</automated>
  </verify>
  <done>User replies "approved" after all 14 UAT steps pass; or issues are listed for revision.</done>
  <resume-signal>Type "approved" once all 14 steps pass, or list failures.</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — clean
- `grep -rE 'dark:' frontend/src/components/settings/sensors/` — zero
- `grep -rE '#[0-9a-fA-F]{6}' frontend/src/components/settings/sensors/ frontend/src/pages/SensorsSettingsPage.tsx` — zero
- i18n parity script (in Task 1 verify) — exits 0
- Human UAT (Task 2) — 14/14 steps approved
- No modification to DeleteConfirmDialog.tsx (upload page regression avoided)
- No modification to scheduler.py, /api/sensors routes, backend settings handler (40-01's scope)
</verification>

<success_criteria>
- SEN-ADM-06: SNMP-Walk OID-Finder functional with host/port/community/base-OID inputs, results table, click-to-assign into sensor row temp/humidity OID fields.
- SEN-ADM-07: per-row Probe button with state machine (idle/running/success/failure/timeout), inline result on success, destructive toast on failure/timeout.
- SEN-ADM-02: sensor removal uses dedicated confirmation dialog (not browser confirm); full CRUD cycle (add + edit + remove) verified in UAT.
- SEN-I18N-01 (walk/probe + link): `sensors.admin.walk.*`, `sensors.admin.probe.*`, `settings.sensors_link.*` keys in EN + DE parity.
- Link from `/settings` → `/settings/sensors` admin-gated via AdminOnly.
- Full admin-onboarding human UAT passes.
</success_criteria>

<output>
After completion, create `.planning/phases/40-admin-settings-docs-hardening/40-02-SUMMARY.md` following `$HOME/.claude/get-shit-done/templates/summary.md`. Document: (1) the walk/probe Promise.race(30_000) mirror of the backend wait_for, (2) the "community required for probe" UX and why we don't auto-fill from stored ciphertext (write-only rule), (3) that blank-to-clear for thresholds remains a known limitation (carry-forward documented for Phase 41 or a /reset-threshold endpoint), (4) UAT approval reference.
</output>
