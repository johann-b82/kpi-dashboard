---
phase: 40-admin-settings-docs-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/docs/en/admin-guide/sensor-monitor.md
  - frontend/src/docs/de/admin-guide/sensor-monitor.md
  - frontend/src/lib/docs/registry.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
autonomous: true
requirements:
  - SEN-OPS-02
  - SEN-OPS-03
  - SEN-OPS-04
must_haves:
  truths:
    - "At /docs/admin-guide/sensor-monitor an admin sees a complete bilingual operator article covering onboarding, thresholds, polling interval, troubleshooting, and the host-mode fallback runbook."
    - "The docs sidebar (both EN + DE) lists the new 'Sensor Monitor' entry in the admin-guide section; clicking it renders the Markdown article."
    - "The article explains the community-as-secret rule + never-use-public warning + Docker bridge prerequisite + host-mode + macvlan fallback options with code snippets."
    - "Both EN and DE articles cover the same scope (structural parity); DE uses 'du' tone."
  artifacts:
    - path: "frontend/src/docs/en/admin-guide/sensor-monitor.md"
      provides: "English admin guide article for sensor monitor configuration + troubleshooting + host-mode runbook"
      min_lines: 150
    - path: "frontend/src/docs/de/admin-guide/sensor-monitor.md"
      provides: "German admin guide article — structural parity, 'du' tone"
      min_lines: 150
    - path: "frontend/src/lib/docs/registry.ts"
      provides: "Registry entry + sidebar list entry for sensor-monitor slug in both locales"
      contains: "sensor-monitor"
    - path: "frontend/src/locales/en.json"
      provides: "docs.nav.adminSensorMonitor title key"
      contains: "adminSensorMonitor"
    - path: "frontend/src/locales/de.json"
      provides: "docs.nav.adminSensorMonitor DE parity"
      contains: "adminSensorMonitor"
  key_links:
    - from: "frontend/src/lib/docs/registry.ts"
      to: "frontend/src/docs/en/admin-guide/sensor-monitor.md"
      via: "import enSensorMonitor from ../../docs/en/admin-guide/sensor-monitor.md?raw"
      pattern: "sensor-monitor"
    - from: "frontend/src/lib/docs/registry.ts"
      to: "frontend/src/docs/de/admin-guide/sensor-monitor.md"
      via: "import deSensorMonitor from ../../docs/de/admin-guide/sensor-monitor.md?raw"
      pattern: "sensor-monitor"
---

<objective>
Author the bilingual admin-guide article for the Sensor Monitor and wire it into the v1.13 docs pipeline. Independent of 40-01 and 40-02 — docs-only, no frontend code beyond registry entries + two i18n title keys.

Purpose: satisfies SEN-OPS-02 (bilingual admin guide), SEN-OPS-03 (docs index updated), SEN-OPS-04 (host-mode fallback runbook embedded in troubleshooting).

Output: An admin navigating to `/docs/admin-guide/sensor-monitor` (language-switch-aware) reads a complete operator article covering prerequisites, onboarding workflow, thresholds, polling interval, troubleshooting (including the host-mode + macvlan fallback with code snippets), and security warnings (community-as-secret, never-use-public).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-admin-settings-docs-hardening/40-CONTEXT.md
@.planning/phases/38-backend-schema-scheduler/38-02-SUMMARY.md

@frontend/src/lib/docs/registry.ts
@frontend/src/docs/en/admin-guide/personio.md
@frontend/src/docs/de/admin-guide/personio.md
@frontend/src/docs/en/admin-guide/system-setup.md
@frontend/src/docs/de/admin-guide/system-setup.md
@frontend/src/locales/en.json
@frontend/src/locales/de.json

<interfaces>
<!-- Docs pipeline contract (v1.13, unchanged) -->

From `frontend/src/lib/docs/registry.ts`:
```typescript
export const sections: Record<SectionId, ArticleEntry[]> = {
  "admin-guide": [
    { slug: "intro", titleKey: "docs.nav.adminGuideIntro" },
    { slug: "system-setup", titleKey: "docs.nav.adminSystemSetup" },
    { slug: "architecture", titleKey: "docs.nav.adminArchitecture" },
    { slug: "personio", titleKey: "docs.nav.adminPersonio" },
    { slug: "user-management", titleKey: "docs.nav.adminUserManagement" },
  ],
};
export const registry = { en: {...}, de: {...} };  // registry[lang][section][slug] = raw md string
```

Add a NEW entry between "personio" and "user-management" (logical grouping: integrations together):
```typescript
{ slug: "sensor-monitor", titleKey: "docs.nav.adminSensorMonitor" },
```

And in `registry.en["admin-guide"]` + `registry.de["admin-guide"]`, add `"sensor-monitor": enSensorMonitor` / `deSensorMonitor`.

Article format reference: `frontend/src/docs/en/admin-guide/personio.md` — H1, H2 section headings, `> **Note:**` callouts, Markdown tables, "Related Articles" footer. No frontmatter — the registry is the authoritative metadata source (v1.13 pipeline does not parse frontmatter).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author bilingual admin article + wire into docs registry + add title keys</name>
  <files>
    frontend/src/docs/en/admin-guide/sensor-monitor.md
    frontend/src/docs/de/admin-guide/sensor-monitor.md
    frontend/src/lib/docs/registry.ts
    frontend/src/locales/en.json
    frontend/src/locales/de.json
  </files>
  <action>
Produce the bilingual admin guide article and wire it in.

**1. `frontend/src/docs/en/admin-guide/sensor-monitor.md`** — follow the exact Markdown style of `personio.md` (H1, H2s, Note callouts, tables, Related Articles). Scope (must cover all sections):

```markdown
# Sensor Monitor

## Overview
The Sensor Monitor polls environmental SNMP sensors (temperature + humidity) from
the shared Docker network, stores readings in PostgreSQL, and surfaces them on the
admin-only `/sensors` dashboard. Onboarding is entirely UI-driven — no YAML or SQL
access required.

## Prerequisites
1. The `api` container must be able to reach the sensor host over SNMP/UDP 161.
   Verify with:
   ```bash
   docker compose exec api snmpget -v2c -c public 192.9.201.27 1.3.6.1.4.1.21796.4.9.3.1.5.2
   ```
   A numeric response means you are good to go. A timeout means the Docker bridge
   cannot route to the sensor subnet — see [Troubleshooting → Host-mode fallback](#host-mode-fallback)
   below.
2. Admin role on the KPI Dashboard (Viewer accounts see neither the launcher tile
   nor `/settings/sensors`).

## Onboarding a Sensor
Navigate to **Settings → Open sensor configuration** (or go directly to
`/settings/sensors`).

### Step 1: Discover OIDs with SNMP Walk
1. Expand **SNMP Walk (OID finder)**.
2. Enter the sensor host, port (default 161), community string, and a base OID
   (for Kirchhoff sensors, start with `.1.3.6.1.4.1.21796.4.9.3.1`).
3. Click **Run walk**. Results appear in a scrollable table with OID, type, value.
4. Identify the temperature and humidity OIDs (typically ending in `.5.1` and
   `.6.1` for Kirchhoff).

### Step 2: Add a sensor row
1. Click **+ Add sensor**.
2. Fill name, host, port, community, and scale factors (1.0 for raw values;
   10.0 if the sensor returns integer deci-°C).
3. In the Walk results, click **Assign** on the temperature OID → pick the new
   row → **Temp OID**. Repeat for humidity.

### Step 3: Probe
1. Click **Probe** on the sensor row. Live temperature + humidity appear inline
   on success.
2. If the probe fails: double-check host reachability, community, and OID validity.

### Step 4: Save
Click **Save**. The sensor persists; the scheduler picks it up on the next tick.
Switch to `/sensors` to confirm the new card appears with live readings.

## Thresholds
Global thresholds apply to all sensors. Set them in the **Global thresholds**
card:
- Temperature min / max (°C)
- Humidity min / max (%)

When a reading falls outside the range, the KPI card shows a destructive
(red-tinted) value and an "Out of range" caption. Charts draw dashed
reference lines at each threshold.

> **Known limitation (v1.15):** Leaving a threshold input blank means "do not
> change". To clear a previously-set threshold, contact the operator to unset
> it directly in the database or wait for a future phase that adds an explicit
> reset action.

Per-sensor threshold overrides are not supported in this release.

## Polling Interval
The **Polling cadence** card sets a single interval (5–86400 seconds) for all
enabled sensors. Saving triggers a live reschedule — no API restart required.

Recommendations:
- 60 s (default): typical production environmental monitoring
- 30 s: for tight threshold alerting (future v1.16 feature)
- 300+ s: for low-frequency baseline monitoring

## Troubleshooting

### Sensor appears "Offline"
The health chip on `/sensors` reads **Offline for Xm** when three consecutive
polls fail. Likely causes:
1. **Docker-to-sensor reachability broken** — run the Prerequisites smoke test.
2. **Community string mismatch** — sensor rotated credentials. Edit the row,
   enter the new community (leave blank to keep the current one), save.
3. **OID drift** — the sensor firmware changed OID mapping. Re-run SNMP Walk to
   rediscover.

### <a id="host-mode-fallback"></a>Host-mode fallback
If `docker compose exec api snmpget ...` times out but the same command on the
host machine succeeds, the Docker bridge cannot route to the sensor subnet.
Two fallback options:

**Option A — `network_mode: host` on the `api` service (simpler, more invasive):**
```yaml
services:
  api:
    network_mode: host
    # NOTE: incompatible with `ports:` + breaks internal Docker DNS (api → db).
    # All internal service references must switch to `localhost:<port>`.
    # Also requires removing the `networks:` stanza for this service.
```
Trade-offs:
- Breaks service discovery by name — change `DATABASE_URL` from `db:5432` to
  `localhost:5432` and adjust every internal reference.
- Exposes the API on the host interface directly (bypasses Docker's network
  isolation).

**Option B — `macvlan` network (less invasive, more config):**
Attach the `api` service to a macvlan network that shares the host's L2 domain
with the sensor subnet. This preserves Docker DNS and service isolation but
requires a dedicated subnet range and IP reservation outside the DHCP pool.
Recommended only when Option A's trade-offs are unacceptable.

**Preferred order:** stay on the default bridge → Option B (macvlan) →
Option A (host mode) as a last resort.

### Polling runs but no readings appear
Check the logs:
```bash
docker compose logs api --tail=100 | grep -i sensor
```
Look for `PollResult` entries. Empty result sets with no errors usually mean
the OID returned `noSuchObject` — re-run SNMP Walk.

## Security

> **Never use `public` as the community string in production.** Most sensors
> ship with `public` as the default; change it to a per-deployment secret at
> the device level before exposing the monitor.

> **Community strings are encrypted at rest** using the application's Fernet
> key (same key as Personio credentials). They are never decrypted into API
> responses and never logged. The admin form treats community as write-only:
> blank on edit preserves the stored value, a new string overwrites it.

## Related Articles
- [System Setup](/docs/admin-guide/system-setup) — Docker Compose overview
- [Architecture](/docs/admin-guide/architecture) — how the scheduler integrates
- [Personio Integration](/docs/admin-guide/personio) — sibling admin integration
```

Aim for ~180 lines of Markdown. Code fences, tables, callouts match the project's v1.13 style.

**2. `frontend/src/docs/de/admin-guide/sensor-monitor.md`** — full structural parity. Use "du" tone ("klicke", "setze", "prüfe"). Every H1/H2/H3/table/callout/code-block from the EN version must exist in the DE version.

DE headings translation guide:
- Overview → Überblick
- Prerequisites → Voraussetzungen
- Onboarding a Sensor → Einen Sensor einrichten
- Step 1: Discover OIDs with SNMP Walk → Schritt 1: OIDs mit SNMP-Walk finden
- Step 2: Add a sensor row → Schritt 2: Sensor-Zeile hinzufügen
- Step 3: Probe → Schritt 3: Prüfen
- Step 4: Save → Schritt 4: Speichern
- Thresholds → Schwellenwerte
- Polling Interval → Abfrage-Intervall
- Troubleshooting → Fehlerbehebung
- Sensor appears "Offline" → Sensor zeigt "Offline"
- Host-mode fallback → Host-Modus-Fallback
- Polling runs but no readings appear → Abfrage läuft, aber keine Messwerte
- Security → Sicherheit
- Related Articles → Verwandte Artikel

The `<a id="host-mode-fallback"></a>` anchor must be preserved verbatim in the
DE file so the inline link from the Prerequisites section resolves. Verify the
DE link text in the Prerequisites body points at the same anchor. Inline code
(command lines, YAML) remains identical — do not translate commands.

Known-limitation callout in DE:
> **Bekannte Einschränkung (v1.15):** Ein leeres Schwellen-Feld bedeutet
> "nicht ändern". Um einen bereits gesetzten Schwellwert zu entfernen, wende
> dich an den Operator, oder warte auf ein zukünftiges Release mit einer
> expliziten Reset-Aktion.

**3. `frontend/src/lib/docs/registry.ts`** — modify:

```typescript
// Add imports (group with other admin-guide imports at top):
import enSensorMonitor from "../../docs/en/admin-guide/sensor-monitor.md?raw";
import deSensorMonitor from "../../docs/de/admin-guide/sensor-monitor.md?raw";

// Add sidebar entry (between personio and user-management — "integrations" cluster):
"admin-guide": [
  { slug: "intro", titleKey: "docs.nav.adminGuideIntro" },
  { slug: "system-setup", titleKey: "docs.nav.adminSystemSetup" },
  { slug: "architecture", titleKey: "docs.nav.adminArchitecture" },
  { slug: "personio", titleKey: "docs.nav.adminPersonio" },
  { slug: "sensor-monitor", titleKey: "docs.nav.adminSensorMonitor" },  // NEW
  { slug: "user-management", titleKey: "docs.nav.adminUserManagement" },
],

// Add to registry.en["admin-guide"] and registry.de["admin-guide"]:
registry.en["admin-guide"]["sensor-monitor"] = enSensorMonitor;
registry.de["admin-guide"]["sensor-monitor"] = deSensorMonitor;
```

**4. Locale keys** — add a single title key pair:

EN `frontend/src/locales/en.json`:
- `docs.nav.adminSensorMonitor`: `"Sensor Monitor"`

DE `frontend/src/locales/de.json`:
- `docs.nav.adminSensorMonitor`: `"Sensor Monitor"` (keep the product name; do not translate "Monitor")

Add these near the existing `docs.nav.adminPersonio` line (around line 267 in en.json).

**Do NOT:** change any other registry entries, add frontmatter to the .md files, or touch docs rendering code (v1.13 pipeline is stable and generic).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && test -f src/docs/en/admin-guide/sensor-monitor.md && test -f src/docs/de/admin-guide/sensor-monitor.md && wc -l src/docs/en/admin-guide/sensor-monitor.md src/docs/de/admin-guide/sensor-monitor.md && grep -q 'sensor-monitor' src/lib/docs/registry.ts && grep -q 'adminSensorMonitor' src/locales/en.json && grep -q 'adminSensorMonitor' src/locales/de.json && grep -q 'host-mode-fallback' src/docs/en/admin-guide/sensor-monitor.md && grep -q 'host-mode-fallback' src/docs/de/admin-guide/sensor-monitor.md && grep -q 'network_mode: host' src/docs/en/admin-guide/sensor-monitor.md && grep -q 'network_mode: host' src/docs/de/admin-guide/sensor-monitor.md</automated>
  </verify>
  <done>
- Both EN + DE Markdown files exist, each ≥150 lines.
- registry.ts has two new imports + one new sidebar entry at the 5th admin-guide position + two registry assignments.
- Both locale files have `docs.nav.adminSensorMonitor`.
- Both Markdown files contain the `host-mode-fallback` anchor.
- Both contain the `network_mode: host` code block.
- Both contain an Option A / Option B pair (host mode + macvlan).
- Both contain the never-use-public warning and the community-encrypted warning.
- `tsc --noEmit` passes.
- Loading `/docs/admin-guide/sensor-monitor` in the running app renders the EN article; switching locale to DE renders the DE article (verifiable via `docker compose up -d` + manual browser check, not required for the automated verify above).
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — clean
- `test -f frontend/src/docs/en/admin-guide/sensor-monitor.md && test -f frontend/src/docs/de/admin-guide/sensor-monitor.md`
- `grep -c '^## ' frontend/src/docs/en/admin-guide/sensor-monitor.md` and DE — same heading count (structural parity)
- `grep 'sensor-monitor' frontend/src/lib/docs/registry.ts` — 4 matches (2 imports + sidebar entry + 2 registry lines = at minimum 5, 4 acceptable if imports combined)
- Manual smoke (optional): `docker compose up -d && curl -s http://localhost:5173/docs/admin-guide/sensor-monitor` returns 200 with article content after build

Parity spot-check (automated): the script in Task 1 verify confirms both files have the host-mode anchor + yaml block. A stricter structural parity (heading count match) can be added if the checker flags it.
</verification>

<success_criteria>
- SEN-OPS-02: bilingual admin article exists, covers onboarding (walk → probe → save), thresholds, polling interval, troubleshooting, security warnings.
- SEN-OPS-03: docs index (registry sidebar) shows the new article in admin-guide section for both locales.
- SEN-OPS-04: host-mode fallback runbook documented with Option A (network_mode: host) + Option B (macvlan) + trade-offs + preferred order.
- Docs pipeline untouched (no changes to rendering code, just content + registry + two title keys).
- DE structural parity with EN; "du" tone for German.
</success_criteria>

<output>
After completion, create `.planning/phases/40-admin-settings-docs-hardening/40-03-SUMMARY.md` following `$HOME/.claude/get-shit-done/templates/summary.md`. Note: (1) independence from 40-01/40-02 (can ship before or after), (2) v1.13 pipeline reused verbatim, (3) where the host-mode fallback fits in the Prerequisites → Troubleshooting flow.
</output>
