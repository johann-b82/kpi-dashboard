---
phase: 29-outline-wiki-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - README.md
autonomous: true
requirements: [WIK-07]
must_haves:
  truths:
    - "Operator can read .env.example and see which two Outline secrets to generate + how"
    - "README documents BSL 1.1 compliance for the Outline deployment accurately"
  artifacts:
    - path: ".env.example"
      provides: "Outline secret placeholders + OUTLINE_DB_PASSWORD"
      contains: "OUTLINE_SECRET_KEY, OUTLINE_UTILS_SECRET, OUTLINE_DB_PASSWORD"
    - path: "README.md"
      provides: "BSL 1.1 Additional Use Grant compliance note"
      contains: "Business Source License"
  key_links:
    - from: ".env.example"
      to: "docker-compose.yml outline service (plan 29-02)"
      via: "${OUTLINE_SECRET_KEY}, ${OUTLINE_UTILS_SECRET}, ${OUTLINE_DB_PASSWORD} substitution"
      pattern: "OUTLINE_(SECRET_KEY|UTILS_SECRET|DB_PASSWORD)"
---

<objective>
Scaffold Phase 29 secrets in `.env.example` (D-01) and add the BSL 1.1 Additional Use Grant compliance note to `README.md` (WIK-07). Pure docs/config — no services change.

Purpose: lays the `.env` contract that plan 29-02's compose YAML will substitute, and discharges the standalone WIK-07 licence obligation so it cannot block integration work.
Output: updated `.env.example`, updated `README.md` with a `## License Note — Outline Wiki` section using accurate BSL wording.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-outline-wiki-deployment/29-CONTEXT.md
@.planning/phases/29-outline-wiki-deployment/29-RESEARCH.md
@.env.example
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Outline secret placeholders to .env.example (D-01)</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (entire file — preserve existing Phase 27/28 block order)
    - .planning/phases/29-outline-wiki-deployment/29-CONTEXT.md §D-01
    - .planning/phases/29-outline-wiki-deployment/29-RESEARCH.md §`.env.example` additions
  </read_first>
  <action>
    Append a new `# --- Outline Wiki (outline, outline-db, outline-redis services) ---` block at the END of `.env.example`, AFTER the existing `DISABLE_AUTH=false` line. Do NOT reorder existing blocks. The block must contain EXACTLY these lines (comments + three empty placeholders):

    ```
    # --- Outline Wiki (outline, outline-db, outline-redis services) ---
    # SECRET_KEY and UTILS_SECRET must be TWO INDEPENDENT 32-byte hex values.
    # Generate each with:
    #   openssl rand -hex 32
    # Never commit real secrets — .env is gitignored.
    OUTLINE_SECRET_KEY=
    OUTLINE_UTILS_SECRET=
    # Password for the dedicated outline-db Postgres container (separate from
    # POSTGRES_PASSWORD which belongs to KPI Light's `db` service).
    OUTLINE_DB_PASSWORD=
    # NOTE: DEX_OUTLINE_SECRET is already defined in the Dex block above;
    # Outline reuses it (single source of truth per Phase 27 D-14).
    ```

    Do NOT add `DEX_OUTLINE_SECRET` again — it already exists in the Dex block (verified in current .env.example line 26). Do NOT add SMTP vars (WIK-06 excludes them).
  </action>
  <verify>
    <automated>grep -c "^OUTLINE_SECRET_KEY=$" .env.example | grep -q "^1$" && grep -c "^OUTLINE_UTILS_SECRET=$" .env.example | grep -q "^1$" && grep -c "^OUTLINE_DB_PASSWORD=$" .env.example | grep -q "^1$" && grep -q "openssl rand -hex 32" .env.example && ! grep -q "^SMTP_" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^OUTLINE_SECRET_KEY=$" .env.example` returns `1` (empty placeholder, not a value)
    - `grep -c "^OUTLINE_UTILS_SECRET=$" .env.example` returns `1`
    - `grep -c "^OUTLINE_DB_PASSWORD=$" .env.example` returns `1`
    - `grep -q "# --- Outline Wiki" .env.example` succeeds
    - `grep -q "openssl rand -hex 32" .env.example` succeeds
    - `grep -c "^DEX_OUTLINE_SECRET=" .env.example` returns `1` (the existing one — NOT duplicated)
    - `! grep -q "^SMTP_" .env.example` (WIK-06: no SMTP vars)
  </acceptance_criteria>
  <done>`.env.example` contains three Outline placeholders + generation comment, no duplicates, no SMTP vars.</done>
</task>

<task type="auto">
  <name>Task 2: Add BSL 1.1 Additional Use Grant compliance section to README.md (WIK-07)</name>
  <files>README.md</files>
  <read_first>
    - README.md (entire file — locate the existing `## License` section near end, line ~277)
    - .planning/phases/29-outline-wiki-deployment/29-RESEARCH.md §BSL 1.1 paragraph for README (authoritative wording — NOTE the correction: NO 50-person cap; the actual grant prohibits offering a "Document Service")
    - .planning/phases/29-outline-wiki-deployment/29-CONTEXT.md §Specifics (WIK-07)
  </read_first>
  <action>
    Insert a new `## License Note — Outline Wiki` section into `README.md` IMMEDIATELY BEFORE the existing `## License` section (which reads `Internal tool — not currently licensed for external distribution.`). Use this exact text (per RESEARCH correction — do NOT mention a 50-person cap):

    ```markdown
    ## License Note — Outline Wiki

    This repository deploys [Outline](https://github.com/outline/outline), which is
    licensed under the Business Source License 1.1 (BSL 1.1). The Additional Use
    Grant permits internal use by your own employees and contractors. It does NOT
    permit offering a competing "Document Service" — a commercial product that
    grants third parties access to Outline's functionality. Our deployment at
    `wiki.internal` is internal-team documentation only and falls squarely under
    the Additional Use Grant.

    Each Outline release converts to an OSI-approved open-source license four
    years after its release date; no end-of-life concern for self-hosters.

    ---

    ```

    The trailing `---` separator keeps the existing `## License` section visually distinct. Do NOT modify the existing `## License` section content. Do NOT claim a 50-person cap exists (the CONTEXT D-07 reference was incorrect — corrected by researcher).
  </action>
  <verify>
    <automated>grep -q "## License Note — Outline Wiki" README.md && grep -q "Business Source License 1.1" README.md && grep -q "Additional Use Grant" README.md && grep -q "Document Service" README.md && ! grep -qi "50-person" README.md && ! grep -qi "50 person" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## License Note — Outline Wiki" README.md` succeeds
    - `grep -q "Business Source License 1.1" README.md` succeeds
    - `grep -q "Additional Use Grant" README.md` succeeds
    - `grep -q 'competing "Document Service"' README.md` succeeds (verbatim)
    - `grep -q "four\s*\nyears\|four years" README.md` succeeds
    - `! grep -qi "50-person" README.md` AND `! grep -qi "50 person" README.md` (researcher correction)
    - The existing `## License` section with text `Internal tool — not currently licensed for external distribution.` is still present (unchanged)
  </acceptance_criteria>
  <done>README has a new `## License Note — Outline Wiki` section with accurate BSL wording (no user-count cap), placed before the existing `## License` section.</done>
</task>

</tasks>

<verification>
Run from repo root:
```bash
grep -c "^OUTLINE_SECRET_KEY=$\|^OUTLINE_UTILS_SECRET=$\|^OUTLINE_DB_PASSWORD=$" .env.example   # → 3
grep -q "## License Note — Outline Wiki" README.md && echo OK
! grep -qi "50-person\|50 person" README.md && echo "no-cap-claim OK"
```
</verification>

<success_criteria>
- `.env.example` contains three new empty Outline placeholders + `openssl rand -hex 32` comment
- README has the BSL section with accurate wording (prohibition on "Document Service", not a 50-person cap)
- No duplicate `DEX_OUTLINE_SECRET` entry
- No SMTP vars introduced
</success_criteria>

<output>
Create `.planning/phases/29-outline-wiki-deployment/29-01-SUMMARY.md` per template — list files modified, which requirements closed (WIK-07), and note the CONTEXT D-07 correction applied.
</output>
