---
phase: 30.1-rebrand-kpi-light-to-kpi-dashboard
verified: 2026-04-15T00:00:00Z
status: passed
score: 7/7 must-haves verified (after gap closure commit 8c0e816)
gaps:
  - truth: "No user-visible \"KPI Light\" remains in runtime service configuration"
    status: resolved
    reason: "dex/config.yaml retains `name: \"KPI Light\"` on the staticClient for client id `kpi-light`. This `name` field is the display label shown on Dex's login / consent screen — it is user-visible UI, not an internal identifier. The phase scope (plan 30.1-03 §D-01 preservation list) only called out the Dex client *id* (`kpi-light`) as protected; the display `name` field was overlooked. Phase goal explicitly includes \"across UI\"."
    artifacts:
      - path: "dex/config.yaml"
        issue: "Line 57: `name: \"KPI Light\"` on staticClient id=kpi-light. Should be `name: \"KPI Dashboard\"` for consistent UI. Client id on line 55 stays `kpi-light` (internal identifier, correctly preserved)."
    missing:
      - "Update dex/config.yaml line 57 from `name: \"KPI Light\"` to `name: \"KPI Dashboard\"` and restart dex (`docker compose restart dex`). Keep line 55 `id: kpi-light` untouched."
  - truth: "Phase 31 ROADMAP summary row references \"KPI Dashboard\" consistently"
    status: resolved
    reason: "ROADMAP.md line 133 (Phase 31 summary checkbox in the v1.11 IN PROGRESS list) still reads \"KPI Light documentation collection authored and published in Outline\". The detail block at line 242 was correctly updated to \"KPI Dashboard collection\" per plan 30.1-03, but the parallel one-line summary was missed. D-02 historical-freeze does NOT apply — Phase 31 is unshipped (checkbox unchecked, IN PROGRESS milestone)."
    artifacts:
      - path: ".planning/ROADMAP.md"
        issue: "Line 133: \"KPI Light documentation collection\" — inconsistent with line 242 \"KPI Dashboard collection\"."
    missing:
      - "Edit .planning/ROADMAP.md line 133 to read \"KPI Dashboard documentation collection authored and published in Outline\"."
---

# Phase 30.1: Rebrand KPI Light → KPI Dashboard Verification Report

**Phase Goal:** Rename the user-facing display name "KPI Light" → "KPI Dashboard" across UI, docs, and defaults while preserving every internal identifier (Dex client id, env var names, session cookie, hostname, compose service names, image names, repo directory).
**Verified:** 2026-04-15
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Display name "KPI Dashboard" appears in all primary user-visible surfaces (UI titles, README, docs, defaults) | VERIFIED | README.md:1, .planning/PROJECT.md:1, docs/setup.md:1, backend/app/main.py:16 (FastAPI title), backend/app/defaults.py:20, frontend/src/lib/defaults.ts:10, frontend/src/locales/{en,de}.json:28 (nav.brand) all read "KPI Dashboard" |
| 2 | No user-visible "KPI Light" remains in runtime service configuration | FAILED | dex/config.yaml:57 still shows `name: "KPI Light"` — rendered on the Dex login/consent screen. Phase-level grep in 30.1-SUMMARY.md excluded `dex/` from its search path, so the gap was not caught. |
| 3 | Internal identifiers fully preserved | VERIFIED | Dex client id `kpi-light` unchanged (dex/config.yaml:55, backend/app/config.py); env var `DEX_KPI_SECRET` unchanged (.env.example, .env, docker-compose.yml); session cookie `kpi_session` unchanged; hostname `kpi.internal` unchanged; compose services (api, frontend, db, migrate, npm, dex, outline, outline-db, outline-redis) unchanged; image-prefix `acm-kpi-light-*` unchanged (docker compose ps output confirms) |
| 4 | Alembic migration `v1_11_rename_app_name` applied and DB reflects new name | VERIFIED | `docker compose exec db psql … -c "SELECT app_name FROM app_settings;"` returns "KPI Dashboard" (1 row); `SELECT version_num FROM alembic_version` returns `v1_11_rename_app_name`; migration upgrade/downgrade SQL is narrow (WHERE app_name = old value) and symmetric |
| 5 | `docker compose config --quiet` succeeds | VERIFIED | Exit code 0 |
| 6 | Phase 31 fix-up applied (forward-looking planning artifacts reference "KPI Dashboard collection") | FAILED (partial) | 31-CONTEXT.md, 31-01-PLAN.md, 31-05-PLAN.md, 31-06-PLAN.md rebranded; 31-RESEARCH.md documented as deliberate out-of-scope in 30.1-03-SUMMARY.md line 101; Phase 31 *detail block* in ROADMAP.md line 242 rebranded. HOWEVER: ROADMAP.md line 133 (Phase 31 one-line summary checkbox) still reads "KPI Light documentation collection" — missed edit. |
| 7 | Historical `.planning/` artifacts and shipped-milestone rows not touched | VERIFIED | git log 2026-04-15 shows only the 19 commits for Phase 30.1 (roadmap insertion, context, plans, impl, fix-ups). No edits to .planning/phases/{1..30}/** or REQUIREMENTS.md or v1.0–v1.10 details blocks. Exceptions are explicit and justified: PROJECT.md (living doc, D-02 → D-05 exception with rename-note sentence) and ROADMAP Phase 31 (unshipped, legitimate in-flight edit). |

**Score:** 5/7 truths verified (Truths 2 and 6 partial failures).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| backend/app/defaults.py | `DEFAULT_SETTINGS["app_name"] = "KPI Dashboard"` | VERIFIED | Line 20 |
| frontend/src/lib/defaults.ts | `app_name: "KPI Dashboard"` | VERIFIED | Line 10 |
| frontend/src/locales/en.json | `nav.brand: "KPI Dashboard"` | VERIFIED | Line 28 (and settings.identity.app_name.placeholder line 135) |
| frontend/src/locales/de.json | `nav.brand: "KPI Dashboard"` | VERIFIED | Line 28 (and placeholder line 135) |
| backend/app/main.py | FastAPI `title="KPI Dashboard"` | VERIFIED | Line 16 |
| backend/alembic/versions/v1_11_rename_app_name.py | Chained from v1_11_app_users; narrow WHERE; symmetric up/down | VERIFIED | Full file read; down_revision = v1_11_app_users; both upgrade and downgrade use WHERE app_name = <old> pattern |
| README.md | Title "# KPI Dashboard" | VERIFIED | Line 1 |
| .planning/PROJECT.md | Title + rename-note sentence present | VERIFIED | Line 1 title, line 9 rename-note ("Formerly called KPI Light (renamed 2026-04-15, Phase 30.1).") |
| docs/setup.md | Title "# KPI Dashboard — Setup Runbook" | VERIFIED | Line 1 |
| CLAUDE.md | Project name "KPI Dashboard" | VERIFIED | Line 4 |
| dex/config.yaml | Client id `kpi-light` preserved; consistent display name | FAILED | Line 55 id `kpi-light` correctly preserved; line 57 `name: "KPI Light"` is user-visible and should read "KPI Dashboard" |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| App runtime (`GET /api/settings` → frontend NavBar) | DB `app_settings.app_name` | SQLAlchemy read of app_settings row | WIRED + FLOWING | Migration v1_11_rename_app_name actually applied; DB row reads "KPI Dashboard"; frontend locale fallback and defaults also read "KPI Dashboard" |
| Alembic migration chain | previous head | down_revision = v1_11_app_users | WIRED | No duplicate heads (only v1_11_rename_app_name.py at tip; alembic_version points at it) |
| Seed default (first-install) | DEFAULT_SETTINGS | Backend defaults.py → migrate service → Alembic seed | WIRED | defaults.py uses new value; historical seed migration b2c3d4e5f6a7 still writes "KPI Light" (expected — migration snapshots are immutable) and the follow-up v1_11_rename_app_name correctly overwrites it for any freshly-seeded DB |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| NavBar / browser tab title | `app_name` settings field | DB `app_settings` table, migrated to "KPI Dashboard" via v1_11_rename_app_name | Yes (live DB query returns "KPI Dashboard") | FLOWING |
| FastAPI OpenAPI UI | hardcoded `title="KPI Dashboard"` | backend/app/main.py:16 | Yes (literal value) | FLOWING |
| Dex login/consent screen | `staticClients[0].name` in dex/config.yaml | Literal string | Yes — but still "KPI Light" | STATIC / incorrect value |

### Requirements Coverage

Phase 30.1 is an **inserted mechanical phase** with empty requirements array per CONTEXT ("phase inserts no new requirements; it realigns artifacts produced under v1.0–v1.11"). No requirement IDs to verify. ROADMAP Phase 30.1 entry has no `Requirements:` line — confirmed consistent.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| dex/config.yaml | 57 | Inconsistent display name left over | Warning | Dex login screen still greets the operator with "KPI Light" — partially defeats the rebrand goal in one user-visible surface |
| .planning/ROADMAP.md | 133 | Inconsistent with line 242 wording | Info | Internal planning-doc drift; no runtime impact, but next human reading the ROADMAP gets mixed signal about the Phase 31 collection name |

Expected-and-OK "KPI Light" occurrences (not gaps):
- `backend/alembic/versions/b2c3d4e5f6a7_v1_1_app_settings.py:27` — historical seed (migration snapshots are immutable; corrected by later migration)
- `backend/alembic/versions/v1_11_rename_app_name.py:21,30` — intentional WHERE / downgrade target
- `scripts/verify-phase-04.sh:84` — historical phase-04 verification fixture frozen at v1.0 state
- `.planning/phases/{1..30}/**`, `.planning/REQUIREMENTS.md`, ROADMAP v1.0–v1.10 `<details>` blocks — historical, frozen per D-02
- `.planning/phases/31-seed-outline-docs/31-RESEARCH.md` — intentional out-of-scope, documented in 30.1-03-SUMMARY.md line 101
- ROADMAP Phase 30.1 heading "### Phase 30.1: Rebrand KPI Light to KPI Dashboard (INSERTED)" — intentional self-reference
- `.planning/PROJECT.md` rename-note sentence + trailer — intentional (D-02 → D-05 living-doc exception)
- Shipped v1.11 phase summary rows (Phase 28/30 entries referencing "KPI Light OIDC Integration" / "KPI Light NavBar") at lines 128, 130, 132 — shipped-milestone historical rows, frozen per D-02

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| DB app_settings.app_name returns "KPI Dashboard" | `docker compose exec -T db psql -U acm_user -d acm_kpi -c "SELECT app_name FROM app_settings;"` | `KPI Dashboard` (1 row) | PASS |
| Alembic head applied | `docker compose exec -T db psql ... -c "SELECT version_num FROM alembic_version;"` | `v1_11_rename_app_name` | PASS |
| docker compose config valid | `docker compose config --quiet` | exit 0 | PASS |
| All v1.11 services healthy | `docker compose ps` | api, db, dex, frontend, npm, outline, outline-db, outline-redis all "healthy" | PASS |

### Human Verification Required

Recommended once the two gaps are closed:

1. **Dex consent screen copy check** — Hit `https://kpi.internal` in a logged-out browser; get redirected through Dex. Expected: the app-name field on the Dex login page reads "KPI Dashboard" (after fixing dex/config.yaml:57). Why human: visual inspection of third-party UI; not grep-able from the repo.
2. **NavBar brand in both locales** — Load `https://kpi.internal` in EN and DE, confirm NavBar shows "KPI Dashboard" in both. Why human: visual rendering, locale switch.
3. **Browser tab title** — Expected: "KPI Dashboard" (from app_name settings flowing into `<title>`). Why human: visual.

### Gaps Summary

Two tightly-scoped misses:

1. **Dex staticClient display name** (`dex/config.yaml:57`) — the `name` field rendered on the Dex login/consent screen was treated as part of the "keep-everything-internal" bucket during planning (plan 30.1-03 listed the Dex *client id* as preserved, but the separate user-visible `name` field was not called out either way and was missed by the dex-excluding grep in 30.1-SUMMARY.md). One-line fix + `docker compose restart dex`.

2. **ROADMAP.md line 133** — Phase 31 summary row was not updated when the parallel detail block at line 242 was rebranded. Because Phase 31 is still IN PROGRESS (checkbox unchecked) this is not a D-02 historical-freeze violation. One-line fix.

Neither gap affects the database migration (which is correct and applied), the compose definition, or any shipped milestone. Both can be closed in a single small follow-up plan.

---

_Verified: 2026-04-15_
_Verifier: Claude (gsd-verifier)_
