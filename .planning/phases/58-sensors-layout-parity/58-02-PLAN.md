---
phase: 58-sensors-layout-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 58-01
files_modified:
  - frontend/src/components/SubHeader.tsx
  - frontend/src/pages/SensorsPage.tsx
autonomous: true
requirements:
  - SENSORS-01
  - SENSORS-02
  - SENSORS-03
must_haves:
  truths:
    - "On /sensors, the SubHeader left slot renders SensorTimeWindowPicker."
    - "On /sensors, the SubHeader right slot renders PollNowButton (size=sm) immediately before SensorFreshnessIndicator."
    - "On any other route, SensorTimeWindowPicker and PollNowButton are NOT rendered in the SubHeader."
    - "/sensors page body contains only SensorStatusCards and SensorTimeSeriesChart — no inline header-level controls."
    - "Changing the time window in the SubHeader re-renders SensorStatusCards and SensorTimeSeriesChart via the hoisted context."
  artifacts:
    - path: "frontend/src/components/SubHeader.tsx"
      provides: "per-route /sensors slot wiring"
      contains: "SensorTimeWindowPicker"
    - path: "frontend/src/components/SubHeader.tsx"
      provides: "per-route /sensors slot wiring"
      contains: "PollNowButton"
    - path: "frontend/src/pages/SensorsPage.tsx"
      provides: "stripped sensors body"
      contains: "SensorStatusCards"
  key_links:
    - from: "frontend/src/components/SubHeader.tsx"
      to: "frontend/src/components/sensors/SensorTimeWindow.tsx"
      via: "SensorTimeWindowPicker import + conditional render on location === /sensors"
      pattern: "location === \"/sensors\" && <SensorTimeWindowPicker"
    - from: "frontend/src/components/SubHeader.tsx"
      to: "frontend/src/components/sensors/PollNowButton.tsx"
      via: "PollNowButton size=sm conditional render before freshness ternary"
      pattern: "location === \"/sensors\" && <PollNowButton size=\"sm\""
    - from: "frontend/src/pages/SensorsPage.tsx"
      to: "frontend/src/components/sensors/SensorTimeWindow.tsx"
      via: "Provider removed from page — now consumed from hoisted context"
      pattern: "^((?!SensorTimeWindowProvider).)*$"
---

<objective>
Wire the hoisted provider + extended PollNowButton from Plan 58-01 into the actual UI: extend SubHeader with a `/sensors`-scoped left-slot block (`SensorTimeWindowPicker`) and a right-slot prepend (`PollNowButton size="sm"`), then strip SensorsPage down to KPI cards + chart only. After this plan, `/sensors` matches `/sales`'s chrome/body split.

Purpose: Directly implements SENSORS-01 (picker in SubHeader left slot), SENSORS-02 (PollNow in SubHeader right slot using shared Button primitive), SENSORS-03 (page body has no header-level controls). Per D-03/D-04/D-05/D-06.

Output: SubHeader.tsx with two new conditional blocks gated on `location === "/sensors"`; SensorsPage.tsx reduced to the stripped form from UI-SPEC.md §Layout Contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-sensors-layout-parity/58-CONTEXT.md
@.planning/phases/58-sensors-layout-parity/58-RESEARCH.md
@.planning/phases/58-sensors-layout-parity/58-UI-SPEC.md
@.planning/phases/58-sensors-layout-parity/58-01-SUMMARY.md
@frontend/src/components/SubHeader.tsx
@frontend/src/pages/SensorsPage.tsx
@frontend/src/components/sensors/SensorTimeWindow.tsx
@frontend/src/components/sensors/PollNowButton.tsx

<interfaces>
<!-- Upstream contracts (from Plan 58-01) -->

From `frontend/src/components/sensors/PollNowButton.tsx` (Plan 58-01 output):
```tsx
interface PollNowButtonProps { size?: "default" | "sm" }
export function PollNowButton({ size = "default" }?: PollNowButtonProps): JSX.Element;
```

From `frontend/src/components/sensors/SensorTimeWindow.tsx` (unchanged):
```tsx
export function SensorTimeWindowPicker(): JSX.Element;
```

From `frontend/src/App.tsx` (Plan 58-01 output): `SensorTimeWindowProvider` now wraps AppShell → both SubHeader and SensorsPage descendants can call `useSensorWindow()` without a local provider.

Existing SubHeader slot structure (DO NOT restructure — only add conditional lines):
```tsx
// left slot: <div className="flex items-center gap-3"> ... </div>
//   contains: isDashboard Toggle, location === "/sales" DateRangeFilter
// right slot: <div className="flex items-center gap-3"> ... </div>
//   contains: isDashboard AdminOnly Upload Link, then a ternary on location for freshness indicator
```
</interfaces>

<i18n_keys>
<!-- No new keys. Existing keys used by the components being wired in: -->
- `sensors.window.aria`, `sensors.window.{1h,6h,24h,7d,30d}` — owned by SensorTimeWindowPicker
- `sensors.poll.now`, `sensors.poll.refreshing`, `sensors.poll.success`, `sensors.poll.failure`, `sensors.poll.timeout` — owned by PollNowButton
</i18n_keys>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire /sensors slot blocks into SubHeader</name>
  <files>frontend/src/components/SubHeader.tsx</files>
  <read_first>
    - frontend/src/components/SubHeader.tsx (current slot structure — preserve isDashboard Toggle + /sales DateRangeFilter + right-slot freshness ternary)
    - frontend/src/components/sensors/SensorTimeWindow.tsx (export name: `SensorTimeWindowPicker`)
    - frontend/src/components/sensors/PollNowButton.tsx (Plan 58-01 output — accepts size="sm")
    - .planning/phases/58-sensors-layout-parity/58-RESEARCH.md §"Example 1" and §"Example 2"
    - .planning/phases/58-sensors-layout-parity/58-UI-SPEC.md §"Layout Contract"
  </read_first>
  <action>
Edit `frontend/src/components/SubHeader.tsx`:

1. Add two named imports (alongside the existing imports at the top of the file):
   ```tsx
   import { SensorTimeWindowPicker } from "@/components/sensors/SensorTimeWindow";
   import { PollNowButton } from "@/components/sensors/PollNowButton";
   ```

2. **Left slot** (inside the first `<div className="flex items-center gap-3">` that contains the `isDashboard` Toggle and the `/sales` DateRangeFilter) — add a new conditional block **after** the existing `{location === "/sales" && ( <DateRangeFilter ... /> )}` block and **before** the closing `</div>` of the left slot:

   ```tsx
   {location === "/sensors" && <SensorTimeWindowPicker />}
   ```

   No wrapping div, no divider (Pitfall 5). Single child.

3. **Right slot** (inside the second `<div className="flex items-center gap-3">` that contains the AdminOnly Upload link + the freshness ternary) — insert a new conditional block **immediately before** the existing ternary `{location === "/sensors" ? (<SensorFreshnessIndicator />) : location === "/hr" ? ...`:

   ```tsx
   {location === "/sensors" && <PollNowButton size="sm" />}
   ```

   This guarantees right-slot order: `PollNowButton` → `SensorFreshnessIndicator` (per D-04). Do NOT move or modify the existing freshness ternary.

4. Do NOT modify the `isDashboard` Toggle, the `/sales` DateRangeFilter block, the AdminOnly Upload link, the SensorFreshnessIndicator / HrFreshnessIndicator / FreshnessIndicator components, the top-level chrome `<div className="fixed top-16 ...">`, or the launcher-hide early return.

5. Per-route visibility (D-05): both new blocks are gated strictly on `location === "/sensors"` — they MUST NOT render on any other route.

Implements SENSORS-01 + SENSORS-02.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -n "SensorTimeWindowPicker\\|PollNowButton" src/components/SubHeader.tsx</automated>
  </verify>
  <done>
TypeScript compiles. SubHeader.tsx imports `SensorTimeWindowPicker` and `PollNowButton` from their canonical paths. The left slot contains `{location === "/sensors" && <SensorTimeWindowPicker />}` after the `/sales` DateRangeFilter block. The right slot contains `{location === "/sensors" && <PollNowButton size="sm" />}` immediately before the freshness-indicator ternary. No existing slot content was moved.
  </done>
  <acceptance_criteria>
    - `grep -qE "^import \\{ SensorTimeWindowPicker \\} from \"@/components/sensors/SensorTimeWindow\";$" frontend/src/components/SubHeader.tsx` exits 0
    - `grep -qE "^import \\{ PollNowButton \\} from \"@/components/sensors/PollNowButton\";$" frontend/src/components/SubHeader.tsx` exits 0
    - `grep -qE "location === \"/sensors\" && <SensorTimeWindowPicker />" frontend/src/components/SubHeader.tsx` exits 0
    - `grep -qE "location === \"/sensors\" && <PollNowButton size=\"sm\" />" frontend/src/components/SubHeader.tsx` exits 0
    - PollNowButton appears before the freshness ternary: `awk '/PollNowButton size="sm"/{p=NR} /SensorFreshnessIndicator/{if(p && NR>p){print "ok"; exit 0}} END{exit 1}' frontend/src/components/SubHeader.tsx | grep -q ok`
    - SensorTimeWindowPicker appears after the /sales DateRangeFilter block: `awk '/DateRangeFilter/{p=NR} /SensorTimeWindowPicker/{if(p && NR>p){print "ok"; exit 0}} END{exit 1}' frontend/src/components/SubHeader.tsx | grep -q ok`
    - Existing chrome preserved: `grep -q "fixed top-16 inset-x-0 h-12 bg-background z-40 shadow-sm" frontend/src/components/SubHeader.tsx` exits 0
    - Existing `/sales` filter still present: `grep -q "location === \"/sales\" &&" frontend/src/components/SubHeader.tsx` exits 0
    - Existing freshness ternary still present: `grep -q "SensorFreshnessIndicator" frontend/src/components/SubHeader.tsx && grep -q "HrFreshnessIndicator" frontend/src/components/SubHeader.tsx && grep -q "<FreshnessIndicator />" frontend/src/components/SubHeader.tsx` exits 0
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Strip SensorsPage to cards + chart only</name>
  <files>frontend/src/pages/SensorsPage.tsx</files>
  <read_first>
    - frontend/src/pages/SensorsPage.tsx (current body with provider wrapper + control bar)
    - .planning/phases/58-sensors-layout-parity/58-RESEARCH.md §"Example 4: SensorsPage after migration" and §"Pitfall 2" (do not leave a nested provider)
    - .planning/phases/58-sensors-layout-parity/58-UI-SPEC.md §"Layout Contract — SensorsPage body"
  </read_first>
  <action>
Replace the entire contents of `frontend/src/pages/SensorsPage.tsx` with the stripped version below. Preserve `pt-4` (Claude's discretion choice: keep — matches /sales and /hr body rhythm per UI-SPEC default, D-07 resolution Pitfall 7).

```tsx
import { SensorStatusCards } from "@/components/sensors/SensorStatusCards";
import { SensorTimeSeriesChart } from "@/components/sensors/SensorTimeSeriesChart";

/**
 * SensorsPage — Phase 58 chrome-parity shell.
 *
 * Time-window picker (SensorTimeWindowPicker) and poll action (PollNowButton)
 * live in the SubHeader on /sensors (Phase 58 D-03/D-04). SensorTimeWindowProvider
 * is hoisted to App.tsx (Phase 58 D-01) so both chrome and body read the same
 * useSensorWindow() context. This page body is now pure data presentation.
 */
export function SensorsPage() {
  return (
    <div className="max-w-7xl mx-auto px-6 pt-4 pb-8 space-y-8">
      <SensorStatusCards />
      <SensorTimeSeriesChart />
    </div>
  );
}
```

Deletions compared to the old file:
- `import { SensorTimeWindowProvider, SensorTimeWindowPicker } from ...` — both removed (provider hoisted to App.tsx; picker now owned by SubHeader).
- `import { PollNowButton } from ...` — removed (now owned by SubHeader).
- `<SensorTimeWindowProvider>` wrapper — removed (Pitfall 2: DO NOT leave a nested provider).
- `<div className="flex items-center justify-between gap-4 flex-wrap">` control bar + its two children (`<PollNowButton />`, `<SensorTimeWindowPicker />`) — removed (D-06, SENSORS-03).

Implements SENSORS-03 and the consumer side of D-01 (removal of local provider).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && ! grep -qE "SensorTimeWindowProvider|PollNowButton|SensorTimeWindowPicker|flex items-center justify-between" src/pages/SensorsPage.tsx && grep -q "SensorStatusCards" src/pages/SensorsPage.tsx && grep -q "SensorTimeSeriesChart" src/pages/SensorsPage.tsx</automated>
  </verify>
  <done>
TypeScript compiles. SensorsPage.tsx no longer imports or references `SensorTimeWindowProvider`, `SensorTimeWindowPicker`, or `PollNowButton`. The page body contains only `<SensorStatusCards />` + `<SensorTimeSeriesChart />` inside the `max-w-7xl mx-auto px-6 pt-4 pb-8 space-y-8` wrapper. No flex control-bar row remains.
  </done>
  <acceptance_criteria>
    - `! grep -q "SensorTimeWindowProvider" frontend/src/pages/SensorsPage.tsx` (exits 0 — absent)
    - `! grep -q "SensorTimeWindowPicker" frontend/src/pages/SensorsPage.tsx` (exits 0 — absent)
    - `! grep -q "PollNowButton" frontend/src/pages/SensorsPage.tsx` (exits 0 — absent)
    - `! grep -q "flex items-center justify-between" frontend/src/pages/SensorsPage.tsx` (exits 0 — control bar gone)
    - `grep -q "<SensorStatusCards />" frontend/src/pages/SensorsPage.tsx` exits 0
    - `grep -q "<SensorTimeSeriesChart />" frontend/src/pages/SensorsPage.tsx` exits 0
    - `grep -q "max-w-7xl mx-auto px-6 pt-4 pb-8 space-y-8" frontend/src/pages/SensorsPage.tsx` exits 0 (pt-4 preserved)
    - File line count: `wc -l < frontend/src/pages/SensorsPage.tsx` returns a number <= 20 (stripped shell)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0 (full bundle builds — no dangling import errors)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Goal-backward check — all five truths from `must_haves.truths`:

1. Left slot on /sensors — `grep -qE "location === \"/sensors\" && <SensorTimeWindowPicker />" frontend/src/components/SubHeader.tsx`.
2. Right slot on /sensors (PollNowButton size=sm before freshness) — `grep -qE "location === \"/sensors\" && <PollNowButton size=\"sm\" />" frontend/src/components/SubHeader.tsx` AND PollNowButton line precedes SensorFreshnessIndicator line.
3. Visibility gated on /sensors — both blocks are inside `{location === "/sensors" && ...}`; no other routes render these components (grep for SensorTimeWindowPicker / PollNowButton in SubHeader returns only these two conditional lines).
4. /sensors page body minimal — SensorsPage.tsx contains only the two data components after the strip.
5. Window-change reactivity — structurally guaranteed by Plan 58-01 (provider hoisted to App.tsx); both SubHeader's `SensorTimeWindowPicker` and the cards/chart inside `SensorsPage` now consume the same context.

Run from `frontend/`:
- `npx tsc --noEmit`
- `npm run build`
- `npm run check:locale-parity` (must pass — zero new keys added)
- Manual smoke (captured in SUMMARY): visit `/sensors`, change window in SubHeader → cards + chart refetch; click PollNow → icon swaps to spinning Loader2, disabled, success toast fires; visit `/sales` → no SensorTimeWindowPicker + no PollNowButton in the SubHeader.
</verification>

<success_criteria>
- All Task 1 + Task 2 acceptance criteria pass.
- `cd frontend && npm run build` exits 0.
- `cd frontend && npm run check:locale-parity` exits 0 (zero new i18n keys introduced — both EN and DE counts unchanged from Plan 57 baseline of 479 keys).
- Visual inspection (SUMMARY captures): `/sensors` SubHeader shows `[SensorTimeWindowPicker]` left, `[PollNowButton(sm)] [SensorFreshnessIndicator]` right. Page body shows only cards + chart — no inline header-level controls. Non-`/sensors` routes unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/58-sensors-layout-parity/58-02-SUMMARY.md` documenting:
- Exact SubHeader diff regions (left + right slot insertions)
- SensorsPage before/after line-count and JSX shape
- Confirmation that no new i18n keys were added (parity check output)
- Manual smoke notes: window change → refetch; poll click → spinner + toast; other routes unaffected
- Closure of SENSORS-01 / SENSORS-02 / SENSORS-03
</output>
