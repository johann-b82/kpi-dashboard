---
phase: 65-foundation-schema-authz-sse-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - directus/snapshots/v1.22.yaml
  - docs/operator-runbook.md
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-04, SCHEMA-05]
must_haves:
  truths:
    - "Fresh `docker compose down -v && up -d` registers 9 v1.22 Directus collections idempotently via `directus-schema-apply` service"
    - "`directus-schema-apply` runs after `directus` is healthy and completes before `directus-bootstrap-roles`"
    - "`DB_EXCLUDE_TABLES` shrunk to minimal set; contains only admin/Personio-raw/sensors/pairing/heartbeat"
    - "Operator runbook documents the 'never edit Data Model UI' rule + #25760 fallback recipe"
  artifacts:
    - path: "directus/snapshots/v1.22.yaml"
      provides: "9-collection Directus snapshot (collections + fields + relations only; no policies/roles)"
      contains: "signage_devices"
    - path: "docker-compose.yml"
      provides: "New `directus-schema-apply` one-shot service + depends_on chain rewire"
      contains: "directus-schema-apply"
    - path: "docs/operator-runbook.md"
      provides: "Data Model UI prohibition + #25760 fallback + DB_EXCLUDE_TABLES up -d note"
      contains: "never edit"
  key_links:
    - from: "docker-compose.yml:directus-bootstrap-roles.depends_on"
      to: "directus-schema-apply"
      via: "condition: service_completed_successfully"
      pattern: "directus-schema-apply.*service_completed_successfully"
    - from: "docker-compose.yml:api.depends_on"
      to: "directus-bootstrap-roles"
      via: "condition: service_completed_successfully"
      pattern: "directus-bootstrap-roles.*service_completed_successfully"
---

<objective>
Bootstrap the Directus schema-apply compose chain and the v1.22 snapshot YAML. Shrink `DB_EXCLUDE_TABLES` to minimal superset. Document operator constraints.

Purpose: Phase 65's deliverable #1. Alembic owns DDL; Directus learns about the tables as metadata. Snapshot YAML is git-checked; compose service re-applies idempotently on every `up -d`.

Output: docker-compose service chain works on fresh volume; 9 collections visible in Directus admin UI post `up -d`; runbook prevents silent drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-foundation-schema-authz-sse-bridge/65-CONTEXT.md
@.planning/phases/65-foundation-schema-authz-sse-bridge/65-RESEARCH.md
@.planning/REQUIREMENTS.md
@docker-compose.yml
@directus/bootstrap-roles.sh

<interfaces>
v1.22 collections to register (9 total):
- signage_devices, signage_playlists, signage_playlist_items
- signage_tags (table name is signage_device_tags per v1_16_signage_schema.py — verify)
- signage_playlist_tag_map, signage_device_tag_map, signage_schedules
- sales_records, personio_employees

`DB_EXCLUDE_TABLES` MUST stay a superset of hard-coded "never expose" list:
alembic_version, app_settings, personio_attendance, personio_absences, personio_sync_meta, sensors, sensor_readings, sensor_poll_log, signage_pairing_sessions, signage_heartbeat_event, upload_batches
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate v1.22 snapshot YAML (collections + fields + relations only)</name>
  <files>directus/snapshots/v1.22.yaml</files>
  <read_first>
    - backend/alembic/versions/v1_16_signage_schema.py (confirm exact table names — especially `signage_device_tags` vs `signage_tags`)
    - backend/alembic/versions/v1_16_signage_devices_etag.py
    - backend/alembic/versions/v1_18_signage_schedules.py
    - backend/alembic/versions/v1_21_signage_calibration.py
    - backend/app/models/signage.py (column types for fidelity)
    - .planning/phases/65-foundation-schema-authz-sse-bridge/65-RESEARCH.md Pitfall 1 (#25760 mitigation)
    - .planning/phases/65-foundation-schema-authz-sse-bridge/65-CONTEXT.md D-01, D-02
  </read_first>
  <action>
    Generate `directus/snapshots/v1.22.yaml` registering the 9 v1.22 collections as Directus metadata (schema: null per collection to avoid #25760 re-CREATE).

    Recommended generation path (per RESEARCH Pitfall 1): run `docker compose up -d` on a clean dev branch, then manually POST `/collections {schema: null, meta: {note: ..., icon: ...}}` for each target collection via curl + admin token, then run `npx directus schema snapshot` against the container to produce the YAML. Commit the result.

    YAML structure (per D-02 — NO policies/roles/permissions/settings):
    ```yaml
    version: 1
    directus: 11.17.2
    vendor: postgres
    collections:
      - collection: signage_devices
        meta:
          collection: signage_devices
          note: "Signage device metadata (calibration cols excluded from SSE trigger WHEN clause)"
          hidden: false
        schema: null
      # ... one entry per: signage_playlists, signage_playlist_items,
      # signage_device_tags, signage_playlist_tag_map, signage_device_tag_map,
      # signage_schedules, sales_records, personio_employees
    fields:
      - collection: signage_devices
        field: id
        type: uuid
        meta: {...}
        schema:
          data_type: uuid
          is_nullable: false
      # ... every column on every collection. Field types MUST match the Alembic DDL.
    relations:
      - collection: signage_playlist_items
        field: playlist_id
        related_collection: signage_playlists
        meta: {...}
        schema: {on_delete: CASCADE}
      # ... include every FK currently defined in v1_16_signage_schema.py + v1_18_signage_schedules.py
    ```

    CRITICAL: `schema: null` per collection. DO NOT include `schema: {...}` with `name`, otherwise `directus schema apply` tries to CREATE TABLE and hits #25760.

    DO NOT include: `directus_roles`, `directus_policies`, `directus_permissions`, `directus_settings` — those stay in `bootstrap-roles.sh`.
  </action>
  <verify>
    <automated>test -f directus/snapshots/v1.22.yaml &amp;&amp; [ "$(grep -c '^  - collection:' directus/snapshots/v1.22.yaml)" = "9" ] &amp;&amp; [ "$(grep -c 'schema: null' directus/snapshots/v1.22.yaml)" -ge 9 ] &amp;&amp; ! grep -E '^\s*schema:\s*\{' directus/snapshots/v1.22.yaml</automated>
  </verify>
  <acceptance_criteria>
    - File `directus/snapshots/v1.22.yaml` exists
    - Contains exactly 9 `- collection:` entries in `collections:` block
    - Every collection has `schema: null` (grep -c "schema: null" >= 9)
    - No `schema: {` inline block under any collection (would trigger #25760 CREATE TABLE)
    - No `permissions:`, `roles:`, `policies:`, `settings:` top-level keys (grep returns 0 for each)
    - 9 collections listed: `signage_devices`, `signage_playlists`, `signage_playlist_items`, `signage_device_tags` (or `signage_tags` per v1_16 check), `signage_playlist_tag_map`, `signage_device_tag_map`, `signage_schedules`, `sales_records`, `personio_employees`
  </acceptance_criteria>
  <done>Snapshot YAML committed with 9 metadata-only collection entries; applies cleanly against existing Alembic-owned tables (verified by Task 2 compose run).</done>
</task>

<task type="auto">
  <name>Task 2: Add `directus-schema-apply` compose service + rewire depends_on chain</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (current state — existing `directus`, `directus-bootstrap-roles`, `migrate`, `api` services; `DB_EXCLUDE_TABLES` at current line; `--workers 1` invariant block)
    - .planning/phases/65-foundation-schema-authz-sse-bridge/65-RESEARCH.md Pattern 1 (compose chain) + Pitfall 4 (DB_EXCLUDE_TABLES up -d) + Pitfall 5 (permissions before collections)
    - .planning/phases/65-foundation-schema-authz-sse-bridge/65-CONTEXT.md D-03, D-04
  </read_first>
  <action>
    Edit `docker-compose.yml`:

    1) Insert new one-shot service `directus-schema-apply` (place immediately AFTER `directus` service block, BEFORE `directus-bootstrap-roles`):

    ```yaml
    directus-schema-apply:
      image: directus/directus:11.17.2
      env_file: .env
      environment:
        DB_CLIENT: pg
        DB_HOST: db
        DB_PORT: 5432
        DB_DATABASE: ${POSTGRES_DB}
        DB_USER: ${POSTGRES_USER}
        DB_PASSWORD: ${POSTGRES_PASSWORD}
        KEY: ${DIRECTUS_KEY}
        SECRET: ${DIRECTUS_SECRET}
      volumes:
        - ./directus/snapshots:/snapshots:ro
      depends_on:
        directus:
          condition: service_healthy
      entrypoint: ["/bin/sh", "-c", "npx directus schema apply --yes /snapshots/v1.22.yaml"]
      restart: "no"
    ```

    2) Rewire `directus-bootstrap-roles.depends_on` to chain on schema-apply:
    ```yaml
    directus-bootstrap-roles:
      depends_on:
        directus:
          condition: service_healthy
        directus-schema-apply:
          condition: service_completed_successfully
    ```

    3) Rewire `api` (or `backend`) service `depends_on` to also require `directus-bootstrap-roles` completion so field allowlists exist before first `/api/*` request (D-03):
    ```yaml
    api:
      depends_on:
        migrate:
          condition: service_completed_successfully
        directus-bootstrap-roles:
          condition: service_completed_successfully
    ```
    (Preserve any existing db healthcheck dep.)

    4) Shrink `DB_EXCLUDE_TABLES` env on the `directus` service. New value (comma-separated, exact list per SCHEMA-04 superset):
    ```
    DB_EXCLUDE_TABLES=alembic_version,app_settings,personio_attendance,personio_absences,personio_sync_meta,sensors,sensor_readings,sensor_poll_log,signage_pairing_sessions,signage_heartbeat_event,upload_batches
    ```
    This is the minimal "never expose" set. All other public tables (the 9 v1.22 collections + anything Alembic added since) become visible to Directus metadata.

    5) PRESERVE the `--workers 1` invariant block and its comment on the `api` uvicorn command line (per D-00c + SSE-05). Do NOT edit the uvicorn command.

    Using snapshot per D-01 (primary apply = CLI). Fallback documented in Task 3, not automated here.
  </action>
  <verify>
    <automated>grep -q "directus-schema-apply:" docker-compose.yml &amp;&amp; grep -q "npx directus schema apply --yes /snapshots/v1.22.yaml" docker-compose.yml &amp;&amp; grep -q "service_completed_successfully" docker-compose.yml &amp;&amp; grep -q "workers 1" docker-compose.yml &amp;&amp; docker compose config > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` has `directus-schema-apply:` service block
    - `entrypoint` contains `npx directus schema apply --yes /snapshots/v1.22.yaml`
    - `directus-bootstrap-roles.depends_on` includes `directus-schema-apply: condition: service_completed_successfully` (grep both on adjacent lines)
    - `api.depends_on` (or equivalent backend service name) includes `directus-bootstrap-roles: condition: service_completed_successfully`
    - `DB_EXCLUDE_TABLES` contains exactly these 11 entries in any order: alembic_version, app_settings, personio_attendance, personio_absences, personio_sync_meta, sensors, sensor_readings, sensor_poll_log, signage_pairing_sessions, signage_heartbeat_event, upload_batches
    - `DB_EXCLUDE_TABLES` does NOT contain any of: signage_devices, signage_playlists, signage_playlist_items, signage_tags, signage_device_tags, signage_schedules, signage_playlist_tag_map, signage_device_tag_map, sales_records, personio_employees
    - `grep -E "uvicorn.*--workers 1" docker-compose.yml` returns a match
    - `docker compose config` exits 0 (yaml is valid)
  </acceptance_criteria>
  <done>Compose chain: postgres → migrate → directus (healthy) → directus-schema-apply → directus-bootstrap-roles → api. `docker compose config` validates. DB_EXCLUDE_TABLES is the documented minimal set.</done>
</task>

<task type="auto">
  <name>Task 3: Write operator runbook section for Data Model UI prohibition + #25760 fallback + DB_EXCLUDE_TABLES note</name>
  <files>docs/operator-runbook.md</files>
  <read_first>
    - docs/operator-runbook.md (if exists — append; if not — create)
    - .planning/phases/65-foundation-schema-authz-sse-bridge/65-RESEARCH.md Pitfall 1 (#25760) + Pitfall 4 (DB_EXCLUDE_TABLES) + Pitfall 7 (missed events SLA)
    - .planning/phases/65-foundation-schema-authz-sse-bridge/65-CONTEXT.md "Claude's Discretion" (rollback recipe is planner's call)
  </read_first>
  <action>
    Create or append section(s) to `docs/operator-runbook.md`:

    Section: "Directus Data Model — DO NOT EDIT IN UI"
    - Explain: Alembic is sole DDL owner. Directus metadata (directus_collections/fields/relations) is drift-detected by CI (Guard B in plan 05). Any UI edit will be flagged as drift and block PRs.
    - Where to change schema: `backend/alembic/versions/*.py` + regenerate `directus/snapshots/v1.22.yaml` + run `make schema-fixture-update` (from plan 05).

    Section: "Recovery: directus-schema-apply fails with 'relation already exists' (#25760)"
    - Symptoms: `docker compose logs directus-schema-apply` shows `Collection already exists` or `relation "<name>" already exists`.
    - Cause: Directus 11.10+ regression; YAML's `schema` block on an existing-table edge.
    - Fallback (operator-run, per D-01):
      1. Get admin token: `curl -sX POST http://localhost:8055/auth/login -H 'Content-Type: application/json' -d '{"email":"$DIRECTUS_ADMIN_EMAIL","password":"$DIRECTUS_ADMIN_PASSWORD"}' | jq -r .data.access_token`
      2. For each failing collection, POST metadata-only: `curl -sX POST http://localhost:8055/collections -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' -d '{"collection":"<name>","schema":null,"meta":{"note":"..."}}'`
      3. Re-run `docker compose up -d directus-schema-apply` to confirm idempotent (no-op).

    Section: "Shrinking or changing `DB_EXCLUDE_TABLES` requires `up -d`, not `restart`"
    - `docker compose restart directus` does NOT pick up env changes. Use `docker compose up -d directus` to recreate the container with new env. (Pitfall 4.)

    Section: "SSE is best-effort; 30s poll is the durability floor"
    - The Postgres LISTEN/NOTIFY SSE bridge is at-most-once. During a DB restart / listener reconnect window (up to 30s backoff), events can be missed.
    - Players poll `/api/signage/player/playlist` every 30s as the durability backup. No event-log table.

    Section: "Rollback recipe — partial `directus-schema-apply` failure on fresh volume"
    - If apply fails mid-way on clean volume: `docker compose down -v && docker compose up -d db directus` to restart from scratch. Inspect `docker compose logs directus-schema-apply`. Apply happy-path fix to YAML, redeploy. If the failure is #25760, follow the fallback recipe above — per-collection REST POSTs are idempotent.
  </action>
  <verify>
    <automated>test -f docs/operator-runbook.md &amp;&amp; grep -q "DO NOT EDIT IN UI" docs/operator-runbook.md &amp;&amp; grep -q "25760" docs/operator-runbook.md &amp;&amp; grep -q "up -d directus" docs/operator-runbook.md &amp;&amp; grep -q "30s poll" docs/operator-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operator-runbook.md` exists
    - Contains heading / section mentioning "DO NOT EDIT" or "never edit" re: Directus Data Model UI
    - Contains `#25760` or `25760` reference with REST fallback recipe (mentions `POST /collections` and `schema:null`)
    - Contains `docker compose up -d directus` (not `restart`) guidance re: DB_EXCLUDE_TABLES
    - Contains "30s poll" or "30-second poll" SSE SLA note
    - Contains rollback recipe section referencing `docker compose down -v`
  </acceptance_criteria>
  <done>Runbook documents the UI prohibition (detected by SCHEMA-03 guards in plan 05), the #25760 operator-run fallback, the DB_EXCLUDE_TABLES recreation gotcha, the SSE at-most-once SLA, and rollback.</done>
</task>

</tasks>

<verification>
Phase-level checks after this plan:
- `docker compose config` validates.
- On a fresh volume: `docker compose down -v && docker compose up -d` brings the stack up through the new chain; `directus-schema-apply` exits 0; 9 collections appear in Directus admin UI.
- `docker compose exec directus npx directus schema snapshot -` output has zero diff against `directus/snapshots/v1.22.yaml` (this is formally asserted by Guard B in plan 05).
- `DB_EXCLUDE_TABLES` superset check: every entry in the hard-coded "never expose" allowlist (see CONTEXT.md cross-cutting hazard #10) appears in the env (formally asserted by guard in plan 05).
</verification>

<success_criteria>
- `directus/snapshots/v1.22.yaml` committed — 9 collections, metadata-only (schema:null).
- `docker-compose.yml` has new `directus-schema-apply` service + rewired depends_on chain.
- `DB_EXCLUDE_TABLES` shrunk to minimal superset (11 entries).
- `docs/operator-runbook.md` has UI prohibition + #25760 fallback + DB_EXCLUDE_TABLES note + SSE SLA + rollback.
- `--workers 1` uvicorn invariant preserved (not touched).
</success_criteria>

<output>
After completion, create `.planning/phases/65-foundation-schema-authz-sse-bridge/65-01-SUMMARY.md`.
</output>
