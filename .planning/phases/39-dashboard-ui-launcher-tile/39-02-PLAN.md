---
phase: 39-dashboard-ui-launcher-tile
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - frontend/src/components/sensors/SensorStatusCards.tsx
  - frontend/src/components/sensors/SensorTimeSeriesChart.tsx
  - frontend/src/components/sensors/PollNowButton.tsx
  - frontend/src/components/sensors/SensorHealthChip.tsx
  - frontend/src/components/sensors/sensorDelta.ts
  - frontend/src/pages/SensorsPage.tsx
  - frontend/src/lib/api.ts
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
autonomous: false
requirements:
  - SEN-FE-07
  - SEN-FE-10
  - SEN-FE-11
  - SEN-FE-12
  - SEN-I18N-01 (dashboard-scope completion — threshold/poll/status/delta keys)

must_haves:
  truths:
    - "Clicking Poll now blocks up to 30s, shows a success or failure toast, invalidates all sensorKeys → cards and charts refetch immediately with the newly-written row"
    - "Cards display threshold-aware value color: destructive token when current temp or humidity is outside the global [min, max] from app_settings"
    - "Cards display delta badges per DIFF-01: '+0.3 °C vs. 1h' and '−2 % vs. 24h' — computed client-side from the readings query data (no new API)"
    - "Cards display DIFF-10 health chip: 'OK seit Xh' (positive token) when online; 'Offline seit X min' (destructive) when offline flag is true"
    - "Empty-state card (latest reading null or error_code set) renders refined messaging; chart continues to respect connectNulls={false}"
    - "Threshold ReferenceLines render on both temperature and humidity charts when global thresholds are defined"
    - "All new visible strings ship in both en.json and de.json with DE 'du' tone — parity gate"
    - "Human-verify checkpoint at end confirms the full flow end-to-end"
  artifacts:
    - path: "frontend/src/components/sensors/PollNowButton.tsx"
      provides: "Blocking mutation + toast + cache invalidation"
      min_lines: 50
    - path: "frontend/src/components/sensors/SensorHealthChip.tsx"
      provides: "DIFF-10 health chip (OK seit / Offline seit)"
      min_lines: 40
    - path: "frontend/src/components/sensors/sensorDelta.ts"
      provides: "Pure delta computation from readings[] and target-offset hours"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/sensors/PollNowButton.tsx"
      to: "/api/sensors/poll-now"
      via: "useMutation + pollSensorsNow + queryClient.invalidateQueries({ queryKey: sensorKeys.all })"
      pattern: "invalidateQueries"
    - from: "frontend/src/components/sensors/SensorStatusCards.tsx"
      to: "threshold colors"
      via: "fetchSettings.sensor_temperature_min/max + sensor_humidity_min/max → destructive token when outside"
      pattern: "sensor_temperature_min|sensor_humidity_min"
    - from: "frontend/src/components/sensors/SensorStatusCards.tsx"
      to: "DIFF-01 delta badges"
      via: "computeSensorDelta(readings, 1) and computeSensorDelta(readings, 24) → DeltaBadgeStack-style render"
      pattern: "computeSensorDelta"
    - from: "frontend/src/components/sensors/SensorHealthChip.tsx"
      to: "/api/sensors/status"
      via: "useQuery(sensorKeys.status, fetchSensorStatus)"
      pattern: "fetchSensorStatus"
---

<objective>
Layer interactivity and differentiators on top of the 39-01 foundation: a blocking Poll-now button with toast + cache invalidation, threshold-aware card coloring and chart ReferenceLines fed by the global thresholds in `app_settings`, DIFF-01 client-side delta badges on each card (`vs. 1h` and `vs. 24h`), DIFF-10 health chip driven by `/api/sensors/status`, refined empty/error states, completion of the `sensors.*` dashboard-scope i18n keys, and a human-verify checkpoint that proves the full flow end-to-end.

Purpose: 39-01 delivered a working admin-only sensor dashboard. 39-02 makes it *useful*: a single Poll-now click refreshes everything within 30s, at-a-glance threshold breach visibility, trend deltas that match the Sales/HR pattern, and a health chip that surfaces silent sensor failures. This closes the Phase 39 acceptance criteria.
Output: PollNowButton + SensorHealthChip + sensorDelta util + threshold logic layered into SensorStatusCards/SensorTimeSeriesChart + 39-02 i18n keys + human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-dashboard-ui-launcher-tile/39-CONTEXT.md
@.planning/phases/39-dashboard-ui-launcher-tile/39-01-SUMMARY.md
@frontend/src/components/sensors/SensorStatusCards.tsx
@frontend/src/components/sensors/SensorTimeSeriesChart.tsx
@frontend/src/components/sensors/SensorTimeWindow.tsx
@frontend/src/pages/SensorsPage.tsx
@frontend/src/components/dashboard/DeltaBadgeStack.tsx
@frontend/src/components/dashboard/DeltaBadge.tsx
@frontend/src/lib/delta.ts
@frontend/src/lib/api.ts
@frontend/src/lib/queryKeys.ts
@frontend/src/locales/en.json
@frontend/src/locales/de.json
@backend/app/routers/sensors.py
@backend/app/schemas.py

<interfaces>
<!-- From 39-01 (already in lib/api.ts): -->
fetchSensors, fetchSensorReadings, fetchSensorStatus, pollSensorsNow
sensorKeys { all, list(), readings(id, hours), status() }
sensorPalette (hex exception in chartDefaults.ts)
SensorTimeWindow context + useSensorWindow + windowToHours

<!-- Existing settings types in lib/api.ts Settings interface — Settings DOES NOT currently include sensor_* threshold fields. -->
<!-- Two options for 39-02 threshold data source: -->
<!-- (a) Extend Settings interface + backend /api/settings response to include sensor_temperature_min/max, sensor_humidity_min/max (the columns exist from Phase 38-01 migration). -->
<!-- (b) Call GET /api/sensors with an added /thresholds endpoint — NOT preferred; Phase 38 didn't build it. -->
<!-- RECOMMENDED: (a) — minimal backend change in a single small task, matches existing Settings pattern. The fields already exist in the DB; the settings router just needs to read+return them. -->

<!-- Existing delta utility (reuse, don't reinvent): -->
From frontend/src/lib/delta.ts:
  export function computeDelta(current: number, baseline: number | null): number | null
From frontend/src/components/dashboard/DeltaBadge.tsx:
  <DeltaBadge value={delta} locale={"de" | "en"} noBaselineTooltip={string} />
From frontend/src/components/dashboard/DeltaBadgeStack.tsx:
  Pattern reference — don't reuse directly (shape differs) but mirror the layout.

<!-- SensorStatusEntry shape (reuse from 39-01 lib/api.ts): -->
{ sensor_id, last_attempt_at, last_success_at, consecutive_failures, offline }

<!-- toast — use project convention: -->
import { toast } from "sonner";   // already installed + mounted in App.tsx
</interfaces>

<design_rules>
D-01 (inherited from 39-01): All net calls through `apiClient<T>()`.
D-02 (inherited): No `dark:` variants; no hex literals outside `chartDefaults.ts sensorPalette`.
D-03 (inherited): DE/EN parity for every new key.
D-11 (new): Delta computation is CLIENT-SIDE — find the reading closest to `now - Xh` within a tolerance window (see sensorDelta.ts spec). NEVER add a backend delta endpoint for sensors (sales/HR do it server-side; sensors fetch raw readings already and the math is trivial).
D-12 (new): Threshold colors + ReferenceLines read from `app_settings` via the extended `/api/settings` response. If a threshold is `null`, the destructive color does NOT apply and the ReferenceLine is OMITTED (not drawn at 0).
D-13 (new): Poll-now mutation uses a 30s client-side timeout on the mutation promise (matches backend `asyncio.wait_for(timeout=30)`); on timeout, show failure toast with `sensors.poll.failure` message.
D-14 (new): Health chip colors come from Tailwind semantic tokens: `bg-primary/10 text-primary` for OK, `bg-destructive/10 text-destructive` for Offline. No hard-coded greens/reds.
</design_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings API extension + sensorDelta utility + 39-02 i18n keys</name>
  <files>
    backend/app/schemas.py
    backend/app/routers/settings.py
    frontend/src/lib/api.ts
    frontend/src/components/sensors/sensorDelta.ts
    frontend/src/components/sensors/sensorDelta.test.ts
    frontend/src/locales/en.json
    frontend/src/locales/de.json
  </files>
  <behavior>
    sensorDelta.ts — `computeSensorDelta(readings, targetOffsetHours)`:
    - Test 1: Returns null when readings is empty.
    - Test 2: Returns null when no reading exists older than (targetOffsetHours - 0.5h) from the latest reading.
    - Test 3: Given readings at t=0, t=-1h, t=-24h with temperature 22, 20, 18, `computeSensorDelta(readings, "temperature", 1)` returns 2.0.
    - Test 4: Given the same, `computeSensorDelta(readings, "temperature", 24)` returns 4.0.
    - Test 5: Returns null when the baseline reading at the target offset has null for that metric.
    - Test 6: Uses the CLOSEST reading to (latest - targetOffsetHours) within a ±0.5h tolerance window; returns null if nothing is within tolerance.
  </behavior>
  <action>
    (1) Backend — extend `/api/settings` response (read-only fields for now; admin UI writes them in Phase 40):
    - `backend/app/schemas.py`: Add four fields to the `Settings`/`SettingsRead` response model (find the class that shapes `GET /api/settings` response — likely `Settings` or `AppSettingsRead`): `sensor_temperature_min: Decimal | None`, `sensor_temperature_max: Decimal | None`, `sensor_humidity_min: Decimal | None`, `sensor_humidity_max: Decimal | None`. Also add `sensor_poll_interval_s: int` so SubHeader next-poll display is unblocked (deferred from 39-01 Task 2 SubHeader).
    - `backend/app/routers/settings.py`: Update the GET handler to include these fields in the response (they already exist on the AppSettings model from Phase 38-01 migration; just surface them). Do NOT add write support — that's Phase 40's scope.
    - Run the backend test suite to ensure no regression.

    (2) Frontend — extend `Settings` interface in `frontend/src/lib/api.ts`:
    ```typescript
    // in existing Settings interface, add:
    sensor_poll_interval_s: number;
    sensor_temperature_min: string | null;   // Decimal → string
    sensor_temperature_max: string | null;
    sensor_humidity_min: string | null;
    sensor_humidity_max: string | null;
    ```
    Do NOT extend `SettingsUpdatePayload` — write support is Phase 40.

    (3) Create `frontend/src/components/sensors/sensorDelta.ts`:
    ```typescript
    import type { SensorReadingRead } from "@/lib/api";

    export type SensorMetric = "temperature" | "humidity";

    /**
     * Compute delta between the latest reading and the reading closest to
     * (latest.recorded_at − offsetHours), within ±0.5h tolerance.
     * Returns null when: no readings, no latest reading of the metric, or no
     * baseline reading within tolerance.
     *
     * DIFF-01: client-side computation; no new API endpoint.
     * D-11: sales/HR compute deltas server-side because they're aggregates;
     * sensors are raw time-series, so client-side is both simpler and faster
     * (readings are already in the TanStack Query cache).
     */
    export function computeSensorDelta(
      readings: SensorReadingRead[],
      metric: SensorMetric,
      offsetHours: number,
    ): number | null {
      if (readings.length === 0) return null;
      const sorted = [...readings].sort(
        (a, b) => new Date(b.recorded_at).getTime() - new Date(a.recorded_at).getTime()
      );
      const latest = sorted[0];
      const latestVal = latest[metric];
      if (latestVal === null) return null;
      const latestMs = new Date(latest.recorded_at).getTime();
      const targetMs = latestMs - offsetHours * 3600_000;
      const toleranceMs = 0.5 * 3600_000;
      const baseline = sorted
        .slice(1)
        .map(r => ({ r, diff: Math.abs(new Date(r.recorded_at).getTime() - targetMs) }))
        .filter(x => x.diff <= toleranceMs)
        .sort((a, b) => a.diff - b.diff)[0]?.r;
      if (!baseline) return null;
      const baselineVal = baseline[metric];
      if (baselineVal === null) return null;
      return Number(latestVal) - Number(baselineVal);
    }
    ```

    (4) Create `frontend/src/components/sensors/sensorDelta.test.ts` covering the 6 behaviors above (vitest style — check existing test pattern in repo; if the project uses vitest, mirror; if it has no frontend tests, add one alongside the util and run via `npx vitest run sensorDelta` or the existing test runner). If no frontend test infra exists yet, do a LIGHTWEIGHT runtime check via a node script and document in the SUMMARY that test infra wiring is deferred.

    (5) Add 39-02 i18n keys to BOTH en.json and de.json (D-03 parity):
    ```
    sensors.threshold.outOfRange    EN: "Out of range"                DE: "Außerhalb des Bereichs"
    sensors.threshold.min           EN: "Min"                         DE: "Min"
    sensors.threshold.max           EN: "Max"                         DE: "Max"
    sensors.poll.now                EN: "Poll now"                    DE: "Jetzt messen"
    sensors.poll.refreshing         EN: "Polling..."                  DE: "Messung läuft..."
    sensors.poll.success            EN: "Polled {{count}} sensors"    DE: "{{count}} Sensoren gemessen"
    sensors.poll.failure            EN: "Poll failed — check sensor config"  DE: "Messung fehlgeschlagen — prüfe die Konfiguration"
    sensors.poll.timeout            EN: "Poll timed out after 30 s"   DE: "Messung nach 30 s abgebrochen"
    sensors.status.okSince          EN: "OK for {{duration}}"         DE: "OK seit {{duration}}"
    sensors.status.offlineSince     EN: "Offline for {{duration}}"    DE: "Offline seit {{duration}}"
    sensors.status.unknown          EN: "Status unknown"              DE: "Status unbekannt"
    sensors.delta.vsHour            EN: "vs. 1h"                      DE: "vs. 1 Std"
    sensors.delta.vsDay             EN: "vs. 24h"                     DE: "vs. 24 Std"
    sensors.delta.noBaseline        EN: "No baseline available"       DE: "Keine Vergleichsbasis verfügbar"
    ```
    Also verify the 39-01 `sensors.subheader.nextPoll` key is present in both locales (already added if 39-01 shipped per spec).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | tail -10 &amp;&amp; node -e "const en=require('./src/locales/en.json'); const de=require('./src/locales/de.json'); const enKeys=Object.keys(en).filter(k=&gt;k.startsWith('sensors.')); const miss=enKeys.filter(k=&gt;!(k in de)).concat(Object.keys(de).filter(k=&gt;k.startsWith('sensors.')&amp;&amp;!(k in en))); if(miss.length){console.error('PARITY FAIL:',miss);process.exit(1)}" &amp;&amp; cd ../backend &amp;&amp; docker compose exec -T api pytest tests/ -x -q 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `/api/settings` response includes `sensor_poll_interval_s` + 4 threshold fields (curl verified)
    - Frontend `Settings` interface extended; tsc passes
    - `computeSensorDelta` implemented with 6 test cases passing (or runtime-verified if test infra absent)
    - All 14 new i18n keys present in both en.json and de.json; parity check exits 0
    - Backend pytest suite still passes (no regressions from settings router change)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PollNowButton + threshold coloring + DIFF-01 delta badges + DIFF-10 health chip + ReferenceLines</name>
  <files>
    frontend/src/components/sensors/PollNowButton.tsx
    frontend/src/components/sensors/SensorHealthChip.tsx
    frontend/src/components/sensors/SensorStatusCards.tsx
    frontend/src/components/sensors/SensorTimeSeriesChart.tsx
    frontend/src/pages/SensorsPage.tsx
  </files>
  <behavior>
    PollNowButton:
    - Test 1: Renders `t("sensors.poll.now")` when idle.
    - Test 2: Disabled + shows `sensors.poll.refreshing` while mutation is in flight.
    - Test 3: On mutation success, calls `queryClient.invalidateQueries({ queryKey: sensorKeys.all })` exactly once and shows `toast.success(sensors.poll.success)`.
    - Test 4: On mutation failure, shows `toast.error(sensors.poll.failure)` and does NOT invalidate.
    - Test 5: On 30s timeout (AbortController or Promise.race with setTimeout), shows `sensors.poll.timeout` toast.

    SensorHealthChip:
    - Test 1: Given `offline=true` + `last_attempt_at`, renders `sensors.status.offlineSince` with computed duration string + destructive token classes.
    - Test 2: Given `offline=false` + `last_success_at`, renders `sensors.status.okSince` with primary token classes.
    - Test 3: Given null status, renders `sensors.status.unknown` in muted tokens.
  </behavior>
  <action>
    (1) `frontend/src/components/sensors/PollNowButton.tsx` — new component.
    - Use `useMutation` from `@tanstack/react-query`:
      ```typescript
      const queryClient = useQueryClient();
      const mutation = useMutation({
        mutationFn: pollSensorsNow,
        onSuccess: (res) => {
          queryClient.invalidateQueries({ queryKey: sensorKeys.all });
          toast.success(t("sensors.poll.success", { count: res.sensors_polled }));
        },
        onError: (err) => {
          const msg = err instanceof Error && err.message === "timeout"
            ? t("sensors.poll.timeout")
            : t("sensors.poll.failure");
          toast.error(msg);
        },
      });
      ```
    - Implement 30s client timeout (D-13) via `Promise.race` wrapping `pollSensorsNow()`:
      ```typescript
      const pollWithTimeout = () => Promise.race([
        pollSensorsNow(),
        new Promise<never>((_, rej) => setTimeout(() => rej(new Error("timeout")), 30_000)),
      ]);
      ```
    - Render a `<button>` with tokens `bg-primary text-primary-foreground hover:bg-primary/90 disabled:opacity-50 rounded-md px-4 h-9 text-sm font-medium`. Label: `t("sensors.poll.now")` idle, `t("sensors.poll.refreshing")` in-flight. `disabled={mutation.isPending}`.
    - Place the button in `SensorsPage.tsx` alongside the SensorTimeWindowPicker (flex row, space-between): `<div className="flex items-center justify-between"><PollNowButton /><SensorTimeWindowPicker /></div>`.

    (2) `frontend/src/components/sensors/SensorHealthChip.tsx` — new component.
    - Props: `{ status: SensorStatusEntry | undefined }`.
    - Compute duration string from `Date.now() - new Date(status.last_success_at|last_attempt_at).getTime()` → format as "Xh", "Xm", "Xd" via a small `formatDuration(ms, locale)` helper (localized via react-i18next pluralization is overkill; use Intl.NumberFormat for numbers, concatenate with unit suffix from i18n: add `sensors.duration.seconds`, `sensors.duration.minutes`, `sensors.duration.hours`, `sensors.duration.days` keys — add to BOTH locales).
    - Render a `<span>` pill: `inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-xs font-medium`. Online: `bg-primary/10 text-primary`; offline: `bg-destructive/10 text-destructive`; unknown: `bg-muted text-muted-foreground`. No hex, no dark: (D-02).

    (3) `frontend/src/components/sensors/SensorStatusCards.tsx` — extend from 39-01:
    - Add a `useQuery({ queryKey: ["settings"], queryFn: fetchSettings })` to read thresholds (or use existing settings hook if one exists — grep for `fetchSettings` usage). Extract `tmin = settings.sensor_temperature_min != null ? Number(settings.sensor_temperature_min) : null` and same for tmax/hmin/hmax.
    - Add `useQuery({ queryKey: sensorKeys.status(), queryFn: fetchSensorStatus, refetchInterval: 15_000, refetchIntervalInBackground: false, refetchOnWindowFocus: true, staleTime: 5_000 })` at the top level (one query, not per-card).
    - For each sensor card:
      - Threshold check: if current temperature is outside `[tmin, tmax]` (when both thresholds are non-null — D-12), apply `text-destructive` to the temp value instead of `text-foreground`. Same for humidity with hmin/hmax.
      - Show `t("sensors.threshold.outOfRange")` as a muted caption beneath the out-of-range value.
      - Render DIFF-01 delta badges under each value:
        ```tsx
        <div className="flex flex-col gap-0.5 text-xs">
          <DeltaBadgeRow
            delta={computeSensorDelta(readings, "temperature", 1)}
            label={t("sensors.delta.vsHour")}
            locale={shortLocale}
            noBaselineTooltip={t("sensors.delta.noBaseline")}
          />
          <DeltaBadgeRow
            delta={computeSensorDelta(readings, "temperature", 24)}
            label={t("sensors.delta.vsDay")}
            locale={shortLocale}
            noBaselineTooltip={t("sensors.delta.noBaseline")}
          />
        </div>
        ```
        where `DeltaBadgeRow` is a small local component wrapping `<DeltaBadge>` + muted label span, mirroring `DeltaBadgeStack.tsx` structure but tailored to this card (pairs of delta + label, one per badge, not the sales/HR remap logic). Use `Number(latest.temperature)` for current value when passing to DeltaBadge via computeSensorDelta.
      - Render `<SensorHealthChip status={statusMap.get(sensor.id)} />` in the card header (top-right), where `statusMap = new Map(statusData.map(s => [s.sensor_id, s]))`.

    (4) `frontend/src/components/sensors/SensorTimeSeriesChart.tsx` — extend from 39-01:
    - Read thresholds from `useQuery({ queryKey: ["settings"], queryFn: fetchSettings })` (dedupes with the Settings query in SensorStatusCards via React Query cache).
    - Add `<ReferenceLine y={Number(settings.sensor_temperature_min)} stroke="var(--color-destructive)" strokeDasharray="4 4" label={t("sensors.threshold.min")} />` inside the temperature `<LineChart>` when `sensor_temperature_min != null`. Same for max. Same for humidity chart with hmin/hmax (D-12).
    - Remove the 39-01 TODO comment now that thresholds are wired.

    (5) `frontend/src/pages/SensorsPage.tsx` — reorganize the control bar per (1) above (PollNowButton + SensorTimeWindowPicker side-by-side).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | tail -10 &amp;&amp; grep -n 'invalidateQueries' src/components/sensors/PollNowButton.tsx &amp;&amp; grep -n 'computeSensorDelta' src/components/sensors/SensorStatusCards.tsx &amp;&amp; grep -n 'ReferenceLine' src/components/sensors/SensorTimeSeriesChart.tsx &amp;&amp; grep -n 'sensor_temperature_min' src/components/sensors/SensorStatusCards.tsx src/components/sensors/SensorTimeSeriesChart.tsx &amp;&amp; ! grep -rnE 'dark:' src/components/sensors/ &amp;&amp; ! grep -rnE '#[0-9a-fA-F]{6}' src/components/sensors/</automated>
  </verify>
  <done>
    - PollNowButton mutates, invalidates `sensorKeys.all` on success, shows success/failure/timeout toasts
    - SensorStatusCards: threshold-aware color (destructive on out-of-range), DIFF-01 deltas (vs. 1h + vs. 24h), DIFF-10 health chip per card
    - SensorTimeSeriesChart: dashed destructive ReferenceLines on both charts when thresholds are defined; omitted when null (D-12)
    - No `dark:` variants, no hex literals added (grep clean)
    - `npx tsc --noEmit` passes
    - Manual smoke: polling 1 sensor → card number updates, chart gets a new point, toast appears
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full-flow UAT — admin vs viewer, interactions, i18n, tokens</name>
  <files>N/A — manual verification only (no files modified by this task)</files>
  <action>See <how-to-verify> below. Executor PAUSES here; user runs through the 12 steps against the running stack.</action>
  <verify><automated>MANUAL — no automated verify; human approval via &lt;resume-signal&gt; is the gate</automated></verify>
  <done>All 12 UAT steps approved by user (or issues recorded for revision plans). Phase 39 acceptance rolls up.</done>
  <what-built>
    Complete Phase 39 Sensor Monitor dashboard:
    - Admin-only launcher tile (Thermometer icon) on `/`
    - `/sensors` route with KPI cards, stacked time-series charts, time-window SegmentedControl, Poll-now button
    - Threshold-aware card coloring + dashed ReferenceLines on charts
    - DIFF-01 delta badges (vs. 1h, vs. 24h) on each card
    - DIFF-10 health chip (OK seit / Offline seit) per card
    - SubHeader route-aware freshness on `/sensors`
    - Full DE/EN parity for all `sensors.*` dashboard keys + `launcher.tile.sensors`
  </what-built>
  <how-to-verify>
    Prerequisites: stack running (`docker compose up -d`), seeded Produktion sensor polling successfully for at least 5 minutes (so readings history exists for DIFF-01), one admin account and one viewer account.

    1. **Launcher tile visibility (admin)** — Log in as admin. Visit http://localhost:5173/. Confirm you see 4 tiles: KPI Dashboard · Sensors (Thermometer icon) · Coming Soon · Coming Soon. Expected: Sensors tile is clickable, icon is Thermometer, label is "Sensors" (EN) / "Sensoren" (DE) depending on locale toggle.

    2. **Launcher tile visibility (viewer)** — Log out. Log in as viewer. Visit http://localhost:5173/. Confirm you see 3 tiles total: KPI Dashboard · Coming Soon · Coming Soon. No Sensors tile anywhere. Expected: the Sensors slot is completely absent (not disabled, not greyed — absent).

    3. **Navigation + page shell** — Log back in as admin. Click Sensors tile. Confirm URL becomes `/sensors`, page renders with the 4-tile launcher chrome replaced by the sensor page (max-w-7xl container, space-y-8 sections).

    4. **KPI cards** — Confirm 1 card per configured sensor (the seeded Produktion row, so 1 card). The card shows sensor name, a numeric temperature (°C) and humidity (%), a freshness footer ("vor X s" in DE, "X s ago" in EN), a health chip in the corner, and two delta rows ("vs. 1 Std" and "vs. 24 Std" in DE).

    5. **Threshold coloring** — Using the settings API or DB directly, set `sensor_temperature_max` to a value below current reading (e.g., `10.0` if current is 22 °C). Refresh the page. Confirm: temp value renders in `destructive` token color (typically red-tinted) and the "Außerhalb des Bereichs" / "Out of range" caption appears. Revert threshold. Refresh — normal color returns.

    6. **Time-window selector** — Click through 1h · 6h · 24h · 7d · 30d segments. Confirm the two charts below the cards update with a wider/narrower window of data each time. Expected: 1h shows ~60 points at 1/min, 24h shows ~1440 points, 30d shows ~30-day span. Chart data points should NOT bridge across gaps (connectNulls={false} → visible line segmentation if any polls failed).

    7. **Threshold ReferenceLines on charts** — Set `sensor_temperature_min=20`, `sensor_temperature_max=25`. Refresh. Confirm two dashed destructive-colored horizontal lines appear on the temperature chart at y=20 and y=25. Clear thresholds (set both to null). Refresh. Confirm lines disappear (not drawn at 0).

    8. **Poll-now button** — Click "Jetzt messen" / "Poll now". Confirm: button shows loading state ("Messung läuft..." / "Polling..."), blocks for <30s, a success toast appears ("1 Sensoren gemessen" / "Polled 1 sensors"), and the card's freshness footer resets to "vor 0–2 s" / "0–2 s ago". Chart gains a new rightmost point.

    9. **Poll-now error path (optional)** — Temporarily disable the sensor (or break its host field). Click Poll now. Confirm: the mutation still succeeds at the HTTP level (pollSensorsNow returns PollNowResult with an error in the `errors` array — consider whether this should show as failure toast; the current implementation treats 200-OK as success. Acceptable either way — document in SUMMARY.) Alternative to trigger: stop the backend container, click Poll now, expect 30s timeout toast.

    10. **i18n parity end-to-end** — Flip DE/EN toggle. Confirm every visible string on `/sensors` changes: title (if any), KPI labels, window segments, chip text, poll button, toast, freshness footer. Open browser console → expect ZERO `i18next::translator: missingKey` warnings.

    11. **SubHeader freshness** — Observe the SubHeader on `/sensors`. Expected: right side shows "Letzte Messung vor X s" / "Last measured X s ago" and updates as new readings arrive (15s refetch interval).

    12. **Token compliance spot-check** — Open DevTools, inspect a card's DOM. Confirm the card uses `bg-card`, `border-border`, `text-foreground` / `text-muted-foreground` / `text-destructive` — no hex literals in the computed class list. Switch OS theme (or force dark via the theme toggle if present) — colors should flip automatically without any `dark:` variants.

    Report any deviations as blockers. Record which threshold values + polling cadence you tested.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 39 complete, or describe issues to drive revision plans.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- Backend pytest clean after settings schema extension
- Parity check: all new `sensors.*` keys present in both locales (Task 1 automated check)
- Grep bans clean: no `dark:`, no hex literals added under `src/components/sensors/`
- Grep ban: no new `fetch(` calls under `frontend/src/`
- Human UAT (Task 3) approved
</verification>

<success_criteria>
- Poll-now click blocks up to 30s, shows toast, invalidates all sensorKeys, cards + charts refresh
- Cards show threshold-aware color + "Out of range" caption when current value breaches min/max
- Cards show DIFF-01 delta badges (vs. 1h, vs. 24h) computed client-side
- Cards show DIFF-10 health chip (OK seit Xh / Offline seit X min)
- Charts show dashed ReferenceLines at thresholds (omitted when null)
- All new `sensors.*` i18n keys parity across en.json/de.json
- Full human UAT approved — all 12 steps pass
</success_criteria>

<output>
After completion, create `.planning/phases/39-dashboard-ui-launcher-tile/39-02-SUMMARY.md` covering: files created/modified, backend Settings schema extension (sensor_poll_interval_s + 4 thresholds exposed read-only), sensorDelta algorithm + tolerance window decision, PollNowButton timeout strategy (client 30s race + backend 30s wait_for), threshold color + ReferenceLine conditional rendering (D-12), health chip token mapping (D-14), DE/EN parity key count, and human-UAT sign-off summary (which of the 12 steps were validated and any deferred-gap notes).

Phase 39 acceptance rolls up from 39-01-SUMMARY + 39-02-SUMMARY + checkpoint approval. Phase 40 kickoff can proceed once this plan's checkpoint is approved.
</output>
