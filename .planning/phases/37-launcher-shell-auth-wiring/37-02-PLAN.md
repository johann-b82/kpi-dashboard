---
phase: 37-launcher-shell-auth-wiring
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified: []
autonomous: false
requirements:
  - LAUNCH-01
  - LAUNCH-02
  - LAUNCH-03
  - LAUNCH-04
  - LAUNCH-05
  - AUTH-01
  - AUTH-02
  - BRAND-01
  - BRAND-02
  - BRAND-03

must_haves:
  truths:
    - "A logged-out user who logs in successfully lands on /home (not /)."
    - "/home renders a visible iOS-style grid of 4 square rounded-corner tile cards (1 active KPI Dashboard tile + 3 greyed placeholders)."
    - "Clicking the KPI Dashboard tile navigates to the Sales Dashboard."
    - "The 3 coming-soon tiles are visually greyed (~40% opacity) and do not respond to click or hover."
    - "Navigating to /home in an incognito window (unauthed) redirects to /login."
    - "Switching language DE<->EN updates the KPI Dashboard tile label and Coming Soon label correctly."
    - "Toggling dark/light theme on /home works without visual regressions (no unreadable text, no washed-out tiles)."
    - "The page heading reflects settings.app_name (changing the app name in Settings updates the launcher heading after save)."
    - "Viewer-role users see the same 4 tiles (no admin-only tiles are defined in v1.14) — the page is identical across roles in this milestone."
  artifacts: []
  key_links:
    - from: "Login page submit"
      to: "/home"
      via: "AuthGate useEffect setLocation on authed-on-login"
      pattern: "manual: successful login lands on /home"
    - from: "Active tile click"
      to: "/ (Sales Dashboard)"
      via: "wouter setLocation"
      pattern: "manual: click active tile -> URL becomes /"
---

<objective>
Interactive human verification of the App Launcher. Plan 01 produced code-level proof (TypeScript compiles, greps pass, Vite build succeeds). Plan 02 is where a real browser session proves the VISUAL and BEHAVIORAL requirements: tile sizing/shape, opacity, hover states, role gating, language toggle, theme toggle, and end-to-end auth redirect.

Purpose: No frontend test framework is configured (RESEARCH.md "Validation Architecture" section confirmed). Manual smoke-verification is the correct test strategy for pure UI phases of this size.

Output: The phase is approved (or a specific issue is reported for gap closure) based on a 10-step scripted browser walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-launcher-shell-auth-wiring/37-UI-SPEC.md
@.planning/phases/37-launcher-shell-auth-wiring/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bring up the app and confirm the /home route is reachable</name>
  <files>(no file changes — environment bring-up only)</files>
  <read_first>
    - .planning/phases/37-launcher-shell-auth-wiring/37-01-SUMMARY.md (MUST read — confirms exact files edited and any deviations)
    - docker-compose.yml (MUST read — confirms the standard bring-up command and service names)
  </read_first>
  <action>
    Bring up the full dev stack so the human can exercise /home in a real browser.

    1. Ensure Docker is running locally.
    2. From the repo root, start the stack in detached mode:
       ```
       docker compose up -d --build
       ```
    3. Confirm all services are healthy:
       ```
       docker compose ps
       ```
       Expected: db, directus, api, frontend (or the equivalent names from docker-compose.yml) all show Up / healthy.
    4. Identify the frontend URL from docker-compose.yml port mappings. Typical values: http://localhost:5173 (Vite dev) or http://localhost:3000 (static/nginx). RECORD this URL — you will paste it into the checkpoint description in Task 2 (replacing "{frontend_port}").
    5. Preflight the SPA routing:
       ```
       curl -sI http://localhost:{frontend_port}/
       curl -sI http://localhost:{frontend_port}/home
       ```
       Both must return HTTP 200 (the SPA shell is served for client-side routes).
    6. Leave the stack running for Task 2 (the human checkpoint). Do NOT kill the containers between Task 1 and Task 2.

    Do NOT modify any source files in this task. Do NOT run database migrations. This is purely a bring-up + URL-reachability preflight.
  </action>
  <verify>
    <automated>docker compose ps --format json | python3 -c "import json,sys; rows=[json.loads(l) for l in sys.stdin if l.strip()]; unhealthy=[r['Name'] for r in rows if r.get('Health') and r['Health']!='healthy' and r.get('State')!='running']; assert not unhealthy, f'Unhealthy: {unhealthy}'; print('OK — all containers healthy')"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ps` shows all services in "running" state (and "healthy" where healthchecks are defined)
    - `curl -sI http://localhost:{frontend_port}/` returns HTTP 200 (frontend is serving)
    - `curl -sI http://localhost:{frontend_port}/home` returns HTTP 200 (SPA catch-all serves index.html for /home — wouter handles client-side routing)
    - Executor records the exact frontend URL in the Task 2 checkpoint body (so the human does not have to hunt for the port)
  </acceptance_criteria>
  <done>
    Docker stack is up, frontend is reachable, /home SPA route returns the app shell (client-side AuthGate will then handle the redirect when the browser loads). The app is ready for human verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies App Launcher across roles, themes, and languages</name>
  <files>(no file changes — visual verification only)</files>
  <read_first>
    - .planning/phases/37-launcher-shell-auth-wiring/37-UI-SPEC.md (MUST read — contains the visual contract: sizes, colors, icons, spacing the human will compare against)
    - .planning/phases/37-launcher-shell-auth-wiring/37-01-SUMMARY.md (MUST read — tells human what EXACTLY to expect on /home)
  </read_first>
  <what-built>
    Plan 01 delivered:
    - New `/home` route rendering LauncherPage.tsx
    - iOS-style 4-tile CSS grid: 1 active KPI Dashboard tile (LayoutDashboard icon) + 3 greyed coming-soon tiles (Box icon)
    - Active tile navigates to `/` (the Sales Dashboard)
    - Post-login redirect changed from `/` to `/home`
    - `launcher.title`, `launcher.tile.kpi_dashboard`, `launcher.tile.coming_soon` keys in en.json and de.json
    - Full NavBar + SubHeader render on /home (no shell changes)
    - Page heading uses `settings.app_name` from useSettings()
    - Tailwind token classes only (`bg-card`, `border-border`, `text-foreground`, `text-muted-foreground`, `bg-accent/10`) — dark mode works automatically
  </what-built>
  <action>
    PRESENT the <how-to-verify> walkthrough to the user, with the frontend URL recorded in Task 1 substituted for {frontend_port}. Then PAUSE and wait for the user to report results.

    The executor's job in this task:
    1. Paste the Task 1 frontend URL into the walkthrough.
    2. Display the 10 numbered steps to the user.
    3. Wait for the resume signal ("approved" OR a description of failing step(s)).
    4. If "approved": mark task complete, proceed to write 37-02-SUMMARY.md.
    5. If failures reported: capture the exact step numbers and failure descriptions in 37-02-SUMMARY.md, THEN stop — the user will invoke `/gsd:plan-phase 37 --gaps` to create targeted fix plans. Do NOT attempt to auto-fix anything; gap closure is a separate workflow.

    Do NOT automate away any of the 10 steps below. They are deliberately manual because no headless-browser test infrastructure exists for this frontend.
  </action>
  <how-to-verify>
    Frontend URL (fill in from Task 1): http://localhost:{frontend_port}

    Credentials: use the existing admin and viewer accounts from your local Directus seed. If you don't have a viewer account, skip Step 6 and note that LAUNCH-05 is verified on scaffold inspection only (there are no admin-only tiles in v1.14 anyway).

    **Step 1 — AUTH-02: Unauthenticated redirect**
    - Open an incognito window.
    - Visit http://localhost:{frontend_port}/home
    - Expected: URL becomes /login, login form renders.
    - PASS/FAIL? ____

    **Step 2 — AUTH-01: Post-login redirect lands on /home**
    - In the same incognito window, log in with admin credentials.
    - Expected: After submit, URL becomes /home (NOT /, NOT /sales).
    - PASS/FAIL? ____

    **Step 3 — LAUNCH-01 / LAUNCH-02: Visual grid**
    - You should see a page with:
      - NavBar at top (logo + Upload + Settings icons + Docs link + theme/lang toggles + logout)
      - SubHeader below (likely empty state for /home)
      - Large page heading = the app name (e.g., "KPI Dashboard")
      - A grid of 4 square rounded tiles (approx 120x120 px each)
      - Tile 1 (top-left): solid, full opacity, `LayoutDashboard` icon, label "KPI Dashboard" (EN) / "KPI-Dashboard" (DE)
      - Tiles 2–4: greyed out (~40% opacity), `Box` icon, label "Coming Soon" (EN) / "Demnächst" (DE)
      - Grid gap approximately 24px
      - Tiles aligned in a row on desktop (≥~640px wide); wrap to 2–3 columns on narrower windows
    - PASS/FAIL? ____ (describe any deviations)

    **Step 4 — LAUNCH-03: Active tile click navigates to Sales Dashboard**
    - Click the KPI Dashboard tile.
    - Expected: URL becomes `/` and the Sales Dashboard (KPI cards, revenue chart, orders table) renders.
    - Note: Requirements text says "/sales" but the actual Sales Dashboard route is `/`. Plan 01 Task 3 resolves this per D-10 — the tile correctly navigates to `/`.
    - PASS/FAIL? ____

    **Step 5 — LAUNCH-04: Coming-soon tiles inert**
    - Browser-back to /home.
    - Hover over a greyed Coming Soon tile. Expected: NO hover color change, NO cursor-pointer (default cursor stays).
    - Click a greyed Coming Soon tile. Expected: nothing happens, URL stays /home.
    - PASS/FAIL? ____

    **Step 6 — LAUNCH-05: Viewer role (skip if no viewer account exists)**
    - Log out. Log in again with viewer credentials.
    - Expected: /home renders the SAME 4 tiles (v1.14 has zero admin-only tiles defined — both roles see the same grid).
    - Open devtools and confirm no "admin"-labelled tile appears in the DOM.
    - PASS/FAIL? ____ (or SKIPPED if no viewer account)

    **Step 7 — BRAND-01: Dark mode**
    - Click the theme toggle in the NavBar.
    - Expected: Background flips dark; tile cards remain legible (text visible, borders visible, icons visible); grid layout unchanged; opacity of coming-soon tiles still looks like ~40% against the dark card bg.
    - Toggle back to light. Expected: returns cleanly.
    - PASS/FAIL? ____

    **Step 8 — BRAND-02: Language toggle**
    - Click the language toggle (DE/EN) in the NavBar.
    - Expected in DE: KPI Dashboard tile label shows "KPI-Dashboard"; Coming Soon tiles show "Demnächst".
    - Expected in EN: "KPI Dashboard" and "Coming Soon".
    - PASS/FAIL? ____

    **Step 9 — BRAND-03: Page heading reflects Settings app_name**
    - Navigate to /settings. Edit the "App name" field to a distinctive value (e.g., "Launcher Test"). Save.
    - Navigate back to /home.
    - Expected: the page heading on /home now reads "Launcher Test".
    - Restore the app name back to "KPI Dashboard" when done.
    - PASS/FAIL? ____

    **Step 10 — D-03: NavBar + SubHeader render on /home**
    - Confirm throughout all above steps that the NavBar is visible (with logo, icons, toggles) and SubHeader renders as on other non-/login routes (even if empty). There must be NO special-casing that hides chrome on /home.
    - PASS/FAIL? ____

    **Summary**
    - All 10 steps PASS → type "approved" to mark the phase verified.
    - Any step FAIL → describe the issue. The orchestrator can invoke `/gsd:plan-phase 37 --gaps` to create targeted fix plans.
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a human verification checkpoint; no automated command exists. Resume signal is "approved" or a list of failing step numbers from the user.</automated>
  </verify>
  <acceptance_criteria>
    - User has received the 10-step walkthrough with the actual frontend URL substituted in (no literal "{frontend_port}" remains in the prompt shown to the user)
    - User replies with either the literal word "approved" OR a list of failing step numbers and descriptions
    - If approved: 37-02-SUMMARY.md records "All 10 steps PASS" with the date/time of verification
    - If any step failed: 37-02-SUMMARY.md records each failing step number and the user's description verbatim, and the task is marked as incomplete for gap closure
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 10 steps pass, or describe the failing step(s).</resume-signal>
  <done>
    User has completed the 10-step walkthrough and either (a) replied "approved" — all phase 37 requirements (LAUNCH-01..05, AUTH-01, AUTH-02, BRAND-01..03) are behaviorally verified; or (b) replied with specific failing step numbers — gaps are recorded in 37-02-SUMMARY.md for the next-loop `/gsd:plan-phase 37 --gaps` invocation.
  </done>
</task>

</tasks>

<verification>
Plan 02 verification is the 10-step human walkthrough above. No automated verification beyond Task 1 (docker healthchecks).
</verification>

<success_criteria>
All 10 ROADMAP success criteria visually/behaviorally verified:
1. Post-login lands on /home (Step 2)
2. /home shows a grid of square rounded tiles with icons + labels (Step 3)
3. KPI Dashboard tile is active and navigates to the Sales Dashboard (Step 4)
4. Coming-soon tiles are greyed with no click action (Step 5)
5. Admin-only tiles would be absent for Viewer role — scaffold confirmed (Step 6; v1.14 has no admin tiles so only negative confirmation)
6. Page title uses settings.app_name; labels correct in both DE and EN (Steps 8–9)
7. Unauthenticated /home redirects to /login (Step 1)
8. Dark mode toggles correctly on /home with no regressions (Step 7)
9. Full NavBar + SubHeader render on /home (Step 10)
</success_criteria>

<output>
After completion, create `.planning/phases/37-launcher-shell-auth-wiring/37-02-SUMMARY.md` following the template at @$HOME/.claude/get-shit-done/templates/summary.md. SUMMARY should record the human verdict (approved or gap list) for each of the 10 verification steps.
</output>
