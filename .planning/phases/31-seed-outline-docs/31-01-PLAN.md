---
phase: 31-seed-outline-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/wiki-seed/README.md
  - docs/wiki-seed/00-landing.md
  - docs/wiki-seed/01-dev-setup.md
  - docs/wiki-seed/02-architecture.md
autonomous: false
requirements: [WMP-01, DOC-09, DOC-01, DOC-02]
must_haves:
  truths:
    - "The 'KPI Dashboard' collection exists in Outline with members read+write permissions (D-08)"
    - "The landing page (DOC-09) renders a TOC listing all 8 pages with working wiki-links"
    - "DOC-01 Dev Setup explains docker compose up --build, /etc/hosts, .env template, DISABLE_AUTH=true toggle"
    - "DOC-02 Architecture renders a Mermaid graph TD diagram of all 9 services with healthcheck-gated depends_on edges and volume mounts (D-03)"
    - "Each authored page is exported from Outline and committed under docs/wiki-seed/ with matching ##-slug.md name (D-05)"
  artifacts:
    - path: "docs/wiki-seed/README.md"
      provides: "Regen recipe per D-05"
      contains: "Download as Markdown"
    - path: "docs/wiki-seed/00-landing.md"
      provides: "Collection landing TOC snapshot"
    - path: "docs/wiki-seed/01-dev-setup.md"
      provides: "DOC-01 snapshot"
      contains: "DISABLE_AUTH"
    - path: "docs/wiki-seed/02-architecture.md"
      provides: "DOC-02 snapshot"
      contains: "```mermaid"
  key_links:
    - from: "docs/wiki-seed/00-landing.md"
      to: "01-dev-setup through 08-admin-runbook"
      via: "wiki-links or relative markdown refs"
      pattern: "dev-setup|architecture"
---

<objective>
Stand up the Outline "KPI Dashboard" collection, author and snapshot the landing page (DOC-09), Dev Setup (DOC-01), and Docker Compose Architecture (DOC-02). This is the foundation wave — subsequent plans depend on the collection existing and on `docs/wiki-seed/` being initialized with its README.

Purpose: Establish the author/export/commit loop and cover the two most structural docs (overview + architecture) before any user guides.
Output: Outline collection + 3 pages + `docs/wiki-seed/` directory with README and 3 snapshot files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-seed-outline-docs/31-CONTEXT.md
@.planning/phases/31-seed-outline-docs/31-RESEARCH.md
@docs/setup.md
@docker-compose.yml

<interfaces>
Git origin (use in DOC-08 cross-reference later): https://github.com/johann-b82/kpi-dashboard

Outline collection target URL pattern after creation: https://wiki.internal/collection/kpi-dashboard-<uuid>

Authoring/export loop (per RESEARCH §Architecture Patterns):
1. Outline UI → New document in "KPI Dashboard" collection
2. Author prose + markdown + Mermaid + wiki-links
3. Operator review
4. Outline UI → ⋯ → Download → Markdown
5. Save to docs/wiki-seed/##-slug.md
6. Rewrite any absolute wiki URLs / `upload://` / `![` image refs (D-07 text-only)
7. Executor commits
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create 'KPI Dashboard' collection in Outline (WMP-01)</name>
  <what-built>Empty "KPI Dashboard" collection in Outline with members read+write permissions per D-08.</what-built>
  <how-to-verify>
    1. Operator signs into https://wiki.internal via Dex
    2. Home → + New collection
       - Name: KPI Dashboard
       - Icon: book or library emoji (operator's choice)
       - Description: "Developer + user docs for KPI Dashboard."
       - Permissions: members read+write (D-08)
    3. Save
    4. Confirm collection is reachable at https://wiki.internal/collection/kpi-light...
    5. Paste the collection URL into chat for the executor to record in the SUMMARY
  </how-to-verify>
  <resume-signal>Paste collection URL and type "created" — or describe issue</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 2: Seed docs/wiki-seed/ directory + README</name>
  <files>docs/wiki-seed/README.md</files>
  <action>
    Create docs/wiki-seed/ directory. Write README.md implementing D-05 regen recipe. Exact required content:

    - Title: "Outline Wiki Seed Snapshots"
    - One paragraph: "These are markdown exports of the KPI Dashboard Outline collection. Outline is canonical; this directory is a snapshot for version history and disaster recovery."
    - Section "Regenerate": bulleted steps — sign into https://wiki.internal, open each page in the KPI Dashboard collection, click the ⋯ menu → Download → Markdown, save as `##-slug.md` (matching filenames below), commit.
    - Section "Files": bullet list of expected filenames `00-landing.md` through `08-admin-runbook.md` matching D-05.
    - Section "Cadence": one sentence — "Re-exported at phase completion and at milestone boundaries (D-06)."
    - TEXT-ONLY invariant: no `![`, no `upload://`, no `ORG/REPO` placeholder.
  </action>
  <verify>
    <automated>test -f docs/wiki-seed/README.md &amp;&amp; grep -q "Download" docs/wiki-seed/README.md &amp;&amp; grep -q "08-admin-runbook" docs/wiki-seed/README.md &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/README.md</automated>
  </verify>
  <done>README exists, explains regen, lists all 9 filenames, passes text-only grep invariants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author DOC-01 Dev Setup page in Outline + snapshot</name>
  <what-built>Scaffolded empty file docs/wiki-seed/01-dev-setup.md ready for pasted Outline export.</what-built>
  <how-to-verify>
    Operator authors in Outline editor inside "KPI Dashboard" collection. Required sections (per RESEARCH §Content Grounding DOC-01, grounded in docs/setup.md §§1-5 and Phase 28 DISABLE_AUTH section):
      - Prerequisites (Docker Compose v2, mkcert, /etc/hosts)
      - /etc/hosts entries: kpi.internal, wiki.internal, auth.internal
      - .env template walkthrough (point to .env.example)
      - `docker compose up --build` flow + healthcheck order
      - DISABLE_AUTH=true local dev toggle
      - Cross-links to DOC-02 Architecture, DOC-08 Admin Runbook via Outline `[[Page Title]]` wiki-link

    Then operator: Outline ⋯ → Download → Markdown → paste the full markdown into the chat.
    Executor then writes the pasted content to docs/wiki-seed/01-dev-setup.md, rewriting any `upload://` / `![` image refs or absolute wiki URLs (D-07 text-only invariant).
  </how-to-verify>
  <resume-signal>Paste exported markdown — executor will write file and run grep invariants</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 4: Verify DOC-01 snapshot invariants</name>
  <files>docs/wiki-seed/01-dev-setup.md</files>
  <action>
    After operator pastes DOC-01 markdown (Task 3), write file exactly as pasted but with the following automated sanitizations:
    - Replace any `![alt](upload://...)` image lines with a removal (D-07 text-only)
    - Replace any `https://wiki.internal/doc/...` absolute links to cross-pages with relative filename form `./NN-slug.md` where possible
    - Leave prose untouched
    Then run grep invariants; if any fail, re-request operator paste.
  </action>
  <verify>
    <automated>test -f docs/wiki-seed/01-dev-setup.md &amp;&amp; grep -qi "DISABLE_AUTH" docs/wiki-seed/01-dev-setup.md &amp;&amp; grep -qi "docker compose up" docs/wiki-seed/01-dev-setup.md &amp;&amp; grep -qi "/etc/hosts" docs/wiki-seed/01-dev-setup.md &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/01-dev-setup.md</automated>
  </verify>
  <done>DOC-01 snapshot file exists with required keywords and passes text-only invariants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Author DOC-02 Architecture page (Mermaid diagram) in Outline + snapshot</name>
  <what-built>Outline DOC-02 with a Mermaid `graph TD` of all 9 compose services (db, migrate, api, frontend, npm, dex, outline, outline-db, outline-redis) per D-03.</what-built>
  <how-to-verify>
    Operator authors in Outline. Required elements:
      - Intro paragraph: "KPI Dashboard stack topology (v1.11)."
      - A Mermaid fenced block ```` ```mermaid ```` containing:
        - All 9 services as nodes
        - Solid arrows for healthcheck-gated depends_on edges (migrate→db, api→db+migrate, frontend→api, npm→frontend+api+dex+outline, outline→outline-db+outline-redis+dex)
        - Dashed arrows for runtime-only deps where applicable
        - Volume labels (named volumes listed as subgraph or inline annotations)
      - Section "Healthcheck order" — short prose
      - Section "Volumes" — bullet list of 7 named volumes
      - Cross-link to DOC-01 Dev Setup via `[[Dev Setup]]` wiki-link
      - Preview in Outline confirms Mermaid renders (no `Parse error`)
    Then: ⋯ → Download → Markdown → paste into chat.
  </how-to-verify>
  <resume-signal>Paste exported markdown</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 6: Write DOC-02 snapshot + grep invariants</name>
  <files>docs/wiki-seed/02-architecture.md</files>
  <action>
    Write pasted DOC-02 markdown to docs/wiki-seed/02-architecture.md with the same sanitization pass as Task 4 (strip `![`/`upload://`, rewrite absolute cross-links).
    Confirm the Mermaid fenced block is present and contains all 9 service node names.
  </action>
  <verify>
    <automated>grep -q '```mermaid' docs/wiki-seed/02-architecture.md &amp;&amp; for svc in db migrate api frontend npm dex outline outline-db outline-redis; do grep -q "$svc" docs/wiki-seed/02-architecture.md || { echo "missing $svc"; exit 1; }; done &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/02-architecture.md</automated>
  </verify>
  <done>DOC-02 snapshot committed with Mermaid block containing all 9 service names, text-only invariants clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Author DOC-09 Landing page in Outline + snapshot</name>
  <what-built>Collection landing page with TOC and cross-links to all 8 subpages.</what-built>
  <how-to-verify>
    Operator authors the landing page (typically the collection's first doc or "Overview" page). Required structure per 31-CONTEXT §Specifics:
      - Intro paragraph describing KPI Dashboard + what this collection covers
      - TOC: 8-item list linking to DOC-01..DOC-08 via Outline `[[Page Title]]` wiki-links (or Outline's "/" slash-menu TOC block if available — see RESEARCH Open Q 5)
      - "See also" footer linking to `docs/setup.md` on GitHub (repo: https://github.com/johann-b82/kpi-dashboard)
    Note: DOC-03..DOC-08 may not yet exist when authoring this page; wiki-links will show as unresolved until those pages land in later waves. That is expected — relink after wave 3.
    Then: ⋯ → Download → Markdown → paste.
  </how-to-verify>
  <resume-signal>Paste exported markdown</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 8: Write DOC-09 landing snapshot + invariants</name>
  <files>docs/wiki-seed/00-landing.md</files>
  <action>
    Write pasted content to docs/wiki-seed/00-landing.md with sanitization pass. Confirm landing references to all 8 downstream pages are present (by title keyword — e.g. "Dev Setup", "Architecture", "API Reference", "Personio", "Sales", "HR", "Settings", "Admin").
  </action>
  <verify>
    <automated>for kw in "Dev Setup" "Architecture" "API Reference" "Personio" "Sales" "HR" "Settings" "Admin"; do grep -qi "$kw" docs/wiki-seed/00-landing.md || { echo "missing TOC entry: $kw"; exit 1; }; done &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/00-landing.md</automated>
  </verify>
  <done>Landing snapshot lists all 8 page titles as TOC entries, text-only invariants clean.</done>
</task>

</tasks>

<verification>
- Outline collection "KPI Dashboard" exists and is discoverable from https://wiki.internal
- docs/wiki-seed/ directory exists with README + 3 page snapshots
- All snapshots pass text-only grep invariants (no `![`, `upload://`, `ORG/REPO`)
</verification>

<success_criteria>
WMP-01, DOC-09 (skeleton — final relink after wave 3), DOC-01, DOC-02 snapshots committed and invariants-clean.
</success_criteria>

<output>
Create `.planning/phases/31-seed-outline-docs/31-01-SUMMARY.md` capturing collection URL, any operator deviations, and the wave 1 snapshot commit hash.
</output>
