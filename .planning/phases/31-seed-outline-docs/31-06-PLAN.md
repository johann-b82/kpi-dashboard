---
phase: 31-seed-outline-docs
plan: 06
type: execute
wave: 3
depends_on: ["31-01", "31-03", "31-04", "31-05"]
files_modified:
  - docs/wiki-seed/README.md
autonomous: false
requirements: [E2E-01, E2E-03, E2E-04, E2E-05]
must_haves:
  truths:
    - "E2E-01: fresh `docker compose up --build` on a clean VM produces a working stack — all 3 hostnames resolve, NPM serves HTTPS, Dex login loads"
    - "E2E-03: Outline JIT provisioning works; user can create + edit a doc in the 'KPI Light' collection"
    - "E2E-04 (reframed per D-10): NavBar wiki icon opens https://wiki.internal in a new tab; if unauthenticated the user is redirected through Dex and can log in with the same credentials used for KPI Light. Silent cross-app SSO is a documented known limitation (docs/setup.md Known limitations, Phase 29)."
    - "E2E-05: all 8 seeded docs are legible, cross-linked, and reflect v1.10 state (not stale)"
    - "All 9 snapshot files + README present under docs/wiki-seed/ with text-only invariants clean"
  artifacts:
    - path: "docs/wiki-seed/README.md"
      provides: "Final phase UAT sign-off stamp (append section)"
---

<objective>
Run the four remaining E2E UATs sequentially and append a sign-off block to `docs/wiki-seed/README.md` capturing the results. This closes Phase 31 and the v1.11 milestone.

Purpose: Validate the full stack + wiki content end-to-end.
Output: 4 UATs signed off; README augmented with phase UAT summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-seed-outline-docs/31-CONTEXT.md
@.planning/phases/31-seed-outline-docs/31-RESEARCH.md
@docs/wiki-seed/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-UAT invariant sweep across all 9 snapshots</name>
  <files>docs/wiki-seed/*.md</files>
  <action>
    Run the text-only invariant sweep across all snapshots and confirm the full file set exists.
  </action>
  <verify>
    <automated>for f in 00-landing 01-dev-setup 02-architecture 03-api-reference 04-personio-sync 05-sales-dashboard 06-hr-dashboard 07-settings 08-admin-runbook; do test -f docs/wiki-seed/$f.md || { echo "missing $f.md"; exit 1; }; done &amp;&amp; ! grep -rE '!\[|upload://|ORG/REPO' docs/wiki-seed/ &amp;&amp; grep -q "johann-b82/kpi-dashboard" docs/wiki-seed/08-admin-runbook.md &amp;&amp; grep -q '```mermaid' docs/wiki-seed/02-architecture.md</automated>
  </verify>
  <done>All 9 snapshots present, no invariant violations anywhere.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E-01 UAT — fresh-VM docker compose up</name>
  <what-built>Full stack from previous phases.</what-built>
  <how-to-verify>
    On a clean environment (or after `docker compose down -v` + pruning named volumes you're comfortable losing):
    1. Follow docs/setup.md from the top (or the cleanest path the operator uses) through `docker compose up --build`
    2. Confirm `docker compose ps` shows all services healthy
    3. Confirm `https://kpi.internal`, `https://wiki.internal`, `https://auth.internal` all load over HTTPS (TLS lock)
    4. Confirm Dex login page loads at https://auth.internal/dex
    5. Type "E2E-01 PASS" or describe failures
  </how-to-verify>
  <resume-signal>"E2E-01 PASS" or failure description</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: E2E-03 UAT — Outline JIT provisioning + doc create/edit</name>
  <what-built>Outline running; "KPI Light" collection exists.</what-built>
  <how-to-verify>
    1. Open https://wiki.internal in a fresh browser profile (no existing Outline cookie)
    2. Click "Sign in with OIDC" → Dex login → complete
    3. Confirm Outline JIT-provisions the user (default workspace visible)
    4. Navigate to "KPI Light" collection
    5. Create a new test document → type a paragraph → save
    6. Edit the document → save
    7. Delete the test document
    8. Type "E2E-03 PASS" or describe failures
  </how-to-verify>
  <resume-signal>"E2E-03 PASS" or failure description</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: E2E-04 UAT (reframed per D-10) — NavBar wiki icon + shared credentials</name>
  <what-built>KPI Light NavBar wiki icon (Phase 30) + Outline (Phase 29).</what-built>
  <how-to-verify>
    Acceptance bar (D-10 reframe, verbatim):
    > Clicking the NavBar wiki icon in KPI Light opens `https://wiki.internal` in a new tab. If the user has not yet authenticated to Outline in this browser, they are redirected through Dex and can log in with the same credentials used for KPI Light. Silent cross-app SSO is a documented known limitation (`docs/setup.md` Known limitations, Phase 29).

    Steps:
    1. Fresh browser profile; log into KPI Light at https://kpi.internal via Dex
    2. Click the wiki icon in the NavBar (book/library glyph, between language toggle and upload icon — NAV-02)
    3. Confirm a new tab opens to https://wiki.internal (target="_blank")
    4. If the new tab shows Outline signed-in: note "no re-prompt" (bonus; not required by reframe)
    5. If the new tab prompts sign-in: click "Sign in with OIDC" → Dex → use the SAME credentials as KPI Light → confirm Outline signs in successfully
    6. Confirm docs/setup.md has a Known-limitations section mentioning this (already present from Phase 29)
    7. Type "E2E-04 PASS (reframed)" or describe failures
  </how-to-verify>
  <resume-signal>"E2E-04 PASS (reframed)" or failure description</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: E2E-05 UAT — docs legibility + cross-link + v1.10-currency review</name>
  <what-built>All 8 Outline pages + landing.</what-built>
  <how-to-verify>
    Review each of the 8 pages (DOC-01..DOC-08) in Outline:
    1. Page renders without markdown/Mermaid parse errors
    2. Cross-links resolve (no red/unresolved `[[...]]`)
    3. Content matches v1.10 state (dark mode present, HR 5-KPI set, delta label variants, multi-select HR criteria, etc.)
    4. DOC-08 link to github.com/johann-b82/kpi-dashboard works
    5. Landing page TOC has all 8 entries linking correctly
    Type "E2E-05 PASS" or list pages needing revisions (which will route back to plan 31-04 / 31-05 for re-author).
  </how-to-verify>
  <resume-signal>"E2E-05 PASS" or revision list</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 6: Append UAT sign-off block to docs/wiki-seed/README.md</name>
  <files>docs/wiki-seed/README.md</files>
  <action>
    Append a new section at the end of docs/wiki-seed/README.md:

    ```
    ## Phase 31 UAT Sign-off

    | UAT   | Status | Date       |
    |-------|--------|------------|
    | E2E-01 | PASS  | YYYY-MM-DD |
    | E2E-03 | PASS  | YYYY-MM-DD |
    | E2E-04 | PASS (reframed per D-10) | YYYY-MM-DD |
    | E2E-05 | PASS  | YYYY-MM-DD |

    v1.11 milestone closed on YYYY-MM-DD.
    ```

    Fill in today's date from the environment. If any UAT failed and was NOT remediated in this plan, mark it FAIL and note the follow-up plan ID.
  </action>
  <verify>
    <automated>grep -q "Phase 31 UAT Sign-off" docs/wiki-seed/README.md &amp;&amp; grep -q "E2E-01" docs/wiki-seed/README.md &amp;&amp; grep -q "E2E-04" docs/wiki-seed/README.md &amp;&amp; grep -q "reframed per D-10" docs/wiki-seed/README.md</automated>
  </verify>
  <done>README has UAT sign-off block with all 4 UATs and the D-10 reframe annotation.</done>
</task>

</tasks>

<success_criteria>
All 5 Phase 31 success criteria met:
1. "KPI Light" collection + 8 pages published and reachable (wave 1-3)
2. Landing page TOC + cross-links work (wave 3 relink)
3. DOC-08 covers bcrypt user-add, Outline backups, OIDC secret rotation — accurate against live stack
4. Collection creation + permission model documented in DOC-08 (WMP-02, WMP-03)
5. Fresh `docker compose up --build` produces working stack (E2E-01)

All 4 E2E UATs signed off (including D-10 reframe for E2E-04).
</success_criteria>

<output>
Create `.planning/phases/31-seed-outline-docs/31-06-SUMMARY.md` with UAT outcomes and milestone closure notes. v1.11 is now ready to ship.
</output>
