---
phase: 31-seed-outline-docs
plan: 05
type: execute
wave: 3
depends_on: ["31-01", "31-03", "31-04"]
files_modified:
  - docs/wiki-seed/08-admin-runbook.md
  - docs/wiki-seed/00-landing.md
autonomous: false
requirements: [DOC-08, WMP-02, WMP-03]
must_haves:
  truths:
    - "DOC-08 contains a 3-paragraph intro, link to https://github.com/johann-b82/kpi-dashboard/blob/main/docs/setup.md, and Outline-specific ops sections (Dex user-add bcrypt workflow, Outline DB + attachment backups, OIDC secret rotation) inline (D-04)"
    - "DOC-08 contains the collection + permission recipe (WMP-02) and the one-collection-per-project convention (WMP-03)"
    - "Canonical repo URL is resolved to github.com/johann-b82/kpi-dashboard — NO 'ORG/REPO' placeholder"
    - "Landing page (00-landing.md) is re-snapshotted with all wiki-links now resolving (relink pass after DOC-08 exists)"
  artifacts:
    - path: "docs/wiki-seed/08-admin-runbook.md"
      provides: "DOC-08 snapshot"
      contains: "johann-b82/kpi-dashboard"
  key_links:
    - from: "docs/wiki-seed/08-admin-runbook.md"
      to: "docs/setup.md on GitHub"
      via: "markdown link to the resolved origin URL"
      pattern: "github\\.com/johann-b82/kpi-dashboard"
---

<objective>
Author DOC-08 Admin Runbook (the multi-purpose admin/ops/multi-project page per D-04 + D-09) and re-snapshot the landing page now that all 8 subpages exist so wiki-links resolve cleanly.

Purpose: Close out the authoring work.
Output: DOC-08 snapshot + final landing snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-seed-outline-docs/31-CONTEXT.md
@.planning/phases/31-seed-outline-docs/31-RESEARCH.md
@docs/setup.md

<interfaces>
Canonical repo origin (already resolved via `git remote -v`):
  https://github.com/johann-b82/kpi-dashboard

Setup.md sections DOC-08 must link to (not duplicate — D-04):
  - "Dex first-login"
  - "Add or rotate a Dex user"
  - "Dex storage and persistence"
  - "Phase 28 — KPI Dashboard login via Dex"
  - "Phase 29 — Outline Wiki deployment"
  - "Backups"
  - "Known limitations" (houses the D-10 SSO limitation E2E-04 refers to)

DOC-08 inline content (NOT in setup.md, per D-04):
  - Dex user-add bcrypt workflow (python:3.12-alpine + bcrypt, cost 10, $2b$10$) — copy-pasteable
  - Outline DB + attachment volume backup commands
  - Rotating OIDC client secrets (kpi-light, outline)
  - WMP-02 collection + permission recipe
  - WMP-03 one-collection-per-project convention
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Author DOC-08 Admin Runbook in Outline + snapshot</name>
  <what-built>DOC-08 per D-04.</what-built>
  <how-to-verify>
    Operator authors in Outline. Required structure:
      - 3-paragraph intro: what this page is for (ops-who-don't-clone audience), relationship to docs/setup.md, relationship to Outline
      - Link to canonical operator runbook: https://github.com/johann-b82/kpi-dashboard/blob/main/docs/setup.md (use this exact URL — repo origin resolved)
      - Section "Adding a Dex user" (inline, copy-pasteable):
        * python:3.12-alpine + bcrypt command producing a $2b$10$... hash
        * paste into dex/config.yaml staticPasswords
        * docker compose restart dex
        * NOTE: "admin@acm.local / ChangeMe!2026-admin" is a placeholder — rotate before sharing (Phase 27 SECURITY NOTE)
      - Section "Outline backups":
        * outline-db pg_dump command
        * outline-data volume tar command
      - Section "Rotating OIDC client secrets" (kpi-light + outline):
        * generate new secret
        * update dex/config.yaml
        * update .env (DEX_KPI_SECRET / OUTLINE_OIDC_CLIENT_SECRET)
        * docker compose up -d dex api outline
      - Section "Adding a new project collection" (WMP-02 + WMP-03 recipe):
        * Outline UI: New Collection → name → icon → members read+write
        * mirror the KPI Dashboard permission model
        * one collection per project (NOT nested)
      - Cross-link to DOC-01 Dev Setup
      - Known-limitations footer referencing D-10: "Dex staticPasswords does not provide silent cross-app SSO. Users may be prompted to log in once per app; credentials are shared. See `docs/setup.md` §Known limitations."
    Invariant: NO `ORG/REPO` placeholder anywhere — use resolved origin URL.
    Then: ⋯ → Download → Markdown → paste.
  </how-to-verify>
  <resume-signal>Paste exported markdown</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 2: Write DOC-08 snapshot + invariants</name>
  <files>docs/wiki-seed/08-admin-runbook.md</files>
  <action>
    Write pasted content with sanitization pass. Confirm:
    - Resolved repo URL present (github.com/johann-b82/kpi-dashboard)
    - No ORG/REPO placeholder
    - bcrypt, backup, rotate, collection recipe sections present
  </action>
  <verify>
    <automated>grep -q "github.com/johann-b82/kpi-dashboard" docs/wiki-seed/08-admin-runbook.md &amp;&amp; grep -qi "bcrypt" docs/wiki-seed/08-admin-runbook.md &amp;&amp; grep -qi "backup" docs/wiki-seed/08-admin-runbook.md &amp;&amp; grep -qi "rotat" docs/wiki-seed/08-admin-runbook.md &amp;&amp; grep -qi "collection" docs/wiki-seed/08-admin-runbook.md &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/08-admin-runbook.md</automated>
  </verify>
  <done>DOC-08 snapshot includes resolved origin URL, all required sections, text-only invariants clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-export DOC-09 landing page after all subpages exist + snapshot</name>
  <what-built>Updated landing page with all 8 wiki-links resolving.</what-built>
  <how-to-verify>
    Operator returns to the landing page in Outline. Previously-unresolved wiki-links (to DOC-03..DOC-08) should now show as resolved (blue/tooltip, not red/unresolved). Confirm visually. Make any cleanup edits (fix anything that still shows unresolved). Then: ⋯ → Download → Markdown → paste.
  </how-to-verify>
  <resume-signal>Paste refreshed exported markdown</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 4: Overwrite 00-landing.md snapshot + re-verify TOC</name>
  <files>docs/wiki-seed/00-landing.md</files>
  <action>Overwrite the landing snapshot with the re-exported content; sanitize. Confirm all 8 TOC entries still present.</action>
  <verify>
    <automated>for kw in "Dev Setup" "Architecture" "API Reference" "Personio" "Sales" "HR" "Settings" "Admin"; do grep -qi "$kw" docs/wiki-seed/00-landing.md || { echo "missing TOC entry: $kw"; exit 1; }; done &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/00-landing.md</automated>
  </verify>
  <done>Refreshed landing snapshot contains all 8 cross-links; invariants clean.</done>
</task>

</tasks>

<success_criteria>
DOC-08 authored with inline ops content + resolved origin URL; landing page re-snapshotted with resolved wiki-links.
</success_criteria>

<output>
Create `.planning/phases/31-seed-outline-docs/31-05-SUMMARY.md`.
</output>
