---
phase: 31-seed-outline-docs
plan: 03
type: execute
wave: 2
depends_on: ["31-01", "31-02"]
files_modified:
  - docs/wiki-seed/03-api-reference.md
  - docs/wiki-seed/04-personio-sync.md
autonomous: false
requirements: [DOC-03, DOC-04]
must_haves:
  truths:
    - "DOC-03 is thin (D-02): one intro paragraph pointing to https://kpi.internal/api/docs, one table of 7 tag groups, Auth + Error shape sections, NO per-endpoint examples"
    - "DOC-04 covers Personio credential test, sync-interval settings, manual refresh, common failure modes + resolutions"
    - "Both snapshots pass text-only invariants (D-07)"
  artifacts:
    - path: "docs/wiki-seed/03-api-reference.md"
      provides: "Thin API Reference"
      contains: "kpi.internal/api/docs"
    - path: "docs/wiki-seed/04-personio-sync.md"
      provides: "Personio Sync Runbook"
      contains: "Personio"
  key_links:
    - from: "docs/wiki-seed/03-api-reference.md"
      to: "https://kpi.internal/api/docs"
      via: "markdown link"
      pattern: "kpi\\.internal/api/docs"
---

<objective>
Author DOC-03 API Reference (thin per D-02) and DOC-04 Personio Sync Runbook in Outline, then snapshot to repo. DOC-03 depends on plan 31-02 (tags fix) so the 7-tag table is accurate.

Purpose: Cover API discovery and Personio operational guidance in wave 2.
Output: Two Outline pages + two markdown snapshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-seed-outline-docs/31-CONTEXT.md
@.planning/phases/31-seed-outline-docs/31-RESEARCH.md

<interfaces>
The 7 FastAPI tag groups (after plan 31-02 lands):

| Tag | Purpose (1-liner for DOC-03 table) |
|-----|-----|
| auth | Dex OIDC login/callback/me/logout |
| data | Raw ingested rows + lookup endpoints |
| hr-kpis | HR KPI aggregates (note: hyphen, not underscore) |
| kpis | Sales KPI aggregates |
| settings | Branding, colors, logo, Personio creds, HR targets |
| sync | Personio manual + scheduled refresh |
| uploads | CSV/TXT/Excel file ingestion |

DOC-04 grounding (RESEARCH §Content Grounding DOC-04):
- backend/app/services/personio_client.py — error classes
- .env.example — PERSONIO_* env vars
- frontend/src/locales/en.json — settings.personio.* labels
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Author DOC-03 API Reference (thin) in Outline + snapshot</name>
  <what-built>Thin DOC-03 per D-02.</what-built>
  <how-to-verify>
    Operator authors in Outline. Required structure (D-02):
      - One intro paragraph: live spec lives at https://kpi.internal/api/docs (FastAPI Swagger UI)
      - Table: 7 rows, one per tag (auth, data, hr-kpis, kpis, settings, sync, uploads) with 1-line description each; each row links to the Swagger section
      - Section "Auth pattern": OIDC session cookie (httpOnly, SameSite=Lax, Secure), `Depends(get_current_user)` guards all business routes, 401 if unauthenticated. Reference KPO-07.
      - Section "Error shape": FastAPI default `{"detail": "..."}` for 4xx/5xx
      - NO per-endpoint request/response examples
      - Cross-link to DOC-02 Architecture via `[[Docker Compose Architecture]]`
    Then: ⋯ → Download → Markdown → paste.
  </how-to-verify>
  <resume-signal>Paste exported markdown</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 2: Write DOC-03 snapshot + invariants</name>
  <files>docs/wiki-seed/03-api-reference.md</files>
  <action>
    Write pasted DOC-03 content to docs/wiki-seed/03-api-reference.md. Sanitization: strip `![` / `upload://`, rewrite absolute cross-links.
    Verify all 7 tag names are present and the live-spec URL is cited.
  </action>
  <verify>
    <automated>for tag in auth data hr-kpis kpis settings sync uploads; do grep -q "$tag" docs/wiki-seed/03-api-reference.md || { echo "missing tag $tag"; exit 1; }; done &amp;&amp; grep -q "kpi.internal/api/docs" docs/wiki-seed/03-api-reference.md &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/03-api-reference.md</automated>
  </verify>
  <done>DOC-03 snapshot contains all 7 tags, live-spec link, text-only invariants clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author DOC-04 Personio Sync Runbook in Outline + snapshot</name>
  <what-built>Operator runbook for Personio integration.</what-built>
  <how-to-verify>
    Operator authors in Outline. Required sections (RESEARCH §Content Grounding DOC-04):
      - Intro: what Personio sync does, where creds are stored (Settings → HR → Personio)
      - Credential test: Settings → HR → Personio → "Test connection" button behavior
      - Sync-interval settings: what the interval knob does, defaults
      - Manual refresh: "Refresh now" button behavior
      - Common failure modes + resolutions:
        - Credential expiry (rotate in Personio, paste new Client ID/Secret in Settings)
        - Rate limits (Personio API throttles; back off and retry)
        - Option-set changes (employee status/department enums changed upstream → re-sync required)
      - Cross-link to DOC-07 Settings Walkthrough
    Then: ⋯ → Download → Markdown → paste.
  </how-to-verify>
  <resume-signal>Paste exported markdown</resume-signal>
  <action>Operator performs the authoring / UAT described in this checkpoint. See &lt;what-built&gt; and &lt;how-to-verify&gt; for the exact steps. Executor waits for operator paste/signal, then proceeds to the next task (snapshot write or follow-up UAT).</action>
</task>

<task type="auto">
  <name>Task 4: Write DOC-04 snapshot + invariants</name>
  <files>docs/wiki-seed/04-personio-sync.md</files>
  <action>Write pasted content with sanitization pass.</action>
  <verify>
    <automated>grep -qi "Personio" docs/wiki-seed/04-personio-sync.md &amp;&amp; grep -qi "credential" docs/wiki-seed/04-personio-sync.md &amp;&amp; grep -qi "refresh" docs/wiki-seed/04-personio-sync.md &amp;&amp; ! grep -qE '!\[|upload://|ORG/REPO' docs/wiki-seed/04-personio-sync.md</automated>
  </verify>
  <done>DOC-04 snapshot covers Personio credential/refresh/failure topics, invariants clean.</done>
</task>

</tasks>

<success_criteria>
DOC-03 thin page + DOC-04 runbook authored in Outline and snapshotted to repo.
</success_criteria>

<output>
Create `.planning/phases/31-seed-outline-docs/31-03-SUMMARY.md`.
</output>
