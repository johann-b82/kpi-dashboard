---
phase: 16-i18n-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/locales/en.json
  - frontend/src/locales/de.json
  - frontend/src/components/settings/PersonioCard.tsx
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "Every visible string in PersonioCard renders in English when language is EN"
    - "Every visible string in PersonioCard renders in German (proper UTF-8 umlauts) when language is DE"
    - "Switching language via NavBar toggle re-renders all PersonioCard strings without page refresh"
    - "en.json and de.json have identical key sets (parity check passes)"
    - "Dead key hr.placeholder is removed from both locale files"
  artifacts:
    - path: "frontend/src/locales/en.json"
      provides: "24 new settings.personio.* keys, hr.placeholder removed"
      contains: "settings.personio.title"
    - path: "frontend/src/locales/de.json"
      provides: "24 new settings.personio.* keys with proper UTF-8 umlauts, hr.placeholder removed"
      contains: "settings.personio.sync_interval.hourly"
    - path: "frontend/src/components/settings/PersonioCard.tsx"
      provides: "useTranslation hook, all t() calls, INTERVAL_OPTIONS inside component"
      contains: "useTranslation"
  key_links:
    - from: "frontend/src/components/settings/PersonioCard.tsx"
      to: "frontend/src/locales/en.json"
      via: "t() calls matching settings.personio.* keys"
      pattern: 't\\("settings\\.personio\\.'
    - from: "frontend/src/components/settings/PersonioCard.tsx"
      to: "frontend/src/locales/de.json"
      via: "t() calls matching settings.personio.* keys"
      pattern: 't\\("settings\\.personio\\.'
---

<objective>
Extract all hardcoded strings from PersonioCard.tsx into i18n locale files, add full DE/EN translations for 24 new keys, remove the dead `hr.placeholder` key, and fix ASCII-safe umlaut substitutes in German values. This completes I18N-01 — full DE/EN parity for all v1.3 strings.

Purpose: The PersonioCard is the only v1.3 component without i18n. After this plan, every visible string in the app has a translation in both languages.
Output: Updated en.json (164 keys), updated de.json (164 keys), PersonioCard.tsx with useTranslation wired in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-i18n-polish/16-CONTEXT.md
@.planning/phases/16-i18n-polish/16-RESEARCH.md

@frontend/src/locales/en.json
@frontend/src/locales/de.json
@frontend/src/components/settings/PersonioCard.tsx

<interfaces>
<!-- Reference pattern from existing i18n'd component -->
From frontend/src/components/dashboard/HrKpiCardGrid.tsx:
```tsx
import { useTranslation } from "react-i18next";
export function HrKpiCardGrid() {
  const { t } = useTranslation();
  // t("hr.kpi.overtimeRatio.label") etc.
}
```

From frontend/scripts/check-locale-parity.mts:
```bash
node --experimental-strip-types frontend/scripts/check-locale-parity.mts
# Exits 0: PARITY OK: {N} keys in both en.json and de.json
# Exits 1: prints MISSING_IN_DE / MISSING_IN_EN lines
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 24 settings.personio.* keys to both locale files and remove dead key</name>
  <files>frontend/src/locales/en.json, frontend/src/locales/de.json</files>
  <read_first>
    - frontend/src/locales/en.json (current 143 keys — see full state before editing)
    - frontend/src/locales/de.json (current 143 keys — see full state before editing)
    - frontend/src/components/settings/PersonioCard.tsx (source of hardcoded strings to extract)
  </read_first>
  <action>
Edit both locale files. Per D-01, all new keys use the `settings.personio.*` namespace. Per D-03, DE values use proper UTF-8 umlauts (not ASCII-safe substitutes). Per D-08, remove the dead `hr.placeholder` key from both files.

**In en.json — ADD these 24 keys** (insert after the last `settings.preferences.*` key, before the closing brace):

```json
"settings.personio.title": "Personio",
"settings.personio.client_id.label": "Client ID",
"settings.personio.client_id.placeholder": "Enter Client ID",
"settings.personio.client_id.saved_hint": "Saved — enter a new value to change",
"settings.personio.client_secret.label": "Client Secret",
"settings.personio.client_secret.placeholder": "Enter Client Secret",
"settings.personio.client_secret.saved_hint": "Saved — enter a new value to change",
"settings.personio.test_connection.button": "Test connection",
"settings.personio.test_connection.testing": "Testing…",
"settings.personio.test_connection.success": "Connection successful",
"settings.personio.test_connection.failure": "Connection failed",
"settings.personio.test_connection.error_fallback": "Connection test failed",
"settings.personio.credentials.configure_hint": "Configure Personio credentials",
"settings.personio.sync_interval.label": "Sync interval",
"settings.personio.sync_interval.manual": "Manual only",
"settings.personio.sync_interval.hourly": "Hourly",
"settings.personio.sync_interval.every6h": "Every 6 hours",
"settings.personio.sync_interval.daily": "Daily",
"settings.personio.sick_leave_type.label": "Sick leave type",
"settings.personio.sick_leave_type.placeholder": "Select absence type",
"settings.personio.production_dept.label": "Production department",
"settings.personio.production_dept.placeholder": "Select department",
"settings.personio.skill_attr_key.label": "Skill custom attribute key",
"settings.personio.skill_attr_key.placeholder": "e.g. dynamic_12345"
```

**In en.json — REMOVE** the line: `"hr.placeholder": "KPI cards coming soon",`

**In de.json — ADD these 24 keys** (same position):

```json
"settings.personio.title": "Personio",
"settings.personio.client_id.label": "Client-ID",
"settings.personio.client_id.placeholder": "Client-ID eingeben",
"settings.personio.client_id.saved_hint": "Gespeichert – zum Ändern neuen Wert eingeben",
"settings.personio.client_secret.label": "Client-Secret",
"settings.personio.client_secret.placeholder": "Client-Secret eingeben",
"settings.personio.client_secret.saved_hint": "Gespeichert – zum Ändern neuen Wert eingeben",
"settings.personio.test_connection.button": "Verbindung testen",
"settings.personio.test_connection.testing": "Teste…",
"settings.personio.test_connection.success": "Verbindung erfolgreich",
"settings.personio.test_connection.failure": "Verbindung fehlgeschlagen",
"settings.personio.test_connection.error_fallback": "Verbindungstest fehlgeschlagen",
"settings.personio.credentials.configure_hint": "Personio-Zugangsdaten konfigurieren",
"settings.personio.sync_interval.label": "Sync-Intervall",
"settings.personio.sync_interval.manual": "Nur manuell",
"settings.personio.sync_interval.hourly": "Stündlich",
"settings.personio.sync_interval.every6h": "Alle 6 Stunden",
"settings.personio.sync_interval.daily": "Täglich",
"settings.personio.sick_leave_type.label": "Krankheitstyp",
"settings.personio.sick_leave_type.placeholder": "Abwesenheitstyp wählen",
"settings.personio.production_dept.label": "Produktions-Abteilung",
"settings.personio.production_dept.placeholder": "Abteilung wählen",
"settings.personio.skill_attr_key.label": "Skill-Attribut-Schlüssel",
"settings.personio.skill_attr_key.placeholder": "z.B. dynamic_12345"
```

**In de.json — REMOVE** the line: `"hr.placeholder": "KPI-Karten folgen in Kuerze",`

**Per D-02:** EN interval labels are exactly: "Manual only", "Hourly", "Every 6 hours", "Daily".
**Per D-03:** DE values use proper UTF-8: "Stündlich" (not "Stuendlich"), "Ändern" (not "Aendern"), "wählen" (not "waehlen"), "Täglich" (not "Taeglich").

After editing, each locale file should have exactly 164 keys (143 - 1 dead + 24 new = 166 — wait, 143 - 1 + 24 = 166). Actually: current files have 143 keys each. Remove 1 dead key = 142. Add 24 new = 166 keys each.

Correction on count: the research doc says 141 keys but the actual files have 143 keys (verified by line count). So: 143 - 1 + 24 = 166 keys each.
  </action>
  <verify>
    <automated>node --experimental-strip-types frontend/scripts/check-locale-parity.mts</automated>
  </verify>
  <acceptance_criteria>
    - en.json contains `"settings.personio.title": "Personio"`
    - en.json contains `"settings.personio.sync_interval.hourly": "Hourly"`
    - en.json contains `"settings.personio.client_id.saved_hint": "Saved`
    - en.json does NOT contain `"hr.placeholder"`
    - de.json contains `"settings.personio.sync_interval.hourly": "Stündlich"` (proper umlaut, not "Stuendlich")
    - de.json contains `"settings.personio.client_id.saved_hint": "Gespeichert – zum Ändern` (proper umlaut, not "Aendern")
    - de.json contains `"settings.personio.sick_leave_type.placeholder": "Abwesenheitstyp wählen"` (proper umlaut, not "waehlen")
    - de.json contains `"settings.personio.sync_interval.daily": "Täglich"` (proper umlaut, not "Taeglich")
    - de.json does NOT contain `"hr.placeholder"`
    - `node --experimental-strip-types frontend/scripts/check-locale-parity.mts` exits 0 with "PARITY OK"
  </acceptance_criteria>
  <done>Both locale files have 24 new settings.personio.* keys with correct EN and DE values, hr.placeholder is removed, parity check passes with identical key counts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire useTranslation into PersonioCard.tsx — replace all hardcoded strings with t() calls</name>
  <files>frontend/src/components/settings/PersonioCard.tsx</files>
  <read_first>
    - frontend/src/components/settings/PersonioCard.tsx (current file with ~20 hardcoded strings)
    - frontend/src/locales/en.json (to verify keys match after Task 1)
    - frontend/src/components/dashboard/HrKpiCardGrid.tsx (reference pattern for useTranslation usage)
  </read_first>
  <action>
Modify PersonioCard.tsx per D-04 to replace every hardcoded string with a `t()` call.

**1. Add import** — add `useTranslation` to imports:
```tsx
import { useTranslation } from "react-i18next";
```

**2. Add hook call** — first line inside the `PersonioCard` function body:
```tsx
const { t } = useTranslation();
```

**3. Move INTERVAL_OPTIONS inside component** — delete the module-scope `const INTERVAL_OPTIONS` (lines 17-22) and declare it inside the component body, AFTER the `useTranslation()` call:
```tsx
const INTERVAL_OPTIONS: Array<{ value: 0 | 1 | 6 | 24; label: string }> = [
  { value: 0, label: t("settings.personio.sync_interval.manual") },
  { value: 1, label: t("settings.personio.sync_interval.hourly") },
  { value: 6, label: t("settings.personio.sync_interval.every6h") },
  { value: 24, label: t("settings.personio.sync_interval.daily") },
];
```

**4. Replace all hardcoded strings** — exact substitutions by line/location:

| Location | Old | New |
|----------|-----|-----|
| Line 58 (catch block fallback) | `"Connection test failed"` | `t("settings.personio.test_connection.error_fallback")` |
| Line 66 (noCredentialsHint) | `"Personio-Zugangsdaten konfigurieren"` | `t("settings.personio.credentials.configure_hint")` |
| Line 72 (CardTitle) | `Personio` | `{t("settings.personio.title")}` |
| Line 79 (Label) | `Client-ID` | `{t("settings.personio.client_id.label")}` |
| Line 87 (placeholder) | `"Client-ID eingeben"` | `t("settings.personio.client_id.placeholder")` |
| Line 91 (saved hint) | `Gespeichert - zum Aendern neuen Wert eingeben` | `{t("settings.personio.client_id.saved_hint")}` |
| Line 99 (Label) | `Client-Secret` | `{t("settings.personio.client_secret.label")}` |
| Line 107 (placeholder) | `"Client-Secret eingeben"` | `t("settings.personio.client_secret.placeholder")` |
| Line 111 (saved hint) | `Gespeichert - zum Aendern neuen Wert eingeben` | `{t("settings.personio.client_secret.saved_hint")}` |
| Line 124 (testing ternary) | `"Teste..."` | `t("settings.personio.test_connection.testing")` |
| Line 124 (testing ternary) | `"Verbindung testen"` | `t("settings.personio.test_connection.button")` |
| Line 135 (success text) | `"Verbindung erfolgreich"` | `t("settings.personio.test_connection.success")` |
| Line 136 (failure fallback) | `"Verbindung fehlgeschlagen"` | `t("settings.personio.test_connection.failure")` |
| Line 144 (Label) | `Sync-Intervall` | `{t("settings.personio.sync_interval.label")}` |
| Line 168 (Label) | `Krankheitstyp` | `{t("settings.personio.sick_leave_type.label")}` |
| Line 179 (placeholder option) | `Abwesenheitstyp waehlen` | `{t("settings.personio.sick_leave_type.placeholder")}` |
| Line 196 (Label) | `Produktions-Abteilung` | `{t("settings.personio.production_dept.label")}` |
| Line 207 (placeholder option) | `Abteilung waehlen` | `{t("settings.personio.production_dept.placeholder")}` |
| Line 224 (Label) | `Skill Custom Attribute Key` | `{t("settings.personio.skill_attr_key.label")}` |
| Line 232 (placeholder) | `"z.B. dynamic_12345"` | `t("settings.personio.skill_attr_key.placeholder")` |

**Critical pitfalls to avoid:**
- Do NOT leave INTERVAL_OPTIONS at module scope — it MUST be inside the component function body so `t()` re-evaluates on language change
- Do NOT miss the catch-block fallback on line 58 (`"Connection test failed"`) — it's in JS logic, not JSX
- Do NOT miss BOTH saved_hint occurrences (lines 91 AND 111) — they have identical text but are separate JSX elements
- Do NOT miss the noCredentialsHint on line 66 — it's a JS expression, not JSX children
  </action>
  <verify>
    <automated>cd /Users/johannbechtold/Documents/acm-kpi-light && npx tsc --noEmit --project frontend/tsconfig.app.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - PersonioCard.tsx contains `import { useTranslation } from "react-i18next"`
    - PersonioCard.tsx contains `const { t } = useTranslation()`
    - PersonioCard.tsx contains `t("settings.personio.sync_interval.manual")`
    - PersonioCard.tsx contains `t("settings.personio.test_connection.error_fallback")`
    - PersonioCard.tsx contains `t("settings.personio.credentials.configure_hint")`
    - PersonioCard.tsx contains `t("settings.personio.client_id.saved_hint")`
    - PersonioCard.tsx contains `t("settings.personio.client_secret.saved_hint")`
    - PersonioCard.tsx does NOT contain the string `"Nur manuell"` (old hardcoded interval label)
    - PersonioCard.tsx does NOT contain the string `"Stuendlich"` (old ASCII-safe umlaut)
    - PersonioCard.tsx does NOT contain the string `"Verbindung testen"` as a raw string (should be wrapped in t())
    - PersonioCard.tsx does NOT contain the string `"Connection test failed"` as a raw string (should be wrapped in t())
    - PersonioCard.tsx does NOT contain the string `"Aendern"` anywhere (old ASCII-safe umlaut)
    - INTERVAL_OPTIONS is declared INSIDE the PersonioCard function body (not at module scope)
    - TypeScript compilation (`npx tsc --noEmit`) passes with no errors
  </acceptance_criteria>
  <done>PersonioCard.tsx has useTranslation wired in, all 20+ hardcoded strings replaced with t() calls, INTERVAL_OPTIONS moved inside component body, TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. Locale parity: `node --experimental-strip-types frontend/scripts/check-locale-parity.mts` exits 0
2. TypeScript: `npx tsc --noEmit --project frontend/tsconfig.app.json` exits 0
3. No hardcoded German strings remain in PersonioCard.tsx: `grep -E "(Stuendlich|Taeglich|Aendern|waehlen|Nur manuell|Verbindung testen|Krankheitstyp)" frontend/src/components/settings/PersonioCard.tsx` returns empty
4. All 24 new keys present: `grep -c "settings.personio" frontend/src/locales/en.json` returns 24
</verification>

<success_criteria>
- Parity check script passes (exits 0) with matching key counts in en.json and de.json
- Zero hardcoded user-visible strings remain in PersonioCard.tsx
- TypeScript compilation passes
- All 24 settings.personio.* keys present in both locale files
- Dead hr.placeholder key removed from both locale files
- German locale uses proper UTF-8 umlauts (no ASCII-safe substitutes)
</success_criteria>

<output>
After completion, create `.planning/phases/16-i18n-polish/16-01-SUMMARY.md`
</output>
