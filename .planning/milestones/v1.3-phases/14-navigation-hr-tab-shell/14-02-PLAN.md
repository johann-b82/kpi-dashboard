---
phase: 14-navigation-hr-tab-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - frontend/src/components/NavBar.tsx
  - frontend/src/pages/HRPage.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Clicking 'Sales' in NavBar navigates to / and shows the existing dashboard"
    - "Clicking 'HR' in NavBar navigates to /hr and shows the HR page shell"
    - "NavBar upload FreshnessIndicator is visible on / and /upload but hidden on /hr and /settings"
    - "HR page shows last sync timestamp when a sync has occurred"
    - "HR page shows 'Not yet synced' with Settings link when no sync has run"
    - "Clicking 'Daten aktualisieren' triggers POST /api/sync with loading spinner and success/error feedback"
    - "HR page shows placeholder text for future KPI cards"
  artifacts:
    - path: "frontend/src/pages/HRPage.tsx"
      provides: "HR dashboard page shell with sync freshness and manual sync button"
      min_lines: 50
    - path: "frontend/src/components/NavBar.tsx"
      provides: "Updated NavBar with Sales link, HR link, conditional FreshnessIndicator"
      contains: "nav.sales"
    - path: "frontend/src/App.tsx"
      provides: "Route for /hr pointing to HRPage"
      contains: 'path="/hr"'
  key_links:
    - from: "frontend/src/pages/HRPage.tsx"
      to: "/api/sync/meta"
      via: "useQuery with syncKeys.meta()"
      pattern: "syncKeys\\.meta"
    - from: "frontend/src/pages/HRPage.tsx"
      to: "/api/sync"
      via: "useMutation with triggerSync"
      pattern: "triggerSync"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/HRPage.tsx"
      via: "Route component prop"
      pattern: "HRPage"
    - from: "frontend/src/components/NavBar.tsx"
      to: "frontend/src/locales/*.json"
      via: "t('nav.sales') and t('nav.hr')"
      pattern: "nav\\.sales|nav\\.hr"
---

<objective>
NavBar updates (Sales rename, HR link, conditional FreshnessIndicator) and the HRPage component with sync freshness and manual sync button.

Purpose: Complete the user-visible Phase 14 deliverables -- renamed Sales tab (NAV-01), new HR tab (NAV-02), and HR page shell with sync freshness indicator (NAV-03) and manual sync trigger (D-07).
Output: Working multi-tab navigation with HR page shell showing sync status and "Daten aktualisieren" button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-navigation-hr-tab-shell/14-CONTEXT.md
@.planning/phases/14-navigation-hr-tab-shell/14-RESEARCH.md
@.planning/phases/14-navigation-hr-tab-shell/14-01-SUMMARY.md

@frontend/src/components/NavBar.tsx
@frontend/src/App.tsx
@frontend/src/components/dashboard/FreshnessIndicator.tsx
@frontend/src/pages/DashboardPage.tsx

<interfaces>
<!-- From Plan 01 outputs — frontend/src/lib/api.ts -->
export interface SyncMetaResponse {
  last_synced_at: string | null;
  last_sync_status: "ok" | "error" | null;
  last_sync_error: string | null;
}
export async function fetchSyncMeta(): Promise<SyncMetaResponse>;
export async function triggerSync(): Promise<SyncResult>;

<!-- From Plan 01 outputs — frontend/src/lib/queryKeys.ts -->
export const syncKeys = {
  meta: () => ["sync", "meta"] as const,
};

<!-- From Plan 01 outputs — locale keys available -->
EN: nav.sales="Sales", nav.hr="HR", hr.sync.lastSynced="Last sync:",
    hr.sync.never="Not yet synced", hr.sync.configureHint="Configure Personio credentials in Settings",
    hr.sync.button="Refresh data", hr.sync.success="Sync complete",
    hr.sync.error="Sync failed", hr.placeholder="KPI cards coming soon"
DE: nav.sales="Vertrieb", nav.hr="HR", hr.sync.lastSynced="Letzte Synchronisierung:",
    hr.sync.never="Noch nicht synchronisiert", hr.sync.configureHint="Personio-Zugangsdaten in den Einstellungen konfigurieren",
    hr.sync.button="Daten aktualisieren", hr.sync.success="Synchronisierung abgeschlossen",
    hr.sync.error="Synchronisierung fehlgeschlagen", hr.placeholder="KPI-Karten folgen in Kuerze"

<!-- Existing NavBar pattern — linkClass helper (line 19-23) -->
const linkClass = (active: boolean) =>
  "text-sm " + (active
    ? "text-primary font-semibold border-b-2 border-primary pb-1"
    : "text-foreground hover:text-primary");

<!-- Existing FreshnessIndicator Intl.DateTimeFormat pattern (lines 23-27) -->
const locale = i18n.language === "de" ? "de-DE" : "en-US";
const formatted = new Intl.DateTimeFormat(locale, {
  dateStyle: "short", timeStyle: "short",
}).format(new Date(data.uploaded_at));

<!-- Existing App.tsx route pattern (lines 19-22) -->
<Switch>
  <Route path="/" component={DashboardPage} />
  <Route path="/upload" component={UploadPage} />
  <Route path="/settings" component={SettingsPage} />
</Switch>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NavBar updates and HRPage component (NAV-01, NAV-02, NAV-03)</name>
  <files>frontend/src/components/NavBar.tsx, frontend/src/pages/HRPage.tsx, frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/components/NavBar.tsx (current NavBar — modify link labels, add HR link, conditionalize FreshnessIndicator)
    - frontend/src/App.tsx (add /hr route)
    - frontend/src/components/dashboard/FreshnessIndicator.tsx (pattern reference for Intl.DateTimeFormat and useQuery)
    - frontend/src/pages/DashboardPage.tsx (reference for page layout wrapper pattern)
    - frontend/src/lib/api.ts (verify fetchSyncMeta, triggerSync, SyncMetaResponse exports exist from Plan 01)
    - frontend/src/lib/queryKeys.ts (verify syncKeys export exists from Plan 01)
    - frontend/src/locales/en.json (verify nav.sales and hr.* keys exist from Plan 01)
  </read_first>
  <action>
    **1. NavBar.tsx — Three changes (per D-01, D-02, D-10, D-12):**

    a) Change the Dashboard link text from `t("nav.dashboard")` to `t("nav.sales")` on line 45. The `href="/"` stays the same (per D-01 — no route change). Do NOT rename the file or component.

    b) Add an HR tab link after the Upload link (after line 48), using the same `linkClass` pattern:
    ```tsx
    <Link href="/hr" className={linkClass(location === "/hr")}>
      {t("nav.hr")}
    </Link>
    ```

    c) Make FreshnessIndicator conditional on route (per D-10, D-12). Add a route guard using the existing `location` variable (already destructured on line 11):
    ```tsx
    const showUploadFreshness = location === "/" || location === "/upload";
    ```
    Then wrap the FreshnessIndicator in the JSX (line 52) with:
    ```tsx
    {showUploadFreshness && <FreshnessIndicator />}
    ```

    **2. HRPage.tsx — New file (per D-04, D-05, D-06, D-07, D-08, D-09):**

    Create `frontend/src/pages/HRPage.tsx` with this structure:
    - Import: `useState` from react, `useMutation`, `useQuery`, `useQueryClient` from @tanstack/react-query, `useTranslation` from react-i18next, `Link` from wouter, `fetchSyncMeta`, `triggerSync` from @/lib/api, `syncKeys` from @/lib/queryKeys, `Loader2`, `CheckCircle2` from lucide-react
    - Export named function `HRPage`
    - Use `useQuery({ queryKey: syncKeys.meta(), queryFn: fetchSyncMeta })` for sync meta data
    - Use `useState<"idle" | "success" | "error">("idle")` for sync feedback state
    - Use `useState<string | null>(null)` for sync error message
    - Use `useMutation({ mutationFn: triggerSync, onSuccess: ..., onError: ... })`:
      - onSuccess: invalidate `syncKeys.meta()`, set feedback to "success", setTimeout 3000ms to reset to "idle"
      - onError: set feedback to "error", set error message from `err.message`
    - Timestamp formatting: reuse FreshnessIndicator pattern with `Intl.DateTimeFormat(locale, { dateStyle: "short", timeStyle: "short" })`
    - Layout wrapper: `<div className="max-w-7xl mx-auto px-6 py-8">` (per D-05)
    - Sync toolbar row (per D-06): `<div className="flex justify-end items-center gap-3 mb-8">`
      - When `meta?.last_synced_at == null` (never synced, per D-08):
        ```tsx
        <span className="text-xs text-muted-foreground">
          {t("hr.sync.never")} —{" "}
          <Link href="/settings" className="underline">{t("hr.sync.configureHint")}</Link>
        </span>
        ```
      - When synced:
        ```tsx
        <span className="text-xs text-muted-foreground">
          {t("hr.sync.lastSynced")} {formatted}
        </span>
        ```
      - Sync button (per D-07): `<button>` with:
        - `onClick={() => { setSyncFeedback("idle"); setSyncError(null); mutation.mutate(); }}`
        - `disabled={mutation.isPending}`
        - Tailwind classes: `inline-flex items-center gap-2 rounded-md border border-border bg-card px-3 py-1.5 text-sm hover:bg-accent/10 transition-colors disabled:opacity-50`
        - Content: When `mutation.isPending`: show `<Loader2 className="h-4 w-4 animate-spin" />` + `t("hr.sync.button")`
        - When `syncFeedback === "success"`: show `<CheckCircle2 className="h-4 w-4 text-green-600" />` + `t("hr.sync.success")`
        - Default: show `t("hr.sync.button")`
      - Error display (per D-07): When `syncFeedback === "error"`:
        ```tsx
        <span className="text-xs text-destructive">
          {t("hr.sync.error")}{syncError ? `: ${syncError}` : ""}
        </span>
        ```
    - Placeholder content (per D-04): Below the toolbar:
      ```tsx
      <p className="text-sm text-muted-foreground">{t("hr.placeholder")}</p>
      ```

    **3. App.tsx — Add /hr route (per D-01):**

    - Add import: `import { HRPage } from "./pages/HRPage";`
    - Add route inside `<Switch>` after the `/upload` route and before `/settings`:
      ```tsx
      <Route path="/hr" component={HRPage} />
      ```
  </action>
  <verify>
    <automated>cd /Users/johannbechtold/Documents/acm-kpi-light/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/NavBar.tsx contains `t("nav.sales")` (not `t("nav.dashboard")`)
    - frontend/src/components/NavBar.tsx contains `href="/hr"`
    - frontend/src/components/NavBar.tsx contains `t("nav.hr")`
    - frontend/src/components/NavBar.tsx contains `showUploadFreshness` variable
    - frontend/src/components/NavBar.tsx contains `{showUploadFreshness && <FreshnessIndicator`
    - frontend/src/pages/HRPage.tsx exists and exports `function HRPage`
    - frontend/src/pages/HRPage.tsx contains `syncKeys.meta()`
    - frontend/src/pages/HRPage.tsx contains `fetchSyncMeta`
    - frontend/src/pages/HRPage.tsx contains `triggerSync`
    - frontend/src/pages/HRPage.tsx contains `mutation.isPending`
    - frontend/src/pages/HRPage.tsx contains `t("hr.sync.lastSynced")`
    - frontend/src/pages/HRPage.tsx contains `t("hr.sync.never")`
    - frontend/src/pages/HRPage.tsx contains `t("hr.sync.button")`
    - frontend/src/pages/HRPage.tsx contains `t("hr.placeholder")`
    - frontend/src/pages/HRPage.tsx contains `Intl.DateTimeFormat`
    - frontend/src/pages/HRPage.tsx contains `max-w-7xl mx-auto px-6 py-8`
    - frontend/src/pages/HRPage.tsx contains `href="/settings"` (configure hint link)
    - frontend/src/App.tsx contains `import { HRPage }`
    - frontend/src/App.tsx contains `path="/hr"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>NavBar shows "Sales" (not "Dashboard") and "HR" tabs. FreshnessIndicator only visible on / and /upload. Clicking HR navigates to /hr showing HRPage with sync freshness, "Daten aktualisieren" button with loading/success/error states, and placeholder text. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of navigation and HR page shell</name>
  <what-built>
    Multi-tab navigation with renamed Sales tab, new HR tab, contextual FreshnessIndicator, and HR page shell with sync freshness indicator and manual sync button.
  </what-built>
  <how-to-verify>
    1. Open the app in browser (http://localhost:5173 or Docker URL)
    2. **NavBar tabs:** Verify "Sales" and "HR" tabs are visible (not "Dashboard"). Verify "Upload" and Settings gear are still present.
    3. **Sales tab:** Click "Sales" -- should navigate to / and show the existing sales dashboard. Verify FreshnessIndicator (upload timestamp) is visible in NavBar.
    4. **Upload page:** Click "Upload" -- verify FreshnessIndicator is still visible in NavBar.
    5. **HR tab:** Click "HR" -- should navigate to /hr. Verify:
       - FreshnessIndicator is NOT visible in NavBar (per D-10)
       - If no sync has run: shows "Not yet synced" / "Noch nicht synchronisiert" with link to Settings
       - If sync has run: shows "Last sync: [timestamp]" / "Letzte Synchronisierung: [timestamp]"
       - "Refresh data" / "Daten aktualisieren" button is present
       - Placeholder text "KPI cards coming soon" / "KPI-Karten folgen in Kuerze" is visible
    6. **Manual sync:** Click "Daten aktualisieren":
       - Button should show spinner and disable during sync
       - On success: green checkmark briefly, then reverts
       - Timestamp updates after sync completes
    7. **Settings page:** Navigate to Settings -- verify FreshnessIndicator is NOT visible in NavBar.
    8. **Language toggle:** Switch language and verify all new strings render in the other language.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- NavBar shows "Sales" (not "Dashboard") and "HR" tabs
- FreshnessIndicator conditionally visible (only on / and /upload)
- /hr route loads HRPage with sync freshness and manual sync button
- Manual sync triggers POST /api/sync with visual feedback
- All strings render in both EN and DE
- TypeScript compiles without errors
</verification>

<success_criteria>
- NAV-01: "Dashboard" tab renamed to "Sales" / "Vertrieb" in both languages
- NAV-02: "HR" tab visible in NavBar, navigates to /hr, shows HRPage
- NAV-03: HR page displays last Personio sync timestamp (or never-synced state)
- D-07: Manual sync button with loading/success/error feedback
- D-10: FreshnessIndicator hidden on /hr and /settings
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/14-navigation-hr-tab-shell/14-02-SUMMARY.md`
</output>
