---
milestone: v1.20
milestone_name: HR Date-Range Filter + TS Cleanup
audited: 2026-04-22T17:20:00Z
status: passed
audit_method: lightweight inline
scores:
  phases: 2/2
  plans: 5/5
  integration: passed (no cross-phase wiring)
  flows: passed (Phase 60 human visual-parity approved; Phase 61 mechanical)
scope:
  phases: [60, 61]
  out_of_scope: []
nyquist:
  enabled: false
  note: "workflow.nyquist_validation = false; skipped."
gaps: []
tech_debt: []
requirements_tracking:
  note: "No REQUIREMENTS.md existed for v1.20 — the milestone grew organically from a Phase 60 roadmap entry (\"post-v1.19\") and the Phase 61 TS-cleanup tech-debt carry-forward from v1.19's audit. Decisions were captured inline in Phase 60 CONTEXT (D-01..D-13) and Phase 61 CONTEXT (D-01..D-08) rather than in a milestone REQUIREMENTS.md. Future milestones should start with /gsd:new-milestone to formalise requirements upfront."
---

# Milestone v1.20 — HR Date-Range Filter + TS Cleanup Audit

**Scope:** Phases 60–61 (2 phases).
**Status:** ✅ **Passed** — ready for `/gsd:complete-milestone 1.20`.

## Scope & method

Milestone v1.20 is the pragmatic bundle: **(a)** Phase 60 HR Date-Range Filter (originally "post-v1.19") and **(b)** Phase 61 TS Cleanup (pre-existing carry-forward flagged in v1.19-MILESTONE-AUDIT). The two phases have zero file overlap and zero cross-phase wiring, so a full `gsd-integration-checker` pass was skipped in favour of this lightweight audit.

Audit sources:
- Phase 60 SUMMARYs (60-01..60-04) + follow-up commit `4d1c5f0`
- Phase 61 SUMMARY (61-01)
- Phase 60 CONTEXT (decisions D-01..D-13)
- Phase 61 CONTEXT (decisions D-01..D-08)
- Human sign-off on Plan 60-04 Task 2 (2026-04-22)

Nyquist disabled in `config.json`; skipped.

## Phase verification summary

| Phase | Name | Verified | Status | Notes |
|------:|------|----------|--------|-------|
| 60 | HR Date-Range Filter | 2026-04-22 (Plan 60-04 Task 2 user-approved) | passed | 4/4 plans; backend + frontend + 13/13 pytest; follow-up `4d1c5f0` added Personio full-backfill, weekly default, 429 backoff, HR delta parity, chart-axis naming, `/sales`-only upload, Balken/Linien toggle ordering. |
| 61 | TS Cleanup | 2026-04-22 (executor Task 3 gate) | passed | 1/1 plan; 31 TS errors → 0; 10 atomic `fix(61):` commits; `npm run build` exits 0; suppression audit + `\|\| true` audit both clean. `useTableState.ts` untouched (D-02 preserved). |

Aggregate: **2/2 phases passed**, **5/5 plans complete**.

## Requirements coverage

No REQUIREMENTS.md existed for v1.20. Decisions were captured per-phase:

| Phase | Decisions | Evidence |
|------|-----------|----------|
| 60 | D-01..D-13 | 60-CONTEXT.md + per-plan SUMMARY files cite coverage |
| 61 | D-01..D-08 | 61-CONTEXT.md; Plan 61-01 SUMMARY confirms all 8 enforced in verify blocks |

Phase 60 decisions all validated by Plan 60-04's pytest (13/13) + human visual-parity checkpoint (approved 2026-04-22). Phase 61 decisions all enforced by atomic commits + build gate + anti-cheat audits.

## Cross-phase integration

Phase 60 and Phase 61 have **zero file overlap**:

- Phase 60 files: `backend/app/routers/hr_kpis.py`, `backend/app/routers/data.py`, `backend/app/services/hr_sync.py`, `backend/app/services/personio_client.py`, `backend/app/services/hr_kpi_aggregation.py`, `backend/app/models/_base.py`, `backend/alembic/versions/v1_19_personio_weekly_default.py`, `backend/tests/test_hr_kpi_range.py`, `frontend/src/components/SubHeader.tsx`, `frontend/src/components/dashboard/HrKpiCardGrid.tsx`, `frontend/src/components/dashboard/HrKpiCharts.tsx`, `frontend/src/components/dashboard/EmployeeTable.tsx`, `frontend/src/components/dashboard/RevenueChart.tsx`, `frontend/src/lib/api.ts`, `frontend/src/lib/queryKeys.ts`, `frontend/src/lib/chartTimeUtils.ts`.

- Phase 61 files: `frontend/src/components/dashboard/HrKpiCharts.tsx` (Recharts Tooltip types), `frontend/src/components/dashboard/SalesTable.tsx`, `frontend/src/components/settings/PersonioCard.tsx`, `frontend/src/components/settings/sensors/SnmpWalkCard.tsx`, `frontend/src/components/ui/select.tsx`, `frontend/src/hooks/useSensorDraft.ts`, `frontend/src/lib/defaults.ts`, `frontend/src/signage/components/ScheduleEditDialog.tsx`, `frontend/src/signage/components/ScheduleEditDialog.test.tsx`, `frontend/src/signage/pages/SchedulesPage.test.tsx`.

Overlap: **`HrKpiCharts.tsx`** — Phase 60 introduced the new Recharts Tooltip formatter signatures that caused 4 of the 31 Phase 61 errors; Phase 61 closed them. The fix was a local type-wrap, no behaviour change — confirmed by `git log -p fix(61): HrKpiCharts` (`3fb4c5e`). No other overlap.

**Integration status:** no broken wiring. Build gate (`npm run build`) is the final cross-phase check and passes.

## End-to-end flows

- **Phase 60 (HR date-range):** Plan 60-04 Task 2 human visual-parity walkthrough approved 2026-04-22 — default `thisYear` landing, `Dieser Monat` / `Dieser Quartal` / custom 45-day range, Sales↔HR preset preservation, empty state, delta-badge copy.
- **Phase 61 (TS cleanup):** `npm run build` exits 0 on main; `tsc -b` clean; 10 atomic fix commits; zero shortcut suppressions introduced (git-blame audit).

## Tech debt carried forward

**None net new.** Phase 61 closed the TS debt carried forward from v1.19. No new debt identified in Phase 60 SUMMARYs beyond these pre-existing items that remain independent:

- `frontend/Dockerfile` `npm install` peer-dep conflict (`vite@8` vs `vite-plugin-pwa@1.2.0`) — does not block dev-server or build (bind-mount + existing node_modules cover it); separate cleanup later.
- The running `frontend` container lacks `@testing-library/*` devDeps (pre-existing env drift caught during Phase 61 Task 3 build gate — host build was used as fallback per plan clause).

Both are infrastructure hygiene, not feature debt, and do not block milestone completion.

## Blockers

**None.** Milestone is ready for archival.

## Recommendation

Proceed with `/gsd:complete-milestone 1.20`:

1. Archive ROADMAP snapshot + this audit to `milestones/v1.20-*.md`
2. Update PROJECT.md Current State → v1.20 shipped
3. Git tag `v1.20`
4. No REQUIREMENTS.md to archive (never existed)

Future hygiene: start v1.21+ with `/gsd:new-milestone` so requirements are captured upfront rather than reconstructed post-hoc.
