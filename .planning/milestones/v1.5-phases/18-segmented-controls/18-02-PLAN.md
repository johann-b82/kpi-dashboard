---
phase: 18-segmented-controls
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - frontend/src/components/NavBar.tsx
  - frontend/src/components/LanguageToggle.tsx
  - frontend/src/components/dashboard/DateRangeFilter.tsx
  - frontend/src/components/dashboard/RevenueChart.tsx
  - frontend/src/components/settings/PreferencesCard.tsx
autonomous: false
requirements:
  - SEG-02
  - SEG-03
  - SEG-04
  - SEG-05
  - SEG-06

must_haves:
  truths:
    - "Sales/HR tab navigation in the navbar renders as a segmented control"
    - "Date range presets in the SubHeader render as a segmented control"
    - "Balken/Linie chart type toggle renders as a segmented control"
    - "DE/EN language toggle in navbar renders as a segmented control with disabled state preserved"
    - "PreferencesCard DE/EN picker renders as a segmented control"
    - "All segmented control labels display correctly in both DE and EN"
  artifacts:
    - path: "frontend/src/components/NavBar.tsx"
      provides: "Sales/HR nav segmented control + language toggle segmented control"
      contains: "SegmentedControl"
    - path: "frontend/src/components/LanguageToggle.tsx"
      provides: "Language toggle using SegmentedControl"
      contains: "SegmentedControl"
    - path: "frontend/src/components/dashboard/DateRangeFilter.tsx"
      provides: "Date range presets as segmented control"
      contains: "SegmentedControl"
    - path: "frontend/src/components/dashboard/RevenueChart.tsx"
      provides: "Chart type toggle as segmented control"
      contains: "SegmentedControl"
    - path: "frontend/src/components/settings/PreferencesCard.tsx"
      provides: "Settings language picker as segmented control"
      contains: "SegmentedControl"
  key_links:
    - from: "frontend/src/components/NavBar.tsx"
      to: "frontend/src/components/ui/segmented-control.tsx"
      via: "import SegmentedControl"
      pattern: "import.*SegmentedControl.*segmented-control"
    - from: "frontend/src/components/NavBar.tsx"
      to: "wouter useLocation"
      via: "active state derived from URL"
      pattern: "useLocation"
    - from: "frontend/src/components/LanguageToggle.tsx"
      to: "frontend/src/components/ui/segmented-control.tsx"
      via: "import SegmentedControl"
      pattern: "import.*SegmentedControl.*segmented-control"
    - from: "frontend/src/components/dashboard/DateRangeFilter.tsx"
      to: "frontend/src/components/ui/segmented-control.tsx"
      via: "import SegmentedControl"
      pattern: "import.*SegmentedControl.*segmented-control"
---

<objective>
Integrate SegmentedControl into all 5 consumers, replacing existing button/link/toggle controls.

Purpose: Complete the visual unification — every toggle and tab in the app now uses the pill-shaped segmented control.
Output: All 5 consumer files updated to use SegmentedControl with full i18n parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-segmented-controls/18-CONTEXT.md
@.planning/phases/18-segmented-controls/18-UI-SPEC.md
@.planning/phases/18-segmented-controls/18-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From frontend/src/components/ui/segmented-control.tsx:
```typescript
interface SegmentedControlProps<T extends string> {
  segments: Array<{ value: T; label: string }>;
  value: T;
  onChange: (value: T) => void;
  disabled?: boolean;
  'aria-label'?: string;
  title?: string;
}

export function SegmentedControl<T extends string>(props: SegmentedControlProps<T>): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate SegmentedControl into NavBar and LanguageToggle (SEG-02, SEG-05, SEG-06)</name>
  <files>frontend/src/components/NavBar.tsx, frontend/src/components/LanguageToggle.tsx</files>
  <read_first>
    - frontend/src/components/NavBar.tsx (current Sales/HR links + LanguageToggle placement)
    - frontend/src/components/LanguageToggle.tsx (current DE/EN toggle with disabled-when-dirty guard)
    - frontend/src/components/ui/segmented-control.tsx (the component from Plan 01)
    - .planning/phases/18-segmented-controls/18-UI-SPEC.md (consumer table and interaction contract)
    - .planning/phases/18-segmented-controls/18-CONTEXT.md (D-04, D-05, D-08, D-09)
  </read_first>
  <action>
**NavBar.tsx changes (SEG-02):**

1. Add import: `import { SegmentedControl } from "@/components/ui/segmented-control";`
2. Add import: `import { useLocation, useRoute } from "wouter";` — keep `useLocation` for other active checks, but derive Sales/HR active from path.
3. Remove the `linkClass` function (no longer needed — was for underline-style links).
4. Replace the two `<Link href="/" ...>` and `<Link href="/hr" ...>` elements (lines 48-53) with a single SegmentedControl:

```tsx
<SegmentedControl
  segments={[
    { value: "/", label: t("nav.sales") },
    { value: "/hr", label: t("nav.hr") },
  ]}
  value={location === "/hr" ? "/hr" : "/"}
  onChange={(path) => navigate(path)}
  aria-label="Navigation"
/>
```

5. Add `navigate` from wouter: change `const [location] = useLocation();` to `const [location, navigate] = useLocation();`
6. Remove `Link` from the wouter import if only `useLocation` remains used (check — Upload and Settings links still use `Link`, so keep it).
7. The `import { Link, useLocation } from "wouter"` stays as-is. Just destructure `navigate` from `useLocation`.

**LanguageToggle.tsx changes (SEG-05):**

1. Add import: `import { SegmentedControl } from "@/components/ui/segmented-control";`
2. Remove import: `import { Button } from "@/components/ui/button";` (no longer used)
3. Replace the entire `<Button variant="ghost" ...>` JSX block (lines 78-101) with:

```tsx
<SegmentedControl<"DE" | "EN">
  segments={[
    { value: "DE", label: "DE" },
    { value: "EN", label: "EN" },
  ]}
  value={isDE ? "DE" : "EN"}
  onChange={(lang) => mutation.mutate(lang)}
  disabled={isDisabled}
  aria-label="Language"
  title={isDirty ? t("settings.preferences.toggle_disabled_tooltip") : undefined}
/>
```

4. Remove the `handleToggle` function (lines 72-75) — `onChange` directly calls `mutation.mutate`.
5. Keep ALL mutation logic, `useSettings()`, `useSettingsDraftStatus()`, and `useTranslation()` hooks unchanged.
6. The `isDE`, `isDisabled`, `isDirty` variables remain unchanged.
7. DE/EN labels are literal strings (not translation keys) — per UI-SPEC Copywriting Contract.

**i18n parity (SEG-06):** Sales/HR labels use `t("nav.sales")` and `t("nav.hr")` which have existing DE/EN translations. DE/EN labels are literal. No new translation keys needed.

**Layout preservation:** NavBar layout structure (`max-w-7xl mx-auto px-6 h-full flex items-center gap-6` + `ml-auto`) is unchanged per D-05. The SegmentedControl replaces inline content only.
  </action>
  <verify>
    <automated>cd /Users/johannbechtold/Documents/acm-kpi-light && grep -c "SegmentedControl" frontend/src/components/NavBar.tsx && grep -c "SegmentedControl" frontend/src/components/LanguageToggle.tsx && grep "linkClass" frontend/src/components/NavBar.tsx; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - NavBar.tsx contains `import { SegmentedControl } from "@/components/ui/segmented-control"`
    - NavBar.tsx contains `SegmentedControl` JSX with `segments={[` containing `nav.sales` and `nav.hr`
    - NavBar.tsx contains `navigate` destructured from `useLocation()`
    - NavBar.tsx does NOT contain `linkClass` function
    - NavBar.tsx does NOT contain `border-b-2 border-primary` (old underline style)
    - NavBar.tsx still contains `<Link href="/upload"` and `<Link href="/settings"` (unchanged)
    - LanguageToggle.tsx contains `import { SegmentedControl } from "@/components/ui/segmented-control"`
    - LanguageToggle.tsx contains `SegmentedControl` JSX with `value: "DE"` and `value: "EN"`
    - LanguageToggle.tsx contains `disabled={isDisabled}`
    - LanguageToggle.tsx contains `title={isDirty ?` for tooltip
    - LanguageToggle.tsx does NOT contain `import { Button }` (removed)
    - LanguageToggle.tsx does NOT contain `<Button` JSX (replaced)
    - LanguageToggle.tsx still contains `useSettingsDraftStatus` (guard preserved)
    - LanguageToggle.tsx still contains `mutation.mutate` (persistence preserved)
  </acceptance_criteria>
  <done>Sales/HR nav and DE/EN language toggle both render as SegmentedControl with all existing behavior preserved (routing, persistence, disabled-when-dirty)</done>
</task>

<task type="auto">
  <name>Task 2: Integrate SegmentedControl into DateRangeFilter, RevenueChart, and PreferencesCard (SEG-03, SEG-04, SEG-06)</name>
  <files>frontend/src/components/dashboard/DateRangeFilter.tsx, frontend/src/components/dashboard/RevenueChart.tsx, frontend/src/components/settings/PreferencesCard.tsx</files>
  <read_first>
    - frontend/src/components/dashboard/DateRangeFilter.tsx (current preset buttons)
    - frontend/src/components/dashboard/RevenueChart.tsx (current chart type toggle, lines 95-112)
    - frontend/src/components/settings/PreferencesCard.tsx (current inline radiogroup)
    - frontend/src/components/ui/segmented-control.tsx (the component from Plan 01)
    - .planning/phases/18-segmented-controls/18-UI-SPEC.md (consumer table)
    - .planning/phases/18-segmented-controls/18-CONTEXT.md (D-08)
  </read_first>
  <action>
**DateRangeFilter.tsx changes (SEG-03):**

1. Add import: `import { SegmentedControl } from "@/components/ui/segmented-control";`
2. Remove import: `import { Button } from "@/components/ui/button";` (no longer used)
3. Replace the entire JSX return. The current return has a wrapper div with `flex flex-wrap items-center justify-end` containing a `div` with `flex flex-wrap gap-2` containing mapped `<Button>` elements.

Replace with:
```tsx
return (
  <SegmentedControl<Preset>
    segments={PRESETS.map((p) => ({
      value: p,
      label: t(`dashboard.filter.${p}`),
    }))}
    value={preset}
    onChange={(p) => selectPreset(p)}
    aria-label="Date range"
  />
);
```

4. The `PRESETS` array, `selectPreset` function, `DateRangeValue` interface, and `DateRangeFilterProps` interface remain unchanged.
5. The `value: _value` destructuring stays (unused but part of props contract).

**RevenueChart.tsx changes (SEG-04):**

1. Add import: `import { SegmentedControl } from "@/components/ui/segmented-control";`
2. Remove import: `import { Button } from "@/components/ui/button";` — BUT first check if Button is used elsewhere in the file. It is NOT used elsewhere (only in the chart type toggle). Remove it.
3. Find the `CHART_TYPES` constant (should be near top of file, something like `const CHART_TYPES = ["bar", "line"] as const;` or similar).
4. Replace the chart type toggle block (the `<div className="flex gap-2">` containing the mapped Buttons around lines 98-110) with:

```tsx
<SegmentedControl
  segments={CHART_TYPES.map((type) => ({
    value: type,
    label: t(`dashboard.chart.type.${type}`),
  }))}
  value={chartType}
  onChange={(type) => setChartType(type)}
  aria-label="Chart type"
/>
```

5. Keep the `Header` const structure — just replace the inner `<div className="flex gap-2">...</div>` with the SegmentedControl. The outer flex-wrap justify-between layout stays.

**PreferencesCard.tsx changes (SEG-08):**

1. Add import: `import { SegmentedControl } from "@/components/ui/segmented-control";`
2. Replace the `<div role="radiogroup" ...>` block (lines 39-63) — the entire inline radiogroup — with:

```tsx
<SegmentedControl<"DE" | "EN">
  segments={LANGS.map((lang) => ({
    value: lang,
    label: lang,
  }))}
  value={value}
  onChange={onChange}
  aria-label={t("settings.preferences.language.label")}
/>
```

3. Keep the `LANGS` constant, `PreferencesCardProps` interface, `Label`, and help text `<p>` element unchanged.
4. The Card/CardHeader/CardTitle/CardContent structure is unchanged.
5. Remove the `border border-border overflow-hidden` inline styles (the SegmentedControl has its own `bg-muted rounded-full` container).

**i18n parity (SEG-06):** All labels use existing `t()` keys: `dashboard.filter.thisMonth`, `dashboard.filter.thisQuarter`, `dashboard.filter.thisYear`, `dashboard.filter.allTime`, `dashboard.chart.type.bar`, `dashboard.chart.type.line`. DE/EN picker uses literal "DE"/"EN". No new translation keys needed.
  </action>
  <verify>
    <automated>cd /Users/johannbechtold/Documents/acm-kpi-light && grep -c "SegmentedControl" frontend/src/components/dashboard/DateRangeFilter.tsx && grep -c "SegmentedControl" frontend/src/components/dashboard/RevenueChart.tsx && grep -c "SegmentedControl" frontend/src/components/settings/PreferencesCard.tsx && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DateRangeFilter.tsx contains `import { SegmentedControl } from "@/components/ui/segmented-control"`
    - DateRangeFilter.tsx contains `SegmentedControl<Preset>` JSX
    - DateRangeFilter.tsx contains `dashboard.filter.` in segments map
    - DateRangeFilter.tsx does NOT contain `import { Button }` (removed)
    - DateRangeFilter.tsx does NOT contain `<Button` JSX (replaced)
    - RevenueChart.tsx contains `import { SegmentedControl } from "@/components/ui/segmented-control"`
    - RevenueChart.tsx contains `SegmentedControl` JSX with `dashboard.chart.type.`
    - RevenueChart.tsx does NOT contain `import { Button }` (removed)
    - RevenueChart.tsx does NOT contain `<Button` JSX for chart type toggle (replaced)
    - PreferencesCard.tsx contains `import { SegmentedControl } from "@/components/ui/segmented-control"`
    - PreferencesCard.tsx contains `SegmentedControl<"DE" | "EN">` JSX
    - PreferencesCard.tsx does NOT contain `role="radiogroup"` (removed — now inside SegmentedControl)
    - PreferencesCard.tsx does NOT contain `border border-border overflow-hidden` (old inline styles removed)
    - `npx tsc --noEmit` exits with code 0 (no type errors)
  </acceptance_criteria>
  <done>DateRangeFilter, RevenueChart chart toggle, and PreferencesCard all render as SegmentedControl with i18n labels, no type errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all segmented controls</name>
  <files>none (visual verification only)</files>
  <action>
Human visually verifies all 5 segmented control integrations in the running application. No code changes in this task.

What was built: All 4 toggle/tab controls replaced with pill-shaped SegmentedControl: Sales/HR nav, date range presets, chart type toggle, DE/EN language toggle (navbar + settings page).

Steps to verify:
1. Open http://localhost:5173 in browser
2. NavBar Sales/HR tabs: Verify pill-shaped segmented control with dark active segment. Click Sales and HR — URL changes, active segment follows.
3. NavBar DE/EN toggle: Verify pill-shaped DE/EN control in the navbar right area. Click to switch language — labels across the page change language.
4. SubHeader date range: On Sales page, verify 4 preset buttons (This Month, This Quarter, This Year, All Time) are now a single pill-shaped segmented control. Click each — charts update.
5. Chart type toggle: On Sales page, verify Balken/Linie (or Bar/Line in EN) is a pill-shaped segmented control next to the chart title. Click to switch chart type.
6. Settings page: Navigate to Settings. In Preferences card, verify DE/EN picker is a pill-shaped segmented control matching the navbar one.
7. Disabled state: On Settings page, change the language picker (makes form dirty). Go back to navbar — the language toggle should be dimmed (opacity-50) and non-clickable. Discard changes — toggle re-enables.
8. Both languages: Switch to DE, verify all labels show German. Switch to EN, verify all labels show English. No missing translation keys visible.
  </action>
  <verify>Human approval — type "approved" or describe issues</verify>
  <done>User confirms all segmented controls render correctly with pill styling, correct active states, and full i18n parity</done>
</task>

</tasks>

<verification>
- All 5 consumer files import and use SegmentedControl
- No `<Button>` elements remain for toggle/tab controls (may still exist for other uses like upload form)
- `npx tsc --noEmit` passes
- Visual check confirms pill-shaped styling across all controls
</verification>

<success_criteria>
- Sales/HR tabs, date presets, chart toggle, and language toggle all render as pill-shaped segmented controls
- Active segment has dark background (`bg-foreground text-background`)
- Inactive segments have `text-muted-foreground` on `bg-muted` container
- Language toggle disabled state works (opacity-50 when settings dirty)
- All labels translate correctly in DE and EN
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-segmented-controls/18-02-SUMMARY.md`
</output>
