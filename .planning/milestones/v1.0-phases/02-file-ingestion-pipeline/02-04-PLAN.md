---
phase: 02-file-ingestion-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/UploadPage.tsx
  - frontend/src/components/DropZone.tsx
  - frontend/src/components/ErrorList.tsx
  - frontend/src/components/UploadHistory.tsx
  - frontend/src/components/DeleteConfirmDialog.tsx
  - frontend/src/components/LanguageToggle.tsx
autonomous: false
requirements: [UPLD-01, UPLD-02, UPLD-03, UPLD-04, MGMT-01]

must_haves:
  truths:
    - "User can drag-and-drop or browse to select a .csv or .txt file"
    - "Unsupported file types are rejected with an inline error message naming the format"
    - "An indeterminate spinner shows during upload/parsing"
    - "On success, a toast shows filename and row count, and upload history refreshes"
    - "On partial/failed upload, an inline scrollable error list shows row-level validation errors"
    - "Upload history table shows filename, timestamp, row count, status badge, error count, and delete button"
    - "Delete button opens a confirmation dialog naming the file and record count"
    - "Confirmed delete removes the batch and refreshes the history list"
    - "Language toggle switches all UI labels between German and English"
  artifacts:
    - path: "frontend/src/pages/UploadPage.tsx"
      provides: "Main upload page layout with DropZone + ErrorList + UploadHistory"
      min_lines: 40
    - path: "frontend/src/components/DropZone.tsx"
      provides: "Drag-and-drop file upload zone using react-dropzone"
      contains: "useDropzone"
    - path: "frontend/src/components/ErrorList.tsx"
      provides: "Scrollable inline validation error list"
      min_lines: 15
    - path: "frontend/src/components/UploadHistory.tsx"
      provides: "Upload history table with status badges and delete buttons"
      contains: "useQuery"
    - path: "frontend/src/components/DeleteConfirmDialog.tsx"
      provides: "Confirmation dialog for upload deletion"
      contains: "Dialog"
    - path: "frontend/src/components/LanguageToggle.tsx"
      provides: "DE/EN language toggle button"
      contains: "i18n.changeLanguage"
  key_links:
    - from: "frontend/src/components/DropZone.tsx"
      to: "frontend/src/lib/api.ts"
      via: "useMutation calling uploadFile"
      pattern: "uploadFile"
    - from: "frontend/src/components/UploadHistory.tsx"
      to: "frontend/src/lib/api.ts"
      via: "useQuery calling getUploads"
      pattern: "getUploads"
    - from: "frontend/src/components/UploadHistory.tsx"
      to: "frontend/src/components/DeleteConfirmDialog.tsx"
      via: "opens dialog on delete click"
      pattern: "DeleteConfirmDialog"
    - from: "frontend/src/pages/UploadPage.tsx"
      to: "frontend/src/components/DropZone.tsx"
      via: "renders DropZone component"
      pattern: "<DropZone"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/UploadPage.tsx"
      via: "renders UploadPage"
      pattern: "<UploadPage"
---

<objective>
Build all React UI components for the upload page: DropZone (drag-and-drop + browse), ErrorList (inline validation errors), UploadHistory (table with status badges + delete), DeleteConfirmDialog (confirmation modal), and LanguageToggle (DE/EN). Wire everything into UploadPage and App. Apply all styling per UI-SPEC.

Purpose: This is the user-facing delivery of Phase 2 — the upload page that ties together the backend pipeline with a polished bilingual interface.
Output: Fully functional upload page at http://localhost:5173 with all interactions working end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-ingestion-pipeline/02-CONTEXT.md
@.planning/phases/02-file-ingestion-pipeline/02-UI-SPEC.md
@.planning/phases/02-file-ingestion-pipeline/02-03-SUMMARY.md

<interfaces>
<!-- From Plan 03 — frontend scaffold already exists -->

From frontend/src/lib/api.ts:
```typescript
export interface ValidationErrorDetail {
  row: number;
  column: string;
  message: string;
}

export interface UploadResponse {
  id: number;
  filename: string;
  row_count: number;
  error_count: number;
  status: "success" | "partial" | "failed";
  errors: ValidationErrorDetail[];
}

export interface UploadBatchSummary {
  id: number;
  filename: string;
  uploaded_at: string;
  row_count: number;
  error_count: number;
  status: "success" | "partial" | "failed";
}

export async function uploadFile(file: File): Promise<UploadResponse>;
export async function getUploads(): Promise<UploadBatchSummary[]>;
export async function deleteUpload(id: number): Promise<void>;
```

From frontend/src/i18n.ts:
```typescript
// i18n initialized with lng: "de", fallbackLng: "en"
// Use: const { t } = useTranslation();
// All keys from locales/de.json and locales/en.json
```

shadcn components available (installed in Plan 03):
```typescript
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Badge } from "@/components/ui/badge";
import { Separator } from "@/components/ui/separator";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all UI components and UploadPage</name>
  <files>
    frontend/src/components/LanguageToggle.tsx
    frontend/src/components/DropZone.tsx
    frontend/src/components/ErrorList.tsx
    frontend/src/components/DeleteConfirmDialog.tsx
    frontend/src/components/UploadHistory.tsx
    frontend/src/pages/UploadPage.tsx
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/App.tsx
    frontend/src/lib/api.ts
    frontend/src/locales/de.json
    frontend/src/locales/en.json
    frontend/src/i18n.ts
    .planning/phases/02-file-ingestion-pipeline/02-UI-SPEC.md
    .planning/phases/02-file-ingestion-pipeline/02-CONTEXT.md
  </read_first>
  <action>
    Create all UI components following the UI-SPEC exactly. Every user-facing string must use `t("key")` from react-i18next — no hardcoded strings.

    **1. frontend/src/components/LanguageToggle.tsx:**
    - Import `useTranslation` from react-i18next and `Button` from `@/components/ui/button`.
    - Render a Button (variant="ghost", size="sm") that displays "DE / EN".
    - On click: toggle language between "de" and "en" using `i18n.changeLanguage(i18n.language === "de" ? "en" : "de")`.
    - Show current language visually: bold the active language. E.g., if current is "de", render `<span className="font-semibold">DE</span> / <span>EN</span>`.

    **2. frontend/src/components/DropZone.tsx:**
    - Props: `onUploadSuccess: (data: UploadResponse) => void`, `onUploadError: (data: UploadResponse) => void`.
    - Import `useDropzone` from react-dropzone, `useMutation`, `useQueryClient` from @tanstack/react-query, `toast` from sonner, `useTranslation`, `Loader2` from lucide-react, `uploadFile` from `@/lib/api`, `Card`/`CardContent` from `@/components/ui/card`, `Button`.
    - Use `useMutation({ mutationFn: uploadFile })`.
    - Configure useDropzone:
      - `onDrop`: if file accepted, call `mutation.mutate(file)`.
      - `accept`: `{ "text/csv": [".csv"], "text/plain": [".txt"] }` — per D-07.
      - `maxFiles: 1` — per D-15.
      - `disabled: mutation.isPending`.
    - On mutation success callback — EXPLICIT STATUS ROUTING (this is critical for correct error display):
      - Show toast: `toast.success(t("upload_success_title"), { description: data.status === "partial" ? t("upload_partial_body", { filename, count: data.row_count, errors: data.error_count }) : t("upload_success_body", { filename, count: data.row_count }) })`.
      - Invalidate queries: `queryClient.invalidateQueries({ queryKey: ["uploads"] })`.
      - Route by status: if `data.status === "success"` call `onUploadSuccess(data)`. If `data.status === "partial"` OR `data.status === "failed"` call `onUploadError(data)`. This ensures that partial uploads route to `onUploadError`, which preserves the error list display. Do NOT call `onUploadSuccess` for partial uploads — that would clear the error list via `setErrors([])`.
    - On mutation error: `toast.error(error.message)`.
    - On dropzone reject (wrong file type): show inline message below drop zone using `fileRejections` state. Display `t("invalid_file_type", { ext: file.name.split('.').pop() })`.
    - Visual states per UI-SPEC Interaction States:
      - Idle: `bg-slate-100 border-2 border-dashed border-slate-300` with muted text.
      - isDragActive: `bg-blue-50 border-2 border-solid border-blue-600` with blue text.
      - isPending: Drop zone disabled, show `<Loader2 className="h-6 w-6 animate-spin text-blue-600" />` centered with processing text.
    - Render: Card wrapping the dropzone div with `min-h-[160px]` (UI-SPEC minimum). Inside: `getRootProps()` on the container div, `getInputProps()` on hidden input. Show dropzone_prompt, "or" separator, browse button, accepted formats hint.

    **3. frontend/src/components/ErrorList.tsx:**
    - Props: `errors: ValidationErrorDetail[]`.
    - Import `useTranslation`, `ValidationErrorDetail` from `@/lib/api`.
    - If errors is empty, render nothing (return null).
    - Render a div with `max-h-[240px] overflow-y-auto` (UI-SPEC constraint).
    - Header: `t("error_heading", { count: errors.length })` in red-600 text.
    - List each error: `t("error_row_format", { row: e.row, column: e.column, message: e.message })` — one per line in a scrollable list.
    - Left border: `border-l-4 border-red-600` for visual emphasis per UI-SPEC destructive color.

    **4. frontend/src/components/DeleteConfirmDialog.tsx:**
    - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `batch: UploadBatchSummary | null`, `onConfirm: () => void`.
    - Import `Dialog`, `DialogContent`, `DialogDescription`, `DialogFooter`, `DialogHeader`, `DialogTitle` from `@/components/ui/dialog`, `Button`, `useTranslation`.
    - Dialog content:
      - Title: `t("delete_title")`
      - Description: `t("delete_body", { filename: batch?.filename, count: batch?.row_count })`
      - Footer with two buttons:
        - Cancel: `<Button variant="outline" onClick={() => onOpenChange(false)}>{t("delete_cancel")}</Button>` — "Upload behalten" / "Keep upload" per UI-SPEC.
        - Confirm: `<Button variant="destructive" onClick={onConfirm}>{t("delete_confirm")}</Button>` — "Loschen" / "Delete".

    **5. frontend/src/components/UploadHistory.tsx:**
    - Import `useQuery`, `useMutation`, `useQueryClient` from @tanstack/react-query, `getUploads`, `deleteUpload`, `UploadBatchSummary` from `@/lib/api`, `Table`/`TableBody`/`TableCell`/`TableHead`/`TableHeader`/`TableRow`, `Badge`, `Button`, `useTranslation`, `Trash2` from lucide-react, `DeleteConfirmDialog`.
    - Use `useQuery({ queryKey: ["uploads"], queryFn: getUploads })`.
    - Use `useMutation({ mutationFn: deleteUpload, onSuccess: () => queryClient.invalidateQueries({ queryKey: ["uploads"] }) })`.
    - State: `selectedBatch: UploadBatchSummary | null` for delete dialog.
    - Loading state: 3 skeleton rows with `animate-pulse bg-slate-200` — per UI-SPEC.
    - Empty state: centered text with `t("empty_title")` (heading) and `t("empty_body")` (muted body) — per UI-SPEC.
    - Populated state: Table with columns per UI-SPEC/D-19:
      - Filename (`t("col_filename")`): display `batch.filename`
      - Uploaded at (`t("col_uploaded_at")`): format `batch.uploaded_at` as locale-appropriate datetime string
      - Rows (`t("col_rows")`): display `batch.row_count`
      - Status (`t("col_status")`): Badge component with color per UI-SPEC:
        - `success`: `className="bg-green-600 text-white"` 
        - `partial`: `className="bg-yellow-400 text-slate-900"`
        - `failed`: `className="bg-red-600 text-white"`
      - Errors (`t("col_errors")`): display `batch.error_count`, muted when 0
      - Delete: `<Button variant="ghost" size="icon" onClick={() => setSelectedBatch(batch)} aria-label={t("delete_title")}><Trash2 className="h-4 w-4" /></Button>` — red on hover per UI-SPEC.
    - Table column headers use Label typography: `uppercase text-xs tracking-wider` per UI-SPEC.
    - Render `<DeleteConfirmDialog open={!!selectedBatch} onOpenChange={(open) => !open && setSelectedBatch(null)} batch={selectedBatch} onConfirm={() => { deleteMutation.mutate(selectedBatch!.id); setSelectedBatch(null); }} />`

    **6. frontend/src/pages/UploadPage.tsx:**
    - Import `useTranslation`, `DropZone`, `ErrorList`, `UploadHistory`, `LanguageToggle`, `Separator`, `useState`, `UploadResponse`/`ValidationErrorDetail` from api.ts.
    - State: `errors: ValidationErrorDetail[]` (initially empty).
    - Layout per UI-SPEC Page Layout:
      - Outer: `max-w-[800px] mx-auto px-4 py-12` (content centered, 48px vertical padding = 2xl).
      - Top right: `<LanguageToggle />` positioned with flex justify-end.
      - Heading: `<h1 className="text-xl font-semibold">{t("page_title")}</h1>` (20px/600 per UI-SPEC).
      - DropZone: `<DropZone onUploadSuccess={(data) => setErrors([])} onUploadError={(data) => setErrors(data.errors)} />`
      - ErrorList: `<ErrorList errors={errors} />` — shown only when errors is non-empty.
      - Separator: `<Separator className="my-8" />` — 32px gap (xl spacing).
      - History heading: `<h2 className="text-xl font-semibold mb-4">{t("history_title")}</h2>`.
      - UploadHistory: `<UploadHistory />`.

    Note: The `onUploadSuccess` callback clears errors (`setErrors([])`), while `onUploadError` populates them (`setErrors(data.errors)`). The DropZone status routing (section 2 above) ensures that `onUploadSuccess` is ONLY called for `status === "success"`, and `onUploadError` is called for both `"partial"` and `"failed"`. This guarantees the ErrorList displays for partial uploads.

    **7. Update frontend/src/App.tsx** — Replace placeholder content:
    ```typescript
    import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
    import { Toaster } from "sonner";
    import UploadPage from "./pages/UploadPage";

    const queryClient = new QueryClient();

    function App() {
      return (
        <QueryClientProvider client={queryClient}>
          <UploadPage />
          <Toaster position="top-right" />
        </QueryClientProvider>
      );
    }

    export default App;
    ```
  </action>
  <verify>
    <automated>cd /Users/johannbechtold/Documents/acm-kpi-light/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 UI components created. UploadPage layout matches UI-SPEC wireframe. DropZone handles drag-and-drop with react-dropzone, upload mutation with TanStack Query, toast notifications with sonner. Status routing is explicit: "success" calls onUploadSuccess (clears errors), "partial" and "failed" call onUploadError (shows ErrorList). ErrorList shows scrollable validation errors. UploadHistory fetches and displays history with status badges and delete. DeleteConfirmDialog uses shadcn Dialog. LanguageToggle switches DE/EN. All strings use i18n `t()` function.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify upload page end-to-end</name>
  <files>.</files>
  <action>
    User verifies the complete upload page end-to-end in the browser.

    Steps to verify:
    1. Open http://localhost:5173 in a browser
    2. Verify the page shows "Datei hochladen" heading (German default)
    3. Click the DE/EN toggle — verify all labels switch to English
    4. Try dragging a .pdf file onto the drop zone — verify it shows an error about unsupported format
    5. Upload a valid .csv or .txt ERP export file:
       a. Verify the spinner appears during processing
       b. Verify a success toast appears with filename and row count
       c. Verify the file appears in the upload history table below
    6. Check upload history table:
       a. Verify it shows filename, timestamp, row count, status badge, error count
       b. Verify the status badge has the correct color (green for success, yellow for partial, red for failed)
    7. Click the delete button (trash icon) on a history entry:
       a. Verify the confirmation dialog appears with the filename and record count
       b. Click "Upload behalten" / "Keep upload" — verify nothing is deleted
       c. Click delete again, then confirm — verify the entry is removed from the list
    8. Upload a file with intentionally bad data to test partial/failed status and error list display
       a. Verify the error list appears below the drop zone for partial uploads
       b. Verify each error shows row number, column name, and error message
  </action>
  <verify>
    <automated>echo "Human verification checkpoint — user approves or reports issues"</automated>
  </verify>
  <done>User has verified: upload page loads at http://localhost:5173, drag-and-drop works, file type rejection works, upload success/partial/failed flows work (error list displays for partial uploads), history table displays correctly, delete with confirmation works, language toggle works. User typed "approved".</done>
</task>

</tasks>

<verification>
- Upload page loads at http://localhost:5173 with correct layout
- Drag-and-drop accepts .csv and .txt, rejects other types
- Upload triggers backend parsing and stores data in PostgreSQL
- Upload history shows all past uploads with correct status badges
- Delete works with confirmation dialog and cascade removal
- Language toggle switches all UI labels between DE and EN
- Error list displays row-level validation errors for failed/partial uploads
</verification>

<success_criteria>
- User can upload a CSV/TXT file and see it appear in upload history
- Invalid files are rejected with clear error messages
- Validation errors show row number, column name, and problem description
- Delete removes batch and associated records
- All UI labels are bilingual (DE/EN toggle works)
- Visual design matches UI-SPEC (colors, spacing, typography)
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-ingestion-pipeline/02-04-SUMMARY.md`
</output>
